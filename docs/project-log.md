# MassageLab Project Log

This is the canonical chronological planning and progress log for MassageLab. Use [project-state.md](project-state.md) first for the current snapshot, then use this file for decisions, completed work, and change history.

Existing plans, audits, roadmaps, and checklists remain source evidence. Keep them for context, but mirror meaningful progress, plan changes, and priority changes in [project-state.md](project-state.md) and here.

Catalog naming note: `Massage Laba Lamp` is the approved catalog label for the original free moving-gradient default. `MassageLaba Lamp` and general prose that says `MassageLab Lamp` are legacy references to that same stable `massage-lab-moving-gradient` entry, not separate backgrounds.

## 2026-09-05 — Layer B release completion and Layer C current-main port

- The user merged PR #202 from exact reviewed head `b5bf0109d8b0c5add23a2642cb73609c56a99d53`; GitHub created signed web-flow merge commit `2c23e825f95d0ae6e55cc26770734bbdb40bc149` with expected parents `93545ed0372ff46e74cffda25320434542fc028d` and `b5bf0109d8b0c5add23a2642cb73609c56a99d53`. The merge tree is the reviewed PR runtime tree; only the separately prepared candidate's two documentation receipts were absent. Post-merge Code quality, Browser build, all four Browser QA lanes, aggregate QA, and all three CodeQL analyzers passed. Vercel reports the exact merge SHA READY on every Production alias, the public homepage and Anatomime return HTTP 200, all 46 migrations remain current, the membership webhook-write pause flag remains exactly `0`, and the checked Vercel/Sentry windows reported no Production errors. No rewrite or rollback is needed.
- Completed the planned Layer C current-main port on `codex/family-friends-07-booking-traffic-r3` from exact deployed base `2c23e825f95d0ae6e55cc26770734bbdb40bc149`, preserving reviewed Layer C source `c8d86e0bc581f88203336cef492fc73c09d306be` and its 15-commit history. The 25-file source delta merged automatically except for the two canonical docs and the shared project-state review-date contract; current main already contains the stronger review-date version, leaving an effective 24-file candidate. Conflict resolution preserves current Layer B release truth and Layer C evidence.
- Fresh exact-candidate evidence passes the exact 137/137 focused booking/limiter/workload matrix, the complete Node suite, Prisma validation/generation, TypeScript, lint with only the established Chimer large-file note, both 115-page Production and Browser-QA builds, and all 8/8 public-booking browser cases across desktop and mobile Chromium. Independent spec review passes. Independent quality/security review found one accessibility issue: per-second retry countdowns were inside polite live regions. The correction keeps the visual countdowns, holds each live-region message stable while time remains, and announces `You can try again now.` exactly once at expiry; focused unit and browser evidence pass and the follow-up review reports no actionable findings. Layer C adds no schema or migration. No database, provider, email, payment, push, PR, deployment, or Production action occurred.

## 2026-09-05 — Layer B current-main integration

- Completed the local `codex/family-friends-06-anatomime-traffic-r3` current-main integration as exact merge commit `f0c1e83c19fbe6297b6a0ded7e9fe86d55ba369a`, with exact parents current main `93545ed0372ff46e74cffda25320434542fc028d` and locally reviewed Layer B source head `6c32b8e767cca220f4c1aa4ffbff4290eaa94b0d`. The source history retains accepted Layer A `6eaa22b45ba5090f0e926cff80f47e175454a2d8` as its actual ancestor. The integration preserves main's merged and deployed Layer A, all 46 current Production migrations, enabled membership writer, and merged/deployed PR #201 dependency maintenance.
- Fresh exact-merge-head local validation passes the exact 174/174 focused Anatomime matrix. The full Node suite reports 4,037 tests: 4,034 passed, 0 failed, and 3 intentionally skipped. TypeScript and lint pass with only the established Chimer large-file note, and the ordinary Production and Browser-QA builds each generate 115 pages.
- Fresh exact-head full intercepted Anatomime Browser QA coverage reports 42/42 desktop/mobile cases ok in one post-fix run against the clean r3 server on task port 3012. After all results reported, the run reproduced the known post-worker teardown hang; the exact owned runner was stopped, so a clean process exit is not claimed. Focused changed public-route overlap against task port 3013 reported 3 passes and 1 intended project skip, then reproduced the same post-worker teardown hang; its owned runner was stopped and no clean exit is claimed. Initial browser attempts that reused a stale source-worktree server on occupied port 3010 were discarded and are not r3 evidence.
- Independent full-integration review reports SPEC PASS and QUALITY/SECURITY PASS with no actionable findings; the quality-focused suite passed 249/249. PR #202 opened from exact head `d140c74621753ede5eef3b5cfba9a0e0e3804a99`. Its first hosted GitHub CodeRabbit review raised three valid test/documentation findings: hardcoded documentation receipt totals in the runtime route suite, missing room-rule constants in the compiled presence mock, and incomplete helper/regex documentation in the operational-limiter schema contract. Exact local review-fix head `02c6ff0d00e5291ea8aab57a425ed86879868b4a` moves receipt ownership to a documentation-focused TypeScript-AST inventory test, derives both Node and browser totals without hardcoding 174 or 42, rejects unsupported array spreads with a direct fixture, supplies and asserts the real room-rule constants, and completes the focused JSDoc/comments. Two independent correction reviews report SPEC PASS and final QUALITY PASS; the corrected exact 174/174 focused matrix, 116/116 affected matrix, TypeScript, lint, and diff checks pass.
- The first PR #202 hosted CI run passed Code quality, Browser build, Browser QA lanes 1, 2, and 4, all three CodeQL analyzers, and Vercel preview. Browser QA lane 3 reported 149 passes, 42 skips, and three failures, all in `tests/browser/music-media-session.spec.ts`, which Layer B does not change; retries also recorded external media transport failures. A corrected-head rerun must establish whether that failure was transient before merge. Corrected-head push, hosted checks, final finite GitHub-hosted CodeRabbit review with no actionable findings, main merge, and deployment remain pending. Layer B needs no migration, and no database, provider, environment, email, payment, or Production action occurred.

## 2026-09-04 — Dependency/action maintenance gate

- Started one focused maintenance branch from exact merged main `94c05b827eb2511659994cdae00cfcb99f909d77` before continuing family-and-friends hardening Layer B. The gate keeps application and provider behavior unchanged while raising only reviewed compatible transitive dependency floors and moving the Next.js build-cache step from `actions/cache@v4` to exact official `v6.1.0` commit `55cc8345863c7cc4c66a329aec7e433d2d1c52a9`, whose action runtime is Node 24. Cache paths, keys, and restore behavior are unchanged.
- Fresh full and `--omit=dev` audits both resolve to five high affected nodes propagated from the single `deepmerge-ts<8` recursive-graph advisory. Static tracing finds no MassageLab import or supported attacker-controlled Production path: the package exists only beneath `@prisma/config` in the trusted Prisma CLI/config graph. Prisma `7.9.1` and available `7.10.0` both declare exact `deepmerge-ts@7.1.5`; an incompatible major-8 force remains deferred and documented. All other current advisory families were raised within their existing major lines, reducing the npm inventory from 36 affected nodes to five without a framework upgrade.
- The first complete Windows suite reproduced nine AtmoShaper authority failures on untouched baseline main. Git-blob SHA-256 values matched the pinned expectations, while Windows had converted the two hash-locked fixtures to CRLF. Exact LF attributes now cover those fixtures and the canonical listening export compared byte-for-byte by the same test; their committed JSON blobs remain unchanged. The test also normalizes only Git's CRLF checkout conversion before hash and renderer comparison so existing Windows worktrees do not require a forced rewrite while all other byte drift remains closed. The focused AtmoShaper suite then passed 10/10 and the complete Node suite passed `3,884`, failed `0`, and intentionally skipped `3` across `3,887` tests.
- Clean-install evidence passes `npm ci`, the 4/4 dependency-security contract, Prisma validation/generation, TypeScript, lint with only the established large Chimer-file Babel note, `git diff --check`, and the 115-page Production build. At that checkpoint, hosted CI, CodeQL, GitHub-hosted CodeRabbit review on the final PR head, merge, deployment, and the default-branch dependency rescan were pending. No database connection or migration, application route, provider/environment setting, email, payment, or Production mutation occurred.
- Initial hosted review of PR #201 at exact head `4e447331e6933852946c8a4cea78c0025513955a` found two valid maintenance defects: `.gitattributes` alone would not rewrite unchanged CRLF files in an existing Windows checkout, and the focused dependency test lacked a package-script owner. The follow-up normalizes only CRLF at the authority-test read boundary, adds `test:dependency-security`, and updates the plan to use it. An independent exact-diff pass then found stale version-history and SemVer wording only; the wiki now distinguishes superseded PostCSS checkpoints, the plan states each brace-expansion compatibility line accurately, and the current snapshot describes the reviewed upgrades as same-major rather than patch-only. The AtmoShaper suite passes 10/10 with all three authority files deliberately converted to CRLF and again with LF restored; the named dependency script passes 4/4, the complete suite passes `3,884` with `0` failures and `3` intentional skips, TypeScript and lint pass, and the 115-page Production build passes. At that checkpoint, hosted CI and a fresh finite CodeRabbit review were still required on the corrected head.
- Exact final PR head `5909354ba7ea42f7bc7ae413a78747d5f0b6cdc2` passes hosted Code quality, Browser build, all four Browser QA lanes, aggregate QA, all three CodeQL analyzers, and Vercel preview. One earlier Lane 2 run exhausted the fixed 20-minute job window only after an Ubuntu package mirror spent more than nine minutes installing Chromium dependencies; the replacement exact-head run completed normally with all 189 lane tests accounted for, so no application, test, or timeout change was introduced. The final eligible GitHub-hosted CodeRabbit request was acknowledged at `2026-09-05T03:07:41Z` and completed at `2026-09-05T03:10:19Z`; it covered the exact final head, generated no actionable comments, and left all three threads resolved. PR #201 then merged to exact current main `93545ed0372ff46e74cffda25320434542fc028d`, and the resulting dependency/action maintenance runtime was deployed.

## 2026-09-04 — Layer A current-main integration and PR gate

- Merged current main `1ed498abce7f8d5d72fdb9faa7368fca6e173ddd` into the reviewed Layer A history without rewriting its commits. Integration head `faed651c4bda23d2ac91b6d4dcdbe295db0aa804` has reviewed Layer A `6eaa22b45ba5090f0e926cff80f47e175454a2d8` and PR #199 merge `1ed498abce7f8d5d72fdb9faa7368fca6e173ddd` as its two parents. The histories changed disjoint paths and merged without conflicts; the resulting PR delta remains 37 files, below CodeRabbit's 100-file hosted-review limit.
- Fresh evidence bound to exact runtime integration head `faed651c4bda23d2ac91b6d4dcdbe295db0aa804` passes Prisma validation/generation, the 168/168 Layer A owner matrix, the 55/55 focused limiter/mail/auth matrix, TypeScript, lint, diff checks, and the 115-page Production build. The complete Windows suite reports 3,885 total, 3,873 passed, nine established AtmoShaper CRLF/fingerprint failures, and three intentional skips. At that checkpoint, hosted Linux, hosted GitHub CodeRabbit review, and hosted CI were still pre-merge gates. The local CodeRabbit CLI is not part of this hosted zero-findings loop.
- Pushed only Layer A and opened PR #200 against current main. The hosted manual CodeRabbit request was acknowledged as a review trigger; no local CodeRabbit CLI loop is used. A separate GitHub review found that an ambiguous initial provider attempt could remain canonically recoverable as `PENDING` after lease expiry while the Activity service and control accepted only `FAILED`. Runtime review-fix commit `901da9499e39114f92ac5128414fb9ec5bb0d7fa` routes canonical unclaimed or expired `PENDING` notices through the existing full-Admin, exact-claim, append-only-key, audited retry owner. Live and malformed claims remain non-attempting, password-reset notices remain excluded, retry audit replays accept their true `PENDING` before-state, and original action replay recognizes a retry-owned ambiguous claim without changing its immutable evidence.
- Fresh current-runtime evidence passes 64/64 directly affected Admin-email, Activity, and server-workload tests, TypeScript, lint, diff checks, and the 115-page Production build. The complete Windows suite reports 3,886 total, 3,874 passed, nine established AtmoShaper CRLF/fingerprint failures, and three intentional skips. Local reproduction identified the first hosted Code quality failure as the project-state review-date ceiling lagging the newly verified September 4 state; the bound now advances with that evidence. At that checkpoint, hosted Linux CI and hosted CodeRabbit still needed to rerun on the final PR head.
- The first finite GitHub-hosted CodeRabbit pass raised ten review threads. Review-fix source head `6238d67209694c71d03b507412809c768919e01e` addresses the valid findings without using the local CodeRabbit CLI: it documents replayed versus newly attempted delivery results and the exact claim-state/CAS owners; corrects SMTP ordering and completed membership-bridge language; bounds the later donation Checkout attempt to the established 23-hour-55-minute Stripe margin with exact boundary coverage; corrects claim/send wording; and closes the zero-row preflight writer race inside the pending migration. The migration takes an access-exclusive `AdminEmailIntent` lock, validates and drops a temporary false constraint inside its explicit transaction, and therefore atomically rolls back if any row exists without leaving the temporary constraint on success. The proposed network quota at the asynchronous `EMAIL_SECURITY` delivery boundary was not applied because that owner deliberately has no originating request-network identity; ingress-specific protection requires a separately designed synchronous mutation/queue owner rather than inventing or persisting a network identity in durable Admin/retry mail.
- Fresh review-follow-up evidence passes 93/93 focused Admin-operation, Activity, migration-contract, and server-workload tests, Prisma validation, TypeScript, lint with only the established Chimer large-file note, and diff checks. Independent migration and quality reviews found no blocking defect after two comment-wording corrections. At that checkpoint, hosted Linux CI and a fresh finite GitHub-hosted CodeRabbit review were still required on the final pushed head.
- Exact pushed PR head `022655587144e0efb6290a1010dc550c240533e5` passed hosted Linux Code quality, Browser build, all four Browser QA lanes, aggregate QA, all three CodeQL analyzers, and its Vercel preview. The next hourly GitHub-hosted CodeRabbit request was acknowledged at `2026-09-04T21:25:49Z` and completed normally with two comment-only maintainability findings. Follow-up head `8cb1465c9bcaa4e73e0de2651cbda824ce15bca2` documents the Activity retry predicate's alignment with the service lifecycle rule plus the direct/retry claim-shape branches, without changing behavior; 57/57 focused Admin tests and TypeScript pass. At that checkpoint, a fresh hosted review and hosted CI on the final pushed head were still required.
- Exact pushed head `de54c70bc57e3780ae66d83dcffe0835b1cce143` passed the complete hosted Linux, four-lane Browser QA, aggregate QA, CodeQL, and Vercel preview matrix with zero unresolved threads. The next eligible hosted CodeRabbit request was acknowledged at `2026-09-04T22:27:42Z` and completed with one valid functional finding: the Activity display condition was broader than the recipient-aware service predicate. Runtime fix `fd3c90be545c32d5085f75ef44e8840150c21b0d` exports one canonical predicate, uses it in retry delivery and the full-Admin Activity projection, and returns only a safe boolean to the page while keeping the historical recipient out of the rendered payload. The focused eligibility matrix covers valid `PENDING`, valid `FAILED`/`DELIVERY_FAILED`, missing recipient, unexpected failure codes, `RECIPIENT_UNAVAILABLE`, and `PASSWORD_RESET`; 58/58 focused Admin tests, TypeScript, lint with only the established Chimer large-file note, and diff checks pass. At that checkpoint, fresh exact-head hosted CI and CodeRabbit review were still required.
- Exact pushed head `506c5a2cdfa701cbaa7dc9b4d078feae70dc94ff` exposed one hosted Code quality failure: `tests/account-activity.test.mjs` still required the superseded page-local retry-status expression even though the runtime now correctly consumes the server-projected `retryEligible` boolean. Test-only correction `44ad59ef5f216859255b89d791c569b031d09f2e` aligns that source contract without changing runtime behavior. The affected 63/63 Admin tests, TypeScript, lint with only the established Chimer large-file note, and diff checks pass. The complete Windows suite now reports 3,887 total, 3,875 passed, nine established AtmoShaper CRLF/fingerprint failures, and three intentional skips. Final exact PR head `57f20767ab98b61cc9c4f1f8e56b629c94171abf` passes the complete hosted Linux, four-lane Browser QA, aggregate QA, CodeQL, and Vercel preview matrix. The fresh GitHub-hosted CodeRabbit review finished with no new actionable findings and all 14 review threads resolved.
- Under exact Production authorization, the selected direct, non-pooler Neon target matched the connected Production default endpoint, database, and owner without exposing its credential. Two immediately adjacent count-only preflights found zero `AdminEmailIntent` rows, and Prisma found all 45 baseline migrations current with exactly `20260831120000_operational_rate_limit_bucket` pending. Only that migration applied. Prisma then reported all 46 migrations current, and aggregate catalog proof confirmed the migration record, all three nullable claim columns, both new tables, the exact enum, all six new indexes ready and valid, the protected table still empty, and the temporary zero-row constraint absent. No provider/environment setting, email, payment, merge, deployment, or other provider action occurred during the migration gate.
- PR #200 then merged as exact main head `94c05b827eb2511659994cdae00cfcb99f909d77`, and the Layer A runtime was deployed. Production now has all 46 migrations current and the membership writer enabled.

## 2026-09-04 — Layer B review closure

- Local CodeRabbit CLI review at runtime commit `49d2c1fd` returned three trivial suggestions. The distinct inner-signal and visibility-harness items were verified and fixed test-only in `1b2d1c6d`; a speculative CI-lane move was rejected because the latest hosted evidence still identifies lane 3 as the critical path and exact-head hosted timing does not yet exist. The user clarified that the zero-comments requirement applies to hosted GitHub review, so the local CLI loop is closed. Independent whole-range reviews approved the corrected committed Layer B tree with no actionable findings; documentation closure produced exact source head `6c32b8e767cca220f4c1aa4ffbff4290eaa94b0d`, while hosted GitHub PR review remained a later pre-merge gate.

## 2026-09-03 — Anatomime traffic hardening reconciliation

- Replayed the 23 approved Layer B source commits in their original order plus the local reconciliation commit, then rebuilt the local topology so accepted Layer A `6eaa22b45ba5090f0e926cff80f47e175454a2d8` is the actual ancestor of `codex/family-friends-06-anatomime-traffic-r2`. The verified rebuilt pre-correction tree is identical to topology source `068e6b20e14be77eb7c5096436d7a147e61cf725`. The reconciliation preserves current-main navigation/loading, identity, membership, billing, music, Admin behavior, and every accepted Layer A safeguard while adding only the approved Anatomime room-create/join limits, joined-player realtime-token proof, bounded HMAC-only poll shedding, durable bogus/unjoined protection, one-room-read polling, 15-second presence coalescing, status-aware cadence/backoff, accessible terminal recovery, and provider-intercepted browser coverage. Public room selectors normalize into one six-character uppercase alphanumeric namespace, and both join-validation phases use one pure snapshot-and-clock admission resolver while retaining authoritative transactional revalidation. Join and realtime entry points now consume network-only ingress before auth or attacker-selected room work; their room-scoped stages run only after bounded validation proves the room exists. A signed-in candidate can claim an unlinked guest player only with its current opaque guest token; account assignment and credential rotation happen in the same write, same-account rows retain priority, rows owned by another account cannot be reassigned, and a write-time row predicate lets only one concurrent token holder claim the guest while room-user uniqueness still fails conflicting account mappings closed. The same predicate protects anonymous same-token rotation: one null-owner request commits, and the loser plus both stale credentials fail generically without another touch or publication. Polls atomically consume one HMAC-only 300/network/10s ingress key before room lookup while treating any existing 150/network+room and 300/room entries as peek-only; rotating nonexistent selectors cannot allocate room keys. This 4,096-entry local boundary is best-effort per warm runtime, not deployment-wide. Shedder initialization uses the canonical configured auth-secret resolver, including its `NEXTAUTH_SECRET` fallback. Failure stays fail closed and emits one fixed structured privacy-safe error diagnostic per runtime without caught-error or request data. After allowed ingress, one loaded snapshot supplies authoritative viewer classification, a pre-resolution quota guard, presence resolution, and the final summary; the joined network-plus-room, room, and player counters increment all together or not at all. Ordinary accepted polls perform no second room query. A post-rollback zero-row idle-expiry race may reread exactly once and trust the fresh graph only when it is already `EXPIRED` or its deadline is strictly later than the captured attempt time. A future extension's current players and host own access/presence; stale credentials cannot write presence, missing or non-`EXPIRED` still-overdue state stays a generic retryable `503`, and the guard/quota is not repeated. Ordinary failure delay retains bounded jitter and a 30-second cap at the terminal step, while server-directed `Retry-After` polling cooldowns are honored up to 10 minutes and use a truthful generic automatic-refresh status instead of a frozen numeric countdown. Create and join manual actions preserve a usable positive integer `Retry-After`; any missing or unusable header gets the shared 10-second cooldown, and expiry only unlocks the control without replaying the request. A delayed create-countdown effect immediately synchronizes its display clock and allocates no interval after expiry. Their transport and response-body parsing share a 20-second deadline; a stalled transport or successful JSON body clears pending state into fixed ambiguous-outcome guidance and the same manual-only cooldown. These writes are not idempotent, so the guidance warns that an explicit retry can create another room or guest if the first request committed without a confirmed response. Team-only local state changes retain an active server cooldown and do not restart polling or realtime-token work when the credential primitives are unchanged. Realtime setup has a distinct 10-second overall deadline across token transport, successful token JSON consumption, and shared Ably-script readiness; failure leaves polling active without replaying token work, while cancellation removes only that waiter's listeners and leaves the shared script reusable. Room-snapshot transport and successful JSON consumption share a 1,500-millisecond client deadline; its internal timeout becomes a retryable failure without aborting the caller-owned sequencing signal. A delayed first room lookup has an accessible `Loading shared game…` status without making ordinary background polls noisy; a failed first lookup retains its accessible feedback and restores the room-code entry panel. Room creation preserves anonymous behavior only for nullish account identity while present malformed identity fails closed through Layer A validation before protected persistence.
- Layer B owns no Prisma schema or migration. It moves the existing 30/network/15m and 100/network/24h join policies to `ANATOMIME_ROOM_JOIN_INGRESS` without changing their IDs or thresholds and adds only `anatomime.realtime-token.network.10m.v1` at 120/network/10m, making the exact closed registry 37 policies. At that source-head checkpoint, the repository had 46 migration directories, all 45 baseline migrations were current in Production, and Layer A's additive `20260831120000_operational_rate_limit_bucket` was still pending before the combined runtime could serve. That migration was later applied; current main and Production now have all 46 migrations current. Fresh corrected-head evidence passes the exact 174/174 focused Anatomime matrix, 135/135 Layer A regression matrix, 32/32 adjacent request-identity and Browser-QA registration tests, Prisma validation/generation, TypeScript, lint, the 115-page Production build, the 115-page Browser-QA build, and schema/migration and diff checks. A separate generic client-fetch matrix passes 10/10, including all-status error preservation, caller cancellation, and transport/body deadlines. Fresh exact-head full intercepted Anatomime Browser QA coverage reports 42/42 desktop/mobile cases ok in one post-fix run on an isolated Playwright-owned port; after all cases reported, the command entered the known post-worker teardown hang and was stopped, its exact owned process ended, and its port was confirmed closed, so a clean process exit is not claimed. On the byte-identical topology-source tree, the complete Windows suite reported 3,988 total, 3,976 passed, nine established AtmoShaper line-ending fixture failures, and three intentional skips; that broader receipt remains prior-tree evidence rather than a corrected-head run. A fresh independent review of the corrected exact head was then still open before this boundary could base Layer C; the following day's review-closure entry records its approval.
- No database connection, schema or migration change, Ably or other provider action, email, payment, environment change, deployment, push, merge, or Production action occurred during the replay.

## 2026-09-02 — Operational limiter and email-ceiling foundation reconciliation

- Replayed the approved Layer A source sequence onto the then-current main in `codex/family-friends-05-abuse-cost-foundation-r2`. Source commits `d4a78647`, `2974bbf2`, and `2fcea1ab` add the closed policy contract and atomic Serializable operational-limiter owner; `451c6f1a` wires the shared SMTP boundary to mandatory `PUBLIC_AUTH` or `SECURITY` classification. Public-auth messages consume both 70/global/fixed-24h and 90-total/global/fixed-24h limits, while security/account-change messages consume only the total 90 limit. Unconfigured SMTP consumes no quota, denied or unavailable quota constructs no transporter, and provider failure or timeout after an allowed decision remains charged.
- Registration no longer reuses the security-only account-change sender; the dedicated existing-account registration notice remains enumeration-safe. Verification, verification resend, reset, password setup, and that notice are public-auth mail, while account changes and Admin notifications remain security mail. Current-main identity account/network limits and the post-response registration boundary remain intact and supplement the deployment-wide backstop.
- Admin email delivery no longer keeps an interactive Prisma transaction open across either the operational-limiter transaction or SMTP. Initial and retry paths commit a five-minute hash-only claim, send outside the transaction, and finalize only the exact random-token hash. Retry keys retain permanent append-only hash-to-intent ownership; regenerated Activity keys can recover an expired ambiguous claim without releasing older keys for another intent. Disposable browser-QA cleanup removes only its own retry-key children before parent intents; Production ownership remains restrictive and append-only.
- The repository now has 46 migration directories. A separately authorized deployment of the then-current main `06a730fcc6b7ed54f91e7d6330c023f9e06262c8` verified all 45 baseline migrations current in Production and membership webhook writes enabled with the pause flag at `0`. At this historical checkpoint, the new `20260831120000_operational_rate_limit_bucket` migration was still pending for this layer; it is additive, creates the operational bucket enum/table/indexes, adds three nullable claim fields to `AdminEmailIntent`, and creates `AdminEmailRetryOperationKey` with unique hash ownership and a restrictive intent relation. It required separate authorization and later verified as the 46th current migration before the merged Layer A runtime was deployed, as recorded in the September 4 entry. This reconciliation did not connect to a database, apply that migration, send SMTP mail, change provider or environment settings, deploy, push, merge, charge a payment, or perform another Production action.
- Fresh local reconciliation evidence passes Prisma validation/generation, the exact 133/133 focused operational-limiter/mail/registration/Admin matrix, TypeScript, and base-to-head diff checks. No full repository suite, lint, build, Browser QA, hosted Linux result, database-backed proof, or live/provider behavior is claimed by this focused task.

## 2026-09-01 — Combined migration-order correction

- Corrected the combined rollout inventory to 45 migration directories and one exact five-migration pre-runtime order: `20260828120000_identity_method_safety`; `20260828121000_identity_normalized_email_index`; `20260828130000_membership_subscription_convergence`; `20260901100000_auth_method_intent_two_factor_purposes`; then `20260901101000_auth_method_intent_registration_callback`. The fourth migration adds the three two-factor `AuthMethodIntentPurpose` values, and the fifth adds the bounded registration `callbackPath` column. All five must verify current, with no extras or failure, before the first bridge-capable deployment. All five remain unapplied locally; no database migration, push, deployment, or live-state change is claimed.
- Tightened the legacy limiter retirement gate. Before any `AuthAttempt` cleanup, read-only evidence must map each immutable Production deployment ID to its full Git SHA and prove separately for `POST /api/account/register`, `POST /api/auth/callback/credentials`, and `POST /api/account/password-reset/request` that zero pre-bridge deployments receive or start requests after alias cutover and zero pre-bridge executions remain at the drain boundary. Aggregate or otherwise ambiguous route/deployment coverage forbids cleanup.

## 2026-08-29 — Bootstrap and public-pricing cost hardening evidence

- Completed the signed-in bootstrap and public-pricing hardening plan introduced at `e4438ad7` on `codex/family-friends-feedback`. One server projection now owns shell preferences and practice membership (`7963a630`, `c9f5458d`); Settings and Music reuse that sanitized owner-keyed context (`11e26bb1`). Therapist local hydration remains immediate, but its cloud GET is lazy and deduplicated until a first actual consumer; zero-practice users skip the calendar endpoint while practice members retain it (`4e48f93b`, `c4e80d1b`). Background commerce is owner-keyed and intent-driven (`c0aaed96`, `61cf511b`). The public display catalog only is shared through one process-local pricing owner (`b741583d`, `51a45ab0`) with a five-minute complete TTL and fifteen-second incomplete/fallback TTL. A real local Browser-QA RSC request measured two underlying auth loader entries before the request-scoped React wrapper, so the wrapper was installed; final local proof measured exactly one, while the ordinary Production proof route returned HTTP `404` (`12afc169`, `5aa5a77d`). No persistent or module-TTL session or user cache was introduced, and this local counter is not Production platform evidence.
- The historical ordinary non-practice shell workload, scoped to no pending guest-cart merge and no active commerce consumer, was measured from an unreproducible dirty Task 9 working-tree snapshot based on runtime SHA `706c52167466f984f3e405986af11ff3d2343a02`: one auth snapshot, one user-graph read, one temporary-grant read, one entitlement build, one preference read, one practice-role read, four logical ORM operations, zero client bootstrap endpoints, and zero ordinary commerce snapshots. Public pricing's concurrent cold callers shared six logical Price reads and a warm caller made zero; each read was bounded by a 2.5-second timeout and one SDK network retry. Checkout, Portal, entitlements, customers, and webhooks remain uncached, current, server-owned authority. Local-first PHI boundaries are unchanged.
- That dirty Task 9 snapshot historically measured 178/178 focused Node tests, Prisma validate/generate, typecheck, lint, both builds, diff checks, and a fresh scoped combined Browser-QA run with 127 Browser-QA passes, 37 documented authorization-gated skips, and zero failures. The skips were not passes. All 21/21 local route samples across seven fixed routes with three samples each returned HTTP `200`; local timing `first` is not platform cold evidence. The uncommitted Task 9 patch identity was not recorded, so the workload, focused-test, Browser-QA, local validation, build, and timing receipts are not reproducible from runtime SHA `706c52167466f984f3e405986af11ff3d2343a02` alone and remain historical measurements rather than exact-head proof. Later commit `f139df0e941e72c87c944ca648c1f021c28ae6ea` owns the evidence/report only; it does not identify or reconstruct the dirty measured patch. The complete Windows Node result remained 3,613 passed, 10 established fixture/line-ending failures, and 3 intentionally skipped across 3,626 tests, so the full suite is not green and hosted Linux remains a release gate.
- `MASSAGELAB_PUBLIC_REGISTRATION_PAUSED` and `MASSAGELAB_SUPPORTER_CHECKOUT_PAUSED` remain independent, default-open, exact-lowercase-`true` controls. Registration pause preserves existing email/password login, existing-account Google sign-in, verification, reset, and recovery; Checkout pause preserves existing subscriptions, feature-key entitlements, membership status, and Portal access. Live Stripe, provider/readback, private database rows, OAuth/mail, deployment, push, merge, and Production actions were `NOT RUN`.

## 2026-08-29 — Server path and family launch cost controls

- Completed the bounded server-cost implementation stack on `codex/family-friends-feedback`. Task 1 commits `4269d7c87b9b6b4309bbb654de86c106dfd1b708` and `bbfabfb1d189e5df3bd0af703daa54c4431b8bef` add the allowlisted anonymous timing harness, fresh-build receipt, dependency-call workload receipt, per-request deadlines, and exact owned-server teardown. Task 2 commit `4be9d6bfcc88475280c8a561ec9ca594ee141051` removes background-credit repair from ordinary verified auth refresh and preserves provisioning at verified account-creation boundaries. Task 3 commit `90e231a2ab78c23701945edb208421c3bdf9cadc` carries computed feature keys through JWT/session state so signed-in sidebar navigation does not reload membership entitlements. Task 4 commit `dcb061b783fdaefd382debf3a9c95ce6c9535d66` adds independent server-enforced registration and new-Supporter-Checkout pauses; `f3b20ab10a3730355c28faa4a86f220710a2a057` preserves Google login for existing users during registration pause, and `606320d78fa20da793c30bb341f447921d768c0b` restores the adjacent donation test harness. Deployment and release operations were documented in `ba9c38f0d8406222e4c1ed7c032d26f9eb55f85d`.
- The exact source/dependency receipt records verified-auth background-credit provisioner calls `1 -> 0` and signed-in-sidebar membership entitlement loads `1 -> 0`. Membership status remains persisted-summary loads `1 -> 1` and Stripe calls `0 -> 0`; valid explicit Checkout remains Checkout-session creates `1 -> 1`, while the tested launch/pricing ordinary-render slice has Checkout-session creates `0 -> 0`; valid explicit Portal remains Portal-session creates `1 -> 1`, while that render slice has Portal-session creates `0 -> 0`. Calendar context remains deferred behind its authenticated endpoint. This workload does not prove site-wide absence of Stripe or email calls; Admin Billing preview is outside this row, and provider-wide/email render verification is `NOT RUN`.
- The local baseline application timing receipt used exact head `383d66db2d95f7bc781d8db591a83acfef9cf985`, returned HTTP `200` for all 21/21 route samples, and tore down its owned port cleanly. `first` means the first measured sample for that route after the fresh server passed readiness; later samples for the route are `warm`. It does not prove a provider or platform cold start. The final local timing comparison and the separate deployed cold/warm aggregate are `NOT RUN`, so no before/after duration or platform-latency improvement is claimed.
- `MASSAGELAB_PUBLIC_REGISTRATION_PAUSED` and `MASSAGELAB_SUPPORTER_CHECKOUT_PAUSED` are independent, default open when absent, and pause only on exact lowercase `true`. Registration pause blocks only new account creation and preserves existing email/password login, existing-account Google sign-in, verification, password reset, and account recovery. Checkout pause blocks only new Supporter Checkout and preserves existing subscriptions, feature-key entitlements, membership status, and billing Portal access. The server is authoritative; client copy and disabled controls are explanatory.
- Read-only Neon pooled-host/connection/compute/transfer, Vercel exact-deploy/usage/error/WAF/latency, SMTP delivery-health, Stripe webhook-failure, R2 cache-header/operation, and Sentry quota/privacy reviews are `NOT RUN`; no identifiers or secrets were read. A clean full Browser-QA exit, hosted Linux results, and separately authorized disposable fingerprint-bound private browser rows remain open. No database connection or migration, provider/environment setting, Google OAuth publication or real-account proof, SMTP send, Stripe Checkout/payment/event/replay/refund/cancellation/Portal action, R2 mutation, Sentry mutation, deployment, push, merge, or Production action occurred.

## 2026-08-29 — Navigation and action feedback Task 4 acceptance

- Completed the Task 4 viewport/accessibility contract on `codex/family-friends-feedback`. Strict RED failed because the interaction browser spec had no explicit desktop 1280×900 contract; the broader source audit also found the planned mobile portrait, compact landscape, enlarged-text, keyboard-focus, reduced-motion, overflow, focus-ownership, pointer-inert, control-coverage, and live-status markers absent. GREEN adds direct built-app coverage for desktop 1280×900, mobile 390×844 at 200% root text, and 844×390 compact landscape with `prefers-reduced-motion: reduce`, plus a focused helper that records transient route feedback without extending production timeouts.
- The reviewed browser receipt proves real Tab/Enter activation and focus-visible state before activation; a capture-phase `focusin` listener observes no focus entering feedback and is removed with its MutationObserver; the top route indicator is visible, in-viewport, nonzero, nontransparent, and pointer-inert; exactly one `Loading page` status appears during the Link-live to root-visual-only handoff; the real Loader canvas changes composited pixels when hidden/restored and two visible rendered samples 400ms apart are byte-identical under reduced motion; document/body widths do not exceed the viewport; app-bar and music-toolbar control centers remain hittable; and the existing music toolbar DOM node survives two held AppToolLink journeys. No claim about WebGL animation-frame scheduling is inferred from the browser screenshots. No root provider/pathname key, global click/history interception, Chimer navigation guard, custom timer navigation, MetalAttentionRing geometry, auth/billing authority, payment authority, or provider/database behavior changed.
- Exact lane verification passes 21/21 and retains 14 ordinary specs, 28 desktop/mobile project-spec assignments, and four nonempty lanes. After the independently reviewed Task 1 held-navigation repair `eb3690791c0fdeb5db2c089102dc88179b9f6104`, the focused Node matrix passes 50/50. A fresh post-repair 114-page Browser-QA build passes, then the complete `interaction-feedback.spec.ts` passes 14 public cases with 3 explicit private authorization-gated skips under `desktop-chromium` and the same 14/3 under `mobile-chromium`, both with clean exits. Task 4 also remained 28 public passes plus 6 gated skips inside the earlier unfiltered one-worker run.
- The complete local gate is not green. Prisma validate/generate, TypeScript, lint, the ordinary 114-page build, diff checks, and the fresh Browser-QA build pass. The complete 3,374-test Windows suite reports 3,361 pass, 10 known host-line-ending failures, and 3 intentional skips: nine AtmoShaper CRLF fixture-hash failures and one auth-migration exact-newline failure. The full parallel Browser-QA run reached about 699/721 and stalled without final totals. Its approved exact one-worker rerun reached 711/721 and produced 39 failure artifacts outside Task 4—app-shell 7, control-system-review 20, public identity 6, public membership return 2, background commerce 1, public routes 2, and WebKit music media 1—then stalled after a completed test with the Playwright worker still alive but no browser descendant. Only the exact owned runner was stopped. No full Browser-QA pass or release approval is claimed.
- The full Node gate exposed test-contract drift owned by earlier tasks, not product defects. Task 1 commit `44da2a3ae2e4140b7a71b611b279b170e3e549c3` refreshed two composed `AppToolLink` assertions and the intake `usePendingNavigation` assertion; Task 3 commit `9fba6a6ea52faeaef994ab871f60cd600ef8bffb` restored the compiled Supporter form fixture's `PendingSubmissionForm` dependency. Their repaired direct matrix passes 43/43. No database, account, OAuth, email, Stripe/payment/refund/cancellation, provider, private fixture, deployment, push, merge, or Production mutation occurred.

## 2026-08-29 — Final membership convergence review fixes

- The current subscription runtime head is `6838ff574695b23d206d2272da241d30bed91507`. Fix A, `1f0250f311ca28f85756a24135e8ef0e03f788bd`, added the exact-`1` `MASSAGELAB_MEMBERSHIP_WEBHOOK_WRITES_PAUSED` bridge: after signature verification, membership-purpose Checkout completion and all five membership subscription events return private retryable `503` before provider, membership database, or cache work, while background commerce and non-membership Checkout routing remain unchanged. Missing, exact `0`, and every value other than exact `1` retain ordinary convergence writes. Fix B, `6838ff574695b23d206d2272da241d30bed91507`, normalized conflicting Account return outcomes to one controller/notice owner without changing persisted membership authority.
- A fresh exact-runtime-head focused rerun of `tests/membership-webhook-route.test.mjs`, `tests/account-page-tabs.test.mjs`, and `tests/billing-portal-route.test.mjs` passed 62/62. This covers the bridge, Fix B return normalization, background-routing non-regression, and the focused Portal's exact active-or-trialing subscription predicate. The last complete Task 5 suite, Prisma/type/lint gate, ordinary 114-page build, and Browser-QA attempt remain the predecessor-head evidence recorded below at `04881a884d6e5cce656e1ff939bd3b998c67bc18`; none was rerun after Fix A/B, so no final full-suite or exact-runtime-head release pass is claimed.
- At the time of this 2026-08-29 entry, Production rollout required the then-current three-migration order recorded below: zero-collision normalized-email preflight; `20260828120000_identity_method_safety`; monitored `20260828121000_identity_normalized_email_index` with its `CREATE UNIQUE INDEX CONCURRENTLY` stop/recovery boundary; then `20260828130000_membership_subscription_convergence`. The 2026-09-01 correction above is the sole current migration inventory and pre-runtime rule. Under the then-current rollout, only after those three migrations verified with no extras or failure could the two separately authorized runtime deployments begin. The first had to serve this bridge-capable runtime on the Production alias with `MASSAGELAB_MEMBERSHIP_WEBHOOK_WRITES_PAUSED` exactly `1`; record the configured maximum invocation lifetime, wait at least that long after alias cutover, and prove no pre-bridge SHA was receiving or executing webhook requests. Stripe owned retries during this bounded pause, membership access could remain in processing guidance, and operators could not manually replay or acknowledge held deliveries. Only after drain proof could the same bridge-capable runtime deploy again with the flag absent or exactly `0`; held deliveries then had to reach `2xx` and terminal receipts, and persisted access had to converge.
- After the first bridge cutover, rollback is permitted only to a bridge-capable deployment with the flag exactly `1`; never restore a pre-bridge legacy writer. Keep the additive schema in place with no destructive migration rollback. Retain the bridge through held-delivery/backlog proof and the rollout window; removing it is a separately reviewed retirement after evidence proves it is no longer needed.
- No live or test Stripe/payment/provider action, Checkout, event emission, delivery replay, refund, cancellation, Portal/provider-setting change, database connection, migration application, private row or fixture, environment mutation, deployment, push, or merge occurred in these review fixes.

## 2026-08-29 — Initial subscription entitlement convergence implementation and Task 5 evidence

- The initial reviewed subscription convergence package reached predecessor head `04881a884d6e5cce656e1ff939bd3b998c67bc18`. The combined branch had 43 migration directories. Its wholly pending order was: zero-collision normalized-email preflight; `20260828120000_identity_method_safety`; monitored `20260828121000_identity_normalized_email_index` with its `CREATE UNIQUE INDEX CONCURRENTLY` stop/recovery boundary; then `20260828130000_membership_subscription_convergence`. The additive membership migration creates the unique `(provider, providerEventId)` `MembershipWebhookReceipt`, its bounded audit/retry metadata and indexes, and three nullable `MembershipSubscription` watermarks. It performs no backfill or existing-row rewrite and stores no raw provider payload, address, payment method, secret, or token. Its historical expansion compatibility with the runtime deployed at that time is not current rollback authority: after the bridge cutover, only a bridge-capable exact-`1` deployment may be used. None of the three migrations was applied by this work.
- Routed membership-purpose Checkout completion and all five signed subscription events through one service after raw-body signature verification. A completed receipt or same-event duplicate is acknowledged without a second state change; an event already proven stale is marked ignored without provider retrieval and cannot overwrite current state; an unambiguous newer event may apply; and equal-time, legacy null-watermark, Checkout-completion, and every different event not already proven stale after an authoritative provider read reconcile through current Stripe retrieval outside database transactions. The short follow-up transaction compares the captured provider-event watermarks and the local authoritative-read marker only with their same-clock counterparts, so a local clock is never ordered against Stripe's event clock.
- Made unfinished receipt ownership explicitly retryable. After an envelope can identify or create a receipt, provider, price, ownership, later malformed-event, or concurrent failure leaves the receipt `RECEIVED` with only an allowlisted failure code. A malformed envelope may reject before any receipt is identified or created, but it still grants nothing. In either case, because there is no durable background worker, the signed webhook returns private non-2xx/retry—`503 { received: false, retry: true }` for the retryable membership error—and Stripe retains retry ownership. Exact concurrent receipt creation retries once from a fresh transaction only for legacy Prisma `P2002` metadata naming the `MembershipWebhookReceipt` model plus its exact ordered target, or for installed PrismaNeon `P2002` / PostgreSQL `23505` / `UniqueConstraintViolation` metadata naming the exact ordered fields or connector-safe index. Wrong code, model, index, order, fields, mixed constraint metadata, and malformed shapes pass through without misclassification.
- Retired both bypass writers, `recordCheckoutSessionCompleted` and `upsertMembershipSubscriptionFromStripe`, in the same route cutover; runtime source scans find no caller or export. Cache invalidation occurs only after a committed `changed: true` membership result. Price mapping, Automatic Tax gates, Checkout serialization/reuse and idempotency, background-commerce receipt/fulfillment/refund/dispute routing, and the pinned combined webhook contract remain unchanged. Portal creation still loads the signed-in user's persisted Stripe Customer and, for focused changes, selects only a persisted active or trialing subscription before creating a customer Portal session. Duplicate and already-proven-stale membership events short-circuit; provider retrieval is limited to Checkout completion, ambiguous or legacy events, and every different event not already proven stale after an authoritative read. The membership Account-return render and membership-status endpoint do not construct or call Stripe.
- Added identifier-free Checkout and Portal return URLs plus an authenticated `private, no-store` database-only membership projection. It exposes only state, paid level, persisted feature keys, safe subscription status/period fields, database revision, and Portal availability. Checkout performs up to five reads/attempts at incremental waits `[0,1000,2000,4000,8000]`, with all five used on exhaustion for the preserved 15-second wait schedule; each attempt bounds fetch plus JSON consumption to 1.5 seconds. Active access can settle immediately, while billing-attention/no-active results require a changed revision after the run's baseline so an older terminal row cannot be mislabeled as the new payment result. Portal shows current persisted state immediately while watching the same bounded revision window. Exhaustion offers status retry only, and `premium_backgrounds` access derives from persisted feature keys rather than a plan label, provider ID, Session parameter, or redirect.
- Fresh local Task 5 evidence: `npm run prisma:validate`, `npm run prisma:generate` with Prisma Client 7.9.1, `npm run typecheck`, `npm run lint`, the complete 379/379 focused membership/Stripe/background/return/CI-lane matrix, the corrected `tests/user-facing-copy.test.mjs` contract at 1/1, `git diff --check`, and the ordinary Production build all pass; the build generated 114 pages including `/api/billing/membership-status`. Lint emitted only Babel's size/deoptimization note for unchanged `app/chimer/running-timer.tsx`.
- The fresh Windows full suite reports 3,342 total, 3,329 pass, 10 fail, and 3 intentional skips. All ten failures are the known host line-ending baseline: nine established CRLF working-tree checksum failures for the two immutable Atmoshaper JSON fixtures, whose committed blobs still equal pinned SHA-256 `0da9ad1dd4b184b059624af11963adbd2d85d4ad6c197b83691af6d58cd70dc0` and `d370788a6ef9af7f147c0dcafda18285b759fbca24249b82224b2c16ba844486`, plus `tests/auth-schema-migration.test.mjs` reading the committed normalized-email index migration as CRLF instead of exact LF. The former branch-related user-facing-copy failure is corrected at head `04881a884d6e5cce656e1ff939bd3b998c67bc18`; no branch-related test failure remains. Hosted Linux remains required for the fixture/newline boundary.
- A fresh `npm run build:browser-qa` reached only Next's Inter request and failed because restricted local network access could not fetch `https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap` from `app/layout.tsx`. No stale Browser-QA artifact, alternate host, or policy workaround was used. Therefore the requested desktop/mobile `membership-return-status.spec.ts` runs were not started. Its database-backed rows remain skipped/unrun absent separate authorization for the exact disposable target/fingerprint and membership migration; public rendered, keyboard/focus, phone portrait/landscape, 200% text, reduced-motion, and screen-reader evidence remain open.
- Remaining release work at the time included fresh hosted Linux and Browser QA; exact-target authorization and application of the then-current three reviewed migrations in their documented order; the two-deployment paused-bridge/drain/unpaused-bridge protocol above; exact deployed-commit plus migration-status proof; read-only live Stripe Price/tax/webhook/Portal configuration checks; and any separately authorized provider/live behavior still required by the family-and-friends gate. See the 2026-09-01 correction above for the current migration inventory and pre-runtime rule. Historical billing smokes were not repeated and are not counted as exact-head evidence. No live or test Stripe/payment/provider action, Checkout, synthetic or real event, refund, cancellation, Portal/provider-setting change, database connection, migration application, private database row or fixture, deployment, push, or merge occurred.

## 2026-08-29 — Final identity safety review remediation

- Closed the final six review blockers in three logical commits: runtime and race safety `39317f958413e4238affb6eb4d52c544e56f7e12`, bounded security-notice retry `70c372739e2e81d8c5a578724c2b57db8db65ca4`, and migration/runbook correction `404598c00d3d8834ca50db6aac9d0ff58e3fd7af`. Fix Round 1 runtime commit `0b9af8aa970b72aad03e9cb4553615a5de82202c` closes the remaining installed-Neon functional-index metadata gap.
- Added one narrow identity-constraint owner for both Prisma metadata families. Legacy recovery requires the exact `User` email or `Account` provider tuple, while PrismaNeon recovery requires outer `P2002`, PostgreSQL `23505`, `UniqueConstraintViolation`, and an approved real index name or exact ordered field tuple. The installed adapter parses PostgreSQL DETAIL `Key (lower(btrim(email)))=...` only through the first closing parenthesis and emits `fields: ["lower(btrim(email"]`; that exact value is now allowlisted only for the User normalized-email owner. Case, whitespace, prefix/suffix, arbitrary expression, multi-field, Account-only, undefined, wrong-model, wrong-code, and mixed metadata still fail rather than being mistaken for a race.
- Moved all account-existence-dependent registration and reset work behind one injected post-response scheduling boundary after the identical ACCOUNT+NETWORK limiter decision. Accepted valid requests return neutral `202` after scheduling exactly one task; captured-work tests prove lookup, Argon2, token writes/pruning, transactions, and mail do not begin before that task runs. The task owns state-specific work, retains recoverable committed state after provider failure, and reduces unexpected failures to a fixed safe diagnostic.
- Changed password ADD/CHANGE so raw `newPassword` exists only at the narrow route/service boundary. CHANGE completes current-password/2FA/limiter proof before one injected hash outside the transaction and rechecks `authSessionVersion` inside it. ADD requires cookie-bound fresh Google reauthentication before the same one-hash boundary, then revalidates and consumes the intent by CAS in the transaction. Invalid, rate-limited, expired, and lost-proof paths perform zero new-password hashes.
- Added dormant `auth:retry-security-notices`. Fingerprint mode parses the exact direct non-pooler target without connecting. A sending scan requires separate `AUTH_SECURITY_NOTICE_RETRY_DATABASE=1` and `AUTH_SECURITY_NOTICE_RETRY_SEND=1` gates, the exact 64-lowercase-hex fingerprint, explicit injected client and delivery owners, and `--max-rows=1..100`. It selects only bounded `PENDING`, `FAILED`, and expired `PROCESSING` rows, delegates claim/lease/CAS ownership, guarantees disconnect, and emits count/status summaries only. The command was not executed.
- Split normalized-email uniqueness into dedicated single-statement migration `20260828121000_identity_normalized_email_index` using PostgreSQL `CREATE UNIQUE INDEX CONCURRENTLY`; the branch then had 42 migration directories. At the time, the two identity migrations plus `20260828130000_membership_subscription_convergence` formed the three-migration pre-runtime set; see the 2026-09-01 correction above for the sole current migration inventory and rule. Runbooks required the count-only collision preflight, ordered expansion then concurrent-index build, monitoring, and a stop/recovery path for a possible invalid index. Neither identity migration was applied. The reset checklist then prohibited a second Admin evidence/action bundle while requiring the transactionally queued `PASSWORD_RECOVERED` notice and account-security delivery/retry ownership.
- Strict RED evidence covered 15 initial failures, two production-shaped Google race failures, one stale integration source-contract failure, one documentation contradiction, one multi-statement index-boundary failure, and two final sanitization/bounding failures. Fix Round 1 added an exact installed-adapter probe plus direct registration and Google race paths; RED was 34/37 with only those three new paths failing, and GREEN is 178/178 across the complete affected identity matrix. Prisma validation/generation, TypeScript, lint, `git diff --check`, and the 113-page Production build pass. Full Windows tests report 3,249 total, 3,237 pass, the nine established CRLF checksum failures, and three intentional skips; fresh committed-blob hashes still match the two pinned Atmoshaper fixture hashes.
- No database connection, target fingerprint disclosure, migration, notice retry, legacy cleanup, private fixture, OAuth/provider change, SMTP/mail, Browser QA, deployment, push, or merge occurred. Those actions remain behind their documented separate authorizations and hosted evidence gates.

## 2026-08-28 — Local identity and account-method safety verification

- Completed the local identity workstream at production-code head `097abae71094d1e56cb76acb4eb7f9d7a55c96e5`, then aligned three stale integration source-contract tests at `d965c293d25d88878961a7c61cc81a102bc94853`. The RED full suite exposed those three obsolete ownership expectations; focused GREEN is 25/25 and now pins password-reset token ownership in the shared service plus thin-route scheduling, excludes password-method changes from verified-user credit provisioning, and requires the private Google-intent POST, response validation, and forwarding of the returned server callback. Callback allowlisting is independently covered by `auth-method-intents` tests and source review.
- Reviewer Fix Round 1 found that stale limiter pruning selected safe ids but originally deleted by id alone. A deterministic RED reactivated and blocked one selected bucket before deletion and observed two deletions instead of one. Commit `5efdf388880a5ece06777592b06388c11302fe42` repeats the exact `updatedAt` and inactive-block predicates in `deleteMany`; focused GREEN preserves the reactivated bucket, deletes only the still-stale row, and reports the exact count. The reset source contract also again forbids account-security email intent ownership in both the shared request service and thin route.
- The branch contains 41 migration directories. `20260828120000_identity_method_safety` is an unapplied local expansion migration: it creates hashed limiter buckets, private auth-method intents, and durable security-email intents and adds `GOOGLE_INTENT` to the shared `AuthAttemptPurpose` enum for forward compatibility. The legacy `AuthAttempt` table's columns, indexes, and data remain untouched through the pre-bridge drain and bridge-capable rollback window. Task 2 atomically cut every new serving caller to `AuthRateLimitBucket`; pre-bridge instances may still write raw legacy keys until the exact-`1` paused bridge is serving and the configured-maximum-invocation-lifetime drain proves them gone. After bridge cutover, pre-bridge code is never a rollback target. Only after exact cutover/drain proof can `AuthAttempt` be called inactive state.
- Documented the mandatory count-only normalized-email collision preflight, expansion-before-runtime order, exact 15-minute limiter thresholds and bounded 24-hour stale-bucket cleanup, private Google intent/cookie and explicit matching-account confirmation contract, provider publishing/callback-origin gate, SMTP sender/SPF/DKIM/DMARC/bounce gate, and at-least-once duplicate-notice runbook. Read-only legacy target fingerprinting and `--max-rows=1..100` cleanup remain separate later actions requiring exact production fingerprint and mutation-scope authorization; evidence may contain counts only. No cleanup or future `AuthAttempt` drop occurred.
- Fresh local evidence: Prisma validation and generation, TypeScript, lint, the 71/71 focused limiter/migration/cleanup/intent/callback/reset/integration suite, the 113-page Production build, and diff checks pass. The focused total includes 16 `auth-method-intents` cases that independently cover callback rebuilding/allowlisting. Full Windows tests report 3,228 total, 3,216 pass, the nine established CRLF checksum failures for the two immutable Atmoshaper JSON fixtures, and three intentional skips. Their HEAD blobs exactly match pinned SHA-256 `0da9ad1dd4b184b059624af11963adbd2d85d4ad6c197b83691af6d58cd70dc0` and `d370788a6ef9af7f147c0dcafda18285b759fbca24249b82224b2c16ba844486`; hosted Linux remains required.
- The local Browser QA build reached only the restricted Google Fonts request and failed to fetch Inter from `fonts.googleapis.com`; no broader network/provider access was requested. Restoring the ordinary Production build let desktop/mobile enumerate the identity spec, but each project's three public rows lacked the Google-enabled control because the QA artifact/config was unavailable, and each project's six private rows hard-skipped before database work. Both runners were stopped. Source scans confirm dangerous linking is absent, private proofs/tokens stay out of owned URLs/storage/logs/UI, limiter persistence is hashed, runtime code does not read/write legacy `AuthAttempt`, the bounded cleanup script is its sole operational mutator, Account provider ids are unique, method removal preserves a credential, and no production billing/entitlement path changed.
- No database URL, fingerprint, or config value was read or printed. No database connection, collision preflight, migration application, cleanup, private fixture, Google/provider setting change, real OAuth, SMTP/provider mail, Vercel/Neon/Stripe action, deployment, push, merge, or private browser proof occurred. All remain pending behind the documented exact authorization and evidence gates, including future legacy-table drop.

## 2026-08-28 — Anonymous operational Sentry provider proof

- Completed the authorized provider and exact-preview proof for PR #186 at runtime commit `77ec81e2db77faaa3fd6694ce62075761e4513db`. Server-side data scrubbing, default scrubbers, IP-storage prevention, geographic/server-name advanced scrubbers, and disabled public issue sharing were saved or read back. The provider did not expose an exact retention duration through the connected organization/project settings readback or visible account UI, so the audit records retention as unverified instead of inventing a duration.
- Added Sentry's documented null-IP opt-out marker after deleting every supplied user value and retained the separate `includeServerName: false` boundary. The marker contains no identity value and prevents provider-side transport-IP location inference. Focused privacy coverage passes `41/41`; TypeScript, lint, and `git diff --check` also pass for this correction.
- One authorized enum-only diagnostic reached the exact preview. Sentry retained only the bounded report tags/context, coarse `/timer` and `/api/[route]` families, exact release/environment, and event-scoped correlation. It retained no user/account/session/IP value, location, server name, full URL, query, body, header, cookie, breadcrumb, Replay, or attachment; affected-user count was zero, provider geo was empty, and `[Filtered]` was accepted as safer message over-filtering. Vercel recorded exactly one application `POST /api/support/problem-report` with status `200`.
- This work adds no product analytics, pageviews, funnels, background impressions or selections, dwell time, user journeys, retention measurement, standard Sentry User Feedback, Replay, screenshots, attachments, Logs, identity collection, analytics cookie, or consent popup.

## 2026-08-27 — Clock subscription access hydration after login

- Traced the premium-background lock after login to Clock's account-sync transport boundary. Both `/api/auth/session` and `/api/account/preferences` used the generic 1.5-second client timeout even though the session callback and entitlement response can require cold database-backed work. A successful but slower response was therefore aborted and the fresh Clock mount correctly remained fail-closed until Account navigation or a lifecycle retry warmed and repeated the same reads.
- Kept the existing canonical session/preferences entitlement owner and focus, online, page-show, and visibility retries. Only Clock's two initial account-access reads now use a documented 10-second deadline spanning fetch plus successful JSON-body consumption; a stalled body aborts and releases the coalesced retry owner. The generic fetch default, preference writes, entitlement computation, sign-out behavior, and fail-closed policy are unchanged.
- Added strict RED/GREEN browser coverage for a newly signed-in Clock with a two-second cold session response and a separate two-second cold subscription response. Both tests failed against the 1.5-second boundary before the fix. After the repair, both pass without Account navigation, the cold-session case proves preferences never begin before session completion, and the pre-existing returning-tab failure/retry regression remains green. A focused transport regression proves a stalled successful JSON body times out under the same request deadline. Focused account-preference/Clock tests, Prisma validation, TypeScript, lint, `git diff --check`, and the 109-page Production build pass.

## 2026-08-27 — Production auth recovery and migration promotion gate

- Diagnosed the live Google sign-in `Configuration` error from Vercel runtime evidence. OAuth reached `/api/auth/callback/google`, then Auth.js's Prisma adapter failed because deployed code selected `User.authSessionVersion` from a Production schema that did not contain it. The failure was a schema mismatch rather than an OAuth credential or account-specific rejection.
- Traced the drift to four committed but unapplied Admin User Operations migrations from merged PRs #173, #175, #178, and #179. PR #186 remains an unrelated draft Sentry-privacy change. CI validated the Prisma schema and Vercel built the application, but neither path applies Production migrations; the separately documented deploy step was not completed.
- After explicit authorization, applied `20260808090000_admin_authorization_audit_foundation`, `20260808093000_admin_jwt_session_version`, `20260808100000_admin_temporary_feature_access`, and `20260808110000_admin_billing_goodwill` through the direct Neon maintenance connection. Prisma reports all 40 migrations current. The required column and six expected tables exist, no partial object preceded the deploy, and the migration transformations completed without retaining database-row details in repository documentation.
- Added a fail-closed Production promotion check before Prisma client generation. It runs only when `VERCEL_ENV=production`, requires `DIRECT_URL` or `DATABASE_URL_UNPOOLED`, and executes read-only `prisma migrate status`. Preview, CI, and ordinary local builds skip without database access. Missing configuration, execution failure, or pending migrations fails the new Production build without applying or resolving migrations, preserving the separately authorized maintenance boundary.
- Verified the gate with seven focused tests, Prisma validation, TypeScript, lint, `git diff --check`, a 109-page Production build, confirmation that both direct migration variables remain configured for Vercel Production, and a live read-only rehearsal reporting all 40 migrations current. A local Windows repository-wide unit run reported nine `atmoshaper-signature-sound-construction-review.test.mjs` checksum failures because Git materialized CRLF working-tree bytes for two immutable JSON fixtures; their committed Git blob hashes match the pinned SHA-256 values, and the hosted Linux unit tests pass. The gate branch modifies neither those fixtures nor the failing test.

## 2026-08-27 — Approved AtmoShaper production catalog and public-media release

- Captured the reviewer's approval of every concept currently in the prepared handoff, closing revised Batch 56 fingerprint `d8aca0fa338435811fb7f8aff453307552deb583a892efff57986672506da05d` and Batch 61 fingerprint `51f255a2bd993831479c0b8e65caa984ab9b711e22b534a3b4b22a6a0bf3f081`. The closed production boundary is 51 concepts: four processed and 47 dynamic. Retired/rejected batches, the source-insufficient hold, and the Moodist recording backlog remain excluded.
- Added a fail-closed production release builder and browser catalog validator. The builder recomposes the exact prepared authorities, checks local source and derived bytes, deduplicates 450 source references to 410 unique payloads, and generates catalog revision `df8cb2ceffa9148194c0e60e8d10b6c0d83ad944e3968bd671f97a72301b65fa`. No audio, credential, or machine-local source path is committed to Git.
- Used the exact previously approved FFmpeg 9.0 toolchain to stage Ogg Opus, AAC/M4A, and MP3 browser renditions alongside each exact reviewed source. The immutable stage contains 1,640 audio objects and 6,308,427,694 audio bytes under `atmosphere/atmoshaper/v1/audio/<payload-sha256>/`; catalog metadata has its own revisioned key. Upload is idempotent, signs each PUT with the exact payload SHA-256, and withholds the catalog until every audio object verifies.
- Published the release to the existing `massagelab-public-media` R2 bucket. All 1,640 public audio objects passed media type, byte length, byte-range, and CORS checks through `https://media.massagelab.app`. The public catalog response is 968,170 decoded bytes and exactly matches staged SHA-256 `3d754d6ab552b452025bb2b1dbcf2f71ff8cc1eaea4996f31508c2ef3b476588`; the verifier accepts chunked JSON only after validating decoded length, hash, and CORS.
- Replaced the production Ambient sounds placeholder with searchable and category-filtered cards for all 51 concepts. White Noise exposes its reviewed one-recording choice before preview/add. The production adapter reuses the exact reviewed scheduler and routes each media element through per-source gain into the private AtmoShaper layer output, preserving preview promotion, per-layer/master volume, mute, solo, pause/resume, failure isolation, and disposal. Boiling Water plays its one-time processed opening and loops from the artifact-local 82-second point so the selected baked 8-second seam remains intact.
- The reproducible release commands are `atmoshaper:production:check`, `plan`, `stage`, `upload`, and `verify`. A substantive automated review correctly found that the first production implementation could await prior-owner cleanup before starting Tone, allowing browsers to consume the one accepted Play gesture before audio-context resume. The workspace now prewarms the shared Tone module, gates Play/Preview on readiness, and calls the shared `toneGlobal.start()` synchronously from the accepted gesture before any asynchronous station fade, preview stop, or mixer creation. A mobile browser regression forces suspended-context behavior and proves that one cold AtmoShaper tap resumes inside the initiating turn and reaches playing; focused desktop coverage also passes. Exact-head CI then exposed one stale short-landscape action count after Play/Pause and Stop became separate controls, plus three consecutive lane-3 cancellations at the 15-minute job ceiling while tests were still passing and progressing; the exact assertion now expects all eight controls, and the unchanged exact-once lane topology has a bounded 20-minute job ceiling. Prisma validation, TypeScript, lint, all 3,075 repository tests (`3,072` pass, `0` fail, `3` intentional skips), `git diff --check`, and the 109-page Production build pass. Two attempts to inspect `/music` through the supported in-app browser were denied because the app-admin localhost policy verifier was unavailable, including read-only inspection of the already-open local tab; no bypass or alternate-host workaround was used. The exact review thread is answered and resolved; exact-head hosted CI, merge, application deployment, and post-deployment smoke remain the active release gates.
- A later exact-head review found three independent production-boundary defects. Changing the selected White Noise recording could stack a second copy of the same one-selection concept; recipe insertion now replaces that prior concept layer, with unit and desktop browser coverage proving that exactly the new choice remains. A scheduler failure after initial ambient startup could remain internal to the scheduler; ambient status now reports the failure through the handle-bound controller relay, which stops and disposes the exact adapter, rejects stale callbacks, survives preview promotion, and publishes retryable failed state. An AtmoShaper-first Play or Preview could prepare the shared context without attaching it to the interruption monitor; both gesture paths now synchronously request resume and then register that same prepared context before asynchronous runtime work. Prisma validation, TypeScript, lint, all 3,078 repository tests (`3,075` pass, `0` fail, `3` intentional skips), `git diff --check`, the 109-page ordinary Production build, and the production-bundle guard pass. The exact White Noise desktop browser regression also passes.
- Final commit `ee9c123bb9b3c7d0636c79d93459b8c907bcca28` received a fresh Codex review with no major issues, zero unresolved review threads, successful Code Quality, both CodeQL analyses, Browser build, all four Browser QA lanes, aggregate `qa`, and Vercel Preview. CodeRabbit's status remained successful but explicitly skipped automatic review because of the repository's star-count policy; it was not treated as substantive review evidence. PR #187 merged through the repository's merge-commit convention as `6f3b9559f85c614d742e0305f74554be0990702c` at `2026-08-27T19:42:04Z`. Vercel Production deployment `HroSneuwSpwVdFQowNhsJJ48YERi` completed successfully at `2026-08-27T19:45:22Z`. A bounded public smoke followed the production alias to `https://www.massagelab.app/music`, received HTTP 200 with the AtmoShaper surface in the response, and re-read the immutable catalog as HTTP 200, `application/json`, and exactly 968,170 bytes.

## 2026-08-27 — AtmoShaper queue and playback-policy corrections

- Retired Batch 01 Campfire from the active processed-review queue and prepared handoff because the passed Batch 30 Fireplace concept replaces it. Old Batch 01 review URLs redirect to Fireplace; the immutable Campfire declaration, manifest, QA, and external audio remain retained as historical evidence and no media was deleted.
- Added the missing design readback and live rendered timeline to Batch 04's final Boiling Water player. It now displays the selected `0:00–1:30` one-time opening, `0:15–1:30` repeating region, 8-second equal-power return crossfade, internal `1:22` artifact loop point, current phase, and elapsed/total artifact time. Batch 05 now identifies the selected original Dry source, reads back the adaptive whole-recording policy and fresh `3.75–10s` boundary range, and shows its exact duration timeline before playback plus the seekable live timeline after start.
- Revised Batch 56 to remove String Loop 02-1, Piano Loop 04-3, and Piano Loop 02, leaving 22 sources (`10 / 1 / 10 / 1`). Each randomly selected 77 BPM recording plays three to six times total. The default switch spans four 4-beat bars (`16` beats, `960/77`, approximately `12.468s`), while the three retained short recordings—String Loop 03, Piano Loop 04, and Piano Loop 06—use two bars (`8` beats, `480/77`, approximately `6.234s`). The old Pass remains historical only and is no longer attached to the revised fingerprint; Batch 56 returns to review. With Campfire retired and revised Ambient Loops pending, the prepared handoff is 49 concepts: four processed and 45 dynamic Passes.

## 2026-08-27 — Prepared AtmoShaper concept handoff

- Added development-only `/dev/candidates/prepared` beside Processed audio in both the candidates navigation and review hub. The page derives its lists from the exact terminal manifest anchors, composed whole-concept catalog, fingerprint-bound chat outcomes, canonical 84-concept Moodist inventory, and current construction groups rather than maintaining a second manual readiness list.
- The current projection contains 51 implementation handoffs: five concepts with terminal processed audio and 46 concepts whose exact dynamic setup passed listening review. Every prepared row links back to its authoritative review batch and reports Moodist/Signature origin, handoff kind, and included source count.
- Every prepared row now has an on-demand Play control beside Review. Only one prepared concept can play at a time. Processed Batches 01–05 also expose one final whole-concept audition above their existing source/comparison detail: Campfire and Air Traffic Control use their terminal level-matched pools, Sci-Fi Whistles uses the directly selected Wide dual echo x2 outputs, Boiling Water uses the directly selected 8-second intro-then-loop artifact, and Dryer preserves the directly selected Dry source. All derived URLs are batch-qualified so catalog-backed audio does not fall through the obsolete single-batch route.
- The recording checklist contains 51 non-generated Moodist concepts with no usable included recording. Forty-seven have no current construction candidate; Subway Station, Underwater, Wind, and Wind in Trees have a construction group but no kept usable recording. White Noise, Pink Noise, and Brown Noise remain native-generated and are explicitly excluded from the recording backlog. Concepts with recordings that still need rebuilding are also excluded.
- Desktop uses the existing review table language, while phone layouts use full-width concept rows so origin, handoff, source count, Play, and Review remain readable without horizontal page overflow. The page is a development implementation handoff only; it creates no audio and does not publish, deploy, or production-qualify any concept.
- Focused prepared/selection/player/UI coverage passes 21/21. The complete repository suite reports 3,062 passes, zero failures, and three intentional skips across 3,065 tests; typecheck, lint, `git diff --check`, and the 109-page Production build pass. Playwright verified the prepared page at 1440×1000 and 390×844 with no horizontal overflow, single-active-player switching, successful Review navigation, successful `206` streams for Batches 01, 02, 03, and 05, and a successful `200` decode/loop start for Batch 04. No commit, push, merge, deployment, publication, or production qualification is inferred.

## 2026-08-27 — Signature expansion second listening pass

- Preserved the reviewer's stable batch references while reducing the active expansion queue from ten to six concepts. Batches 53 Forest Atmospheres, 54 Forest Textural Sounds, 58 Spoken Word Cass, and 59 Serbian Orthodox expanded are removed rather than redirected or renumbered. Batches 52 Grinding Pepper, 55 Bagpipes Outside, 56 Ambient Loops, 57 Open Fields, and 60 Crowd Walla are now exact fingerprint-bound chat Passes; Pepper retains 100 events per minute and 15% timing jitter.
- Batch 56 Ambient Loops excludes the nine exact reviewer-named files from its checksum-bound selectors, leaving 25 auditionable sources: 10 Pads, 2 Strings, 12 Piano, and 1 Mallet. The prior four simultaneous lanes are replaced by one randomized pool. A selected recording receives two to four consecutive plays total, then a different recording enters through a four-beat bar at 77 BPM (`240/77`, approximately `3.117s`); that exact setup is the reviewed Pass.
- Moved retained processed-audio Batches 01–05 into the existing Concept review queue dropdown with every surviving concept. Their routes and review clients are unchanged; only the five separate navigation buttons were removed.
- Batch 60 Crowd Walla keeps all 13 recordings. Fresh EBU R128 readback measured integrated loudness from `-44.5` to `-15.5 LUFS`; transparent per-recording gain targets `-27.2 LUFS`, the median-derived target reduced only enough to preserve at least `1 dB` of measured true-peak headroom. The preview applies static gain only: no compression, limiting, EQ, within-file attenuation, or source rewrite is implied. That exact setup is the reviewed Pass.
- Batch 61 White Noise still requires the reviewer to select exactly one of 11 recordings. The chosen recording now starts a fresh copy `0.25s` before the current copy ends; both copies remain at full level and the outgoing copy finishes naturally, producing a plain self-overlap with no fade.
- The current Batch 52/56 and unified-queue boundary passes 38/38 focused tests. The final exact repository suite reports 3,062 passes, zero failures, and three intentional skips across 3,065 tests. Typecheck passes; lint exits zero with only the unrelated existing `nextLayer` warning; the 109-page production build passes. Live Batch 56 shows 22 sources, the exact repeat block and both beat-timed boundary policies, no excluded filename, seekable overlapping concept playback, no narrow-viewport horizontal overflow, and no browser console warning or error. No source audio was rewritten and no publication, deployment, staging, commit, push, or merge is inferred.

## 2026-08-27 — Signature folder expansion review

- Re-audited every requested Signature Sounds folder against the checksum-bound 3,693-source discovery owner and current construction groups. Cave Room Tone already contains all five Cave recordings; Coffee Shop contains all 13 files across parts 1 and 2; Light Rain owns all six files from its directory plus one separately sourced rain file; London Ambience owns all 12 files from its directory; and the five-source Children's Choir remains distinct.
- Added a separate fail-closed review owner and appended Batches 52–61 to the existing button-free processed-audio queue. Exact selector counts are locked to the discovery fingerprint, source identity remains path-derived, and folder/count/field drift fails validation rather than silently changing an audition.
- The new queue contains Grinding Pepper candidate (18 events at 90 events/minute with 15% jitter), Forest Atmospheres reference (23 long files), Forest Textural Sounds (23 short events), Bagpipes Outside (4), four-track Ambient Loops 77 BPM (34 total sources with four-second lane crossfades), Open Fields (11), Spoken Word Cass (38), Serbian Orthodox expanded (32), Crowd Walla assignment review (13), and White Noise selection (11). Pepper, both Forest comparisons, Crowd Walla, and White Noise include on-demand individual-source controls; White Noise starts only the reviewer-selected source and loops it with a two-second crossfade.
- The Forest folders are intentionally not merged: fresh WAV-header readback found the Atmospheres set spans about 12–50 seconds with a roughly 27-second average, while the corresponding Textural Sounds set spans about 1.2–5.1 seconds with a roughly 2.7-second average. Crowd Walla remains a source-level assignment review because generic numbered filenames do not justify guessing semantic subgroups without listening.
- Current focused catalog-expansion, finalization, whole-concept, UI, and speech-routing tests pass 34/34. The complete repository suite reports 3,058 passes, zero failures, and three intentional skips across 3,061 tests; typecheck and `git diff --check` pass; lint exits zero with only the unrelated existing `nextLayer` warning; and the Production build generates all 109 pages. Live Batch 45 and representative Batches 52–61 return HTTP 200 with no load error; Batch 45 is Pass with no remaining-treatment copy, and representative Stadium, Pepper, and White Noise ranges return HTTP 206 with exactly 100 bytes of `audio/wav`. Source audio remains read-only; no processing, production qualification, publication, upload, deployment, staging, commit, push, or merge is inferred.

## 2026-08-27 — Stadium Crowd final dynamics and pool leveling

- Closed Batch 45's two remaining processing requirements against the exact six speech-stage WAVs without changing or overwriting that retained bundle. The final v1 recipe uses one fixed downward RMS compressor at `-20dBFS`, `3:1`, `20ms` attack, `750ms` release, knee `4`, full wet and no makeup, followed by one per-file static gain toward `-23 LUFS` constrained by a `-1dBTP` ceiling. No gain riding, EQ, extra speech treatment, limiter, or source rewrite was added.
- The no-overwrite external render produced six stereo 48kHz/24-bit PCM WAVs totaling 196,689,846 bytes, with no partial directory. Declaration SHA-256 is `9c80ffb6d9beb09a6760a7dba03245b0a8e6f909ffe6551b3990d2eedffb0cb0`; manifest SHA-256 is `0c133b36810b433a527d36b2a1c24bec86e2b5841606b40ae54fc0da0d4e2e3a`. Independent validation reports `-23.2` to `-23.0 LUFS`, a `0.2 LU` pool spread, `4.8–11.3 LU` loudness ranges, and `-10.2` to `-1.0dBTP` peaks.
- The reviewer compared the result with the prior six-file speech-reduced version, preferred the prior sound, and directed that Batch 45 revert to it and be considered finished from there. The development page and route therefore select retained-v1 speech output again. Processed-stage fingerprint `581844bfabfe92024656ea7686c8aff4e729bc0bd575da5316634571e4254ea1` remains the exact input identity; final current Pass `9db93767ce5667a5831a6b803520712b657634eee88268769088886aac17bf35` closes the rejected later dynamics/leveling requirements, and the later final-stage page binding/anchor were removed.
- The technically valid dynamics/leveling render is retained externally as an inactive experiment rather than deleted or treated as approved. No current page or route selects it, and no publication, production qualification, upload, deployment, staging, commit, push, or merge is inferred.

## 2026-08-27 — Signature whole-concept review follow-up

- The reviewer passed the final four open auditions: Heavy Rain, Church Bells, Transit Announcements, and the current six-source speech-reduced Stadium Crowd processing stage. These are bound to the exact setups heard; Stadium's later cheer/shout dynamics control and across-pool leveling remain pending rather than inheriting the stage approval. The restart-safe outcome catalog now contains 41 exact audition Passes.
- Recorded eight earlier exact chat Passes without page buttons: genuinely reprocessed nine-source Traffic, Waves, Bus Station Announcements, Children’s Choir Ambience, four-second-crossfade Orthodox Choir, School Playground, Spaceship Interior, and Subway Interior. The restart-safe outcome catalog had reached 37 exact Passes before the final four decisions above.
- Heavy Rain selects a fresh random window anywhere inside the sole `0–143.413s` Outside5 source; every window is at least 20 seconds and returns through a 10-second crossfade. Church Bells keeps its existing transparent constant-gain measurements and uses a 15-second complete-recording crossfade. Both exact auditions are now Pass.
- Applied the uniquely named next-station correction to stable Batch 49 Transit Announcements: only `0:00–0:06` of `The next station is - Announcement. 2.wav` is auditioned, with everything after six seconds excluded non-destructively. Stable Batch 47 Train Station remains unchanged.
- Diagnosed Batch 45's disabled Start control at the review-availability owner. Its six exact speech-treated v1 sources were complete and playable, but the UI treated later dynamics/level requirements as if no audition existed. Complete processed intermediate treatments are now playable without clearing the remaining production gate; the reviewer passed that exact processing stage, while untreated processing-required concepts still fail closed and processed concepts never fall back to raw audio.
- After recording the four final decisions, all 3,044 executable repository tests pass with zero failures and three intentional skips; the focused outcome, processed-binding, and stage-copy suites pass 26/26. Typecheck, lint with the one unrelated existing `nextLayer` warning, `git diff --check`, and the 109-page Production build pass. Live Batches 17, 27, 45, and 49 return HTTP 200 with their Pass markers, and Batch 45 alone shows the stage-specific Pass plus its remaining-treatment notice.
- Focused review/policy/binding coverage passes 48/48. The complete repository suite reports 3,044 passes, 0 failures, and 3 intentional skips across 3,047 tests; typecheck passes; lint exits 0 with only the unrelated existing `nextLayer` warning; and the 109-page Production build passes. Live affected pages returned HTTP 200, Batch 45's Start control was enabled, and its exact speech-treated WAV returned HTTP 206 for the requested range. No source audio, rendered bundle, production runtime, publication, upload, deployment, staging, commit, push, or merge changed.

## 2026-08-26 — Traffic reduced-pool reprocess and dual speech review routing

- Applied the reviewer's exact Batch 21 removals before processing: both named Aberdeen City `-2` recordings and `Cars Passing During Rain On A Wet Road-1 (1).wav` are absent from the active nine-source declaration. The user explicitly chose a real reprocess over a post-render playable-pool exclusion, so the temporary follow-up-filter implementation was removed rather than retained.
- Rendered and independently revalidated a new no-overwrite v2 speech bundle through the checksum-bound RipX CPU HTDemucs producer. All 27 declared outputs and receipts are complete with zero partials. Declaration SHA-256 is `45706b1994b3fcd219bf3102bffab9cbf41d8ccae3c991945eb9a7040acf2378`; the 84,920-byte manifest SHA-256 is `9e4fc27a0eac42681ddf995a0dbee2e6a23fe72a14fccb2f926eb5153a4db302`; audio totals 754,951,254 bytes. The active nine Traffic outputs total 180,596,094 bytes.
- Compared all 12 regenerated London outputs to the physical v1 WAVs by stable source ID, manifest checksum, and fresh file checksum. Every v2 London hash differs, so the direct Batch 35 Pass was not transferred to unheard bytes. Recovered the exact v1 producer declaration from the development build cache, preserved it as a closed immutable owner, and verified that it recomputes declaration `d87f5ede54226278b846bdb69894f293cb76b57da636ccde6540326c6dfd2ad7` with 30 outputs.
- Added fail-closed dual-bundle review routing: Batch 21 validates and streams from v2, while Batches 35 and 45 validate and stream from v1. The page and range route share the same stable-batch selector. This preserves Batch 35's exact processed Pass and Batch 45's existing downstream processing gate without exposing the unused regenerated v2 London/Stadium bytes as approved.
- Closed an independent-review failure edge in that split: if either immutable bundle is unavailable, only its owned batches become inactive. Stored outcomes are still schema-validated but are not attached to a raw fallback identity, so a missing retained-v1 root cannot prevent valid v2 Traffic from rendering and no saved Pass can migrate to unheard bytes.
- Corrected the separate raw-playback outage at its configuration source. The previous restart set only the speech root, so raw routes returned 404 and the browser reported no supported source. Local-start guidance now keeps the raw, derived catalog, single-derived compatibility, retained-v1 speech, and Traffic-v2 speech roots together; no player fallback was added.
- Focused producer, review binding, route, whole-concept, and UI coverage passes 33/33. The complete repository suite reports 3,043 passes, 0 failures, and 3 intentional skips across 3,046 tests; typecheck passes; lint exits 0 with only the unrelated existing `nextLayer` warning; the 109-page Production build passes; and `git diff --check` passes. Live Batches 21, 35, 45, and 49 return HTTP 200 without runtime/audio-load error; they expose 9 v2, 12 retained-v1, 6 retained-v1, and raw sources respectively. Representative Traffic, London, Stadium, and Batch 49 WAV ranges return HTTP 206 with exactly 100 bytes. No publication, deployment, staging, commit, push, or merge is inferred.

## 2026-08-26 — Signature second whole-concept review

- Corrected the review page's presentation-only batch mismatch. The dropdown's stable `batchId` and amended concept label were already correct; the concept page had called its filtered queue position a Batch number after merged entries disappeared. It now shows stable `Batch NN` and separate `Review X of Y` progress.
- Bound seven more direct chat Passes to their exact current fingerprints: Washing Machine, Fireplace, Keys Jingling, Lunar Wind, Space Tension Bed, Vintage Radio Broadcast, and Waterfront Cafe. The complete raw queue now preserves 28 direct Passes without requiring page buttons.
- Applied the returned source and playback changes. Heavy Rain now keeps only the unchanged Outside5 recording after exact Thunder 10, 02, 04, and 07 were removed. Children's Choir Ambience now includes all five distinct `Choirs_Children_Ambience` sources through path-hashed additions above immutable construction evidence. Orthodox Choir removes `_4.wav` and `_-18.wav` and caps audible recordings at two. Spaceship Interior removes Track 2 and its ten sources, leaving the nine-source three-second-crossfade sequence. Transit Announcements removes both Waverly Automated Announcement sources and restores the exact next-station file to its full uncut 13.418520833 seconds. Bus Station Announcements uses a one-second fade in and five-second fade out; School Playground uses a three-second regional-loop crossfade; Subway Interior uses a 20-second crossfade.
- Replaced the new leveling assumption with the reviewer's actual goal: consistent perceived level using transparent overall gain only. Waves and Church Bells now target the measured pool median, reduced only when needed to retain one dB of true-peak headroom. The preview uses a separate Web Audio gain stage when a safe boost is required, so fades retain their full envelope and no EQ, compression, filter, or source rewrite is implied. A reusable read-only level-measurement command records the exact FFmpeg build used.
- Recovered and verified the exact FFmpeg 9.0 binaries, then built and independently reviewed a closed speech-reduction producer. It checksum-binds the RipX Python executable, complete Demucs 4.0.0 package tree, HTDemucs YAML and weights, adapter, FFmpeg, FFprobe, and all 30 exact source files; rejects links/junctions, overwrites, duration drift, incomplete pools, and tool drift; and resumes only from exact receipt/output checksums. Traffic uses no vocals, London restores vocals at `-20dB`, and Stadium restores vocals at `-12dB` before constant-gain source-loudness matching under a `-1dBTP` ceiling.
- The authorized no-overwrite render produced 30 stereo 48 kHz/24-bit PCM WAVs totaling 817,796,154 bytes with zero partial bundles. Declaration SHA-256 is `d87f5ede54226278b846bdb69894f293cb76b57da636ccde6540326c6dfd2ad7`; the 94,022-byte manifest SHA-256 is `a4e86fd41de6a295b8bb7628822b35330d676b5c1cb6d4b20c94976bd44e7a9d`. Fresh read-only validation returned 30 complete and zero missing. A dedicated concept-scoped route and processed-review fingerprint layer prevent raw fallback and cross-concept replacement: Batches 21 and 35 are ready for listening, while Batch 45 exposes the bound speech treatment but stays blocked on dynamics control and pool leveling.
- Focused speech, binding, serving, whole-concept, amendment, player, and UI coverage passes 52/52. A preview-player regression exposed by the complete suite was traced to an unconditional asynchronous pause in the positive-gain addition; ordinary and attenuated sources now preserve the established synchronous `audio.play()` handoff while only positive-gain sources resume Web Audio first. Its focused preview coverage passes 31/31. The complete repository suite reports 3,042 passes, 0 failures, and 3 intentional skips across 3,045 tests; typecheck, lint with only the unrelated existing `nextLayer` warning, the 109-page Production build, and `git diff --check` pass. Independent specification and quality/security reviews found no Critical or Important integration issues. Live Batches 21, 35, and 45 return HTTP 200 with processed URLs and no raw fallback; exact speech output ranges return HTTP 206 with `audio/wav`, an exact `Content-Range`, and `private, no-store`. The port-3013 server is configured with the immutable speech root. No audible approval, production qualification, publication, upload, deployment, staging, commit, push, or merge is inferred.

## 2026-08-26 — Signature Batch 09–51 reviewer amendments

- Added a fingerprint-bound amendment owner above the immutable construction and morning-review evidence. It applies every returned Batch 09–51 decision while keeping original batch numbers stable: B32 redirects to Supermarket, B39 to renamed Radio Static, B40 to Traffic, and B48 to Transit Announcements. The resulting selector contains 42 surviving concepts and preserves 21 direct chat Passes without decision buttons or invented listening timestamps.
- Added exact preview policies for Washing Machine's `0:15–0:55` loop with a four-second return crossfade; the requested crossfade changes; 90-events-per-minute Keys Jingling; randomized bounded loops; pause-separated announcements; strict up-to-three/four concurrent sources; and Spaceship Interior's two independent lanes. The shared development player enforces source windows, randomized starts/ends, boundary fades, strict active-source limits, non-duplicate selection when possible, and lane/source telemetry.
- Reconciled source pools and labels: Highway is Cars Passing; Serbian Orthodox and Children's Choir are separate; Grocery Store is merged into Supermarket; Roadside into Traffic; Tuning Radio is Radio Static; Train Station Announcements into Transit Announcements; and the specified Rain, Bus Station, Choir, and Spaceship recordings are excluded. The reviewer clarified that Batch 49 must discard the beginning of `The next station is - Announcement. 2.wav`; its non-destructive audition now begins at `0:05` and retains the remainder through the exact `13.418520833s` source end.
- Measured and attenuation-matched Heavy Rain, Beach Ambience, Bus Station Announcements, Church Bells, Fireplace, and Subway Interior. The moving sirens in Heavy Rain could not be conventionally filtered without damaging the rain, so the seven exact named thunder recordings were excluded. No Outside11 file exists, and Outside5 was measured but not confirmed to contain a siren; it therefore remains among five level-matched sources for reviewer listening instead of being removed by inference. Traffic, London Ambience, and Stadium Crowd are disabled pending exact speech-removal, voice-ducking, and dynamics-treatment audition artifacts; Train Station remains visible but marked insufficient for production.
- Independent final review also closed impossible randomized-loop geometry, reserved a transition voice for multi-lane crossfades, and applied pause-sequence fade-in gain before playback begins so a source cannot click at full level first. Regression coverage binds those safety rules without changing the approved Batch 41, 43, or 51 settings.
- Verification passed 57 focused behavior tests, 409 adjacent AtmoShaper tests with two host-limited skips across 411 tests, and 3,025 repository tests with three existing skips across 3,028 tests. Typecheck, lint with one unrelated existing warning, the 109-page Production build, `git diff --check`, every 42 surviving concept URL, all four redirect URLs, and three representative checksum-bound HTTP 206 audio ranges passed.
- No raw or derived audio was changed by these amendments. Production qualification, publication, runtime wiring, upload, deployment, staging, commit, push, and merge remain separate.

## 2026-08-26 — Signature master whole-concept review queue and requested revisions

- Expanded the retained raw review queue from Batches 06–15 to Batches 06–51. All 46 current accepted, source-backed, processing-free concepts validate against canonical construction/discovery fingerprints and project 390 exact source references across 342 unique recordings. The compact concept selector states that cleanup-, rebuild-, and policy-gated concepts stay outside this truthful raw queue; current inventory also retains 29 source-backed exclusions and 18 source-empty groups for later work.
- Preserved seven direct chat Passes as exact restart-safe outcome records: Droplets, Electrical Interference, Washing Dishes, Cave Room Tone, Light Waves, Dark Ambient Pad, and Walk in Snow. No page buttons or fabricated hearing timestamps are required. With retained derived Batches 01–05 terminal, the bare processing URL now opens the first concept without a direct Pass, Washing Machine.
- Added a closed revision owner above immutable construction evidence. Washing Machine records an `opening-then-loop` request as `needs-timing` without guessing loop points or changing the active audition. Room Tone uses exact FFmpeg 9.0 EBU R128 readback and attenuation-only gains of `-6.1`, `-19.2`, `-6.4`, `-21.3`, `0`, and `-11.7dB` against the quietest `-60.8 LUFS` source. Experimental Atmosphere uses full-length plain overlaps with a fresh `2–6s` transition lead and no fade or outgoing cutoff.
- Extended the single shared preview player with base-gain-aware volume envelopes, immutable per-voice telemetry, clamped per-voice seeking, and full-length overlap protection at its eight-voice safety limit. Every concept page now shows concrete selection/timing/boundary policy plus one seekable elapsed/duration row per active recording; simultaneous rows explicitly report overlap. Changing timeline values remain outside the screen-reader live status region.
- Strict RED produced nine expected failures for absent telemetry, restart-safe outcomes/revisions, the complete catalog, policy copy, timeline controls, and compact navigation. Final focused GREEN passed 50/50 across player, source routing, catalog, lifecycle recovery, accessibility, and UI; both independent re-reviews ended with no findings. The complete repository suite, typecheck, lint with the unrelated existing `nextLayer` warning only, and the 109-page Production build pass. All 46 raw concept pages returned HTTP 200, and all 342 unique source routes returned HTTP 206 for the requested byte range with an audio content type.
- Automated visual interaction was attempted through the in-app browser, but its admin-enforced localhost policy could not be verified. The policy was not bypassed; direct server readback verified the rendered policy/timeline/revision text, while audible review remains with the user. No source or derived audio was changed or rendered, and no publication, production wiring, upload, deployment, staging, commit, push, or merge occurred.

## 2026-08-25 — Signature Dryer Batch 05 whole-concept audition

- Added an immutable Dryer trim declaration for exact source `a2cdc5b801058999b253de905dcdc45e612c5e944ef6e4202a0d815b91bf8d4f`: keep `1.8–17.7s`, apply `0.15s` fades at both boundaries, preserve stereo 48 kHz output, and do not impose loudness normalization. Strict RED/GREEN made the shared derived-audio planner, FFmpeg adapter, manifest validator, registry, catalog route, and review dispatch recipe-aware without adding Dryer behavior to the Boiling-specific edit owner.
- The authorized external FFmpeg 9.0 render produced one 15.9-second stereo 48 kHz/24-bit PCM WAV measuring `-19.5` LUFS and `-9.1` dBTP. The physical file is 4,579,302 bytes with SHA-256 `a7d6dc5e5195d4dc7d37d2e0ff9898c71b4752e71203f3ec3b0730034c25e546`; it exactly matches the portable manifest, and no partial file remains. Declaration SHA-256 is `15ae5817239e3b43da3a520bae261fdf03756401d05220b06885996b18cfe2b9`; manifest SHA-256 is `2612f3cf58c2be61ad3f609fc5e6237af34143c1185a983f5a892a58a11d9d9d`.
- The button-free review presents Dry concept and Trimmed candidate as complete repeating concepts using the already approved adaptive whole-source scheduler and a fresh `3.75–10s` crossfade at each boundary. Start, Stop, and Next transition are the only controls; the reviewer can reply in chat with approval or changes. The live page returned HTTP 200 with both comparisons and its exact derived range returned HTTP 206 with 100 requested bytes and `Content-Range: bytes 0-99/4579302`.
- The initial live source request exposed an orphaned older dev listener that lacked the source-audio root; the derived WAV remained valid. The exact listener and parent were replaced with the configured server, after which the raw MP3 and derived WAV both returned checksum-bound HTTP 206 ranges with their correct media types.
- The reviewer directly selected `Dry concept`. A committed validator-backed record binds that choice to the exact source ID, the comparison output identity, manifest, declaration, and approved `3.75–10s` playback setup. The trimmed artifact remains an unselected comparison rather than an inferred rejection, and the terminal anchor state is `audible-qa-complete-dry-selected`. The page visibly restores `Dry concept` as selected; no review buttons are required.

## 2026-08-25 — Signature Boiling Water Batch 04 v1 and v2 review audition

- Corrected Batch 04 after the reviewer supplied the exact full-concept behavior: play source `0:00–1:30` once, then repeat only `0:15–1:30` with the return crossfade. Strict RED failed on the old declaration, render graph, directory mapping, and whole-file loop player. GREEN retains `2s`, `4s`, and `8s` equal-power comparisons while giving each artifact a one-time opening plus a manifest-bound repeat region; Web Audio now loops that region sample-accurately without replaying `0:00–0:15`.
- The immutable v2 external render produced three stereo 48 kHz/24-bit PCM WAVs totaling 134,496,306 bytes. Exact durations are `161`, `157`, and `149s`; first-loop offsets are `88`, `86`, and `82s`; loop-region durations are `73`, `71`, and `67s`; measured loudness is `-37.1`, `-37.0`, and `-36.9` LUFS; and every output peaks at `-14.3 dBTP`. Every physical size/hash matches declaration `7ba27607e61fdd09f3cad2c6898fec8726dd9762d798ad1437ff8120ae6e16e4` and manifest `dbbdb177e8cd47fa2923528f9de834a5ad1cdb87fe3c2cc3c780bf67a2a6c5c6`. The v1 directory remains untouched and superseded only for current review.
- The v2 processing page returns HTTP 200 with the exact full-concept explanation and controls; its first manifest-bound output returns HTTP 206 with 100 requested bytes and `Content-Range: bytes 0-99/46368102`. The reviewer then directly selected `Long 8-second crossfade` as Pass. A closed selection record binds that choice to declaration `7ba27607e61fdd09f3cad2c6898fec8726dd9762d798ad1437ff8120ae6e16e4`, manifest `dbbdb177e8cd47fa2923528f9de834a5ad1cdb87fe3c2cc3c780bf67a2a6c5c6`, and output `d63ea4082951404502d5d48c3fdfc2df6af907ca897c06f95956f52b0b96bf04` without fabricating browser playback timestamps. Batch 04 is now `audible-qa-passed`; the `2s` and `4s` comparisons remain undecided, and production/publication authority remains separate.
- Strict RED/GREEN covered the absent direct-selection record/application path, passed manifest anchor, visible selection state, and a reload edge where a later browser-local draft could hide the committed choice. The selection now re-applies over a restored draft while retaining its later notes and timestamps. Focused validation passed 17/17; all 2,978 repository tests produced 2,975 passes, 0 failures, and 3 intentional skips; typecheck passed; lint exited 0 with only the existing unrelated `nextLayer` warning; and the Production build generated all 109 pages. Live readback returned HTTP 200 with the 8-second direct selection visible and HTTP 206 for the exact selected WAV's 100-byte range. No audio was regenerated, modified, published, production-wired, uploaded, deployed, staged, committed, pushed, or merged.

- The earlier v1 audition measured one immutable review matrix over the `4.0–111.4s` clean region: `2s`, `4s`, and `8s` equal-power cyclic seams. A dedicated edit owner and process adapter verify exact source bytes, the recorded FFmpeg 9.0 build, deterministic identities and portable argv, no-overwrite rollback, output format/duration/peak, and manifest closure under strict RED/GREEN.
- The authorized external render produced three stereo 48 kHz/24-bit PCM WAVs with exact durations `105.4`, `103.4`, and `99.4s`; every physical hash and size independently matches the manifest, and all three measure `-1.9 dBTP`. Declaration SHA-256 is `228d2dcf42b7e04b4ae0768748f5fd6d42847099a4d12a67199c246e41845fda`; manifest SHA-256 is `189d5b59d51ec36a878d9f61b245b8c6e44a83a1d78c24d4fd1bcfa43a806687`. An initially rendered but unregistered manifest with machine-expanded placeholder paths was removed and regenerated after a focused RED exposed the portability defect; no earlier batch changed.
- The retained processing tool now selects among immutable server-owned batch roots without restarting, preserves the single-root fallback, and uses batch-qualified exact-output routes. A bare catalog URL selects the first non-passed anchor, currently Batch 04. Boiling Water uses a dedicated tail-to-real-boundary-to-head loop player, persists exact seam-crossing evidence, and requires dry playback, candidate playback, and two crossings before Pass. All 2,977 repository tests produced 2,974 passes, 0 failures, and 3 intentional skips; typecheck passed; lint exited 0 with only the unrelated existing `nextLayer` warning. Batch 04 remains `rendered-pending-audible-qa`; no seam was approved, published, production-wired, uploaded, deployed, staged, committed, pushed, or merged.
- The first live launch exposed Next's same-segment dynamic-slug rule: the legacy one-segment route and new batch-qualified route used different folder parameter names. Strict RED reproduced the conflicting route tree. Both handlers now share one generic first-segment owner without changing either public URL; the legacy endpoint translates it to an output identity and the new endpoint translates it to a batch id. Post-repair full tests are green. The catalog-backed bare page returns HTTP 200 with Boiling Water and all three seams, and the first exact Batch 04 output returns HTTP 206 with 100 bytes, `audio/wav`, and `Content-Range: bytes 0-99/30355302`.

## 2026-08-25 — Signature Sci-Fi Whistles Batch 03 audible selection

- The reviewer directly passed `Wide dual echo ×2` after hearing the complete Sci-Fi Whistles concept comparison. A closed concept-selection QA record binds that Pass to Batch 03 v2 declaration `e05aadb04e87f56e9df4f69cba180ce37ca0f4481fbfc0fa2c981bdd4fb4163a`, manifest `8ff6856b1342a8d88366eeae48e1307b7cbc0921ef0ef7755fce8ac4f7faa6c6`, and the exact 18 ×2 output identities. The other three treatment variants remain undecided; no rejection was inferred.
- Strict RED/GREEN added the manifest-bound selection validator and restart-safe processing-page baseline, then changed only Batch 03's portable anchor to `audible-qa-passed`. Focused validation passed 22/22; the full suite passed 2,954 tests with three intentional skips; typecheck, lint with one existing unrelated warning, and the 109-page Production build passed. Live readback returned HTTP 200 with the ×2 Pass selected and HTTP 206 for the first exact ×2 WAV range. No audio was regenerated, modified, published, production-wired, uploaded, deployed, staged, committed, pushed, or merged.

## 2026-08-25 — Signature walking triage and derived-audio Batches 01-02

- Corrected construction QA after Walk on Gravel and Walk on Leaves could not be marked negative without confirming a setup the reviewer did not accept. Pass remains exact-heard only; Needs Rebuild and Reject may be note-backed without an audition or QA scope, and unexplained negative decisions remain fail-closed. The two exact notes and `needs-rework` decisions are preserved in `signature-sound-construction-qa-decisions.json`; both concepts stay active for later ingredient repair.
- Added the separately planned derived-audio pipeline: a closed portable Batch 01 declaration, pure planner, FFmpeg process adapter, checksum/size preflight, canonical repository/source/root fencing, exact-tool enforcement, quietest-input attenuation planning, deterministic identities and argv, no-overwrite temporary rendering, full technical inspection, rollback, and atomic external manifest publication. Boiling Water is declared but remains parameter-gated with null trim/seam values, so no timestamps were guessed and no Boiling artifact was created.
- The authorized Campfire run measured the four exact inputs at `-23.1`, `-33.0`, `-38.3`, and `-25.4` LUFS and rendered attenuation-only gains of `-15.2`, `-5.3`, `0`, and `-12.9` dB. Four stereo 48 kHz/24-bit PCM WAVs totaling 129,799,918 bytes passed source-duration, format, checksum, and loudness verification at `-38.3`/`-38.4` LUFS. The external manifest contains no machine root, no transaction residue remained, and its committed checksum anchor is `04f96575eb156fc39913244b9e7f30d46025f7b3580031e15af2cc1e8c3b53a8`.
- Added retained development-only `/dev/candidates/processing`, source-versus-derived native players, measurements, output-identity-bound notes/decisions, note-backed negative QA, local persistence, and JSON export. A separate manifest-closed route rechecks the committed manifest anchor and exact artifact size/SHA-256 before serving byte ranges. Fresh live checks returned HTTP 200 for the page and HTTP 206 with 100 requested bytes, `audio/wav`, and `Content-Range: bytes 0-99/7188582` for the first exact artifact.
- Reconciled returned artifact QA SHA-256 `eeb1bf74e559cea236f8d68fb9ec972a4d1cde078db9fdf7542c3c071715aef3`: it exactly matched the Batch 01 and manifest fingerprints, contained three Pass decisions, and showed both source and processed audio heard for the undecided Fireplace 2 output. The user's explicit follow-up supplied that fourth Pass. `signature-sound-derived-audio-qa.json` now records four exact passes, the manifest anchor state is `audible-qa-passed`, and one closed shared validator rejects stale, unknown, incomplete, or evidence-free QA. The review page starts from this newer committed baseline while preserving genuinely later valid browser edits. Artifact QA does not publish or production-qualify Campfire.
- Final focused Signature verification passed 47/47 after artifact-QA ingestion. Typecheck passed; lint exited 0 with only the unrelated existing `nextLayer` warning; all 2,937 repository tests produced 2,934 passes and three existing skips; and the 109-page Production build passed. Final diff/scope checks found no Git audio or binary payload. No source audio, production runtime/provider, publication, upload, deployment, staging, commit, push, or merge changed.
- Continued with Air Traffic Control as Batch 02 because its exact 12-source pool, speech-retention requirement, 1-7 second spacing, four-selection repeat window, and relative-normalization intent were already construction-owned. A closed batch registry now selects immutable declarations; output planning derives a concept-specific directory instead of assuming Campfire; and the narrow CLI keeps exactly one ready concept per batch without growing the process owner past its recorded budget.
- Real measurement matched every Batch 02 source checksum/size and revealed all 12 recordings are mono 44.1 kHz/16-bit PCM. The initial copied stereo recipe was stopped before rendering, corrected under strict RED/GREEN to preserve mono, and remeasured. Input loudness ranged from `-14.0` to `-23.1` LUFS; attenuation-only gains ranged from `0` to `-9.1` dB. Twelve mono 48 kHz/24-bit PCM outputs totaling 8,802,360 bytes independently measured `-23.1` LUFS, matched every manifest checksum/size, and left zero partial files. Declaration SHA-256 is `87a79b019a119ef781640fb344a8f3b688a2aa7fd0a0457a7da29a1b1da17008`; external manifest SHA-256 is `fb0163b77a656f92f61bcc2bd63d46b9ad74bbc55fb9b4557268beab95a1ef94`.
- Extended the retained processing page and exact byte-range route to select either committed manifest anchor and corresponding declaration from the configured external batch root. Batch 01's committed four-pass QA remains immutable; Batch 02 initially began with a complete-key, decision-empty QA draft. Focused multi-batch tests passed 33/33; all 2,941 repository tests produced 2,938 passes, 0 failures, and 3 skips; typecheck, lint, and the 109-page Production build passed. After a clean restart, the live Air Traffic page returned HTTP 200 with 12 comparisons and its first exact WAV returned HTTP 206 with the requested 100 bytes.
- Reconciled the user's direct follow-up that all 12 Air Traffic source/processed comparisons pass. Strict RED first proved the absent Batch 02 QA record and single-committed-QA page limitation; GREEN added a separate exact QA owner instead of replacing Campfire evidence. The Batch 02 QA SHA-256 is `34d183353ed80d2c306f2aa5dbaf91f4ad029e0ee492387ec8ae1c30bdf5be2d`; shared validation matched all 12 output identities to manifest `fb0163b77a656f92f61bcc2bd63d46b9ad74bbc55fb9b4557268beab95a1ef94`, and both retained manifest anchors are now `audible-qa-passed`. Post-ingestion verification passed 34/34 focused tests, all 2,942 repository tests with 2,939 passes and three skips, typecheck, lint, the 109-page Production build, and live 200/206 checks with the page showing 12 passed. No publication or production qualification is claimed.

## 2026-08-25 — Sci-Fi Whistles treatment-audition Batch 03

- A read-only processing-intent audit found no remaining concept with a fully reproducible final recipe. Sci-Fi Whistles was the bounded exception because its exact 18-source pool and `0–8` second scheduler were already approved while the requested echo/delay character remained a listening choice. Batch 03 therefore declares three exploratory treatments—short delay, medium echo, and wide dual echo—without presenting any as final or approved; other concepts remain behind their existing trim, speech, separation, ingredient, or selection gates.
- Strict RED/GREEN added a separate 412-line effect planner and 317-line process adapter instead of expanding the normalization owners. The closed declaration, source-byte verification, exact FFmpeg build, deterministic 54-output plan, parameter-bound identities, no-overwrite preflight, effect-tail/channel/peak verification, rollback, portable manifest, manifest drift rejection, decision-empty QA anchor, processing-page grouping, and exact range route are covered. One real measurement attempt exposed a missing default process adapter and created no output; a focused RED reproduced it before the adapter correction.
- The authorized external render expanded 18 exact stereo sources through all three variants into 54 stereo 48 kHz/24-bit PCM WAVs totaling 53,034,894 bytes. Independent readback found 18 outputs per variant, zero missing or partial files, zero size or SHA-256 mismatches, and true peaks from `-23.3` to `-6.2` dBTP. Declaration SHA-256 is `d37531e47076d5eb10f3dcb6ea708df34d593a444c81fc36d675e2b44acbab7c`; external manifest SHA-256 is `70a172f0e32437f1b795e14e26b0dc3a36ebcfa1b7d627d1c5d3ddd9b5ce0de3`, and its recorded FFmpeg arguments are portable rather than machine-rooted.
- Development-only `/dev/candidates/processing` now groups one dry control with the three labeled variants for each exact source. Heard timestamps and Pass/Needs Rebuild/Reject decisions bind each variant independently; notes and export stay browser-local. Focused derived-audio/UI checks passed 44/44, all 2,952 repository tests produced 2,949 passes and three intentional skips, typecheck passed, lint exited 0 with only the unrelated existing `nextLayer` warning, and the 109-page Production build passed. After replacing the older Air Traffic process on port 3013, the Batch 03 page returned HTTP 200 with all three variant labels and the first exact treatment returned HTTP 206 with 100 requested bytes, `audio/wav`, and `Content-Range: bytes 0-99/530636`. Batch 03 remains `rendered-pending-audible-qa`; no source audio, Git media, publication, upload, deployment, staging, commit, push, merge, or production runtime changed.
- Corrected the Batch 03 acceptance surface after reviewer feedback that an effect must be heard across the complete concept rather than judged one recording at a time. The primary page now uses one shared scheduler to play all 18 dry sources or the corresponding 18 short-delay, medium-echo, or wide-dual-echo outputs through the approved `0–8` second event spacing. Start, Stop, Next event, and explicit heard confirmation are available for each whole concept. A separate closed browser-local QA record binds one note/decision per complete effect to its exact 18 output identities; Pass requires both dry and effect confirmation, while note-backed negatives remain possible. The previous 54 artifact cards are retained only inside collapsed optional diagnostics, and their evidence is not promoted to concept approval.
- Strict RED failed on the absent concept-QA owner/component and the shared player still resolving only raw-source URLs. GREEN added one injected manifest-closed URL seam without duplicating scheduler policy, a 269-line concept-QA/model owner, and a 270-line concept-review client. Focused scheduler/QA/page/treatment regression passed 46/46; all 2,956 repository tests produced 2,953 passes and three intentional skips; typecheck passed; lint exited 0 with only the unrelated existing `nextLayer` warning; and the 109-page Production build passed. After restart, the live page returned HTTP 200 with all concept controls, every dry/effect source set independently resolved to 18 unique source ids, and exact raw plus derived WAV ranges each returned HTTP 206 with 100 bytes. In-app click automation remained unavailable because the admin-enforced localhost policy could not be verified and was not bypassed, so hands-on audible acceptance remains the reviewer’s next step.
- The reviewer reported that wide dual echo sounded best and asked to hear the effect doubled. Batch 03 therefore advances through a new immutable v2 declaration rather than overwriting v1. The new `Wide dual echo ×2` option retains the `260/520 ms` taps, doubles decay from `0.34/0.17` to `0.68/0.34`, and applies `-3 dB` safety attenuation. Strict RED/GREEN expanded the closed matrix and complete-concept QA from three to four effects while preserving the existing scheduler and diagnostic UI owners.
- The checksum-verified FFmpeg 9.0 archive was restored from the exact local WinGet-manifest URL after WinGet had replaced the executable with 9.0.1; its archive SHA-256 matched `f42f0c4b04eae3ac918707ff66e3e0ff0cee527bfa6d322624d4bc1160d5055e`. The no-overwrite v2 render produced 72 stereo 48 kHz/24-bit WAVs totaling 71,680,872 bytes. Independent physical readback found 18 files per effect, zero missing/partial/size/hash mismatches, and peaks from `-23.9` to `-6.2` dBTP; the ×2 set peaks from `-23.9` to `-8.7` dBTP. The original three effects are byte-identical to their retained v1 counterparts. Declaration SHA-256 is `e05aadb04e87f56e9df4f69cba180ce37ca0f4481fbfc0fa2c981bdd4fb4163a`; manifest SHA-256 is `8ff6856b1342a8d88366eeae48e1307b7cbc0921ef0ef7755fce8ac4f7faa6c6`. Audible acceptance of ×2 remains pending.
- Final v2 verification passed 34/34 focused checks and all 2,956 repository tests with 2,953 passes and three intentional skips; typecheck passed, lint returned only the unrelated existing `nextLayer` warning, and the 109-page Production build passed. The first restart attempt safely failed because the verified stale v1 server still owned port 3013. After stopping only that exact Next.js listener/parent and starting the v2-configured server, the processing page returned HTTP 200 with the complete-concept comparison and both wide labels; an exact ×2 output returned HTTP 206 with 100 bytes, `audio/wav`, and `Content-Range: bytes 0-99/645836`.

## 2026-08-25 — Signature playback-construction audition

- Recorded the reviewer's direct approval of Sci-Fi Whistles' exact `0–8` second spaced-event sequence as a `playback-only` Pass. The decision is bound to all 18 current source IDs, settings, construction policy, scheduler algorithm, fingerprint, and heard timestamp. The pending echo/delay time-effect processing remains a separate gate and is not treated as satisfied by this scheduler approval. The eight-decision owner SHA-256 is `2f62735527f1eea50afbcf6c933f754c048850da5492af573d68eabf7fe9dbb3`; no other concept decision changed. Strict RED failed only on the missing Sci-Fi Whistles approval; GREEN passed 13/13, the adjacent construction suite passed 50/50, and the repository suite produced 2,939 passes, zero failures, and three existing skips.
- Recorded the reviewer's direct approval of Dryer's exact per-boundary `3.75–10` second transition behavior as a `playback-only` Pass. The decision is bound to the current source, settings, construction policy, scheduler algorithm, fingerprint, and heard timestamp. Dryer's trim-boundary processing remains pending and is not treated as satisfied by this scheduler approval. The seven-decision owner SHA-256 is `a0eb83c0c6ba51031be6600b0656aae2f9850c25c894c0f402d9937f72590921`; no other concept decision changed. Strict RED failed only on the missing Dryer approval; GREEN passed 13/13, the adjacent construction suite passed 50/50, and the repository suite produced 2,939 passes, zero failures, and three existing skips.
- Recorded the reviewer's direct approval of Air Traffic Control's exact `1–7` second spaced-event sequence with four intervening selections before reuse. Its `playback-only` Pass is bound to the current 12 source IDs, settings, construction policy, scheduler algorithm, fingerprint, and heard timestamp. This decision is intentionally separate from the 12 already approved normalized artifacts: combining those artifacts with the scheduler remains future production construction rather than inferred heard evidence. The six-decision owner SHA-256 is `5c65f52c089a613a6c3f9512acb1999e31bac73a8e91b43ddd0afa7c4dc45139`; no other concept decision changed. Strict RED failed only on the missing Air Traffic approval; GREEN passed 13/13, the adjacent construction suite passed 50/50, and the repository suite produced 2,939 passes, zero failures, and three existing skips.
- Recorded the reviewer's direct all-pass decision for Horror Suspense, Moon Footsteps, and Walking in Puddles as exact `complete-construction` approvals. Each decision is bound to its current source IDs, preview settings, construction policy, scheduler algorithm, construction fingerprint, and heard timestamp. The decision owner SHA-256 is `6cd09d6f1939264dd27044f2c100a37fbb587cfa85aeacc1f41ae287424eecd6`. Walk on Gravel and Walk on Leaves remain unchanged as note-backed `needs-rework`; no Dryer, Air Traffic Control construction, Sci-Fi Whistles, or Walk on Stone decision was inferred. Strict RED failed only on the missing three approvals; GREEN passed 13/13, the adjacent construction suite passed 50/50, and the full repository suite produced 2,939 passes, zero failures, and three existing skips.
- Added one pure construction-audition projection above the immutable construction review and one separately fingerprinted construction-QA owner. The exact browser projection is generated from and regression-checked against the full canonical authority chain; it contains eight playable rebuild auditions plus Walk on Stone as visibly blocked because its Change decision still has no construction instruction.
- Extended the existing single development preview player instead of adding a second scheduler. Construction policies now support three/four-selection rolling history, a fresh continuous transition duration inside the approved range at each boundary, cadence crossfade or overlap, and explicit next-event overlap while retaining all prior v2/v3 settings, identities, Stop cleanup, and late-error behavior.
- Added retained development-only `/dev/candidates/construction` and its hub link. It shows exact source paths, required playback behavior, pending processing intents, Gravel boundary A/B controls, one-group-at-a-time Start/Stop/Next transport, an explicit Confirm Current Setup Heard action, notes, scoped Pass/Needs Rebuild/Reject decisions, isolated cross-tab browser storage, and JSON export. Every judgment binds the exact active construction fingerprint, scheduler algorithm, source IDs, settings, policy, and heard timestamp. Processing-pending groups expose playback-only QA; blocked Walk on Stone exposes no QA. Changing Gravel's boundary or receiving a cross-tab configuration change stops playback so stale audio cannot be confirmed. Storage denial preserves exportable in-memory QA with a visible warning, while invalid saved JSON remains untouched for recovery.
- Strict RED/GREEN covered the missing projection, scheduler helpers, player behavior, QA owner, affected-group invalidation, page, generated browser projection, checksum drift, delayed-play cleanup, explicit heard confirmation, storage failures, and cross-tab configuration identity. Final focused verification passed 37/37; typecheck passed; lint exited 0 with only the unrelated existing `nextLayer` warning; all 2,913 repository tests produced 2,910 passes and three existing skips; and the 108-page Production build passed. Final complexity is within the recorded budget: QA core 497 lines plus a 41-line storage adapter, and review component 387 lines plus a 119-line pure model.
- The existing local server returned HTTP 200 and 194,524 response characters for the construction route with all nine groups. A checksum-verified real Dryer source range returned HTTP 206, 32 requested bytes, `Accept-Ranges: bytes`, `Content-Range: bytes 0-31/285536`, and `audio/mpeg`; the response uses the same verified byte snapshot and never reopens the source path. The in-app browser's admin-enforced localhost policy could not be verified, so interactive clicking/listening was not claimed or bypassed. This slice invoked no media tool, processed or wrote no audio, changed no source library or external output, and performed no production runtime, provider, upload, deployment, Git staging, commit, push, or merge action.
- Reconciled the returned construction-QA export after the reviewer recovered the correct complete-review file. The complete-review SHA-256 is still `d370788a6ef9af7f147c0dcafda18285b759fbca24249b82224b2c16ba844486`, exactly matching the preserved canonical fixture already used for construction. The construction-QA SHA-256 is `752a75f2ed5e18539df69312cea3135d6450615167df971e14d128fbf3fe8ac9`; it validates against construction fingerprint `a3e782b6c1c2d808bd7e8214cb655163f1bdfbc473318ae0ac9c916ccb84954d` and contains eight configurations, six exact heard records, two notes, and no final decisions. Added closed JSON import so the six heard setups can be resumed without replay; malformed or stale imports preserve current work. Gravel and Leaves remain active ingredient-pool rework candidates rather than inferred group rejections because their notes identify suspect recordings but contain no exact heard confirmation or decision.
- Corrected the returned-QA decision presentation after the reviewer reasonably interpreted a single-option QA-scope selector as a disabled decision control. A sole Playback Only scope now renders as fixed status, while Pass, Needs Rebuild, and Reject are separately labeled Construction Decision with exact confirmed-versus-locked guidance. This changes no QA schema, evidence identity, imported decision, or confirmation rule.

## 2026-08-25 — Signature review construction reconciliation

- Preserved the returned version-1 listening export and version-3 complete review as raw-hash-locked test evidence. Their SHA-256 values remain `0da9ad1dd4b184b059624af11963adbd2d85d4ad6c197b83691af6d58cd70dc0` and `d370788a6ef9af7f147c0dcafda18285b759fbca24249b82224b2c16ba844486`; the canonical projection remains 3,693 recordings, 93 groups, 27 group notes, 11 ingredient notes, and 57 inherited overall-recording notes identical to the earlier listening export.
- Added a bounded construction-review owner and exact interpretation declaration. It validates the full discovery, strategy, listening, and workspace authority chain; requires exact note coverage; rejects stale, fabricated, mismatched, or removed-source assignments; applies source-over-group speech precedence; and derives deterministic group playback, processing intent, review state, and SHA-256 identities without reading or processing media.
- The generated owner contains 44 closed resolutions and all 38 note dispositions: 36 structured, none deferred, and two unresolved naming decisions for Ceiling Fan and Road. Air Traffic Control is recorded as spaced 1–7 seconds with four intervening selections and relative normalization; Horror Suspense is spaced 0–16; Sci-Fi Whistles retains 0–8 spacing with an echo/delay audition; leaves require three intervening selections; Underwater Effects becomes Walking in Puddles pending a rebuilt cadence audition. Speech treatment distinguishes complete voice removal from obscured intelligibility and gives removed Busy Street and Supermarket sources no processing assignment.
- Added `npm run atmoshaper:sounds:reconcile-review`. Default JSON and `--format markdown` write nothing; only an explicit `--output data/atmoshaper/signature-sound-construction-review.json` may publish, using canonical repository containment, same-directory transaction files, rollback, and full reread validation. Real no-write output contains no machine path and byte-equals the 193,309-character generated owner.
- Independent review hardened the fail-closed contract before closeout: removed recordings can reference only their matching no-assignment resolution, overlapping source/group speech treatments cannot create ID-order ambiguity, every audio-changing intent requires audible QA, and disposition classification/state must agree with the linked resolution families. Both independent specification and code re-reviews then approved the corrected result with no remaining Critical or Important findings.
- Final corrected-state verification produced 16 focused construction/CLI passes with one expected host-limited Windows file-symlink skip; the directory-junction escape and rollback cases ran. Typecheck passed; lint passed with only the unrelated existing `nextLayer` warning; the full repository suite passed 2,888 tests with zero failures and three skips; and the 107-page Production build passed. Fresh real-input regeneration remained byte-identical at 193,309 bytes, retained both external input hashes, contained no machine path, and left no transaction residue. This slice copied, decoded, processed, encoded, qualified, published, or uploaded no audio and changed no development server, production runtime/provider, deployment, Git staging, commit, push, or merge state.

## 2026-08-24 — Shared Signature recording and concept review workspace

- Split the retained development review tool into a lightweight `/dev/candidates` hub, `/dev/candidates/recordings`, and `/dev/candidates/concepts`. Only the review routes load the 3,693-recording manifest; live responses are currently 200 for all three routes, and the hub response dropped from roughly 2.6 MB to 98 KB after moving the shared provider out of the segment layout.
- Added one closed, discovery-and-curation-fingerprint-bound version-3 browser workspace and complete export. Valid version-1 recording and version-2 group drafts migrate independently on first open, retain safe prior decisions, notes, settings, and approval evidence, and remain untouched as recovery inputs. Cross-tab updates validate before replacing local state.
- Recording review now supports overall Keep/Maybe/Reject and notes, concept-specific Include/Remove and notes, assigning one recording to multiple existing concepts, and creating custom concepts that immediately appear in concept review. Removing a recording from one concept does not affect its use elsewhere.
- Concept review now uses exact included source IDs instead of Keep/Maybe pool shortcuts. Every included or removed recording remains visible in a collapsed ingredient section, can carry a concept-specific note, and an included recording can start the single dynamic preview through Play This in Setup. Inclusion, strategy, or tuning changes stop playback and invalidate stale audition/approval evidence; note edits preserve it. No media, production runtime, qualification gate, upload, deployment, push, merge, staging, or commit action is part of this local review slice.
- Strict focused GREEN passed 22/22; the adjacent catalog/AtmoShaper boundary passed 175 with one existing Windows symlink skip; the repository suite passed 2,871 with two existing skips; and lint exited 0 with only the unrelated existing `nextLayer` warning. The source graph typechecked cleanly with generated `.next` folders excluded; normal typecheck reached only the active-development versus stale-prior-build Next route-type mismatch. Final live checks returned HTTP 200 for all three review routes and HTTP 206 `audio/wav` for a real manifest-listed snow recording.
- Corrected a post-handoff empty-concept render failure. Seven intentionally active concepts have no included recording, but the concept page tried to create their exact audition identities even though that identity correctly requires at least one source. Strict RED passed 7 page checks and failed the new empty-concept case; the consumer guard then made the page/workspace/preview focus pass 23/23. Empty concepts remain visible with Needs Included Sources, preview and approval stay unavailable, and the live concept route returns HTTP 200 without weakening the exact-key validator.

## 2026-08-23 — Audible Signature group preview and retained review tool

- Extended development-only `/dev/candidates` from a paper strategy queue into the recommended audible review workflow. Every group can audition Keep + Maybe or Keep-only ingredients through its selected dynamic behavior; whole-source and adaptive one-shot groups tune end-to-end, crossfade, or overlap boundaries, walking groups tune steps per minute and cadence variation, and spaced-event groups tune minimum/maximum gaps. One shared player stops the prior group before another starts, avoids an immediate source repeat when alternatives exist, caps concurrent preview voices, and tears down every owned timer and audio element on Stop or unmount.
- Versioned the sparse group export to v2. Approve Heard Setup remains disabled until the exact current strategy, source pool, and closed preview settings start successfully. The export records those settings, an audition timestamp, and a deterministic audition identity; changing a strategy, pool, or tuning stops the active preview and clears stale audition and approval evidence. Needs Changes remains available without listening, and the raw individual players remain below the group queue for ingredient inspection.
- Corrected the successful-preview handoff after direct review found that Birth and Boiling Water could play without enabling Approve Heard Setup. The caller had passed the full review entry, including notes and decision metadata, into the closed audition-identity validator after playback began; that rejected the record inside the playback error boundary. The page now derives the identity from only the current strategy, source pool, and preview settings, so approval becomes available as soon as the current setup starts successfully.
- Kept the scheduler/settings owner reusable while leaving its browser audio adapter and `/dev/candidates` development-only. The page is intentionally retained for future concept intake and audition. This slice does not process, copy, qualify, publish, upload, deploy, or connect the Signature catalog to production playback; raw preview behavior is not final loudness or seam evidence.
- Strict RED passed 5 existing tests and failed 9 expected preview/schema/page cases before implementation. A final one-case RED proved that a retired group's late media failure could overwrite the next group's visible status before the session guard was added. Focused GREEN passed 16/16, including exact real binding for all 93 groups, 8 snow-footstep ingredients, and both 60-source gravel/stone cadence pools. The adjacent AtmoShaper set passed 164 tests with one existing Windows symlink skip; typecheck passed; the final full repository suite passed 2,861 tests with 0 failures and 2 existing skips; lint exited 0 with only the unrelated existing `nextLayer` warning; and the 105-page Production build passed after the last runtime correction. A live page request returned 200 and a real snow-footstep byte range returned 206 `audio/wav` with the expected inventory size. The in-app browser's admin localhost check remained unavailable and was not bypassed.

## 2026-08-23 — Signature group-strategy review workflow

- Made the recommended group review the first workflow on development-only `/dev/candidates`. All 93 active groups now show their proposed playback family and details, Keep/Maybe/Reject ingredient counts, a selectable alternative among the four existing strategy families, Approve or Needs changes controls, and a group note. Seven zero-ingredient groups remain active and visibly need usable sources.
- Added a separate closed group-review owner and browser-local record tied to the exact curation fingerprint. The sparse JSON export preserves partial notes and strategy selections as well as decisions, rejects stale fingerprints and unknown group/strategy identities, and never changes the imported recording review or human-owned strategy declaration. `approve` means accepted playback design for later reconciliation, not listening, technical, processing, publication, or production approval.
- Strict RED produced 5 expected failures after 3 existing page tests passed; focused GREEN passed 8/8. The adjacent catalog/runtime boundary passed 135 tests with one existing Windows file-symlink capability skip. Typecheck passed, lint exited 0 with the same unrelated existing `nextLayer` warning, the full repository suite passed 2,853 tests with two existing skips, and the 105-page Production build passed. After the Codex restart interrupted only running processes, the build was rerun from scratch and the dev server was restored; `/dev/candidates` returns HTTP 200 with the group controls. The in-app browser's admin-enforced localhost policy remained unavailable and was not bypassed, so browser click-through is still the bounded manual review step.

## 2026-08-23 — Signature listening-review curation and dynamic strategy handoff

- Imported the fingerprint-matched `/dev/candidates` export through `npm run atmoshaper:sounds:curate-review -- "<exported-review-json>"` into a deterministic, path-free repository curation. The 926 proposed sources now reconcile as 354 explicit Keeps, 113 explicit Maybes, 360 explicit Rejects, and 99 contextual Maybes covering 98 untouched proposals plus the note-only Forest Atmosphere 19 entry. Reject remains an individual-recording decision; excluded and unclassified discovery sources are not promoted.
- Kept all 93 proposed concept groups active. Seven groups currently have no non-rejected recording and therefore need a different usable ingredient, but their concept and strategy remain active. The dynamic strategy policy uses shuffled whole-source sequencing by default; walking-cadence sequencing for snow, gravel/stone, leaves, and moon footsteps; adaptive one-shot sequencing for Horror, Keys Jingling, and underwater effects; and spaced-event sequencing for whistles. Crowd, public-place, and wave families retain dynamic whole-recording order with end-to-end, crossfade, or overlap boundary treatment.
- Updated the development-only candidates page to project imported decisions, notes, group status, and proposed strategy separately from the editable localStorage draft. A second real import was byte-identical, the page returned HTTP 200 with the curation and strategy sections, focused curation/discovery/page coverage passed 25/25, adjacent catalog/runtime coverage passed 121 tests with one existing host-capability skip, and the full repository suite passed 2,849 tests with two existing skips. Typecheck, lint with one unrelated existing warning, Prisma validation, the 105-page production build, source syntax checks, and diff checks also passed. This curation does not implement the production scheduler, choose exact cadence/crossfade parameters, qualify a production candidate, process media, upload, deploy, push, or merge.

## 2026-08-23 — Exhaustive Signature Sounds review page

- Corrected the earlier declaration-only interpretation after the user identified obvious omissions such as walking on snow and stone. The new deterministic discovery owner and 100-pack review map partition every one of the 3,693 scanned recordings into exactly one visible state: 926 proposed candidate sources, 2,738 explicit exclusions, and 29 unclassified sources. Proposals cover 34 Moodist concepts and 59 Signature-only ideas, including all 8 snow-footstep files and all 246 Walking On Stoney Pathway files. The original four-list audit remains the conservative production declaration and gate owner; its 74 gaps now explicitly mean undeclared mappings rather than verified absence from the library.
- Added `npm run atmoshaper:sounds:discover -- "<signature-root>"`, exact filesystem-pack coverage, closed rule/manifest schemas, stable fingerprints, atomic in-repo manifest publication, and no source-root serialization. The generated 2,646,555-byte manifest retains source checksums, sizes, proposal reasoning, confidence, and declared-candidate links without copying audio.
- Added the development-only `/dev/candidates` page and byte-range audio route. It provides search plus discovery-state, concept, and local-decision filters; paginated one-source players; Keep/Maybe/Reject and notes stored only in browser `localStorage`; and JSON export. The server owns `ATMOSHAPER_SIGNATURE_SOUNDS_ROOT`, canonical containment and byte-size drift fail closed, and both page and route are unavailable in Production. A real local request returned HTTP 200 for the page, while a snow-footstep `bytes=0-31` request returned HTTP 206, 32 bytes, `Accept-Ranges: bytes`, and `Content-Range: bytes 0-31/6890138`. The in-app browser could not perform visual interaction because its admin-enforced localhost policy could not be verified; that policy was not bypassed.
- Strict-TDD receipts are 10/10 discovery tests and 3/3 dev-page/audio tests. The combined catalog/audit/planner/discovery/page set passed 82 tests with zero failures and one host-limited file-symlink skip. The isolated worktree received its lockfile-pinned dependencies and generated Prisma client; task-owned type errors are clear. No declaration promotion, technical or listening pass, processing, media copy, Git media, upload, production wiring, deployment, push, or merge occurred.

## 2026-08-23 — AtmoShaper Signature Sounds catalog audit and inert processing plan

- Recorded Moodist's exact 84 non-binaural concept identities and upstream references as research evidence in local commit `a5b0f0a2`; Moodist media is retired, no Moodist binary fallback exists, and the five AtmoShaper binaural presets remain separate from the 84 concepts. Native white, pink, and brown noise remain generated sources rather than recording gaps.
- Added the repeatable read-only Signature Sounds audit and repo-relative [dated catalog report](superpowers/reports/2026-08-23-atmoshaper-signature-sound-catalog-audit.md) in local commit `9cce4abf`. The full scan records 100 top-level packs, 3,693 audio files totaling 9,995,726,103 bytes, 3,587 WAV, 69 MP3, 35 AIF, 2 AIFF, and 239 duplicate-checksum groups; MIDI is excluded. The user explicitly accepted Signature Sounds' own site-wide CC0 statement for this downloaded library, while pack-specific CC0 files remain stronger evidence and no license evidence satisfies technical, listening, or processing gates.
- The four current catalog decisions are 0 qualified Moodist matches; 7 mapped candidates needing audition or processing (Campfire, Cafe, Crowd, Road, Traffic, Waves, and Wind Chimes); 74 recording/source gaps; and 9 exploratory Signature-only candidates (Air Traffic Control, Cave Room Tone, Church Bells, Lunar Wind, Male Choir, Open Field Ambience, Room Tone, Stadium Crowd, and Train Station Announcement). All 16 candidates remain active with technical, listening, and processing pending. No listening approval or production qualification is claimed, and Signature-only concepts remain outside the 84-concept production catalog unless separately approved.
- Added the inert processing planner in local commit `75b0f422`. It accepts only Moodist-mapped, accepted-evidence, active candidates with technical and listening passes and processing pending or verified; binds assignments to fresh audit checksums and duration/channel/rate measurements; plans a `T + X` internal cyclic seam, two-pass loudness placeholders, WebM/Opus plus M4A/AAC outputs, full content identities, no-overwrite arguments, and anchored publication history; and emits `ffmpeg` argument arrays without executing them. The current real result is `no-qualified-assignments`, `not-run`, and zero sources, with no output directory created. Actual encoding and post-output decoded seam/loudness verification remain future authorized work.
- Added linked-worktree output-root confinement in local correction commit `afb4e30c`: the planner now fences both the active worktree and its main checkout by resolving Git's gitfile/commondir metadata. The same correction removed user-specific absolute paths from the task plan and work record.
- Pre-correction focused and adjacent coordinator verification finished at 150 passes, 0 failures, and one Windows file-symlink capability skip. After the linked-worktree correction, the affected focused and adjacent set passed 136 tests with 0 failures and the same one host-capability skip; the final full suite passed 2,824 tests with 0 failures and two existing skips. Lint exited 0 with one unrelated warning. Typecheck is not recorded as passing because this worktree lacks the existing `@dnd-kit` packages imported by unchanged accepted UI; that dependency condition was not introduced by the processing planner. No Git media, hosted manifest, R2 or other upload, runtime wiring, deployment, push, or merge occurred.

## 2026-08-22 — AtmoShaper visual-review refinements

- Refined the approved AtmoShaper workspace after direct user review. The Sound Library glow-tab rail is transparent and reserves its glow instead of clipping it. Temporary preview status, volume, Retry, and Stop Preview now stay inside the matching source card without shifting the tab rail or card grid. Binaural and isochronic views share one centered, feathered wave-and-gradient canvas sized to 120% of its inner owner and verified to exceed the Sound Library surface by at least 10% before that outer boundary clips it, so the visible waves extend behind immediately reachable presets and settings without a separate nested panel or hard image border.
- Simplified Current Mix without changing audio ownership: it is one continuously mounted, flush, full-height screen-edge rail that honors shell/player reserves and widens inward from the same outer edge. The compact rail and expanded controls are not separate surfaces. Mix, Play/Stop, whole-mix volume, and each layer keep the same DOM identity and vertical anchors while their slots stretch into the expanded layout; the compact whole-mix Volume button is constrained to that shared slot so its complete Outline border remains visible, then reveals the precise outlined horizontal slider instead of exposing an undersized vertical control. Each expanded row keeps its name/status, destructive Remove, drag handle, Mute, and Solo together, while every square collapsed layer uses a full-width source label above equal, centered Mute/Unmute and Solo/Unsolo icon actions. Solo is transient UI state backed by the existing mute actions; it survives expansion/collapse, snapshots the exact earlier mute pattern, and later restores that pattern. Stop uses destructive intent. Clicking the Sound Library outside a roomy modeless panel collapses the same rail; narrow modal focus and semantic opener restoration remain intact without remounting the controls.
- The complete AtmoShaper source/model/provider set passes `138/138`. Its desktop/mobile Chromium browser acceptance passes `57` cases with one intentional desktop-only touch exclusion; the seven-size open-drawer geometry matrix passes independently in both projects with no document overflow or player/app-bar overlap.
- Prisma generation and validation, typecheck, lint with one pre-existing unused-variable warning in the mixer-controller test, all `2,756` deterministic tests with `2,755` passes and one intentional skip, the ordinary `104/104`-page Production build, the production-bundle QA-marker absence assertion, and `git diff --check` pass.
- The user accepted the final visual-review pass after confirming the widened backdrop and the shared expanded/collapsed rail alignment.

## 2026-08-22 — AtmoShaper overlay drawer and preview acceptance

- Completed the redesigned AtmoShaper browser acceptance without introducing a second audio owner or player. The provider-owned temporary preview remains outside global player and Media Session identity until promotion; replacement, volume, failure/Retry, Stop Preview, station replacement in both directions, route cleanup, interruption recovery, Stop-all, and same-handle/id promotion are covered. A failing acceptance run first exposed an explicit-Stop race that could republish late playing or failed committed runtime snapshots, including preview-facing state; the provider now rejects every non-stopped committed callback before any public snapshot mutation until a later explicit Play intent accepts new snapshots, while legitimate preview-only publication remains available. A held-teardown race additionally proved that a stale Stop continuation must recheck its captured request, session generation, playback kind, and committed runtime owner immediately before synchronous lease invalidation; retained-player Play now remains current and playing when that older cleanup resumes. A separate failing 200% text acceptance run exposed an interruption notice whose Close action could leave the viewport; the notice now scrolls inside the available player-safe height and subtracts the configured top safe area. An unchanged Media Session acceptance case also exposed that async preview teardown could delay the stopped-player retention window; its exact 60-second deadline now starts from explicit Stop before teardown, while later Play or replacement cancels retirement and stale callbacks cannot retire a newer owner.
- Verified the opposite-sidebar rail and overlay drawer on both sidebar sides, modeless roomy interaction without Sound Library width loss, modal narrow focus containment/backdrop/Escape/exact opener restoration, first-layer discovery with stable later additions, ordered layer status/mute tiles, and pointer, touch, and keyboard handle sorting without changing live layer ids or playback. Timestamped TouchSensor acceptance proves activation does not occur before the configured 180ms delay, movement beyond tolerance remains cancelled across that boundary, and a successful same-target drop activates only after the delay. Sound Library cohesion, generated and canonical artwork, forest-green actions, accessible labels, reduced motion, and 200% text pass. Geometry receipts at `375×667`, `412×915`, `844×390`, `768×1024`, `912×1368`, `1440×900`, and `2560×1440` record workspace, library, rail, open drawer, interruption notice, app bar, player, configured safe top, root font, and document rectangles with no document overflow; the measured `42rem × 32rem` workspace formula, not device or zoom detection, selects roomy versus narrow behavior.
- The expanded AtmoShaper suite passes 23 cases with one intentional touch-project exclusion on desktop Chromium and all 24 cases on mobile Chromium. The required unchanged combined AtmoShaper plus Media Session gates pass `58` with `24` intentional exclusions on desktop Chromium and `73` with `9` intentional exclusions on mobile Chromium. The exact stopped-player boundary and restart/replacement race cases pass unchanged in both required projects.
- Prisma generation and validation, typecheck, lint with one existing unused-variable warning in an unchanged mixer-controller test, all 2,741 deterministic tests with 2,740 passes and one intentional skip, the 104-page ordinary Production build, the production-bundle marker-absence assertion, the retired Current Mix/TODO scan, and `git diff --check` pass. The 84-sound catalog, saving and subscription recall, permanent slots, artwork upload, public sharing, Lo-Fi, commerce, deployment, push, and merge remain outside this package.

## 2026-08-21 — AtmoShaper core-mixer acceptance

- Completed browser acceptance for the live-session AtmoShaper core mixer: one or multiple generated layers without a station, exclusive station/binaural/isochronic limits, ordinary-station replacement in both directions, silent stopped edits, live per-layer changes, isolated one-shot failures with Retry/Remove, keyboard operation, stable failure announcements, narrow-Sheet focus return, reduced motion, 200% text, and no document overflow across the requested phone, landscape, tablet, laptop, and TV viewport matrix.
- Kept the global music provider as the single playback and Media Session owner. AtmoShaper publishes one mix identity with the standard 512×512 MassageLab platform artwork, Play/Pause/Stop, and no Previous/Next actions; stopped Media Session restart retains a null station id. Public-snapshot diagnostics and one-shot failure injection compile only into the explicit browser-QA build, remain loopback/request guarded there, never expose private Tone nodes, and are marker-scan absent from the ordinary production client bundle.
- Added the acceptance spec once per Chromium project to the existing music/media CI lane and documented the live-session-only boundary. Prisma generation/validation, typecheck, lint, all 2,680 deterministic tests with one intentional skip, the 104-page Production build, the combined AtmoShaper/full Media Session run with 106 passes and 32 intentional project exclusions, the ordinary production marker-absence scan, and diff checks passed. This records no deployment or ambient-catalog licensing completion; ambient media, saved mixes, Supporter recall, permanent slots, user artwork, sharing, and lo-fi remain later packages.

## 2026-08-17 — Atmosphere Android media and responsive-player physical acceptance

- Completed the affected Round 4 physical matrix for PR #183 on the recorded Samsung Galaxy S24 Ultra with Chrome `151.0.7922.137`, using immutable deployment `https://massagelab-1ia9a0227-dsbteam.vercel.app/music` at exact merge head `ca429f7f4c3cc1c40bac2a850be73a8226981c2e`. The user confirmed first-touch playback; sharp, correctly paired notification art through Previous/Next; 52-second vinyl motion with static reduced motion; approximately 7px rail-art clearance; stable portrait center-card height across expanded/collapsed player states; conditional touch-only station controls; bidirectional looping; global constrained-landscape side-rail placement; and exact 60-second stopped-player retirement with restart cancellation.
- The candidate branch retained `origin/main`'s split browser-QA workflow and added the Atmosphere media spec to the exact-once four-lane inventory. Post-branch verification passed 170 focused Node tests, the 16-test browser harness, 529-test Playwright discovery across ten specs, lint, typecheck, all 2,577 unit selections with one intentional skip, the 104-page Production build, desktop Chromium at 141 pass/48 intentional skips, mobile Chromium at 157 pass/32 intentional skips, and WebKit media smoke at 22 pass/33 intentional skips. Hosted run `32073060398` reached a coherent all-green third attempt after two excluded infrastructure/artifact attempts; CodeQL and Vercel were also green.
- Hosted proof for deployment `dpl_8buijut3cNtX8ReUVTJmh77be9EA` returned `/music` HTTP 200, three inline SVG artwork surfaces with zero fallback or artwork-route references, stable distinct revisioned 512×512 station PNGs that decoded in Chromium, and no artwork application errors in the bounded log sample. The three final screenshot rasters are retained in the QA report as `1492×667`, `1492×667`, and `720×1452`. Android Chrome searched the requested `javascript:` diagnostic, so CSS viewport, DPR, and safe-area dimensions are not inferred from those pixels.
- The requested notification Favorite/heart remains explicitly deferred to a future native Android wrapper; no duplicate web notification or unrelated Media Session action is substituted. Installed-PWA, Bluetooth/headset, real-call, meeting-app, controlled carrier-failure, broader lock/background-return, current-session preference, and physical Apple rows remain pending. This docs-only closeout changes no tested bundle path; PR #183 remains unmerged pending a fresh substantive CodeRabbit review and green hosted checks.

## 2026-08-17 — Faster pull-request feedback proof

- Completed the PR #185 CI split without reducing the repository-owned QA surface. Code quality runs independently from one Production browser build; that exact runtime artifact feeds four validated Playwright project/spec lanes; and the stable final `qa` job reports and fails for every non-success dependency. The canonical lane manifest preserves every one of the 18 desktop/mobile project/spec units exactly once. Implementation head `728eaa405e5e1a00730ece36dd0a43b2db460547` received a substantive hosted CodeRabbit review with zero actionable comments, and all six earlier review threads are resolved.
- Fresh local exact-head validation passed the 15-test CI/browser harness, typecheck, lint with only the known `app/chimer/running-timer.tsx` Babel large-file notice, the full 2,507-test suite with 2,506 passes and one intentional skip, the 104-page Production build, ordinary Playwright discovery at 310 tests in nine files, lane discoveries of `64 / 94 / 61 / 91` tests, and `git diff --check`.
- Hosted run `32039251082` accepted [cold attempt 4](https://github.com/dsbowersock/massagelab/actions/runs/32039251082/attempts/4), [warm attempt 5](https://github.com/dsbowersock/massagelab/actions/runs/32039251082/attempts/5), and [warm attempt 6](https://github.com/dsbowersock/massagelab/actions/runs/32039251082/attempts/6). The cold attempt missed and saved exact Next.js cache ID `6717066121`; both warm attempts restored the exact primary key and did not save again. Runner-to-final-`qa` times were 8m 58s, 8m 50s, and 9m 00s, with created-to-completed times of 9m 03s, 8m 55s, and 9m 05s. All jobs were green, every attempt discovered `64 + 94 + 61 + 91 = 310` tests, and no Playwright retry or flaky marker appeared. Browser-step timings were `242 / 297 / 316 / 325s`, `288 / 291 / 284 / 322s`, and `294 / 289 / 291 / 328s`; per-lane medians were `288 / 291 / 291 / 325s`, for a 12.85% spread under the approved 20% limit. This proves the healthy cached target against the observed roughly 25-minute sequential baseline.
- Attempts 2 and 3 of the same run are excluded infrastructure samples, not failed acceptance samples: GitHub codeload returned HTTP 429 while downloading the pinned `actions/download-artifact` archive, stopping lane 4 and lane 2 respectively before Playwright started. Each attempt created a cache after a genuine miss, but neither executed all 310 tests; their exact caches were deleted only after explicit authorization, and a 37-minute cooldown preceded the accepted cold attempt. The PR remains open and unmerged, and the repository ruleset still does not require `qa` or provider checks.

## 2026-08-16 — Post-PR #181 Admin access and browser audit

- Completed the user-approved cleanup after PR #181 and retained only the Admin-panel audit corrections. Verified full `ADMIN` assignments now receive every currently implemented non-PHI paid feature through explicit Admin entitlement provenance without fabricating a Stripe subscription or changing truthful billing level data. `cloud_storage` and `phi_storage_tools` remain excluded behind their existing compliance gates.
- Preserved completed two-factor-reset feedback after server revalidation disables the form, and tightened the Admin browser selectors for the exact Security heading and `$20.00` billing row. Background and Chimer access responses now carry the explicit Admin provenance needed to render these entitlements consistently.
- Validation passed 170 focused tests; the full 2,500-test suite with 2,499 passes and one intentional skip; typecheck; lint with only the existing Babel large-file note for `app/chimer/running-timer.tsx`; the 104-page Production build; and all 16 desktop/mobile Admin browser journeys against a disposable Neon branch. The disposable branch was removed afterward and verified absent. Production data was not mutated, and no live Stripe, email, deployment, or merge action occurred.

## 2026-08-13 — Mobile media carousel and player controls

- Moved Background metadata, access/commerce state, favorite, navigation, automatic-preview preference, and compact accessible details from artwork cards into one permanent responsive tray. The tray sits below the carousel normally and to its right in short landscape; short landscape uses a radius-one three-card window while other Background profiles retain radius two. Cards now contain only preview media and selected styling, and the panel keeps the stage plus controls reachable without required horizontal or vertical scrolling.
- Animated Background previews now default on after hydration and persist as one device-local preference. Reduced motion temporarily keeps posters visible without erasing saved intent, and unavailable storage—including a throwing `localStorage` property getter—falls back safely to the default-on session choice. Every authoritative commerce state has a concise direct access label, including Free, Owned, membership or temporary inclusion, credit-distinguished Locked states, Unavailable, In cart, and Reserved.
- Made station artwork/title/description the shared swipe-and-tap details surface: horizontal drag navigates, a short tap opens information, and keyboard activation, vertical scrolling, analytics, Play/Stop, and Favorite remain protected. A truthful carousel-ready signal prevents browser acceptance from interacting with server-rendered markup before Embla initializes.
- Reworked the sitewide audio toolbar around state-derived Play/Stop rather than separate Restart and Stop actions. Narrow expanded mode uses identity/status above a complete icon-only green action row; narrow collapsed mode keeps identity/status with Play/Stop and Expand in one row; wide volume remains. Top and bottom safe areas, loading, increased text, shell reservation, and Chimer offsets are covered without toolbar scrolling. Casting, Android notification implementation, fullscreen notice placement, and fullscreen size controls remain deferred or unchanged.
- The branch was refreshed with current `origin/main`, preserving temporary premium-background provenance through the carousel commerce mapper. Final local evidence after that merge passed 36 focused access/tray tests, typecheck, lint, 2,495 unit tests with one intentional skip, the 104-page Production build, and `git diff --check`; the feature browser matrices had already passed Background preview, commerce/panel, mobile shell/routes, and desktop route/visualizer coverage.

## 2026-08-13 — Billing-goodwill reconciliation correction

- Corrected known-transaction reconciliation to verify the exact balance-transaction ID, expected Customer, live/test mode, `usd`, exact negative amount, originating operation/idempotency evidence, and safe non-positive transaction `ending_balance`. The persisted `endingBalanceCents` is immutable evidence of the Customer credit immediately after that exact transaction, not a required match to preview-derived starting balance plus amount. A later current Customer balance is an optional separate observation: a safe current credit may be reported, while an unavailable read or legitimate current debit makes no current-credit claim and does not block exact historical verification. Malformed, wrong-ID, deleted, wrong-mode, or unsafe returned Customer evidence still fails closed. Later invoices, refunds, and balance adjustments do not require a replacement operation or idempotency key.
- Added the final authority boundary immediately before any new provider create, after eligibility reads and the final replay-clock check. Only a typed database-confirmed full-Admin denial can record `FAILED_BEFORE_MUTATION` / `ADMIN_AUTHORITY_REVOKED`, and only while the creating invocation still exclusively owns a never-attempted no-ID `PREPARED` operation. Infrastructure/unknown failures, pre-existing or lost ownership, and post-check races remain unresolved and provider-ambiguous without persisting raw errors. A reconciliation-only caller first durably moves an existing no-ID `PREPARED` operation to `RECONCILIATION_REQUIRED` before provider I/O, preserving the same operation, key, and origin evidence and preventing a creator from falsely claiming definite no mutation.
- Updated Admin presentation to distinguish `Credit immediately after this credit` from an optional freshly read `Current Stripe credit`. Local replay without that read makes no current claim, while pre-mutation `Resulting credit` remains a projection. The earlier 2026-08-11 test-mode proof remains accurate as one historical observation in which transaction ending balance and the refreshed Customer balance matched; that equality is not the general reconciliation rule.
- These Admin Operations Closure Branch 2 slices are based on `origin/main` merge `132b59cd`, with Task 1 at `46de4bf2`, Task 2 at `45691cd7`, Task 3 at `4ba4c8b6`, and the whole-branch reconciliation-ownership correction at `6bda6746`. Focused slice evidence is 68/68 for Task 1, 82/82 for Task 2, and 31/31 UI tests for Task 3; each slice passed its independent specification and quality reviews after fixes plus typecheck and diff checks. The first whole-branch review then found that generic stale creator-side pre-provider failures could overwrite a reconciler's durable claim. Strict-TDD regression coverage now proves all five definite pre-provider routes use the same exact creator-only `PREPARED`/null-ID compare-and-set; a lost claim preserves canonical `RECONCILIATION_REQUIRED` or transaction-bearing `APPLIED` evidence, and clean independent spec and quality re-reviews approved the correction. Corrected-head validation passed 232/232 focused and adjacent tests, `npm run typecheck`, `npm run lint` with only the existing Babel large-file note for `app/chimer/running-timer.tsx`, `npm run prisma:validate` with no schema change, and `npm run test` with 2,479 passes, one intentional skip, and zero failures across 2,480 tests. Prebuild Prisma generation and `npm run build` also passed, generating 104/104 pages. No live Stripe/provider, database, email, or local browser action occurred. Ready PR #181 remains open and unmerged. Exact pushed head `182ed627` passed all non-CodeRabbit hosted checks, including Browser QA. A substantive hosted CodeRabbit review completed at `2026-08-13T19:54:03Z` as review `4931055672` / run `c4c9b211-a7ff-4ada-9155-379caaa0197f`; it verified the four earlier fixes and raised two valid minor follow-ups. The four earlier threads are resolved. The required follow-up head must contain both current corrections: the focused fixture documents its per-actor initial load versus final provider-boundary recheck, and canonical/lifecycle status reflects the completed hosted review. Fresh substantive review of that exact head and resolution of the two current threads remain required. No Production activation is claimed, and the live gate remains closed. Eligibility, `$0.01`-`$100.00`, USD, confirmation, 23-hour-55-minute, live-gate, mutation-entry, QA zero-mutation, and separate exact account-plus-amount live-authorization rules remain unchanged. Admin Operations Closure Branch 3 Production activation, migration deployment, and browser acceptance must wait for Admin Operations Closure Branch 2 to merge.

## 2026-08-11 — Atomic password-reset consumption

- Completed the password-reset integrity branch for both ordinary self-service and Admin-requested links. Successful consumption now claims one unexpired unused link and, in the same serializable transaction, changes the password credential, consumes every outstanding reset link for the account, increments `User.authSessionVersion` exactly once, and deletes Prisma `Session` rows for adapter compatibility. A failed transaction leaves all of those effects unchanged, and concurrent submissions cannot produce a second successful password change.
- Link consumption does not create an Admin action, target Activity entry, or account-change email intent. An Admin-requested reset keeps the immutable evidence created when the request was issued, but successful use of its link creates no second Admin evidence bundle.
- Existing JWTs become invalid immediately in version terms. Auth.js observes that invalidation when an old token next reaches a successful database-backed refresh. The adapter `Session` delete count remains compatibility evidence only; it is not an active-JWT count or an exact count of users signed out.
- Branch validation passed Prisma client generation; 69 focused and adjacent password-reset, route, authentication, session-version, and Admin security tests; typecheck; lint with only the existing Babel large-file note; the full 2,447-test suite with 2,446 passes and one intentional skip; the 104-page Production build; and `git diff --check`.

## 2026-08-11 — Admin billing-goodwill controls and test-mode proof

- Added the original Admin User Operations Branch 8 Billing-detail surface around the test-first invoice-credit ledger/service. A read-only provider preview supplies the current Stripe credit and projected next invoice, while the existing privacy-bounded local projection supplies the current Supporter amount/interval when safely known. The form offers `$1`, `$2`, `$5`, `$10`, `$20`, and `$50`, a custom `$0.01`-`$100.00` amount, and the projected invoice only when it remains inside the same bounds.
- Apply independently reloads full-Admin authority, route-binds the target, and requires one stable rendered UUID, the expected starting credit, shared reason/note, exact normalized target email, exact dollar amount, and a fresh authoritative USD subscription. Only `VERIFIED` results enter the existing locked initial email delivery owner. The canonical unresolved set is `PREPARED`, `APPLIED`, and `RECONCILIATION_REQUIRED`; each unresolved state sends no notification and exposes the same-operation reconciliation action with freshly typed exact target email and stored two-decimal amount. No-ID replay uses a conservative 23-hour-55-minute margin and rereads the clock immediately before creation. Known historical transactions remain verifiable after later balance activity, and another current full Admin can reconcile while preserving the originating Admin in immutable evidence. Successful reconciliation uses neutral verified copy rather than claiming the operation had already been verified.
- Directory filtering and aggregate metrics include all three canonical unresolved billing-goodwill states; directory rows continue to display only a count and never expose Stripe Customer, subscription, or balance-transaction identifiers. Billing detail loads 26 rows to truthfully flag truncation while displaying only the newest 25 recovery actions and their local status. Apply and reconcile result regions are owned outside revalidated preview/row content so returned feedback survives a missing fresh preview or removed reconciled row, while keyed confirmation inputs still reset. Desktop/mobile Playwright source covers preview and confirmation presentation through a dedicated database-sentinel-gated read-only Stripe fixture whose mutation methods throw; both server actions also reject the exact opted-in disposable QA identity before service or Stripe-client construction, and the browser spec records zero matching form submissions and POST requests.
- Live Stripe creation now requires all three independent signals: `NODE_ENV=production`, exact `VERCEL_ENV=production`, and `ADMIN_BILLING_GOODWILL_LIVE_ENABLED=true`. Preview deployments and missing Vercel environment identity fail before provider creation; test keys retain their ordinary non-live path.
- Focused action/UI contracts and adjacent billing-goodwill, Admin service/detail/directory/dashboard, Stripe, fixture, and browser-harness tests use injected stubs or source checks only. Separately, the user-authorized integration proof cloned the selected subscribed account into a disposable Neon branch, replaced only the clone's billing identity with one disposable non-live Customer and active USD test subscription, and applied exactly `$0.01` through the production `previewInvoiceCredit()` / `applyInvoiceCredit()` service. Authoritative readback proved one `-1` cent USD Customer balance transaction, `-1` cent Customer balance, local `0` to `1` cent `VERIFIED` evidence, a succeeded Admin action, target Activity, and an `example.test` intent still `PENDING` with zero attempts. The test Customer/subscription and disposable branch were deleted afterward and verified absent. No email was sent; live Stripe and the production database were untouched, and the separate live gate remains closed.

## 2026-08-10 — Temporary feature-access controls and visibility

- Completed the original Admin User Operations Branch 7 full-Admin temporary-access flow for the exact low-risk allowlist: `premium_backgrounds`, `therapist_documentation_tools`, `calendar_basic_scheduling`, `calendar_full_scheduling`, and `external_calendar_sync`. `chimer_custom_colors`, practice management, team scheduling, cloud storage, and PHI storage remain excluded. The grant UI offers `7`, `30`, and `90` day presets plus a custom whole-number `1`-`365` duration, uses one request-time start/end preview, requires a shared support reason/note and explicit confirmation, and clears confirmation whenever feature or duration changes.
- Grant and revoke actions reload full-Admin authority before parsing, bind the hidden target to the route, validate one stable server UUID and a sorted complete per-feature active-grant snapshot, and call exactly one canonical append-only service. Self-target temporary grants are allowed. Each active grant remains separately visible and separately revocable; revocation appends a row and never claims that membership or another temporary source stopped granting the feature.
- Post-commit email delivery uses the locked initial-delivery owner. An exact replay can recover a still-`PENDING` delivery; `FAILED` does not auto-resend, `DELIVERED` never resends, and mutation/replay copy remains distinct from notification state. Target detail, the directory, dashboard, and Account are revalidated after every committed result, including uncertain delivery, while self-target messages never promise the Activity retry control that is suppressed on self detail.
- Account Membership now shows each active temporary feature with a friendly label and its expiration, without grant, actor, internal-note, or idempotency evidence. Expired entries disappear through the uncached request-time entitlement load; there is no scheduled expiration job. Admin detail truthfully labels bounded/truncated evidence and withholds mutation controls unless the active snapshot is complete.
- Added `temporaryAccess=active|none` directory filtering with the exact `startsAt <= now`, `expiresAt > now`, `revocation: null` predicate and stable cursor behavior. Full-Admin directory/dashboard summaries count active grants and grants expiring before the exclusive end of one named 30-day operator window; they do not count users. Desktop/mobile Playwright source covers the gated SMTP-blank disposable-QA grant, Account/Admin expiration, append-only revoke, exclusions, and fresh confirmations. Fixture cleanup now deletes temporary revocations before grants before exact users; no live database, email, or browser mutation was run without the required database sentinel.
- Closed the PR review provenance gap for premium backgrounds: authoritative preference GET/PUT responses now preserve whether `premium_backgrounds` is membership-backed or temporary-only, with membership winning when both sources are active. Chimer carries that privacy-safe enum through every access refresh, and the production Background carousel renders temporary-only access as `included-temporary` instead of “Included with membership”; ownership, free, locked, and legacy aggregate snapshots retain their established precedence.

## 2026-08-10 — Admin background-credit goodwill grants

- Added a positive-only full-Admin goodwill path that reuses the canonical verified-user background-credit wallet and immutable ledger. A grant accepts only a whole number from `1` through `25`, compares the prepared persisted balance, and atomically records the wallet increment, `ADMIN_CORRECTION` entry, commerce event, Admin action, target-visible Activity, and durable email intent. There is no subtract, negative adjustment, or exact-balance operation, and self-target grants remain allowed.
- The target Access section now offers `1`, `2`, `5`, and `10` presets plus a bounded custom amount. It shows the target, current balance, Admin delta, and resulting balance before explicit confirmation. A verified account with no wallet truthfully previews persisted `0`, the automatic canonical `+2` initial allocation, and then the Admin delta while submitting prepared balance `0`; unavailable or malformed evidence renders no mutation form.
- The route action begins with fresh full-Admin authorization, binds the hidden target to the route, validates the server-rendered UUID, amount, prepared balance, shared reason, optional 500-character note, `OTHER` requirement, and confirmation before calling the Task 14 service exactly once. Post-commit delivery uses the locked initial email owner, and success/warning/replay copy distinguishes the committed grant from notification state. Access, Activity, and the directory are revalidated after every committed result.
- Extended desktop/mobile Playwright acceptance source for preset and custom preview math, keyboard operation, explicit confirmation, one positive grant, updated balance and Activity evidence, and a fresh unconfirmed form after revalidation. The production action persists the grant and its immutable database evidence. The existing exact-ID fixture supplies the required verified missing-wallet target and FK-safe cleanup, but no disposable database authorization is present in this workspace, so implementation tests, documentation checks, and source-contract QA claim no live browser, email, or database mutation; the Playwright-owned server contract continues to blank SMTP configuration.

## 2026-08-09 — Confirmed Admin security remediation controls

- Added a full-Admin-only Security section for three bounded operations: invalidate older sign-in tokens and compatibility sessions, request a standard password reset, and reset enabled two-factor authentication. Self-target detail remains read-only; each form binds the route target, retains one server-rendered UUID, validates the shared reason/note contract, exposes a disabled pending state and accessible result regions, and rechecks full-Admin authority in both the action and service.
- Revoke and 2FA reset increment the canonical `User.authSessionVersion` in the same transaction as their mutation and immutable evidence, then call the locked initial account-change delivery owner only after commit. A pending replay may recover initial delivery, while `FAILED` never auto-retries and `DELIVERED` never resends. Copy states that Auth.js observes invalidation on the next successful database-backed refresh and never treats adapter Session rows as an active-JWT or signed-out-user count.
- Password reset delegates token creation, hashing, immutable action/activity/`PASSWORD_RESET` marker creation, and standard reset-mail delivery to the dedicated security service. A failed reset Activity entry now offers `Send a new reset link` with a fresh server-rendered key; it creates a new token/action/intent and never enters the account-change email retry path or reuses unrecoverable old token material.
- The Security projection and responsive controls show only provider types, bounded connection-row evidence, verified-email/password/2FA booleans, canonical invalidation explanation, and an explicitly compatibility-only Session-row count. Passwords, hashes, raw tokens, reset links, encrypted secrets, backup/recovery material, session tokens, provider account identifiers, and impersonation remain absent.
- Extended exact-ID desktop/mobile Playwright coverage for keyboard operation, confirmation disabled/enabled state, self-target suppression, fresh reset resend, typed-email mismatch/success, responsive overflow, and target JWT rejection after revocation. The fixture remains gated to the disposable database sentinel, uses SMTP-blank Playwright-owned servers, and cleans its exact authentication/evidence rows in foreign-key-safe order.
- Hardened the post-send password-reset boundary: if either an accepted or failed transport outcome cannot be persisted, the service logs no provider, recipient, token, or persistence details and returns the durable `PENDING` intent with delivery-attempted truth. The action still revalidates and warns Admin to inspect Activity before another request, avoiding a false rollback error and an immediate duplicate-email prompt.

## 2026-08-09 — Confirmed delegated anatomy role controls

- Added full-Admin-only Access-section controls to assign or revoke only `ANATOMY_REVIEWER` and `ANATOMY_EDITOR`. Each form shows exact current and planned state, requires a shared allowlisted support reason plus explicit sign-out confirmation, and reuses one server-generated UUID for idempotency.
- Role actions bind the route target, reload full-Admin authority, reject stale or unsupported state, and reuse the original Admin User Operations Branch 4 transaction that writes the role, increments `User.authSessionVersion`, deletes adapter sessions, and creates immutable audit, target activity, and durable email evidence atomically. The increment immediately invalidates existing non-matching tokens; Auth.js signs the user out when one next reaches a successful database-backed refresh. Email delivery starts only after commit; a transport failure is reported as notification failure without falsifying or rolling back the role change.
- Exact role-action replays now recover a notification left `PENDING` by a post-commit interruption through the same intent advisory lock. The initial-delivery owner never resends `FAILED` or `DELIVERED` intents; only the existing audited Activity action may retry an eligible failure, and replay copy never claims a new role mutation or sign-out.
- Extended desktop/mobile browser acceptance for the Reviewer matrix, keyboard submission, responsive overflow, and a separate target JWT becoming invalid after the role change. Playwright-owned servers explicitly blank SMTP variables, and fixture cleanup removes only deterministic Admin evidence, sessions, roles, commerce rows, and fixture users in foreign-key-safe order.

## 2026-08-09 — Versioned JWT session invalidation

- Added `User.authSessionVersion` as the canonical Auth.js JWT invalidation owner. New sign-ins adopt the current database version; later requests require an exact match, with legacy unversioned JWTs accepted only while the account remains at version zero. Database refresh outages retain the existing restricted-identity fallback.
- Delegated anatomy-role changes increment the target version inside the same serializable transaction as the role mutation, adapter-session deletion, immutable audit, target activity, and email intent. Exact and concurrent replay reuse and revalidate the persisted before/after version evidence without incrementing again, while failed evidence creation rolls the increment back.
- Prisma `Session` deletion remains compatibility-only under the JWT strategy. Deleted-row counts are not active JWT-session counts, and future security-support copy must not describe them that way.

## 2026-08-09 — Account activity and failed-email retry

- Completed original Admin User Operations Branch 3 Tasks 7-9 on top of the original Admin User Operations Branch 2 foundation owner: the full-Admin user directory, bounded read-only account detail, safe dashboard account metrics, and signed-in Account Activity consumer surfaces are implemented. Directory filters and displayed subscription state are Supporter-scoped; retired role values normalize only on persisted reads; and every detail collection has deterministic bounded projection evidence.
- Detail access now consumes the canonical membership entitlement resolver's per-feature provenance. Free baseline features remain `FREE` without an expiry, paid-only features identify the actual active paid source, and locally unprovable Supporter pricing or reconciliation facts render as explicitly unavailable/unknown rather than being inferred from processor identifiers or raw payloads. Truncated refund, payment, dispute, connection, and provider-type samples retain exact connection counts or sampled lower bounds without presenting them as complete relation totals.
- Added the signed-in Account Activity tab. It reads only the current user's newest fifty target-visible activity entries and renders the title, explanation, optional effective value, and accessible occurrence time; actor, internal-note, and delivery metadata remain absent from the account payload.
- Extended read-only full-Admin Activity detail with the safe delivery state, failure code, attempt count, and last-attempt timestamp. An explicit retry is available only for already-failed, non-password account-change intents and begins with `requireFullAdminUser()` before using the established `retryAdminEmailIntent()` contract. The retry verifies the locked intent belongs to the route target and reuses the stable rendered operation ID as its idempotency key. It does not mutate the target account.
- Failed password-reset intent rows deliberately show future-action copy only. No password-reset resend action, fresh-token path, or target-account write was introduced in original Admin User Operations Branch 3.
- The original Admin User Operations Branch 4 is the next serial gate and must start only after original Admin User Operations Branch 3 merges and its worktree refreshes from the new `main`.

## 2026-08-08 — Admin authorization and audit foundation

- Split delegated anatomy authority into `ANATOMY_REVIEWER` and `ANATOMY_EDITOR`, retained `ADMIN` as the only full account/commerce administrator, and normalized the retired `ANATOMY_ADMIN` role to Editor. Administrative access now reloads verified assignments from the database; session claims provide identity but do not grant authority.
- Added immutable admin-action, target-visible account-activity, and durable account-change email-intent records with target-consistent foreign keys, bounded safe metadata, transactional bundle creation, idempotent replay checks, serialized delivery/retry attempts, and explicit full-Admin retry authorization. Password-reset delivery remains outside this support-email path.
- Made `/admin` capability-aware: Reviewers receive the anatomy review queue and review metrics, Editors also receive the full anatomy browser and editor metrics, and full Admin receives User-operations status plus Commerce. The page loads one fresh actor and skips metrics for hidden sections; every linked destination still performs its own guard.
- Added the [Admin user operations](wiki/admin-user-operations.md) runbook. The original Admin User Operations Branch 3 user directory and support actions remain the next serial branch and must start only after this original Admin User Operations Branch 2 PR merges and a new worktree refreshes from `main`.

## 2026-08-08 — Source-visible proprietary licensing

- Established MassageLab as source-visible proprietary software, not open-source software, while keeping the repository public for CodeRabbit and source inspection. Public visibility grants no permission to reuse, modify, or redistribute MassageLab-owned source code or assets.
- Added the root proprietary `LICENSE` for Copyright © 2025–2026 Derrick Bowersock, doing business as Massage Lab; replaced the provisional README copy; added the compact ownership notice to expanded and drawer sidebar footers; and marked both package manifests `UNLICENSED`.
- Added a focused repository contract test covering the license terms, canonical README notice, package metadata, and both responsive footer placements. Third-party components remain governed by their own license terms, and no repository-visibility setting changed.

## 2026-08-07 — Background initiative closeout and returning-tab access recovery

- Consolidated the long-form Chimer snapshot around the shipped stable-ID branding catalog and active immutable R2 preview release. Removed the obsolete MassageLab-prefixed label inventory and the superseded lightweight-WebM description so the canonical state now matches the approved 84-background naming layer, poster-first behavior, five-card motion window, truthful static entries, and loop-boundary adaptation already verified in Production.
- Corrected the returning-tab access contract in Clock/Chimer. A transient or explicitly non-authoritative session/preferences response no longer erases the last in-memory verified subscription and ownership snapshot, while a fresh mount still starts fail-closed and an authoritative signed-out response still removes premium access. Clock/Chimer now retries the same account-preference synchronization when the page becomes visible, receives focus, comes online, or returns from the browser page cache, with resume bursts coalesced into at most one queued retry.
- Added a mobile browser regression that starts with an unavailable preference endpoint, restores it, simulates returning focus, and verifies a membership-included background becomes available without navigating through Account. The immutable R2 release, active Production deployment, and retained rollback deployment are unchanged.

## 2026-08-07 — Approved background preview catalog activated in Production

- After PR #168 merged as `ead19cd3138434f4fa1dcda2ecf2fcf5f1da4670`, uploaded exactly the reviewed 1,728-object allowlist (862,078,635 bytes / 822.1 MiB) to the immutable R2 prefix `massagelab-public-media/chimer/background-preview-catalog/catalog-approved-1`. The 577.5 MiB of local capture, intermediate, and validation material outside the serialized manifest was not uploaded.
- Downloaded and verified every public object from `https://media.massagelab.app/chimer/background-preview-catalog/catalog-approved-1`: all byte counts and SHA-256 digests match the approved manifest, every MIME type matches its rendition, and every response carries `public, max-age=31536000, immutable`. The immutable release remains retained for normal service and rollback safety.
- Added the Production-only `NEXT_PUBLIC_CHIMER_PREVIEW_CATALOG_BASE_URL` for the immutable release and redeployed the exact merged source. Deployment `dpl_BU9RUjSNWigCEe6oEL5Xx5o3rWp2` (`https://massagelab-n4lximqq5-dsbteam.vercel.app`) reached READY and owns `massagelab.app`, `www.massagelab.app`, and the standard Production aliases.
- Exercised the real public Chimer carousel in a browser. Its initial state had zero videos and five complete 540-pixel-wide vertical posters from R2. Play Preview loaded only eligible cards; an all-animated position produced exactly five playing vertical high-quality VP9 videos (center, two left, and two right), all at media ready state 4 and all from the immutable prefix. Pausing returned the DOM to zero videos. Observed catalog requests returned `200` for posters and `206` for video ranges, no Next.js error overlay appeared, and the bounded Vercel error-level scan for the deployment returned no logs. The automation environment's blocked Cloudflare Insights beacon was the only console error and did not affect the app or media requests.
- The pinned rollback target is prior READY deployment `dpl_EHT1dFikZJzDG26VCxd6UP5vYHjo` (`https://massagelab-crvxw5uj9-dsbteam.vercel.app`). Rollback removes the Production catalog-base setting and restores that deployment, which resumes the fail-closed v1 fallback without deleting the immutable R2 objects.

## 2026-08-08 — Retired custom-color entitlement

- Superseded the former `chimer_custom_colors` gate. Active subscriptions now sell premium-background access only; Chimer colors are unrestricted except that free, membership-included, credit-owned, or purchased backgrounds expose their supported color controls through the canonical selected-background access decision.
- Removed the obsolete entitlement and session capability from membership, account, sidebar, Chimer, Clock-review, and preference-sanitization paths. Existing saved palettes and Clock/Lamp settings remain intact; only a selected premium background without current access remains restricted.

## 2026-08-07 — Background preview publication gate prepared

- Added focused desktop-Chromium coverage on the real production Clock Background carousel. It proves zero video elements before the user opts in, at most five after Play Preview, one vertical `src` and no alternate `<source>` per playing card, no video for Solid Color, reduced-motion poster-only behavior, hidden/inactive pausing, no mid-loop quality switch after a connection change, `ended`-only handoff and boundary restart, and VP9-to-H.264 recovery after a runtime codec error. The existing full-catalog review proof still passes with its intentional six simultaneous comparison renditions and poster-only static entries.
- Ran the credential-free real publication dry run against `https://media.massagelab.app`. Its machine-readable evidence is `MASSAGELAB_CATALOG_R2_DRY_RUN_SUMMARY={"dryRun":true,"catalogRevision":"catalog-approved-1","bucket":"massagelab-public-media","publicBaseUrl":"https://media.massagelab.app","objectPrefix":"chimer/background-preview-catalog/catalog-approved-1","objectCount":1728,"totalBytes":862078635,"cacheControl":"public, max-age=31536000, immutable","uploaded":false}`.
- Stopped at the publication gate. Hosted verification was not performed. No live upload, R2 mutation, Vercel environment change, deploy, push, Production activation, or cleanup occurred. A separately authorized rollout must upload the fixed immutable prefix, verify all 1,728 public responses against manifest byte counts, SHA-256 digests, MIME types, and immutable cache headers, configure Production with `NEXT_PUBLIC_CHIMER_PREVIEW_CATALOG_BASE_URL=https://media.massagelab.app/chimer/background-preview-catalog/catalog-approved-1`, deploy, and smoke the hosted carousel. Rollback removes that setting and restores the prior READY deployment, returning Production to the existing v1 fallback without deleting the immutable release.

## 2026-08-07 — Full-catalog visual approval

- The user completed visual review and approved every one of the 84 local full-catalog background previews. All catalog recipes and serialized entries now use `approved` review status, and the serializer emits `catalog-approved-1` while failing closed if any candidate entry is supplied.
- The published catalog references exactly 1,728 immutable media objects: 1,476 videos and 252 posters totaling 862,078,635 bytes (822.1 MiB). The remaining 577.5 MiB of the 1.367 GiB local catalog directory is generation and validation material, not publishable payload.
- This approval changes catalog metadata only; all recipe settings, rendition URLs, hashes, and approved media bytes remain preserved. Production preview v1, R2, hosted runtime activation, and adaptive upgrade/downgrade integration remain separately gated.

## 2026-08-07 — Complete local adaptive background preview catalog

- Completed local generation for all 84 enabled background IDs using the approved manifest-driven rendition ladder and stable ID plus recipe-revision paths. The catalog contains exactly 1,476 videos and 252 WebP posters (about 1.367 GiB): 82 animated backgrounds each provide independently captured landscape, square, and vertical compositions with low/standard/high VP9 and H.264 renditions, while Static Gradient and Solid Color truthfully provide three posters and no fabricated video timeline.
- Preserved the eight visually approved pilot entries and their hashed media byte-for-byte. The remaining entries retain `candidate` review status. Liquid Ether alone required a per-background `recipe-3` adjustment to the maximum truthful two-second crossfade to satisfy the unchanged `0.120` seam gate in all aspects; no global motion, seam, codec, dimension, or duration threshold was relaxed.
- Added a guarded approved-pilot importer and hardened resumability so an interrupted incomplete catalog recipe revision is rebuilt from its exact local revision directory instead of trusting a partial encoder file. The renderer still refuses the production and approved-pilot directories, exposes no upload path, and updates a complete manifest entry only after all three aspects pass.
- The final standalone validator passes the exact 84-entry, 1,476-video, and 252-poster contract, including presence, byte count, SHA-256, FFprobe metadata, decoded motion, and loop evidence for every referenced rendition. The local review surface is `/dev/bgpreviews?catalog=full`. Production preview v1, R2, hosted runtime activation, and adaptive upgrade/downgrade integration remain untouched pending visual approval and a separate publication gate.

## 2026-08-06 — Approved adaptive background preview pilot

- Completed and received explicit user visual approval for the local-only eight-background adaptive-preview pilot. The frozen `recipe-1` passive captures are: Massage Laba Lamp (`12s`, poster `4s`, `0.9s` crossfade, 24 fps), Silk (`10s`, poster `3.333s`, `0.8s` crossfade, 24 fps), Tidal Glass (`10s`, poster `3.333s`, `0.8s` crossfade, 24 fps), DNA (`18s`, poster `6s`, `1s` crossfade, 24 fps), Hypercube (`12s`, poster `4s`, natural loop, 24 fps), Constellations (`12s`, poster `4s`, `0.9s` crossfade, 30 fps), Scanline (`8s`, poster `2.667s`, `0.6s` crossfade, 30 fps), and Quiet Mosaic (`12s`, poster `4s`, `0.9s` crossfade, 24 fps). Every capture uses the production renderer's passive default state after a `2.2s` warmup; no pointer, hover, tap, reverse, ping-pong, morph, or speed-ramp motion is introduced.
- Froze three aspect-specific low/standard/high ladders: landscape `384×216`, `640×360`, `960×540`; square `256×256`, `512×512`, `768×768`; vertical `216×384`, `360×640`, `540×960`. Each aspect is captured independently and emits VP9/WebM plus H.264/MP4 at every tier and one high-dimension WebP poster. VP9 uses `libvpx-vp9 -deadline good -cpu-used 2 -crf 30 -b:v 0`; H.264 uses `libx264 -preset slow -crf 21 -profile:v high -pix_fmt yuv420p -movflags +faststart`. Both paths use Lanczos scaling, `yuv420p`, and no audio; posters use WebP quality 84.
- Full validation accepts exactly 8 complete entries, 144 final videos, and 24 posters. FFprobe metadata, SHA-256 hashes, decoded-frame variation, dimensions, durations, stream counts, codecs, `yuv420p`, and first/last seam evidence all pass; natural loops reject normalized seam differences above `0.080` and crossfades reject values above `0.120`. Desktop and mobile Chromium review-route coverage also proves that background/aspect changes reload all six comparison players instead of retaining stale sources. The final repository checks pass all 2,030 tests, typecheck, lint, and `git diff --check`. The frozen v2 JSON/TypeScript sidecars and generated binaries remain local pilot evidence under `/chimer/background-preview-pilot`; production preview v1, R2, and the hosted runtime remain untouched. Full-catalog recipe authoring, generation, adaptive runtime integration, publication, and upgrade/downgrade behavior remain future gated work.

## 2026-08-06 — Approved background catalog naming layer

- Applied the seven reviewed naming batches to all 84 enabled backgrounds through a compact generated branding catalog while preserving every existing background ID, renderer, entitlement, preference, and commerce key. The runtime registry now exposes the canonical label, literal visual descriptor, hidden legacy search aliases, and internally conceived signature-original status from one source rather than coupling preview paths or stored state to display names.
- Added descriptor context to the background carousel, acquisition and credit confirmation dialogs, and account ownership cards. Historical order rows resolve their current catalog label and descriptor by stable ID while retaining the stored display name as a compatibility fallback. Legacy labels participate in catalog search but are not rendered as ordinary labels.
- Updated current user-facing accessibility and browser contracts for `Massage Laba Lamp`, `In Transition`, `Endless Perspective`, and `Hypercube`. Focused branding, audit, catalog, and registry coverage passes 114/114. The representative local-only preview pilot remains the next review-gated slice; no generated pilot media, production manifest, R2 object, or hosted runtime has changed.

## 2026-08-05 — Solid Color background

- Added the free `solid-color` background to Chimer, Clock, Music, and Ambient as a low-cost static option. Its full-screen renderer and registry fallback preview both default to MassageLab orange (`#FF7A1A`), with no authored video required.
- The shared palette adapter exposes one Color role at Swatch 1. Source preserves the authored orange, Custom uses the saved first swatch, Swatches 2-7 remain unused, and Harmony is unavailable because a single color has no inter-role relationship to generate. The option has no background-specific tuning controls or special persistence branch.
- The focused registry, palette, entitlement, UI-contract, and 84-background branding-audit tests pass; all 2,010 repository tests, typecheck, lint, `git diff --check`, the 102-page Production build, and the localhost-only `/dev/clock` HTTP check pass. Direct rendered automation remains blocked because the in-app Browser's administrator policy cannot verify localhost access, so final visual judgment remains in the user's Device Toolbar review.

## 2026-08-05 — Bubble Field independent background, colors, and tuning

- Promoted Bubble Field from an unsupported CSS/DOM palette adapter to six concrete roles. Source preserves its authored compound background and five bubble colors. Custom maps Bubble 1-5 to Swatches 1-5 and Background to Swatch 7; Harmony regenerates only the five bubble colors while keeping the saved Swatch 7 background visible, editable, and independent.
- Added persisted Motion speed (`0.25x`-`2.00x`), Intensity (`10%`-`100%`), Bubble size (`50%`-`200%`), Blur (`0`-`80px`), and Blend strength (`10`-`30`) controls to both Visual panels, preserving the authored `1.00x`, `100%`, `100%`, `40px`, and `18` defaults. The renderer applies them to the full animation cycle, field opacity, bubble geometry, gooey blur, and color-matrix blend. Cursor interaction and the sixth mouse-following bubble remain intentionally omitted so Bubble Field stays a passive background. The `massage-lab-bubble` ID and entitlement behavior are unchanged.
- The focused renderer, role-mapping, Harmony-isolation, settings, entitlement, and UI-contract tests pass; all 2,008 repository tests, typecheck, lint, `git diff --check`, the 102-page Production build, and the localhost-only `/dev/clock` HTTP check pass. Direct rendered automation remains blocked because the in-app Browser's administrator policy cannot verify localhost access, so final visual judgment remains in the user's Device Toolbar review.

## 2026-08-05 — Tile Grid and Hex Grid fade sliders

- Replaced the shared Tile Grid and Hex Grid Fade time hour/minute/second inputs with the standard Visual-panel range control in both setup and running-timer surfaces. One logarithmic `0-100` presentation preserves useful control over short fades while still reaching the existing `0.25s` through `23h 59m 59.9s` domain; the value label reports the resulting real duration.
- Both backgrounds now default to a `20s` fade. A versioned exact-default migration updates only untouched former `1.2s` values; customized saved fade times retain their authored values. The renderer and persistence boundary remain seconds, including exact minimum and maximum endpoints. Colors, density controls, background IDs, and entitlement behavior are unchanged.
- The focused slider/settings/entitlement contracts pass; all 2,005 repository tests, typecheck, lint, `git diff --check`, and the 102-page Production build pass. Direct rendered automation remains blocked because the in-app Browser's administrator policy cannot verify localhost access, so final slider feel remains in the user's `/dev/clock` Device Toolbar review.

## 2026-08-05 — Swatch 7 background-role audit

- Audited every enabled shared-palette adapter whose literal full-canvas `Background` role still defaulted to Swatch 1. Retro Grid, Aerial Rays, Wave Current, Photon Beam, Reveal Dots, 3D Globe, Lamp Glow, Wave flow, Pixel liquid, and Aurora bars now reserve Swatch 7 for that saved backdrop. Their visual roles shift into Swatches 1-6 in renderer order; 3D Globe's Wireframe moves from Swatch 7 to Swatch 6 to avoid a collision.
- Harmony now regenerates only the foreground, line, ray, wave, signal, globe, lamp, liquid, and bar roles while retaining the user's saved Swatch 7 backdrop. Source colors, renderer behavior, user-authored role remaps, background IDs, and entitlement behavior are unchanged. A registry-wide contract prevents any enabled literal `Background` role from returning to Swatch 1.
- The focused palette registry contracts, all 2,004 repository tests, typecheck, lint, `git diff --check`, and the 102-page Production build pass. Final visual judgment remains in the user's `/dev/clock` Device Toolbar review.

## 2026-08-05 — Vortex independent background and procedural hue control

- Changed Vortex's shared-color contract so its background is independently owned by Swatch 7 in Custom mode. The particle field retains the source renderer's continuous 100-degree HSL span instead of flattening it into discrete swatch colors.
- Added a persisted `0°-360°` Particle hue slider directly below the Custom swatches to control the start of that continuous range. Harmony is disabled for Vortex because a discrete harmony choice cannot truthfully describe the procedural field; stale saved Harmony mode resolves safely to Source. The existing `massage-lab-vortex` ID, entitlement behavior, particle geometry, motion, and other tuning controls are unchanged.
- The focused palette/editor/renderer contracts, all 2,002 repository tests, typecheck, lint, `git diff --check`, the 102-page Production build, and the localhost-only `/dev/clock` HTTP check pass. Final visual judgment remains in the user's Device Toolbar review.

## 2026-08-05 — Reveal Dots visible distributed motion

- Corrected Reveal Dots after Device Toolbar review showed that its former `0.86x-1.10x` randomly phased opacity shimmer was technically repainting but visually static at phone scale. Individual anchored dots now brighten and dim smoothly on their existing seeded phases and speeds across a wider `0.65x-1.45x` opacity multiplier, so the field visibly twinkles without relying on pointer input.
- Removed the first coherent-wave attempt after direct review showed that its screen-wide sweep changed the background's intended character. The final renderer keeps the source-like grid, dot positions, dot sizes, colors, spacing, gradient choice, settings, entitlement behavior, and `massage-lab-reveal-dots` ID unchanged. Reduced motion returns the authored steady frame.
- The focused motion regression, all 2,000 repository tests, typecheck, lint, the 102-page Production build, and the localhost-only `/dev/clock` HTTP check pass. Final visual judgment remains in the user's Device Toolbar review.

## 2026-08-05 — Light Lines independent background, colors, and expanded tuning

- Promoted Light Lines from an unsupported CSS/DOM palette adapter to seven concrete roles. Source preserves the authored compound background and complete 21-color line cycle. Custom/Harmony repeat six foreground colors from Swatches 1-6 across the SVG paths while keeping the saved Swatch 7 background independent and editable.
- Retained the existing persisted Line duration control (`4s`-`18s`, default `10s`) and added Intensity (`10%`-`100%`, default `68%`), Line count (`6`-`26`, default `26`), Line width (`0.5`-`6.0`, default `2.3` SVG units), and Glow strength (`0`-`24px`, default `10px`) in both Visual panels. The renderer applies them directly to the complete SVG field opacity, actual rendered path count, stroke geometry, and drop-shadow blur. The `massage-lab-background-lines` ID and entitlement behavior are unchanged.
- The focused renderer, role-mapping, Harmony-isolation, settings, entitlement, UI-contract, and diagnostics tests pass; all 1,999 repository tests, typecheck, lint, `git diff --check`, the 102-page Production build, and the restarted localhost-only `/dev/clock` HTTP check pass. Direct rendered automation remains blocked because the in-app Browser's administrator policy cannot verify localhost access, so final visual judgment remains in the user's Device Toolbar review.

## 2026-08-05 — Glowing Stars and Meteors independent backgrounds, colors, and tuning

- Promoted Glowing Stars and Meteors from unsupported CSS/DOM palette adapters to concrete renderer roles. Glowing Stars Source preserves its authored compound background and base-star, peak-star, afterglow, glow-core, and glow-aura colors; Custom/Harmony map those five foreground roles to Swatches 1-5 while keeping the saved Swatch 7 background independent. Meteors Source preserves its authored compound background and meteor-head, tail, glow, and edge colors; Custom/Harmony map those four foreground roles to Swatches 1-4 while keeping the saved Swatch 7 background independent. Unassigned swatches remain visibly marked as unused.
- Added persisted Glowing Stars controls for Motion speed (`0.25x`-`2.00x`, default `1.00x`), Intensity (`10%`-`100%`, default `94%`), Active stars (`1`-`18`, default `5`), Star size (`0.5px`-`3.0px`, default `1.0px`), and Glow strength (`0%`-`200%`, default `100%`). The renderer applies them directly to its three-second selection interval, two-second pulse/glow cycle, grid opacity, selected-star count, and star/glow geometry.
- Added persisted Meteors controls for Motion speed (`0.25x`-`2.00x`, default `1.00x`), Intensity (`10%`-`100%`, default `82%`), Meteor count (`4`-`48`, default `28`), Meteor size (`0.5px`-`5.0px`, default `2.0px`), and Tail length (`15px`-`140px`, default `50px`). The renderer applies them directly to its authored nine-to-fourteen-second cycles, full meteor-layer opacity, deterministic full-width streak distribution, head geometry, and tail geometry. The `massage-lab-glowing-stars` and `massage-lab-meteors` IDs and entitlement behavior are unchanged.
- The focused renderer, role-mapping, Harmony-isolation, settings, entitlement, UI-contract, and diagnostics tests pass; all 1,994 repository tests, typecheck, lint, `git diff --check`, the 102-page Production build, and the restarted localhost-only `/dev/clock` HTTP check pass. Direct rendered automation remains blocked because the in-app Browser's administrator URL policy rejects localhost, so final visual judgment remains in the user's Device Toolbar review.

## 2026-08-05 — Collision Beams independent background, colors, and tuning

- Promoted Collision Beams from an unsupported CSS/DOM palette adapter to five concrete roles. Source preserves the authored layered background and beam, accent, particle, and impact-surface colors. Custom maps Beam, Accent, Particles, and Impact surface to Swatches 1-4 and Background to Swatch 7; Harmony regenerates only the four foreground colors while keeping the saved Swatch 7 background visible and editable. Swatches 5-6 remain unused by default.
- Added persisted Motion speed (`0.25x`-`2.00x`), Intensity (`10%`-`100%`), Beam width (`0.5px`-`4.0px`), and Burst size (`50%`-`200%`) controls to both Visual panels, preserving the authored `1.00x`, `100%`, `1px`, and `100%` defaults. The renderer applies those controls to the complete beam/collision cycle, foreground layer opacity, beam geometry, and composed explosion/particle scale. The `massage-lab-collision-beams` ID and entitlement behavior are unchanged.
- The focused renderer, role-mapping, Harmony-isolation, settings, entitlement, UI-contract, and diagnostics tests pass; all 1,987 repository tests, typecheck, lint, `git diff --check`, the 102-page Production build, and the restarted localhost-only `/dev/clock` HTTP check pass. Final visual judgment remains in the user's Device Toolbar review.

## 2026-08-05 — Beam Field independent background, colors, and tuning

- Promoted Beam Field from an unsupported CSS/DOM palette adapter to four concrete roles. Source preserves the authored compound background and three traveling SVG gradient colors. Custom maps Beam 1-3 to Swatches 1-3 and Background to Swatch 7; Harmony regenerates only the three beam colors while keeping the saved Swatch 7 background visible and editable.
- Added persisted Motion speed (`0.25x`-`2.00x`), Intensity (`10%`-`100%`), Beam width (`0.2`-`2.0` SVG units), and Glow strength (`0`-`20px`) controls to both Visual panels, preserving the authored `1.00x`, `82%`, `0.6`, and `10px` defaults. The renderer applies these controls directly to native SVG timing, field opacity, path stroke width, and drop-shadow blur. The `massage-lab-background-beams` ID and entitlement behavior are unchanged.
- The focused renderer, role-mapping, Harmony-isolation, settings, entitlement, UI-contract, and diagnostics tests pass; all 1,982 repository tests, typecheck, lint, `git diff --check`, the 102-page Production build, and the restarted localhost-only `/dev/clock` HTTP check pass. Final visual judgment remains in the user's Device Toolbar review.

## 2026-08-05 — Selected-background swatch availability cues

- The shared seven-swatch editor now marks each swatch with semantic assigned/unused state derived from the selected background's active role mapping. Assigned swatches reuse the Visual header Apply action's leaf-green token family; unused swatches reuse the destructive action's red token family and retain their dashed border. The tint remains a redundant visual cue alongside the existing role names and `Not used by this background` labels, and follows the existing 7/4/2-column responsive layouts in Source, Custom, and Harmony modes without changing editability, palette behavior, mappings, saved settings, or background IDs.
- The focused palette contract, all 1,978 repository tests, typecheck, lint, `git diff --check`, the 102-page Production build, and the restarted localhost-only `/dev/clock` HTTP check pass. Direct rendered automation remains blocked because the in-app browser's administrator-enforced localhost policy could not be verified, so final tint judgment remains in the user's Device Toolbar review.

## 2026-08-05 — Dotted Glow independent background, colors, and tuning

- Promoted Dotted Glow from an unsupported canvas palette adapter to three concrete roles. Source preserves the authored background, dot, and glow colors. Custom maps Dots to Swatch 1, Glow to Swatch 2, and Background to Swatch 7; Harmony regenerates only the dot and glow colors while the saved Swatch 7 background remains visible and editable.
- Added persisted Motion speed (`0.25x`-`2.00x`), Dot size (`0.5px`-`4.0px`), Dot spacing (`8px`-`28px`), Dot opacity (`10%`-`100%`), and Glow strength (`0`-`12px`) controls to both Visual panels, preserving the authored defaults. Removed the fixed center radial depth/cloud layer and the radial fallback flare so the field stays even and only individual pulsing dots glow. The `massage-lab-dotted-glow` ID and entitlement behavior are unchanged.
- The focused renderer, role-mapping, Harmony-isolation, settings, entitlement, UI-contract, and diagnostic tests pass; all 1,977 repository tests, typecheck, lint, `git diff --check`, the 102-page Production build, and the restarted localhost-only `/dev/clock` HTTP check pass. Direct rendered automation remains blocked by the unresolved administrator-enforced in-app-browser localhost policy, so the user's Device Toolbar review remains the visual acceptance path.

## 2026-08-05 — Aurora Field independent Harmony background

- Corrected Aurora Field's Swatch 7 role semantics after Device Toolbar review: Harmony now generates colors only for the five aurora bands. The background resolves from the saved Swatch 7 value, remains visibly editable while Harmony is active, and no longer changes when the user changes the primary color or harmony choice. Other backgrounds retain their existing generated Harmony behavior.
- The focused palette resolver, adapter, and editor contracts pass; all 1,973 repository tests, typecheck, lint, `git diff --check`, the 102-page Production build, and the restarted localhost-only `/dev/clock` HTTP check pass. Direct rendered automation remains blocked by the unresolved administrator-enforced in-app-browser localhost policy, so the user's Device Toolbar review remains the visual acceptance path.

## 2026-08-05 — Aurora Field visual tuning and shared colors

- Added Aurora Field controls to both Chimer/Clock Visual panels for Motion speed (`0.25x`-`2.00x`), Intensity (`10%`-`100%`), Blur (`0`-`30px`), and Field reach (`30%`-`100%`), retaining the source-matched `1.00x`, `50%`, `10px`, and `70%` defaults.
- Promoted Aurora Field from an unsupported color adapter to six concrete shared-palette roles: its background and five aurora bands. Source mode preserves the authored blue/violet palette. Custom and Harmony map the bands to Swatches 1-5 and reserve Swatch 7 for the background so it remains independent from the primary harmony color; Swatch 6 remains available and unused by default. The `massage-lab-aurora` ID, entitlement behavior, and source defaults are unchanged.
- The focused Aurora renderer, role-mapping, settings, entitlement, UI-contract, and account-persistence tests pass; all 1,971 repository tests, typecheck, lint, `git diff --check`, the 102-page Production build, and the restarted localhost-only `/dev/clock` HTTP check pass. Direct rendered automation remains blocked by the unresolved administrator-enforced in-app-browser localhost policy, so the user's Device Toolbar review remains the visual acceptance path.

## 2026-08-05 — Photon Beam moderate defaults

- Reduced Photon Beam's new/reset and direct-renderer defaults from 80 lines and 94 signals to 12 lines and 12 signals for a calmer phone presentation. A versioned exact-default migration updates only the untouched former 80/94 pair; customized saved densities retain their authored values. Control ranges, colors, motion, bloom, entitlement behavior, and background ID are unchanged.
- The focused Photon Beam renderer, sanitizer, migration, and entitlement contracts pass, all 1,967 repository tests pass, and typecheck, lint, `git diff --check`, the 102-page Production build, and the restarted localhost-only `/dev/clock` HTTP check pass. Direct rendered automation remains blocked by the unresolved administrator-enforced in-app-browser localhost policy, so the user's Device Toolbar review remains the visual acceptance path.

## 2026-08-05 — Chrome Flow speed correction

- A Device Toolbar screenshot disambiguated two currently duplicate Chrome Flow catalog labels. The requested control was the React Bits renderer at ID `massage-lab-liquid-chrome`, identifiable by Speed, Amplitude, Frequency X/Y, and Cursor interaction—not the Chamaac renderer at ID `massage-lab-chrome-flow`, which exposes Flow speed and Time scale. The first pass changed the latter and is fully reverted here.
- The screenshot-matched Chrome Flow now stores raw Speed from `0.001` to `3.0` while both Visual panels present it as `0.1%-100%` with `0.1%` steps. Its raw `0.2` default, amplitude, frequencies, cursor interaction, color behavior, entitlement behavior, and background ID are unchanged.
- The four focused exact-ID, renderer, control, and sanitizer contracts pass, all 1,967 repository tests pass, and typecheck, lint, `git diff --check`, the 102-page Production build, and the restarted localhost-only `/dev/clock` HTTP check pass. Direct rendered automation remains blocked by the unresolved administrator-enforced in-app-browser localhost policy, so the user's Device Toolbar review remains the visual acceptance path.

## 2026-08-05 — Shape Grid speed scale

- Changed Shape Grid's authored speed default from `1.0` to `0.25`, corrected its renderer and sanitizer range to `0.0-2.0`, and presented that raw range as a true `0%-100%` slider in both Visual panels. The default appears as `12.5%`, while the `0%` endpoint now leaves the grid stationary instead of silently applying a `0.1` motion floor. A versioned exact-default migration moves only untouched former `1.0` settings to `0.25`; customized legacy speeds retain their meaning and are clamped only when outside the corrected range. Direction, cell size, shape, trail, cursor behavior, colors, entitlement behavior, and background ID are unchanged.
- The focused Shape Grid/default/entitlement contracts pass, all 1,966 repository tests pass, and typecheck, lint, `git diff --check`, the 102-page Production build, and the restarted localhost-only `/dev/clock` HTTP check pass. Direct rendered automation remains blocked by the unresolved administrator-enforced in-app-browser localhost policy, so the user's Device Toolbar review remains the visual acceptance path.

## 2026-08-05 — Grid Distortion simulated cursor controls

- Added an opt-in Simulate cursor interaction toggle and persisted `0.3x-2.0x` Fake cursor speed control with a `1.0x` default to both Visual panels. The passive path feeds Grid Distortion's existing normalized pointer/data-texture displacement model, yields briefly after genuine pointer movement, and remains additive to the already-approved continuous ambient drift; simulation is off by default and existing controls, colors, entitlement behavior, reduced-motion handling, and background ID remain unchanged.

## 2026-08-05 — Dot Grid simulated cursor controls

- Added an opt-in Simulate cursor interaction toggle and a persisted `0.3x-2.0x` Fake cursor speed control with a `1.0x` default to both setup and running Visual panels. The dependency-free canvas renderer feeds a smooth viewport-relative passive path through the same proximity and inertia physics as real pointer movement, temporarily yields to genuine pointer input, does not synthesize click shocks, and remains enabled at compact viewports using the existing reduced-DPR path. Real cursor interaction, click shock, existing Dot Grid controls, colors, entitlement behavior, and background ID remain unchanged; simulation is off by default.

## 2026-08-05 — Dot Field authored defaults

- Updated Dot Field's new/reset presentation to use Sparkle on, a `4.0` dot radius, and `6.0` dot spacing in both the shared Chimer/Clock settings defaults and the renderer's direct fallback. The exact former `1.5` / `14` / Sparkle-off tuple migrates once so untouched persisted settings receive the new presentation; any customized saved tuple remains unchanged. Sanitization ranges, controls, colors, cursor behavior, and background ID are unchanged.

## 2026-08-05 — Grid Scan shared tunnel depth

- Corrected Grid Scan's perspective room so its ceiling, floor, and both walls use proportional camera-relative extents, matching corner-edge calculations, and one shared far-depth fade into an opaque black cap. The former fixed `0.2` ceiling/floor versus `0.5` wall extents produced mismatched depth, while the first equal-extent correction forced a square distant opening. Grid Scan now keeps a `0.5` half-height, derives the half-width from the live render width/height, and terminates every plane at the same camera depth, so the black opening and full tunnel cross-section match the user's viewport aspect ratio without any pair of surfaces appearing shorter in portrait, landscape, resize, or rotation states. Saved settings, controls, colors, scanning motion, and pointer behavior are unchanged.

## 2026-08-05 — Gradient Blinds fixed-slat light motion

- Superseded the earlier autonomous-motion choice that translated Gradient Blinds' gradient and stripe phase. The gradient and blind cell boundaries are now time-independent, preserving the position of every slat. Every slat retains a source-color visibility floor, the spotlight is bounded so it only illuminates rather than darkening areas outside its reach, and its slow damped passive path gently flexes the shading inside each fixed cell like a light breeze. Enabling cursor interaction still hands that light and internal flex to the pointer, and the shader noise is spatially stable instead of flickering frame to frame. Reduced motion continues to render one stable representative frame.
- The focused renderer/catalog/settings contracts pass 182/182, all 1,965 repository tests pass, and typecheck, lint, the 102-page Production build, and the restarted localhost-only `/dev/clock` HTTP check pass. Direct rendered automation remains blocked because the in-app browser's administrator-enforced localhost policy could not be verified, so final motion judgment remains in the user's Device Toolbar review.

## 2026-08-04 — Dark Veil color and resolution controls

- Dark Veil Hue shift now uses the shared color-channel slider. Its transform-only adapter can enter Custom mode without falsely enabling Harmony or editable shared swatches; in the immersive Visual panel the slider appears directly beneath the seven read-only reference swatches only while Custom is selected, participates in the existing draft/apply history, and stays absent from Source. The renderer's clipped YIQ transform produces a reverse, nonlinear hue response rather than a rotated HSL wheel, so the thumb and track now use an unwrapped calibration curve measured from nine Device Toolbar screenshots across `-180°` through `166°`, with the periodic `180°` endpoint joined back to the same rendered hue as `-180°`.
- Resolution scale now presents the stored `0.25-1` range as `25%-100%`. Reduced drawing buffers retain nearest-neighbor pixels so the quality/performance tradeoff is visible, while the CSS canvas remains full-viewport and the shader resolution continues matching the real drawing buffer.
- Validation passed after the Custom-mode and initial hue-control corrections: 110 focused background/palette/shared-control tests, the full 1,965-test suite, typecheck, lint, production build, diff check, and an HTTP 200 response from the restarted localhost-only `/dev/clock` server. The subsequent renderer-calibrated curve also passed its focused Dark Veil/shared-slider contracts, the full 1,965-test suite, typecheck, lint, production build, and an HTTP 200 response from the restarted localhost-only `/dev/clock` server; the user's nine Device Toolbar screenshots supply the multi-point visual calibration because direct automated in-app-browser interaction remains unavailable while its administrator-enforced localhost security check cannot be verified.

## 2026-08-04 — Liquid Ether control-label cleanup

- Liquid Ether now labels its autonomous animation controls Motion, Speed, Intensity, Resume, and Ramp in both setup and running Visual panels. Matching accessible names also omit the redundant `Auto` prefix; internal persisted keys and renderer behavior are unchanged.
- The focused 79-test background contract, all 1,965 repository tests, typecheck, lint, `git diff --check`, and the auth-free `/dev/clock` HTTP check pass. The in-app browser remains unable to inspect localhost because its admin policy is unavailable, so final rendered confirmation remains in the user's Device Toolbar review.

## 2026-08-04 — Electric Mist speed-scale normalization

- Electric Mist animation speed now uses a bounded `1%-100%` user-facing scale. The original authored `100%` pace remains the default and is displayed as `50%`; the new `100%` endpoint renders at the requested authored `200%` maximum.
- A versioned settings migration halves previously saved direct-percent values so their rendered pace remains unchanged, clamps only values above the new authored ceiling, and leaves the existing brightness percentage semantics intact.
- Focused migration/renderer/entitlement contracts, all 1,965 repository tests, typecheck, lint, the 102-page Production build, and `git diff --check` pass. The in-app browser can claim the open `/dev/clock` tab but cannot inspect localhost because its admin policy is unavailable, so final visual judgment remains in the user's Device Toolbar review.

## 2026-08-04 — Twisted Cubes depth and rotation follow-up

- Rotation speed is now bounded at `0.01x-2x`. Layer depth uses a stronger bounded orthographic projection that changes the rendered layer scale noticeably without adding perspective or reducing the approved layer count and wireframe quality.
- An initial continuous linear `rotate3d` experiment was rejected during user review. The exact staged three-axis sequence and cubic-bezier easing are restored; the observed lag and jitter occurred while the review machine was under unusually heavy load and are not treated as a confirmed renderer defect. Rotation retains no persistent `will-change` allocation.
- Focused domain, renderer, preference, controls, and palette contracts pass; the configured desktop-Chromium acceptance matrix passes 13/13 after correcting its DNA percentage-translation and compact-viewport test races; typecheck, lint, the 102-page Production build, and `git diff --check` pass. The in-app browser can claim the open `/dev/clock` tab but cannot inspect localhost because its admin policy is unavailable, so final visual judgment remains in the user's Device Toolbar review.

## 2026-08-04 — DNA control semantics and geometry remediation

- DNA preserves the reviewed authored raw defaults (`0.06` node speed, `0.02` rotation speed, and `0.5` scale) while presenting them as `1.0x`, `1.0x`, and `100%`. Node and rotation speeds now use bounded `0.1x-2.0x` user-facing controls, and scale uses a real `1%-100%` range with a DNA-specific responsive floor so every position remains visually distinct.
- Whole-strand rotation now has persisted on/off and clockwise/counterclockwise controls. Node size has its own persisted `25%-200%` control. Strand spacing now applies centered per-strand offsets over a gap-free grid, so changing spacing no longer changes row height or node diameter.
- The palette adapter, shared Visual draft, local/account settings sanitizer, all four Host contexts, reduced-motion behavior, and computed-consumer acceptance matrix cover the three new settings and normalized values. Typecheck, lint, all 1,965 unit/source-contract tests, the 102-page Production build, `git diff --check`, and the auth-free `/dev/clock` HTTP check pass. The local server remains available at port 3000 for Device Toolbar review; automated in-app-browser rendering remains unavailable because the admin policy could not verify localhost access.

## 2026-08-04 — Retro Grid motion-contract remediation

- Retro Grid now uses the shared Background Host `reduceMotion` decision instead of independently re-reading `prefers-reduced-motion`. Its WebGL frame loop and CSS fallback therefore agree with the Visual panel's background-animation state and the app's resolved ambient-motion setting. The focused Retro Grid contract test, typecheck, lint, dev-route HTTP check, and whitespace check pass; browser frame comparison remains blocked by the in-app browser's admin policy.

## 2026-08-04 — Static Gradient visual controls

- Static Gradient is now a real low-cost CSS renderer with two to seven active colors, seven shared palette roles, linear and radial modes, linear direction, radial circle/ellipse shape and reach, movable radial center, and individually adjustable ordered color stops. The geometry persists through the existing per-background Visual draft without changing the background ID, and the auth-free `/dev/clock` surface exposes the same controls for Device Toolbar review.

## 2026-08-04 — Auth-free development Clock review surface

- `/dev/clock` now renders the production Clock and background controls in local development with the canonical `premium_backgrounds` and `chimer_custom_colors` feature keys. It skips session and account-preference requests, persists review changes only under a separate local development key, and returns 404 in production. This gives Device Toolbar review the subscribed-user visual experience without login, account data, or copied environment files while leaving `/clock` behavior and stored preferences unchanged.

## 2026-08-04 — Grid Motion responsive mantras remediation

- Grid Motion now fills phone viewports with 6-14 responsive rows of seven tiles and moves continuously without pointer input while retaining cursor response as an additive option. Setup and running Visual controls reuse one editor with ten wellness-first starter affirmations and the persisted 1-3-word, 28-Unicode-character, 1-10-entry contract, so users can personalize the background without starting from a blank slate; Add deterministically selects the first unused valid seed even when `I am calm` already exists. Real 390×844 and 844×390 browser proofs cover layout, autonomous motion, starter presentation, normalization, collision-safe add/remove limits, remount persistence, and exact reduced-motion stability with the production `chimer-running` route class; Grid Motion joins the existing narrowly scoped route-owned fallback freeze. Background IDs, palette roles, entitlements, and preview media are unchanged.

## 2026-08-04 — Phone background animation autonomy remediation

- Gradient Blinds originally moved both its gradient and blinds in this remediation; the 2026-08-05 correction above supersedes that motion design with fixed slats and moving illumination. Pixel Snow animates from a representative nonzero scene time at compact DPR, Faulty Terminal has visible autonomous defaults, and Grid Distortion combines ambient and pointer deformation. Real-browser phone captures prove motion at 390×844, exact-threshold motion at 360×780 for Faulty Terminal and Grid Distortion, and exact reduced-motion stability for all four renderers even with the production `chimer-running` route class present. Both Grid Distortion moving proofs wait 160 browser frames after readiness so seeded displacement has decayed and their 700ms pixel delta isolates autonomous time-driven movement. Preview generation remains deferred until the remaining live-background audit is resolved.

## 2026-08-04 — Background layering and framing remediation

- Active Ripple Grid, Dot Field, Dot Grid, and Shape Grid now remove only their duplicate patterned fallback after the current renderer completes a frame. Initial paint, lazy import, initialization failure, WebGL context loss, and reduced-motion non-mounts retain the fallback; Ripple Grid rebuilds after context restore before suppressing it again. Ripple Grid also has bounded non-transparent edge coverage, and Dark Veil uses drawing-buffer resolution. Focused contracts, the desktop-Chromium phone visual gate at 390×844 and 844×390 (forced failure, first-draw transition, context loss/restore, initial/later frames, runtime health, reduced motion, overflow, underlay, edge alpha, and Dark Veil framing), typecheck, lint, build, and diff checks passed.

## 2026-08-03 — Background branding audit readiness

- All 83 enabled backgrounds have complete branding proposals in seven review
  batches, and the focused audit/catalog/registry tests, typecheck, lint, and
  `git diff --check` all pass. No product labels changed. Batch 1 is the next
  user-review gate; the audit remains proposal evidence only, with no catalog
  rollout, media generation, or preview-runtime completion claimed.

## 2026-08-03 — Security dependency closure after PR #164

- Verified PR #164 merged Fix Wave 2 as
  `b7dbe6a36b13005b3748fa46e5c6a064355bddf2`. GitHub's post-merge rescan
  reduced the open inventory from 27 alerts to one medium, development-only
  `@hono/node-server` alert in shadcn's MCP toolchain.
- The closure raises the reviewed override, exact lockfile copy, and regression
  floor from `2.0.5` to the first patched release, `2.0.10`. MassageLab has no
  Hono import or WebSocket upgrade route, so the vulnerable public handshake
  path is not present in the deployed application.
- Exact installation, focused dependency tests (4/4), Prisma generation and
  validation, the shadcn CLI smoke, typecheck, lint, all 1,902 unit tests, and
  the 101-page Production build pass. npm's install summary reports 10
  aggregate findings (1 low, 9 high), but detailed mapping is unavailable
  because this environment does not authorize uploading the repository
  dependency graph to npm. The optional AgentOps security-gate scripts are
  absent and remain a scanner coverage gap. GitHub's public advisory catalog
  reports no advisory affecting the warned development-only `glob@10.5.0`
  copy. Default-branch Dependabot rescan remains the closure proof after merge.

## 2026-08-02 — Security dependency Fix Wave 2 local implementation

- Verified PR #162 merged Fix Wave 1 to `main` as
  `1f31a1295343e19bad9978363cf7f5a59faeabd3`, removed its clean merged local
  worktree and branch, and created `codex/security-triage-wave2` from that
  exact merge commit without changing the dirty primary checkout.
- The post-merge Dependabot inventory contained 27 open alerts across seven
  package families. The local exact-lock install reported 22 aggregate
  vulnerabilities (2 low, 5 moderate, 15 high). Wave 2 upgrades Next and its
  ESLint config to `16.2.12`, Prisma packages to `7.9.1`, shadcn to `4.16.1`,
  and top-level PostCSS to a patched `8.5.x` resolution. Narrow overrides keep
  every reviewed Next PostCSS/Sharp, Babel, Hono, `fast-uri`, and
  `brace-expansion` copy above its advisory floor.
- Auth.js keeps its optional Nodemailer peer absent because its email provider
  is disabled. MassageLab's account mailer instead imports patched
  `nodemailer@9.0.3` through `nodemailer-v9`, accepts only fixed text message
  fields, and disables file and URL access. A runtime regression proves the
  alias can deliver that bounded message shape.
- The updated exact lockfile installs successfully, focused security/auth tests
  pass 9/9, Prisma generation and schema validation pass, the upgraded shadcn
  CLI starts, typecheck and lint pass, all 1,902 unit tests pass, the
  Production build generates 101/101 pages, and three focused desktop Chromium
  public-route, registration, and PWA/icon checks pass. The repo does not
  contain the optional AgentOps security-gate scripts, so no result is claimed
  from that scanner.
- The clean install summary is reduced to 12 aggregate findings (1 low,
  2 moderate, 9 high), but a standalone detailed npm audit was unavailable
  under the environment security policy and GitHub's default-branch inventory
  cannot rescan the new lockfile before merge. The branch therefore does not
  claim a clean dependency inventory yet. Ready PR #164 is open at exact head
  `066cd60cdd6bd6962367983a695c90cdf8e279dc`; QA, CodeQL, Vercel,
  CodeRabbit, and exact-head Codex review pass with no review threads. Merge
  remains a separate user action.

## 2026-08-02 — Security dependency Fix Wave 1 PR #162

- Triaged all 42 open default-branch Dependabot alerts against the installed
  dependency paths and current runtime configuration. Two findings were
  directly reachable: the Next.js App Router Server Action CPU
  denial-of-service and unauthenticated Server Function endpoint-ID disclosure.
  The remaining 40 alerts lack their advisory-specific runtime preconditions;
  they remain tracked for dependency cleanup rather than application
  workarounds.
- Ready PR #162 upgrades `next` and `eslint-config-next` to `16.2.11`,
  `next-auth` to `5.0.0-beta.32`, and `@auth/prisma-adapter` to `2.11.3`,
  deduplicating `@auth/core` at patched version `0.41.3`. A focused
  dependency-security test prevents those framework and authentication
  packages from falling below the patched floors.
- Focused dependency/authentication coverage passes 24/24; Prisma generation,
  typecheck, lint, all 1,899 unit tests, the 101-page Production build, two
  focused desktop Chromium runtime/auth smokes, and `git diff --check` pass.
  GitHub CodeQL, Vercel, and CodeRabbit passed the first PR head, with CodeRabbit
  reporting no actionable comments. Remaining advisory families are reserved
  for Fix Wave 2, and merge remains a separate user action.

## 2026-08-02 — July 18 numbered-program Production closure

- PR #160 merged Track 4B to `main` as `3ababd8d3754419ecd7d3cc076fe69b8604dc246`, and the exact merge commit completed its Vercel Production deployment successfully.
- Live verification at `https://www.massagelab.app/chimer` exercised the setup flow through the production Background carousel, confirmed DNA and Twisted Cubes as items 5 and 6, and loaded each centered vertical WebM preview to a complete ready state with its WebP poster and no media error. The public R2 manifest contains both entries, and all twelve Track 4B WebM/WebP assets across landscape, square, and vertical layouts return HTTP 200 with the declared MIME types and manifest-matching byte lengths. The only browser console failure was an unreachable external Cloudflare analytics beacon in the verification environment, not a MassageLab application or media request.
- Tracks 1A/1B, 2, 3A/3B, 4A, and 4B are now complete. The July 18 numbered program has no remaining implementation or rollout gate; its detailed plan checklists remain historical execution evidence rather than active work.

## 2026-08-02 — Track 4B PR #160 review loop

- Opened ready PR #160 for `codex/track4b-dna-twisted-cubes` after the user explicitly waived the missing final local CodeRabbit-clean artifact. The waiver was limited to the truncated, unpersisted local CLI result; GitHub QA, CodeQL, Vercel, CodeRabbit, Codex, issue comments, and review threads remain required gates, and merge remains a separate user action.
- The first GitHub CodeQL scan identified one valid branch-owned regular-expression injection path in the Playwright development-review selector. The configuration no longer compiles a positional command-line argument with `RegExp`; it recognizes the required bounded filter subset through literal normalization and ordered fragment matching, rejects oversized or unsupported patterns, and has direct regression coverage. The focused harness passes 10/10, typecheck and lint pass, and the exact-head GitHub checks will rerun after the fix is pushed.
- The first Codex review identified two additional valid P2 findings. After the user separately authorized hosted-media publication, the exact 13-object dry run was accepted and `index.json`, six DNA/Cubes WebM videos, and six matching WebP posters were uploaded under `massagelab-public-media/chimer/background-previews/`. All public objects return 200 with correct MIME types and browser CORS, and their byte lengths and SHA-256 hashes match the committed files. Cloudflare currently exposes `max-age=14400, must-revalidate` for the media objects despite the uploader's five-minute object metadata. Carousel preview playback now follows the centered card instead of the separately persisted selection.
- The first full GitHub CodeRabbit review was verified finding by finding. Nine branch-owned items were fixed: aspect-matched video/poster selection, comment-safe source contracts, bounds-derived persistence expectations, local transient-field rejection, a stable DNA composition selector, stricter computed-style fixtures, complete compact-header bounds, and explicit mutation-free R2 dry-run evidence. Four claims were rejected against direct repository/runtime evidence, and two broad duplication suggestions were deferred as unrelated refactors because the compared helpers have different contracts or existing synchronization coverage. The focused Node suite passes 146/146 on Node 24.15.0, typecheck and lint pass, focused desktop Chromium passes 4/4, `git diff --check` passes, and a fresh production build completes successfully.
- The exact-head Codex review identified one additional valid P2: Twisted Cubes layer depth changed Z state without a visible projection. The first local-Z translation fix passed its matrix contract but moved the production-captured default mostly off-screen; a real perspective correction then changed the approved orthographic composition too heavily. The final implementation projects count-relative vmin depth into a bounded centered per-layer scale, keeps the no-perspective nested-cube identity, and directly proves a rendered geometry change without rewiring unrelated controls. The focused source/domain suite passes 94/94, the named-slider and reduced-motion Chromium proofs pass 2/2, and the fresh 101-page Production build passes.
- The next exact-head CodeRabbit/Codex pass raised six valid final hardening items. The branch now keeps project-state verification dates aligned, handles Playwright wildcards beside anchors, proves depth through rendered geometry without assuming a computed-matrix layout, bounds the host source scan, retains paused first frames for legacy posterless cards while limiting playback to the centered card, and regenerates Twisted Cubes media after the depth projection. The 13-object R2 batch was republished after a clean dry run; cache-busted public GETs confirm HTTP 200, correct MIME and CORS, exact byte length, and exact SHA-256 equality for every manifest/video/poster object.

## 2026-08-01 — Track 4B final visual acceptance and PR-loop authorization

- The user completed the final local visual pass after the Cube performance correction, accepted the current DNA and Twisted Cubes result, and explicitly authorized the PR loop. Merge remains a separate user action.
- During the pre-PR review the user identified one final educational DNA mismatch: complementary letters were correct, but endpoint colors were still independently assigned. DNA now maps A, T, G, and C to stable Adenine, Thymine, Guanine, and Cytosine roles whether letters are visible or hidden. Focused coverage, typecheck, and desktop/mobile real-browser Source-mode proofs passed, and the user accepted the corrected local result before the PR loop resumed.
- The next exact-head local CodeRabbit pass on `0f403df` identified nineteen valid branch-owned code, test, and documentation gaps. The fixes harden source-comment masking, share property-control contracts and bounds, cover boolean review properties, use manifest-backed preview media, correct the twelve-edge and responsive-unit browser oracles, move computed-matrix parsing to a focused unit contract, memoize the fallback remount key, consolidate identical side-sheet CSS, document touch-target and complete-progression behavior, and bring canonical state in line with the accepted nucleotide and wireframe implementation. Its repeated preview-label suggestion remains an unrelated product decision: canonical source docs/tests intentionally preserve `MassageLaba Lamp`, and duplicate `Wave Current` naming is separate catalog debt. After the fixes, focused coverage, the affected browser matrix, typecheck, lint, all 1,875 repository tests, a 101-page Production build, and `git diff --check` pass.
- The following exact-head local CodeRabbit pass on `acb21e1` produced sixteen valid branch-owned hardening findings. The fixes make comment masking fail closed with direct regressions, correct remaining DNA documentation, provide missing-preview fallbacks, derive Playwright ignore globs from the review-spec list, reject non-CSS matrix numbers, verify the reduced-motion renderer contract, validate positional palette mappings and explicit source-color formats, bound draft-source matching, add focused intent comments, and validate preview dimensions before reuse or publication. Focused source/review tests pass 85/85; typecheck, lint, all 1,877 repository tests, the affected desktop Chromium geometry/reduced-motion case, the 101-page Production build, and `git diff --check` pass. The full Track 4B browser matrix was not repeated because the directly affected browser contract passed and the remaining changes are covered by focused source and unit contracts.
- The next exact-head local CodeRabbit pass on `47744fd` returned nineteen findings. Eighteen valid branch-owned items were fixed: rejected preview playback now falls back safely to its poster; Background Host and carousel motion share one ambient-reduction hook; the accepted twelve-edge Twisted renderer replaces stale face-model documentation; gallery props, source scanners, CSS color normalization, responsive probes, and focused browser/source oracles are hardened. The proposed listener-count `Map` was rejected because the DOM deduplicates identical listener registrations and the existing `Set` correctly models active listeners. Focused source tests pass 107/107; typecheck and lint pass; all 1,877 repository tests pass; the five affected desktop Chromium cases pass, including the 83-background palette sweep and rejected-playback fallback; the Production build generates 101/101 pages; and `git diff --check` passes.
- The next exact-head local CodeRabbit pass on `1e36385` returned twenty findings. Seventeen valid branch-owned items were fixed: Playwright file-filter parsing now covers raw regexes, optional refs, and variadic projects; palette construction fails closed on missing defaults and requires complete DNA/Cubes host options; preview playback distinguishes harmless aborts from real failures; the Twisted scale contract records only the wireframe size consumer; source assertions, uploader JSON parsing, compact-header diagnostics, retry visibility, and test organization are hardened. Three suggestions were rejected after verification: Guanine remains the user-approved white source role, duplicate `Wave Current` copy remains a separate product naming decision, and the Clock/Visual header rules retain distinct 420px/48rem breakpoints to preserve the accepted responsive layout. Focused source tests pass 99/99; typecheck and lint pass; all 1,881 repository tests pass; the four affected desktop Chromium cases pass; the Production build generates 101/101 pages; and `git diff --check` passes.
- The next exact-head local CodeRabbit pass on `9ca53fc` returned twenty-one findings. Seventeen valid branch-owned items were fixed: computed-matrix assertions cover exact 2D/3D values; preview-duration fallback behavior and dry-run JSON output are directly testable; preview media probes restore browser APIs; Visual draft fixtures and source assertions are registry-complete and comment-safe; DNA consumer structure fails clearly; manifest fallback URLs and media-query tracking are shared helpers; and focused documentation now records scanner limits and test intent. Four suggestions were rejected after verification: Playwright's variadic `--project` option intentionally consumes following non-option operands until `--`; malformed direct DNA/Cubes host counts intentionally fail closed to inert DOM below persisted product minima; and the complete fallback-style signature intentionally remounts mixed shorthand/longhand legacy styles. Focused source/review tests pass 94/94; typecheck and lint pass; all 1,884 repository tests pass; the four directly affected desktop Chromium cases pass; the Production build generates 101/101 pages; and `git diff --check` passes. Repeating the complete Track 4B browser matrix was not proportionate because the focused cases exercise every changed runtime seam and the remaining changes are source, unit, and documentation contracts.
- Review-pass numbering is cumulative: `fdbcf3e` was the fifteenth pass before five follow-up passes, making `9ca53fc` the twentieth local pass.
- The twenty-first exact-head local CodeRabbit pass on `b58f789` returned fifteen valid branch-owned findings. The fixes require unique Playwright regex/spec matches before enabling the development server; route guessed preview fallbacks through the configured media base; gate review attributes behind real diagnostics; explicitly prove both moved Twisted depth layers; correct historical round references and cumulative review numbering; and tighten focused source, process-start, matrix, feature-key, CSS-coordinate, ARIA, and scanner contracts. Focused regressions pass 48/48; typecheck and lint pass; all 1,885 repository tests pass; the two directly affected desktop Chromium diagnostic/coupling cases pass; the Production build generates 101/101 pages; and `git diff --check` passes.
- The twenty-second exact-head local CodeRabbit pass on `7f2cd73` returned sixteen findings. Fourteen valid branch-owned items were fixed: reduced-motion users receive a conservative hydration frame; production and review share compact/reduced media-query contracts; malformed DNA colors and HSL Twisted anchors retain source roles; FFprobe failures keep their diagnostics at publication; preview cleanup is exception-safe; and focused docs, types, fixtures, metadata, history, and canonical state are hardened. Two suggestions were rejected after source verification: open Visual drafts intentionally hide the legacy Primary/Secondary rows for every background while Track 4B controls receive the same access decision directly, and `fallbackStyle` is serialized only once inside the existing memo. Focused regressions pass 94/94; typecheck and lint pass; all 1,885 repository tests pass; the four directly affected desktop Chromium cases pass; the Production build generates 101/101 pages; and `git diff --check` passes.
- The twenty-third exact-head local CodeRabbit pass on `e3ad32a` returned eleven findings. Ten valid branch-owned test and documentation gaps were fixed: browser geometry oracles now derive from the exported DNA source contract; Twisted outline display precision, entitlement diagnostics, trailing Playwright options, interface extraction, CSS comment masking, and host-option intent have direct coverage; and the specification now distinguishes count-relative wireframe extent from z-axis layer spacing. The generated catalog-label suggestion was rejected again because it targets established product naming outside Track 4B. Focused review contracts pass 120/120; typecheck and lint pass; all 1,888 repository tests pass; the directly affected desktop Chromium geometry case passes; the Production build generates 101/101 pages; and `git diff --check` passes. The complete Track 4B browser matrix was not repeated because this pass changes test or documentation contracts only, and its sole browser-oracle change is covered by the focused real-browser case.
- The twenty-fourth exact-head local CodeRabbit pass on `900dee2` returned twenty-five findings. Twenty-four valid branch-owned runtime, test, and documentation gaps were fixed: Twisted source palette defaults now live in the pure domain module and guard incomplete renderer input; invalid DNA random values normalize into an explicit safe interval; computed/browser oracles, source slices, media-query use, preview visibility cleanup, CSS interaction checks, Visual-header containment, and failure diagnostics are hardened; and the design contract now distinguishes complementary-pair randomness from stable nucleotide roles and count-triggered regeneration. The claim that five poster assets were missing was rejected with repository evidence: all six declared DNA/Twisted `.webp` posters are tracked, present, non-empty branch additions. Focused contracts pass 61/61; typecheck and lint pass; all 1,889 repository tests pass; the six directly affected desktop Chromium cases pass; the Production build generates 101/101 pages; and `git diff --check` passes. The complete Track 4B browser matrix was not repeated because the six focused cases cover every changed browser seam and the remaining changes are pure-domain, source, and documentation contracts.
- The twenty-fifth exact-head local CodeRabbit pass on `35c95b2` returned twenty-five findings. Twenty-four valid branch-owned items were fixed: renderer defaults now derive from their domain contracts; missing Twisted anchors retain source colors; the preview-manifest generator emits its checked-in fallback helper; Playwright subcommands, browser DOM probes, shared color normalization, Visual source slices, interface diagnostics, and malformed-input coverage fail closed; and review-only diagnostics report only actual ambient-motion overrides. The poster-asset finding was rejected with the same repository evidence: all six declared Track 4B posters are tracked, present, and non-empty. Focused source/unit contracts pass 185/185; typecheck and lint pass; all 1,890 repository tests pass; the five directly affected desktop Chromium cases pass serially; the Production build generates 101/101 pages; and `git diff --check` passes. The first browser rerun also exposed and closed a missing DNA/Twisted source-prop path in the general development palette fixture. The complete Track 4B browser matrix was not repeated because those five focused cases exercise every changed runtime seam and the remaining changes are pure-domain, generator, source, and unit contracts.
- The twenty-sixth exact-head local CodeRabbit pass on `2cec6c6` returned eighteen findings. Fourteen valid branch-owned items were fixed: shared DNA/Twisted sanitizers now own the persisted option semantics; registry-loaded renderers fail closed on missing required option objects; DNA scene geometry derives from the exported source contract and preserves its intended 3D crossover context; base letters retain contrast against the white Guanine role; and Playwright parsing, preview visibility cleanup, CSS/source masking, malformed-input coverage, failure diagnostics, and browser color validation are hardened. Four suggestions were rejected after source verification: CI and the package engine already require Node 24 TypeScript stripping; unknown Host categories intentionally fail closed; DNA position controls intentionally use the approved center-relative percentage contract; and the accepted Twisted wireframe intentionally renders with `perspective: none` to preserve its full-bleed count-relative envelope. Focused contracts pass 203/203; typecheck and lint pass; all 1,891 repository tests pass; the four directly affected desktop Chromium cases pass serially; the Production build generates 101/101 pages; and `git diff --check` passes. The complete Track 4B browser matrix was not repeated because the focused cases cover every changed runtime seam and the remaining changes are sanitizer, source, and test contracts.
- The twenty-seventh exact-head local CodeRabbit pass on `619ead0` returned sixteen findings. Fifteen valid branch-owned items were fixed: partial preview renders preserve generator-only poster metadata from the checked-in manifest; vertical fallbacks stay vertical and honor the configured media base; stable preview videos, posters, and metadata revalidate consistently; the development Apply fixture commits and persists one reducer-owned snapshot; narrow Visual headers no longer override side-sheet spacing; DNA/Twisted numeric option clamping is shared; and focused source, type, license-date, uploader, and failure diagnostics are hardened. The recurring generated-catalog rename was rejected because canonical product docs and tests intentionally preserve `MassageLaba Lamp` outside Track 4B. Focused regressions pass 69/69; typecheck and lint pass; all 1,891 repository tests pass; the three directly affected desktop Chromium Apply, 16:9 side-sheet, and 4:3 bottom-dock cases pass serially; the Production build generates 101/101 pages; and `git diff --check` passes. The complete Track 4B browser matrix was not repeated because those three cases cover every changed runtime seam and the remaining changes are pure-domain, generator, source, test, and documentation contracts.
- The twenty-eighth exact-head local CodeRabbit pass on `f61c4aa` returned seven valid branch-owned findings. Partial aspect-ratio preview renders now merge manifest variants per key and recompute every top-level media URL while preserving untouched hashes and dimensions; DNA and Twisted Cubes share one accessible property-group wrapper; and mode-specific Twisted color documentation, computed-matrix cardinality, uploader failure diagnostics, and browser-oracle intent are explicit. Focused regressions pass 57/57; typecheck and lint pass; all 1,893 repository tests pass; the directly affected desktop Chromium named-control/accessibility case passes; the Production build generates 101/101 pages; and `git diff --check` passes. The complete Track 4B browser matrix was not repeated because the one focused browser case covers the only changed runtime DOM seam and the generator merge has a direct pure-unit regression.
- The twenty-ninth exact-head local CodeRabbit pass on `d70a2ae` returned sixteen findings. Thirteen valid branch-owned items were fixed: DNA's accepted render-only geometry is explicit in the design contract; shared control-group CSS is co-located with its component; DNA/Twisted count bounds and passive-renderer source guards are shared; applied review diagnostics use the reducer-owned committed snapshot; preview cleanup cannot mask an earlier browser failure; RGB and RGBA browser colors share one path; and focused source labels, comments, defaults, and fixtures are hardened. Three suggestions were rejected after verification: every supported palette role already proves a source fallback at registry initialization and in an exhaustive test; Guanine intentionally remains the user-approved white source role; and the Playwright harness already directly covers every requested file-filter form. Focused contracts pass 99/99; typecheck and lint pass; all 1,893 repository tests pass; the two directly affected desktop Chromium named-control and preview-media cases pass; a clean-cache Production build generates 101/101 pages; and `git diff --check` passes. The complete Track 4B browser matrix was not repeated because valid renderer geometry and behavior did not change and the two focused cases cover the changed runtime seams.
- The thirtieth exact-head local CodeRabbit pass on `0a07f15` returned ten findings. Eight valid branch-owned items were fixed: every collected preview upload uses the reachable revalidating cache policy; corrupt or structurally invalid generated manifests fall back to registry sources; empty matrix arguments have direct coverage; duplicated current-state copy is removed; the ambient-motion hook declares its client boundary; bounded option fallbacks share integer and range semantics; Guanine's accepted pure-white role uses canonical achromatic HSL without changing its rendered color; and focused helper intent is documented. The newly invalidated TypeScript dependency graph also exposed two broad UI casts, which now bridge to the exact DNA/Twisted control types. Two suggestions were rejected after verification: guessed vertical poster paths are intentional, configured-base-aware, directly tested fallback discovery before the player's error handling; and the source scanner explicitly does not parse regex literals, so a comment delimiter inside a regex is outside its documented contract. Focused contracts pass 81/81; typecheck and lint pass; all 1,893 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes. No browser rerun was proportionate because the pure-white normalization is visually identical and the remaining changes are scripts, docs, pure sanitization, and a module-boundary declaration.
- The thirty-first exact-head local CodeRabbit pass on `60c7616` returned nine findings. Eight valid branch-owned items were fixed: the specification distinguishes the approved Twisted scale default from its derived render-only viewport extent; the scanner contract directly preserves URL-style regex literals with escaped slashes; Playwright regex matching drops redundant state resets; the development gallery uses the host resolver's exact return type; the Background Host mount assertion is scoped to its extracted gate; passive-renderer dependency guards match whole words; DNA/Twisted settings document their dedicated sanitizer boundary; and empty generated preview variant maps fail with a direct descriptive regression. The request for another dedicated scanner test file was rejected because `tests/source-structure.test.mjs` already owns those unit contracts and nested template parsing is explicitly outside the scanner's documented scope. Focused contracts pass 159/159; typecheck and lint pass; all 1,894 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes. No browser rerun was proportionate because this pass changes documentation, source/test scoping, pure manifest guards, and type/assertion contracts without changing renderer behavior.
- The thirty-second exact-head local CodeRabbit pass on `8a7a977` returned fourteen findings. Thirteen valid branch-owned items were fixed: Visual Cancel/Apply assertions are button-scoped; DNA assignment stability explicitly covers every non-count edit; the hook-free Twisted guard is documented; positional palette mismatch errors identify the role family and both counts; supported Twisted anchor syntax is explicit; malformed direct Twisted host values are sanitized once while raw invalid layer counts still fail closed to inert DOM; Playwright review matching delegates to exact, unique-substring, and unique-regex helpers; custom-palette tests prove all DNA/Twisted geometry and motion values remain unchanged; passive source-guard families and complete regex literals have direct tests; preview fallback tests use the named structural constant; and focused host/interface assertions avoid order-dependent or chained matches. The repeated missing-poster suggestion was rejected because configured guessed poster discovery is intentional, directly tested, and still falls through the existing media error handling when the asset is absent. Focused contracts pass 108/108; typecheck and lint pass; all 1,895 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes. No browser rerun was proportionate because valid renderer input is unchanged and the only runtime hardening affects malformed direct host props.
- The thirty-third exact-head local CodeRabbit pass on `69c0ee2` returned twelve findings. Eleven valid branch-owned items were fixed: DNA/Twisted editor adapters now expose compile-checked option shapes without double casts; the preview playback probe documents its prototype cleanup; Visual draft assertions inspect executable source; partial preview manifests prune IDs removed from the current category registry; non-finite fallback naming is accurate; computed-consumer cardinality is a named Playwright contract; browser source-palette expectations reuse implementation constants; compact header geometry allows the existing one-pixel tolerance; DNA Host interface assertions are order-independent; bounded source-region extraction is shared; and Twisted delay/alpha expectations document their formulas. The request to hash only uploaded media object keys was rejected because Track 4B intentionally publishes stable manifest URLs with revalidating cache metadata; versioning object keys without coordinating every generated and runtime manifest URL would make those assets unreachable. Focused contracts pass 207/207; typecheck and lint pass; all 1,895 repository tests pass; the Production build generates 101/101 pages; the two directly affected desktop Chromium contract and compact-header cases pass; and `git diff --check` passes.
- The thirty-fourth exact-head local CodeRabbit pass on `8589351` returned twelve findings. Ten valid branch-owned items were fixed: redundant dry-run object summaries are removed; partial manifest merging preserves metadata omitted as `undefined`; malformed existing manifest entries without string IDs are discarded; Twisted sanitizer assertions are order-independent; preview visibility-listener accounting is scoped to the mounted carousel video; Playwright option values receive direct development-server regression coverage; the reduced-motion DNA half-rotation oracle is documented; preview generator source assertions are bounded to named functions; DNA matches the renderer containment contract; and settings reset reuses the canonical DNA/Twisted default maps. Two findings were rejected after verification: `ambient` is explicitly supported by the shared Host resolver, and adding `preserve-3d` to the Twisted scene would alter the user-approved rendering despite the accepted depth and geometry contracts. Focused contracts pass 127/127; typecheck and lint pass; all 1,895 repository tests pass; the Production build generates 101/101 pages; the directly affected desktop Chromium preview-cleanup and source-default cases pass; and `git diff --check` passes.
- The thirty-fifth exact-head local CodeRabbit pass on `b1a1796` returned five valid branch-owned findings. Playwright substring and regex filters that select either or both review specs now enable the required development server; a redundant Harmony midpoint assertion is removed; the Track 4B registry test verifies the stable dynamic-import module path instead of function stringification; preview-manifest cache busting uses a guaranteed-unique token; and slider source contracts validate each attribute independently inside its named element. Focused contracts pass 140/140; typecheck and lint pass; all 1,895 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes. No browser rerun was proportionate because the pass changes only test selection and assertion robustness without changing product rendering.
- The thirty-sixth exact-head local CodeRabbit pass on `acfc644` returned eight findings. Six valid branch-owned items were fixed: the established `MassageLaba Lamp` catalog label is explicitly distinguished from general descriptive prose; Playwright review matcher helpers document their exact, substring, and cross-separator regex contracts; partial preview merges preserve metadata when derived optional URLs are undefined; CDP cleanup cannot mask the original browser assertion; the Twisted alpha oracle documents index clamping; and source-region assertions mask comments first. The Twisted sanitizer inventory is now derived from every canonical default except intentionally raw `layerCount`, whose separate fail-closed path remains directly asserted. The repeated immutable-cache suggestion was rejected because Track 4B publishes stable mutable media keys, so long-lived immutable caching would serve stale previews. Focused contracts pass 56/56; typecheck and lint pass; all 1,895 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes. No browser rerun was proportionate because product rendering is unchanged.
- The thirty-seventh exact-head local CodeRabbit pass on `db47e3f` returned eight valid branch-owned documentation and test-maintainability findings. DNA assignment prose now refers to the new strand count; the plan identifies its 23 settings as new; preview listener instrumentation measures every registration against its post-mount baseline; the development gallery and both browser suites share one custom-palette fixture; the computed-consumer comment records its persistent paused-host contract; the carousel documents the shared ambient-motion authority; positive Visual source assertions mask comments; and passive-source fixtures prove both pattern and example coverage without order coupling. Focused source/unit contracts pass 42/42; typecheck and lint pass; the two directly affected desktop Chromium preview-lifecycle and Twisted-palette cases pass; and `git diff --check` passes. Repeating the full unit suite or Production build was not proportionate because production behavior is unchanged and the prior exact head already passed all 1,895 repository tests and generated 101/101 pages.
- The thirty-eighth exact-head local CodeRabbit pass on `5c47be5` returned eight findings. Seven valid branch-owned items were fixed: direct DNA host input sanitizes every bounded option before CSS use while raw invalid strand counts retain their inert-DOM fail-closed path; scene capture helpers report named missing-element diagnostics; preview probe restoration failures surface; Chimer setup relies on actionable readiness assertions instead of a swallowed network-idle wait; FFprobe dimension invocation is shared; Visual lifecycle fixtures isolate registry values from reducer mutation; and the preview fallback source contract tolerates formatting. The request to replace production DNA randomness was rejected after quantifying the default 70-rung comparison: a regenerated sequence collision is `4^-70`, approximately `7×10^-43`, so the existing real-randomness contract does not create a non-negligible flake risk. Focused source/unit contracts pass 67/67; typecheck and lint pass; all 1,895 repository tests pass; the four directly affected desktop Chromium mount, assignment, preview-cleanup, and responsive-geometry cases pass; the Production build generates 101/101 pages; and `git diff --check` passes.
- The thirty-ninth exact-head local CodeRabbit pass on `69da811` returned twelve valid branch-owned documentation, generator, runtime, and test-contract findings. The historical implementer hash records its post-commit provenance; DNA documents and tests exact half-open random intervals plus boolean-specific sanitization; preview generation fails early without `libwebp`; R2 dry-run cache expectations cannot inherit ambient environment drift; Twisted size math is documented and outline thickness serializes explicitly; the development gallery shares the complete ambient-motion authority; interface extraction documents its intentionally unmasked return; Playwright review-ignore resolution is pure and deterministic; and reduced-state cube capture fails with a named diagnostic. Preview-probe cleanup now reconciles both review requirements: restoration failures surface when the test otherwise passes, while an existing primary assertion remains authoritative. Focused contracts pass 86/86; typecheck and lint pass; the four directly affected desktop Chromium ambient-motion, compact, preview-cleanup, and responsive-geometry cases pass; and `git diff --check` passes. Repeating the full unit suite or Production build was not proportionate because the prior exact head passed all 1,895 repository tests and generated 101/101 pages, while every changed runtime/config seam in this pass has direct focused or real-browser coverage.
- The final Cube implementation applies Scale uniformly to the complete count-relative wireframe progression. At the approved 20-layer, 30% default the outer layer reaches `120vmax`; twelve thin CSS edges per layer replace full-size transparent faces, keeping the viewport filled without allocating the oversized raster surfaces that made the page and device slow or jittery. DNA retains the approved dense continuous strand, biological base-pair model, optional letters, and final defaults.
- The local preview renderer was force-run for both backgrounds and all three aspect ratios. The generated WebM and WebP files remained byte-identical to the committed accepted captures; the local manifest regeneration timestamp was refreshed, and no R2 upload or other remote media mutation occurred.
- The initial pre-PR validation before the first local review passed: focused Track 4B coverage was 203/203; the serial development-gallery matrix was 12/12 desktop and 12/12 mobile; typecheck and lint passed; all 1,865 repository tests passed; the Production build generated 101/101 pages; and `git diff --check` passed. A two-worker local gallery attempt was discarded after one development-asset 404 repeated across cases; the strict health assertion was retained, error reporting now includes the source URL, and the CI-matching single-worker rerun passed 24/24. The later exact-head review bullets supersede these historical initial totals with the final evidence.
- The first pre-PR local CodeRabbit pass reviewed committed head `304e87f` against refreshed `origin/main` and returned five valid documentation/test-contract findings: historical validation provenance, source-derived versus Track 4B-only defaults, strand-count assignment regeneration, interface-scoped source assertions, and attached-DOM transform oracles. All five were minimally corrected, and the affected focused tests, serial desktop/mobile browser matrix, typecheck, lint, all 1,865 repository tests, Production build, and `git diff --check` passed again.
- The second local CodeRabbit pass on `3fac7bf` found three valid branch-owned hardening gaps: incomplete color-role arrays when renderer props and theme roles are both missing, an implicit long-running responsive browser-test timeout, and a boolean sanitation fixture that did not exercise rejection. Source fallbacks, explicit timeout coverage, and the malformed-input regression now close those gaps. Focused tests pass 56/56, the serial desktop/mobile browser matrix passes 24/24, typecheck and lint pass, all 1,866 repository tests pass, the Production build generates 101/101 pages, and `git diff --check` passes. Its separate preview-label suggestions were verified as unchanged `origin/main` product naming; `MassageLaba Lamp` is explicitly established by canonical docs/tests, while renaming the duplicate `Wave Current` catalog label requires a separate product decision.
- The third exact-head local CodeRabbit pass on `652f809` found one remaining valid source-assertion scope gap. DNA and Twisted Cubes component-contract tests now share the same balanced-interface extractor, so each assertion is confined to its named TypeScript interface instead of matching fields from later declarations.
- The fourth exact-head local CodeRabbit pass on `4da9022` identified branch-owned documentation, storage-resilience, haptics, browser color-normalization, and source-contract hardening opportunities. The valid findings were corrected without changing the accepted visuals. Separate suggestions to rename pre-existing catalog labels were rejected as an unrelated product decision, and the intentional white DNA source role remains part of the user-accepted yellow/blue/white/pink palette rather than being changed to magenta. Focused tests pass 56/56; the 42 unaffected serial browser cases and the corrected desktop/mobile 83-background sweep pass; typecheck and lint pass; all 1,866 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes.
- The fifth exact-head local CodeRabbit pass on `c4eb95b` returned one valid test-only consistency finding. The DNA stylesheet isolation assertion now checks comment-stripped CSS, matching the Twisted Cubes contract and preventing a commented global selector from producing a false failure.
- The sixth exact-head local CodeRabbit pass on `ea80eca` returned two valid test/documentation maintainability findings. Direct render-change exceptions now live beside computed-coupling exceptions in the single frozen 22-control consumer contract, so the browser matrices cannot drift through a second keyed allowlist. The historical Fix Round 4 report now describes only its own captured validation state and points to this canonical log for current final evidence without embedding a stale total. The focused source checks pass 17/17, the affected exact browser matrix passes, typecheck and lint pass, all 1,866 repository tests pass, the Production build generates 101/101 pages, and `git diff --check` passes.
- The seventh exact-head local CodeRabbit pass on `5a49168` found four valid branch-owned hardening opportunities. Dirty Visual drafts now report `Unsaved changes` ahead of any stale account-sync message; Playwright development-gallery detection accepts safe standalone filenames and substring filters; DNA role-index evidence checks both array bounds before indexing; and poster extraction probes the actual encoded video duration before seeking, including the existing-video reuse path. The separate request to rename the original `MassageLaba Lamp` and review other catalog labels was rejected because those labels are established pre-existing product decisions outside Track 4B. Focused tests pass 63/63, the affected desktop browser proof passes, typecheck and lint pass, all 1,867 repository tests pass, the Production build generates 101/101 pages, and `git diff --check` passes.
- The eighth exact-head local CodeRabbit pass on `5954564` found seven final branch-owned contract and documentation gaps. The accepted `120vmax` Twisted Cubes envelope is now explicitly documented as derived, non-persisted layout; DNA randomness is described precisely as mount-stable endpoint color roles; Playwright development-gallery detection ignores option values; zero-byte posters are regenerated instead of reused; the direct-DOM base-letter toggle is explicitly separated from the numeric computed-consumer map; Twisted outline helpers cover zero/one-layer inputs; and component/style source contracts consistently ignore comments. The repeated request to rename pre-existing catalog labels was again rejected as an unrelated product decision. Focused tests pass 40/40, the affected desktop browser proof passes, typecheck and lint pass, all 1,867 repository tests pass, the Production build generates 101/101 pages, and `git diff --check` passes.
- The ninth exact-head local CodeRabbit pass on `d7c0544` found four valid runtime/documentation gaps. DNA count changes now replace mount assignments during the guarded render restart instead of painting one stale frame before an effect; decorative fallback remount identity includes the complete resolved style; an unsaved Visual draft suppresses Retry sync until the draft is applied or canceled; and the Task 8 matrix report explicitly separates the boolean base-letter DOM proof from its 22 numeric slider contracts. The repeated pre-existing `MassageLaba Lamp` rename was again rejected because it is an established catalog/product label outside this branch. The focused DNA browser proof passes; typecheck and lint pass; all 1,867 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes.
- The tenth exact-head local CodeRabbit pass on `6c59153` found two valid Playwright parser gaps. Standalone filename substrings now enable the development-only gallery only when they identify exactly one review spec, and the parser skips the values for `--trace`, `--repeat-each`, and `--tsconfig` along with the existing value-taking options. The recurring catalog-label rename was again rejected as outside Track 4B. A real development-gallery browser case passes with the hardened value-taking options; typecheck and lint pass; all 1,867 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes.
- The eleventh exact-head local CodeRabbit pass on `72f9c43` found six valid branch-owned generator, fixture, documentation, and test-contract gaps. Poster encoding now fails before manifest publication if FFmpeg does not produce a non-empty WebP; the development review exposes a deterministic missing-video path while retaining a valid poster; the two Track 4B source rows remain part of the active Markdown table; effect-gate markers are asserted before slicing; the shared interface extractor ignores braces in comments and quoted text without changing returned source offsets; and the Task 8 report identifies its reduced DNA count as a browser-review DOM cap rather than a production limit. Requests to rename established or duplicate catalog labels were rejected as unrelated product decisions. Focused source coverage passes 29/29 and the desktop/mobile preview-media browser proof passes 2/2; typecheck and lint pass; all 1,869 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes.
- The twelfth exact-head local CodeRabbit pass on `8210155` found six final branch-owned robustness and evidence-precision gaps. Historical validation wording now distinguishes the initial accepted-visual gate from later exact-head totals; the Task 8 report records its post-commit handoff hash without implying foreknowledge; the plan separates bounded browser-review DOM counts from the production DNA ceiling; preview reuse requires a non-empty video as well as poster; interface extraction preserves UTF-16 offsets around non-BMP text; and Twisted face assertions remain inside the `.face` rule block. Focused source coverage passes 16/16; typecheck and lint pass; all 1,869 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes.
- The thirteenth exact-head local CodeRabbit pass on `d4a39b0` found one final development-gallery fail-closed gap. Track 4B draft creation now supplies an empty mapping when its palette adapter is absent, allowing the component's existing missing-adapter guard to render nothing instead of throwing first. The focused source contract passes 15/15; typecheck and lint pass; all 1,869 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes.
- The fourteenth local CodeRabbit pass on `b377447` identified the remaining branch-owned maintainability and test-contract gaps without changing the accepted visuals. DNA and Twisted Cubes now share their exported option bounds with persistence sanitization and renderer caps; the renderers are memoized; DNA source connector/outline roles are named constants; the shared host resolver publishes its exact return type; poster generation safely handles successful duration probes with empty output while preserving real probe failures; source-interface extraction fails closed on unbalanced quoted state; Playwright argument parsing recognizes its remaining value-taking options and unique partial spec filters; and focused tests now cover the complete palette, numeric bounds, source structure, draft contracts, upload dry-run, and responsive geometry. The focused suite passes 198/198; typecheck and lint pass; all 1,870 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes.
- The fifteenth local CodeRabbit pass on `fdbcf3e` returned 22 observations. Eighteen valid branch-owned findings were corrected: unavailable local storage no longer reports Visual settings as saved; the plan uses the current DNA assignment helper; duplicated DNA geometry is a shared CSS variable; carousel preview motion uses the central ambient-motion decision; host input types distinguish adapter-filled colors from required renderer controls; the development gallery applies consecutive patches from current state; manifest generation shares its local media prefix and documents partial-run preservation; browser fixtures share typed probe reads, named filter cases, source-normalized color comparisons, default-derived counts, helper JSDoc, and comment-stripped source scans; registry loaders are named; numeric fallback clamping is bounded; and corresponding source contracts follow the new boundaries. Four suggestions were rejected after verification: guessing that unknown Playwright options consume the next positional filter, extracting a browser matrix oracle into a Node helper, splitting one exhaustive all-key contract into six redundant tests, and changing a nonexistent `assertSlider` regex. The affected focused source checks pass 83/83; the exact desktop browser matrix passes 22/22; typecheck and lint pass; all 1,874 repository tests pass; the Production build generates 101/101 pages; and `git diff --check` passes.
- The authorized workflow requires a real clean exact-head local CodeRabbit result before push/opening the PR, then clean exact-head GitHub checks and reviews, verified and minimally fixed valid findings, replies and explicit resolution for every thread, and a stop before merge.

## 2026-07-31 — Track 4B DNA and Twisted Cubes implementation and source review

- Completed Track 4B Tasks 1-9 on `codex/track4b-dna-twisted-cubes`: DNA domain and responsive rules (`2175d6b4`); DNA renderer and its source-geometry follow-up (`b46a87d7`, `5901cb65`); Twisted Cubes domain and gradients (`1c78893a`); Twisted Cubes renderer (`354c8942`); shared Track 4A palette adapters (`96b8570b`); catalog/host registration and follow-ups (`92035f67`, `61747de3`, `8e70e432`); Visual controls, persistence, and shared consumer wiring (`f39e684f`, `4971109b`); and the `/dev/buttons` review matrix plus six WebM previews, six WebP posters, and hardening follow-ups (`abf4bfaf`, `f891b97f`, `45a5641b`, `18e0e28a`, `57f27162`).
- `massage-lab-dna` and `massage-lab-twisted-cubes` are enabled premium catalog entries in the exact `chimer`, `clock`, `music`, and `ambient` categories and render through the shared `BackgroundHost`. `premium_backgrounds` membership access and permanent credit/purchase ownership use the existing canonical resolver; color and property editing follows selected-background access. Both renderers share the Track 4A Source/Custom/Harmony palette, draft/preset, Apply/Cancel/retry, sanitized local/account settings, and Chimer/Clock/Music/ambient consumer conversion contracts without storing derived randomness, colors, or viewport-only values.
- DNA renders 7-81 strands with mount-stable biologically valid A-T/T-A/G-C/C-G base pairs and deterministic base-specific colors: every A uses Adenine's role, every T uses Thymine's role, every G uses Guanine's role, and every C uses Cytosine's role. The saved Show base letters toggle defaults Off and changes only label visibility, never node color. It has separate node and strand speeds down to `0.01x`, JavaScript sine phases for cross-browser support, and responsive render-only geometry bounds. The final pre-PR refinement defaults to 70 strands, `0.06x` node motion, `0.02x` strand rotation, 30-degree angle, `0.50vmin` spacing, 50% scale, centered position, 94% connector width, 15% connector thickness, and `0.10vmin` outline. Its fixed `26vmin` by `max(240vmin, 230vmax)` authored scene preserves the prior off-screen envelope and node density at 50%, so neither end appears during horizontal rotation. Twisted Cubes renders 6-30 count-relative layers with continuous 180-340 degree Source HSL, smooth six-anchor Custom/Harmony interpolation, and separate speed and stagger; rotation defaults to `0.25x` with a `0.01x` minimum, while scale defaults to 30% with a 10% minimum. Scale applies uniformly to the complete progression; at the approved default the twentieth layer reaches `120vmax`. Each layer is twelve thin CSS edges instead of six viewport-size transparent faces, preserving the nested outline and full-bleed envelope without the rejected page-wide rendering slowdown. Both effects are decorative native React/scoped-CSS renderers with representative static reduced-motion states and no new dependency.
- The review gallery covers Source, Custom, Harmony, reduced motion, compact viewport, locked access, dirty draft, applied settings, palette edits, control bounds, persistence behavior, shared consumer rendering, DOM caps, and preview fallback behavior. Its development-only motion override bypasses ambient reduced-motion gating for review without overriding an explicit `motionEnabled=false`; the reviewer can still pause both effects. The Visual panel keeps Undo/Redo/Cancel/Apply in its header and changes to a two-row header at narrow widths so the actions do not overlap the title, visual toggle, clock color, or close action. Each effect has three committed WebM previews and three matching WebP posters for landscape, square, and vertical picker media. All 12 assets were force-regenerated after the final approved defaults and geometry while retaining all 83 manifest entries; capture suppresses development-only issue chrome, and representative inspection confirms continuous DNA and full-bleed Twisted layers without claiming the user's final visual sign-off. A local R2 upload script is available, but no preview object was uploaded and no remote mutation was performed during this branch.
- Task 9 extracted and inspected `css-trigonometric-function-dna-strand.zip` and `cubies.zip` from the supplied local downloads. The archives match the approved CodePen sources by Jhey and include the reviewed MIT license with 2026 copyright. The comparison corrected DNA's composition-owned in-place rotation, source grid and gap geometry, percentage/vmin connector and outline units, connector collapse timing, and opposite-depth node crossover; later user visual passes deliberately lengthened the authored scene and paired the 50% default with 70 strands so no DNA ends appear and density remains continuous. The comparison also corrected Twisted Cubes to the exact shared `hsl(210 20% 12%)` root fill, source four-stage 90-degree sequence, source easing, and count-relative cube growth; the final visual pass retained a `120vmax` outer envelope at the 30% default, made Scale affect the full progression, and replaced large transparent faces with twelve thin edges per layer so all nested outlines remain visible without oversized rendering work.
- Approved adaptations remain intentional: DNA computes sine phases in JavaScript; Twisted Cubes keeps the approved normalized 180-340 degree Source endpoints and count-relative delay; both use a relative negative depth offset rather than the source's global camera placement; and MassageLab retains its palette/access wiring, responsive clamps, representative reduced-motion states, native React/CSS implementation, and host lifecycle constraints. The decorative fallback now remounts on palette/access changes so exhaustive Source/Custom/Harmony switching does not trigger React shorthand/longhand style conflicts, without remounting the active effect or disturbing timer/Music state.

## 2026-07-31 — Track 4A acceptance and Production closure

- PR #158 merged to `main` as `dd481ad85056f968b3c58674038d7ef1ea983baf`. Vercel reported the exact commit ready in Production, and the post-deploy one-hour runtime-error check found no errors.
- The final branch passed focused coverage, typecheck, lint, all 1,815 tests, a 101-page Production build, and `git diff --check`; QA, CodeQL, Vercel, CodeRabbit, and exact-head Codex review were clean, and every review thread was resolved before merge.
- The user accepted the final local visual result, including real background rendering for palette review, color-picker behavior, generated Harmony previews, Apply/Cancel/Retry styling, the responsive bottom-dock and short-landscape side-sheet Visual layouts, and access-based background customization.
- Track 4A is fully accepted. Track 4B Task 1, responsive layout and DNA domain rules, is now the next numbered dependency-backed work.

## 2026-07-30 — Track 4A visual-review corrections

- The first manual `/dev/buttons` palette review found that reduced-motion preferences left the guarded live-renderer panel on its common fallback, while the Custom and Harmony static specimens exposed real controls through no-op callbacks. The development-only live review now explicitly mounts the selected real renderer without changing production motion behavior, and the editable specimens retain local palette/mapping state while Source and Access locked preserve their intended read-only behavior.
- Complete six-digit HEX edits now immediately update the controlled swatch, saturation/value handle, and hue control. Harmony choices now preview their generated seven-color palettes from the current Primary color instead of showing only the generic blue button face.
- Added the semantic shared `success` button variant using the established leaf-green material. Visual Apply uses Success, Cancel uses Destructive, and Retry/Retry sync uses CTA in both the real Visual draft row and its review specimen.
- Focused contracts passed 38/38; the reduced-motion real-renderer Playwright regression passed on desktop and mobile; typecheck, lint, all 1,801 tests, the 101-page production build, and `git diff --check` passed. The corrected gallery recheck confirmed that real backgrounds render, colors update through the repaired pickers, Harmony previews match the intended direction, and Apply/Cancel/Retry use the approved intent colors; the reviewing device still held animation under reduced motion, which was sufficient for this palette review. The remainder of the plan's route/device acceptance matrix stays open.
- A subsequent Clock review found that the expanded Visual dock hid too much of the selected background. The first responsive revision capped Visual to half the visual-viewport height and used a half-width side sheet for every landscape viewport, opposite the configured left/right app sidebar.
- The 1024x768 review showed that broad landscape detection moved Visual sideways too early, did not shift the protected clock, and let the seven-swatch desktop minimum create horizontal scrolling. Clock and Visual now share bottom-dock behavior below 16:9; at 16:9 or wider they share the opposite-sidebar half-width side sheet and reserve that horizontal stage edge so the clock refits into the visible area. Side-sheet controls shrink to their container, horizontal overflow is suppressed, and the immersive Visual swatch faces are approximately one-third smaller.
- The follow-up 4:3 review aligned Visual's stable stage reservation with Clock, placed Color source beside the Shared Colors introduction on wide bottom docks, and confirmed the 24-case desktop/mobile immersive browser matrix. The same review rejected the retired subscription-specific color message. Background customization now uses the canonical selected-background access decision: free, active credit-owned, active purchased, and membership-included backgrounds expose colors, presets, and renderer properties; locked backgrounds remain read-only until access is acquired.

## 2026-07-29 — Track 4A shared background palette atomic cutover

- Completed the Track 4A Task 10 implementation cutover from the old Global Colors and per-renderer color-control state to the nested seven-swatch Source/Custom/Harmony preference contract established by Tasks 1-9. Sanitized Chimer and entitlement payloads now omit renderer color fallbacks plus obsolete palette mode, Primary, harmony, and renderer metadata fields while retaining non-color settings.
- Finalized every enabled background adapter as typed `supported` or explicitly `unsupported`. Source colors live in the adapter-owned source inventory, and `BackgroundHost` delegates the selected background to `resolveBackgroundEffectProps` without heuristic property-name matching or generated tint for unsupported media.
- Active Chimer, ordinary Clock, and the Music visualizer now pass the same committed-or-draft palette, selected-background mapping, and feature/ownership access contract through `BackgroundHost`. The original Moving Gradient also uses that shared host path, while setup keeps its existing site backdrop and does not mount the selected live effect.
- Removed legacy Global Colors local-storage hydration/writes and its independent saved-palette state from the immersive runtime. The one-time migration in `lib/background-palette.js` remains the only reader for the two old storage keys when nested v1 preferences are absent. Clock digit/stroke/shadow/glow colors and every specialized non-color background control, including 3D Globe location/lighting behavior, remain separate and intact.
- Replaced brittle source-count assertions with final-adapter, persisted-state, staged-resolver, access fallback, and real-renderer behavior checks. The focused contract gate passed 280 tests; typecheck passed; lint passed with no errors; and `git diff --check` passed.
- Recorded the Visual dirty-guard history-integrity boundary: anchors and native unload remain guarded everywhere, while a custom same-document Back/Forward prompt is installed only when the browser or router exposes a stable history index. The rare indexless case intentionally avoids a directionless `popstate` fallback, history sentinels, and Next-owned state mutation because those approaches can trap navigation or corrupt Forward history.
- The Playwright palette sweep passed all 16 desktop/mobile scenarios. It exercised every enabled background through Source, Custom, and Harmony, truthful CSS/DOM/Canvas/WebGL diagnostics, special Source and unsupported behavior, swatch/mapping isolation, Music Tone continuity, real Chimer timer identity/progress, and short-landscape reduced motion. The first run exposed an omitted legacy Lightning source-color dereference before adapter resolution; the source-input fallback was hardened and the complete rerun passed 16/16.
- Manual acceptance remains open for `/dev/buttons`, Clock, active Chimer, and Music at desktop, phone portrait, short landscape, 200% zoom, and reduced motion, including draft/Undo/Redo/Apply/Cancel, unsaved-change guards, local/account persistence and retry, preset limits/defaults, and timer/music continuity. Track 4B remains dependent on that user review.

## 2026-07-28 — Track 1 rollout closure and July 18 reconciliation

- PR #156 merged as `4b9bb291820edd67f184e51f0ad9d7cbe9bea881`, and Vercel reports that exact `main` commit READY in Production.
- The user explicitly authorized and completed one $5 Production one-time-support smoke. The Pricing return reported completion; no payment details or customer/processor identifiers were recorded.
- Sanitized GET-only Stripe retrieval found the newest matching live payment-mode Session paid and complete. Stripe-powered Automatic Tax was enabled and complete with self liability, billing-address collection was required, and the single exclusive line used exact product tax code `txcd_90000001`. The $5.00 subtotal produced $0.00 calculated tax and a $5.00 total; the PaymentIntent succeeded and received exactly $5.00.
- Stripe's live tax-code lookup names `txcd_90000001` `Cash Donation` and describes a nothing-in-return monetary payment. The zero-tax result is therefore recorded as the successful applied tax outcome for the reviewed one-time-support classification, not as disabled or failed Automatic Tax.
- The completed Session attached no Customer, client reference, Subscription, or Invoice. The existing source and test contract ignores one-time-support completion for membership reconciliation, so the payment granted no membership, feature, background credit, or ownership.
- Track 1A, Track 1B, and their rollout gates are complete. Attorney review of legal/trust language and first-cohort Production monitoring remain external invite-readiness gates rather than Track 1 implementation.
- Reconciled the release checklist with the explicitly approved final Track 1 rollout scope. The taxed low-dollar enrollment, webhook-backed entitlement, focused $1-to-$2 change, anchor/no-proration contract, return path, and one-time-support smoke are complete. A $5 change, payment/address update, period-end cancellation, and annual Checkout/cancellation-or-switch are retained as an expanded billing matrix for first-cohort monitoring or separately authorized follow-up smokes, not retroactive Track 1 blockers.
- Reconciled the July 18 numbered plans against landed source and canonical state: Track 2 and Track 3A/3B are landed; the complete Track 4A palette/draft/preset/adapter contract and Track 4B DNA/Twisted Cubes are not. The next numbered work is Track 4A Task 1, defining the pure shared-palette, preset, migration, and access contract.

## 2026-07-28 — One-time-support live readiness verification

- PR #155 merged as `6e5d65106d8a16f1c2723311dc41c884f7c522c2`.
  Vercel built that exact `main` commit as a READY Production deployment.
- The Production build ran the exact GET-only
  `stripe:readiness --live --verify-stripe --no-dotenv` path before the
  application build. Stripe API retrieval was requested and performed, all
  five one-time-support tax gates were true, and the command reported
  one-time-support, recurring Supporter, and background-commerce readiness as
  ready without printing secret values.
- The subsequent application build passed, and a post-deploy Production
  runtime scan found no error or fatal logs.
- Removed the temporary Production build entrypoint after its single intended
  verification run. The ordinary Vercel application build remains unchanged.
- Did not invoke `/api/billing/donation`, create a Checkout Session, or charge
  a payment. Track 1 rollout closure now requires only an explicitly authorized
  live one-time-support smoke with completed Session and line-item tax
  verification.

## 2026-07-28 — One-time-support Production Tax gates

- The user reverified that live Stripe Tax is ready and that the applicable
  Ohio registration is registered and collecting. This confirmation remains
  separate from the reviewed one-time-support classification
  `txcd_90000001`.
- Added all five one-time-support Production gates as sensitive Vercel
  variables: Automatic Tax enablement, exact Product tax code, provider
  readiness, registrations readiness, and classification confirmation.
- Redeployed the exact PR #153 merge commit
  `5f96bc029759d04e5b79aeb548508cef77a312d1`. Vercel reports the new
  Production deployment READY on every canonical alias, `/pricing` returns
  200, and the post-deploy runtime error scan found no errors.
- Completed the bounded read-only backend verification for the controlled
  Supporter change. Stripe reports one active $2 monthly Supporter
  subscription with exclusive tax behavior and Automatic Tax enabled. Its
  billing anchor and current-period start remain the original subscription
  start, and its latest invoice remains the paid, non-prorated initial $1
  subtotal plus $0.07 Ohio tax; the switch therefore created no immediate
  proration invoice or charge. The next $2 renewal has not occurred, so no
  completed $2 tax invoice is claimed.
- Read-only Production persistence verification confirms that the app resolves
  the current subscription as active Supporter access and continues to grant
  `premium_backgrounds` and `chimer_custom_colors`. Supplied Production
  screenshots separately confirm the Account return and membership-included
  background presentation.
- The protected Production secret could not be exported, so the required
  `stripe:readiness --live --verify-stripe` command was not rerun against the
  deployed values. Keep one-time-support Production readiness pending until
  that GET-only check passes from an explicit Production environment.
- Did not invoke `/api/billing/donation`, create a Checkout Session, or charge
  a payment. Only after readiness passes should the controlled live
  one-time-support smoke and completed Session/line-item tax verification move
  through the explicit payment authorization gate.

## 2026-07-28 — Track 1 rollout deployment verification

- Verified PR #152 merged as commit
  `45d5a247f1a133fe9f8eaa291c87fe928bba1a4a` and that Vercel reports that
  exact commit READY in Production on the `main` branch.
- Read the Production Vercel environment inventory without exposing values.
  None of the five independent one-time-support tax gates is present, so the
  deployed Checkout correctly remains fail-closed.
- Track 1 rollout closure still requires a separately reviewed Production
  configuration for Automatic Tax, exact `txcd_90000001` classification,
  provider readiness, registrations readiness, and classification
  confirmation; the readiness command must then pass before an explicitly
  authorized live one-time-support payment.
- Bounded read-only verification of the completed Supporter switch's backend
  amount, tax, billing anchor/no-proration behavior, and webhook-persisted Price
  mapping also remains open. Attorney review and first-cohort Production
  monitoring remain external invite-readiness gates rather than unfinished
  Track 1 implementation.

## 2026-07-27 — Track 1 rollout follow-up candidate

- Verified that PRs #150 and #151 are merged and that their exact merged commit
  has a READY Production deployment serving the canonical hosts.
- The controlled user completed the focused Supporter change from $1 monthly to
  $2 monthly. Supplied Production screenshots confirm return to Account,
  continuing Supporter status, and membership-included premium backgrounds.
  Subscriber-specific Stripe amount/tax/anchor/proration evidence and the
  webhook-persisted database Price mapping remain a bounded read-only
  verification gate; the available Production env export was redacted and
  enterprise browser policy blocked the authenticated dashboards, so those
  backend facts were not inferred from screenshots.
- Recorded the separately reviewed one-time-support classification
  `txcd_90000001`. The rollout candidate enables exclusive Automatic Tax,
  requires a billing address, and fails closed unless enablement, exact code,
  provider readiness, registrations, and classification confirmation are all
  explicit. Purpose metadata and webhook handling remain non-entitling.
- Updated Account membership copy to lead with all premium backgrounds, saved
  Chimer colors, and Supporter status. Subscriber cards use a dollar icon to
  explain optional permanent purchase/credit ownership and an authoritative
  crown for active purchase or credit ownership.
- Lowered the immersive toolbar's visible-overflow threshold so ordinary phone
  landscape layouts do not clip the three individual control glows into a
  faint shared rectangle. Truly narrow layouts retain horizontal scrolling.
- Focused Node coverage for billing, readiness, Account, background commerce,
  and the immersive toolbar passes all 181 tests. Repository typecheck, lint,
  all 1,680 Node tests, the production build, and focused desktop/mobile
  Playwright checks also pass. Production configuration, deployment, and an
  explicitly authorized live one-time-support payment remain pending.

## 2026-07-27 — Background membership ownership-card clarity

- Clarified the subscriber card state across Clock, active Chimer, and Music:
  `Included with membership` now identifies current subscription access, while
  `Keep after membership` identifies the optional permanent-ownership action.
- Reserved a dedicated responsive card column for the favorite control and
  allowed the ownership action to wrap inside its own column, preventing the
  favorite star from being squeezed beyond narrow active cards.
- Added focused source coverage and a 360px browser containment scenario for
  the shared card. Typecheck, lint, all 1,666 Node tests, and the production
  build pass. Local rendered Browser verification remains blocked by the
  desktop enterprise policy for localhost.

## 2026-07-27 — Supporter Portal action clarity

- Completed the post-repair controlled Supporter subscription retry. Production
  created the selected $1 monthly subscription with Stripe-calculated Ohio tax,
  and Account displayed current Supporter access.
- Confirmed that the general Customer Portal and its approved subscription
  options work, but the intermediate Stripe label `Update subscription` was not
  sufficiently clear after MassageLab's combined Manage or change action.
- Split the member action into Change support amount or billing period, which
  requests Stripe's focused `subscription_update` flow for the persisted
  current subscription, and Manage billing account, which preserves the
  general Portal homepage for payment methods, billing address, invoices, and
  cancellation.
- Kept the completed three-Product/six-Price catalog and Portal configuration
  unchanged. The remaining smoke is one real amount or interval switch after
  this focused action branch merges and deploys.

## 2026-07-27 — Supporter production rollout and checkout-origin repair

- Completed the guarded live three-Product Supporter migration and its
  identifier-safe verification. The catalog is `COMPLETED`, legacy cleanup and
  coupon removal are complete, Production holds the exact six current Price
  mappings and recurring-tax gates, live Stripe readiness passes, and the
  pinned webhook endpoint remains current.
- A controlled Supporter subscription attempt from `www.massagelab.app`
  returned `billing=invalid-request` before legal acceptance, Customer,
  Checkout Session, subscription, or charge creation. The exact user-facing
  fallback wording, two immediate 303 responses, empty account billing records,
  absent Stripe Customer search result, and configured-origin policy localized
  the failure to the form-origin guard rather than Stripe or tax.
- Added alias-safe origin validation: the canonical configured origin remains
  authoritative and derives only the matching apex/`www` MassageLab alias.
  Browser-confirmed `same-origin` requests may select one of those explicit
  hosts; unconfigured, mismatched, forged, and contradictory Origin/Referer
  or Fetch Metadata evidence still fails before request parsing or billing.
  Metadata-free requests are accepted only for the supported JSON media type.
  Explicit evidence returns directly from the allowlist decision instead of
  retaining an unreachable request-URL fallback.
- Added explicit account guidance for a genuinely invalid checkout request.
  Reconciled the billing runbook with the completed recurring-tax
  authorization, Ohio registration, catalog migration, and live readiness.
  Focused security/Checkout coverage passes all 85 tests; repository lint,
  typecheck, all 1,666 tests, and the production build pass. The candidate must
  merge and deploy before the controlled Supporter smoke is retried.

## 2026-07-26 — Three-Product Supporter Portal recovery

- Confirmed `txcd_10000000` as the intended current classification for the
  app's present electronically supplied Supporter access.
- The guarded live migration verify passed after the dormant, still-disabled
  Portal policy was aligned with the reviewed pre-migration contract.
- The first live apply safely stopped when Stripe rejected multiple monthly
  and annual Prices on one Customer Portal Product. Before stopping, it
  classified the reusable Supporter Product and created the six approved
  Prices. It did not enable Portal switching, retire any legacy Product or
  Price, delete either coupon, change a subscriber, or replace production
  runtime Price IDs.
- Approved a three-Product Stripe representation for the one user-facing
  MassageLab Supporter Membership: $1/$10, $2/$20, and $5/$50 each use one
  amount-specific Product with identical benefits, entitlement metadata, and
  tax classification. This is not three feature tiers.
- Approved easy cross-Product amount changes in Customer Portal with the
  billing-cycle anchor unchanged, no proration, and no period-end scheduling.
- Added a fail-closed recovery plan and migration coverage that reuses the
  classified $1/$10 Product, creates the other two Products, replaces and then
  retires the four wrong-owner Prices, installs the exact three-Product Portal
  allowlist, and remains idempotent after interruption.
- Merged PR #146 and deployed its production source. The official live
  preflight passed every dependency and classified the catalog as
  `PRE_MIGRATION`.
- The resumed live apply created and reread the $2/$20 and $5/$50 Products and
  their four correct Prices, classified the reused $1/$10 Product, transferred
  the managed lookup keys, and installed the exact three-Product/six-Price
  Portal allowlist. Its post-write gate then stopped before cleanup because
  Stripe omits the expandable Portal Product list on an ordinary retrieve and
  preserves a nested schedule condition when the SDK serializes
  `conditions: []`.
- Live inspection with the documented Product expansion confirmed the exact
  allowlist was installed. A reversible inactive test-mode Portal experiment
  proved that an empty array leaves `decreasing_item_amount` in place while
  Stripe's empty-value encoding clears it and returns canonical
  `conditions: []`.
- Added the focused recovery fix: every Portal verification read expands the
  Product allowlist, the update requests expanded Products, immediate amount
  switching uses Stripe's verified empty-value encoding, and the fixture
  models Stripe's canonical response. The apply remains stopped before legacy
  Price/Product retirement and coupon deletion until this recovery is merged.
- Merged PR #147 and confirmed its production deployment was ready. The
  identifier-safe live inventory still matched the paused checkpoint, with no
  subscriber, legacy cleanup, coupon deletion, or runtime Price change.
- The post-merge verify and apply both stopped before mutation because the
  state classifier did not recognize the exact intermediate where the target
  three-Product allowlist is installed but the dormant schedule condition
  remains. This was a second fail-closed code gap, not Stripe drift.
- Added a second narrow recovery state. Verify continues to reject the
  intermediate, while apply may repair it only when the target topology and
  dormant condition are exact, every cleanup Price and Product remains active,
  and both zero-redemption coupons remain present. The Portal update and
  canonical reread still precede every destructive cleanup mutation.

## 2026-07-26 — Supporter production inventory and migration topology hardening

- Confirmed the merged Supporter-only application baseline from PR #144 in an
  isolated rollout worktree; Prisma generation plus all 1,625 repository tests
  passed before production inventory began.
- Completed identifier-free read-only Stripe inventory. The live account has
  no non-terminal subscription; its one production Stripe subscription record is
  currently canceled. The Student-to-Therapist and Early Access coupons both
  have zero redemptions, and
  the single default Customer Portal configuration preserves customer details,
  payment methods, invoices, and cancellation while subscription switching is
  currently disabled.
- Reconciled the production database and Stripe subscription inventories
  without exposing account identifiers. No non-terminal live subscriber needs
  a grandfathering decision before catalog migration.
- The live catalog contains both the unapproved $9/$90, $29/$279, and
  $79/$759 Prices and the older $1/$10, $2/$20, and $5/$50 Prices. The older
  amount objects are split across Supporter, Therapist, and Practice Products,
  and Stripe Prices cannot be reassigned to the one approved Supporter Product.
- Hardened the migration against that exact production topology. It now
  verifies and retires the six older amount objects under their expected legacy
  owners before recreating those amounts under the classified Supporter
  Product, permits the exact legacy Supporter Product when optional `app`
  metadata is absent but its name and membership metadata remain correct, and
  accepts the reviewed disabled pre-migration Portal state before enabling the
  exact six-Price switching allowlist. Focused regression coverage passes.
- No Stripe Product, Price, coupon, subscription, Portal, Vercel production
  environment, or database row was mutated. Live-key migration verify/apply,
  final recurring-tax confirmation, production environment changes, and the
  controlled subscription smoke remain gated follow-up work.

## 2026-07-24 — Supporter membership deployable migration candidate

- Replaced public Supporter/Therapist/Practice enrollment with one Supporter
  Membership at $1/$2/$5 monthly or $10/$20/$50 yearly while preserving legacy
  Price recognition for webhook reconciliation until the controlled migration.
  Every approved amount maps to identical Supporter entitlements, including all
  backgrounds; Therapist and Practice Checkout fail before Customer or Session
  creation.
- Removed the Early Access and Student-to-Therapist application discount paths,
  changed public/Checkout language to One-time support with the not-charitable,
  not-tax-deductible, no-membership disclosure, and added independent editable
  roadmap-interest preferences that never enter Stripe metadata, entitlement
  resolution, or tax classification.
- The recurring Supporter Membership and one-time support are separate payment
  models: one-time support is not a membership price and grants no entitlement.
- Added the independently reviewed fail-closed Supporter catalog command. Its
  read-only verify mode inventories all relevant subscriptions and validates
  account mode, exact Product/Price topology, zero-redemption coupons, preserved
  Customer Portal behavior, and pre/completed migration states. Apply uses
  deterministic create idempotency, ordered re-retrieval, safe retry/resume, and
  identifier-free output.
- Implemented the deployable recurring-tax contract after preliminary
  `txcd_10000000` confirmation: new Supporter Checkout fails closed unless
  Automatic Tax, provider, registrations, exact classification, and final
  professional-confirmation gates are explicit; ready Sessions require billing
  addresses, update Customer addresses, and enable Automatic Tax. Stripe
  readiness also requires exclusive tax behavior on every approved Price and
  the exact tax code on the expanded Supporter Product.
- The isolated worktree had no database URL, Stripe key, migration inputs, or
  local environment file, so database/Stripe subscriber inventory and live
  migration verify were not attempted. No deployment, migration apply, live
  Product/Price/coupon/portal/subscription mutation, or live smoke occurred.
  Final professional confirmation and those operational gates remain required.
- Final-review hardening prevents a second membership Checkout for every
  persisted relevant/canceling subscription before Stripe Customer or Session
  creation, routes current and historical members to Customer Portal, removes
  the obsolete live catalog setup command, accepts and upgrades only the
  validated normal legacy Supporter Product pre-state, shares exact recurring
  Price semantics between migration and ongoing readiness, retains legacy
  runtime Price mappings strictly for webhook reconciliation, and lists
  all-background access in public Current benefits.
- Whole-branch review follow-up closes the pre-webhook concurrency window:
  membership Checkout now paginates only MassageLab-owned customer Sessions,
  reuses only an exact current contract-versioned open Session after verifying
  its six-Price catalog, Product, tax, and address fields; expires and confirms
  every recognized incompatible historical open Session; treats completed
  historical Sessions with relevant Stripe subscriptions as authoritative; and
  safely serializes or recovers competing creates and expirations.
  Migration preflight now also requires exact Therapist/Practice Product names
  and rejects contradictory optional MassageLab metadata before any mutation.
- RED-first focused billing/readiness/route coverage passed after
  implementation. The historical full lint, test, typecheck, build, and diff
  batch is recorded in the
  [Task 5 final fixes report](../.superpowers/sdd/final-fixes-report.md);
  subsequent review-fix gates are separate evidence.

## 2026-07-23 — Background purchase surfaces visual review

- Merged Track 1B in PR #141, deployed the merged source to Production, and
  enabled paid background purchasing only after all operational gates passed:
  36 current migrations, two zero-grant idempotent credit-backfill passes,
  clean reconciliation, active Ohio Stripe Tax configuration, exact
  `txcd_10000000` classification, and the enabled pinned 15-event webhook
  contract at the app API version.
- Completed the taxed sandbox smoke with a one-dollar subtotal, eight cents of
  Ohio tax, webhook-backed fulfillment, replay-safe idempotency, and clean
  reconciliation. The controlled live Silk purchase then recorded a one-dollar
  subtotal, seven cents of Stripe-calculated Ohio tax, a successful $1.07
  payment, processed completion webhook, fulfilled order, active purchase
  ownership, and clean post-fulfillment reconciliation.
- Corrected two issues found during the controlled live flow: cart copy now
  says purchased backgrounds stay available to the purchasing account
  permanently instead of assuming a membership, and guest cart authentication
  now preserves the originating Background route and query, then reopens the
  cart after existing login, Google registration, or email verification.
- Pinned paid background readiness to the reviewed Stripe Tax code
  `txcd_10000000` (General Electronically Supplied Services) instead of
  accepting any syntactically valid `txcd_` value. Earlier in the rollout,
  verified one active Ohio state sales-tax registration in Stripe and kept
  `BACKGROUND_COMMERCE_PURCHASING_ENABLED=false` pending the taxed test-mode
  Checkout/fulfillment smoke; that temporary hold was superseded by the
  successful sandbox and live smokes recorded above.
- Versioned the Digital Purchases and Refund Policy to
  `2026-07-digital-purchases-v2`. One purchase now expressly permits the
  purchaser's personal use and purchaser-operated display while serving
  clients through their own sole proprietorship or practice, without a
  personal-versus-business checkout question. The non-transferable license
  prohibits shared logins, staff/team operation through the purchaser account,
  redistribution, resale, sublicensing, asset extraction/distribution,
  packaging into another product or service, and unrelated-party access.
- Approved a separate Supporter Membership restructuring track without
  mutating live subscription objects in Track 1B: one Supporter product with
  identical benefits at $1/$2/$5 monthly or $10/$20/$50 annually; Therapist
  and Practice unavailable until differentiated professional features are
  ready for beta; roadmap interests separate from amount and entitlements;
  unused Student-to-Therapist and Early Access coupon paths removed after
  re-verification; portal switching limited to Supporter amounts; and public
  donation language replaced by One-time support with a not-charitable,
  not-tax-deductible, no-benefits disclosure.
- Replaced disabled-looking locked Select buttons with visibly actionable Unlock
  controls across Chimer, Clock, and the Music visualizer. Signed-out cards now
  explain that the background can be added immediately and that sign-in or
  account creation happens at checkout; activation still opens the shared
  credit, one-dollar purchase, and membership dialog.
- Removed the redundant outer visual shell from Chimer's Choose background step
  and stopped rendering the selected-controls card when a locked background has
  no controls, eliminating the empty bordered container from the setup flow.
- Hardened the review follow-up by serializing Checkout with the existing cart
  mutation queue, disabling cart mutations during redirect, surfacing removal
  failures, and clearing stale acquisition errors when a dialog is reopened or
  changes backgrounds.
- Kept checkout-return order ids across restored Clock/Chimer routes, merged
  refreshed recent-order status into itemized Account history without dropping
  older rows, and made non-finite setup-step inputs fall back to the first step.
- Closed the next review pass by making checkout-return polling advance after
  refresh failures, loading signed-in account state independently from guest
  cart merging, ignoring aborted queued mutations before they enter mutating
  state, and making local guest-cart uniqueness/removal explicit.
- Preserved the duration gate when Chimer opens directly on Choose background,
  hid picker invitations for inactive ownership history, clarified the order
  support action, and synchronized route-derived support topics on soft
  navigation.
- Separated successful commerce writes from their follow-up snapshot refresh so
  a read outage cannot misreport a committed action, added the signed-out
  reservation guard, reset nested credit confirmation after ownership lands,
  aligned carousel hint references, and restored active-Chimer haptics for
  acquisition actions.
- Made public cart normalization fail closed for malformed item prices and cart
  subtotals, documented the client projection invariants, secured HTTPS browser
  QA session cookies, and pinned nested Calendar/booking cart-trigger coverage.
- Replaced the provisional disabled-tax release contract before shipping:
  paid background readiness now requires Stripe Tax mode, a reviewed product
  tax code, and explicit provider/registration readiness. Checkout always uses
  exclusive automatic tax and billing-address collection; paid webhook
  fulfillment atomically freezes processor order/item tax totals before its
  existing evidence checks and sends incomplete, unsupported, or conflicting
  tax evidence to review without granting ownership. Cart and Checkout copy now
  says applicable tax is calculated at Stripe Checkout.
- Added a fail-fast guard for the derived Chimer background step and documented
  the explicit purchase-support reference gate.
- Earlier in the rollout, set the Vercel Production
  `BACKGROUND_COMMERCE_PURCHASING_ENABLED=false` kill switch and redeployed the
  existing PR #139 production source (`def0c365`) while the tax-aware branch,
  Ohio registration, and reviewed product classification remained incomplete.
  That temporary safeguard was later lifted only after the gates and smokes
  recorded at the start of this section passed.

## 2026-07-22 — Background purchase surfaces release candidate

- Completed the Track 1B release candidate on
  `codex/background-purchase-surfaces`: the shared Clock, active Chimer, and
  Music visualizer Background picker now shows authoritative credits,
  subscription inclusion, permanent ownership source, cart/reservation state,
  and reversals without forking the landed carousel or immersive controls.
- Added explicit credit confirmation, one-dollar multi-background cart review,
  U.S.-only consent, Stripe redirect validation, webhook-delayed return
  recovery, reservation cancellation, and a full Account/Billing wallet,
  portfolio, order, reversal, and privacy-safe support surface. Nested commerce
  dialogs sit above the full-screen Background panel and return focus through
  the acquisition flow.
- Extended the approved cart placement after review: a generic conditional site
  cart opens the shared cart from the configured desktop/mobile app bar while
  staying absent from Calendar and nested provider-selling routes. This keeps
  site purchases distinct from services a provider sells and leaves a generic
  shell contract for later physical-store lines.
- Approved and implemented checkout-first guest acquisition. Signed-out users
  can store only validated background IDs in a browser-local intent cart, then
  sign in or create an account at checkout. After authentication, IDs are
  revalidated and merged through the existing account cart API; credits,
  reservations, payment, and ownership remain account/server-only authority.
- Final validation passed: 49 focused commerce/account/support tests, the full
  1,357-test Node suite, typecheck, lint, the 101-route production build, and
  all 16 desktop/mobile browser scenarios covering guest intent, credit
  redemption, zero-credit Checkout failure, cancellation recovery,
  active-Chimer ownership states, Music visualizer minimize/restore, global
  cart placement, and webhook-delayed confirmation.
- Kept the public Roadmap and user-owned `TODO.md` out of scope.

## 2026-07-22 — Background commerce readiness and cleanup proof correction

- Corrected Stripe verify mode to validate the one pinned webhook endpoint
  against the app's shared contract: enabled status, exact
  `2026-02-25.clover` API version, five membership events plus ten commerce
  events, and strict event-set equality that rejects wildcard, extra, duplicate,
  or missing subscriptions. The local `BACKGROUND_COMMERCE_WEBHOOK_EVENTS`
  signal remains the exact commerce-only subset.
- Added pure RED/GREEN endpoint-contract coverage and shared the event/version
  constants with the webhook route and Stripe client so readiness cannot drift
  from handled events or the app API version.
- Replaced best-effort Stripe test cleanup with a tested fail-closed helper. It
  rejects live mode/keys before any API call, expires and re-retrieves the test
  Checkout Session, deletes and re-retrieves the test Customer, aggregates only
  safe failure codes, and withholds successful rollout evidence unless every
  cleanup response and terminal-state check passes.
- Repeated migration, two-pass verified-user backfill, the full synthetic
  lifecycle rollout backed by a real Stripe test Customer and two-item Checkout
  Session, and read-only reconciliation on a freshly reset disposable Neon
  branch with all database variables pinned to its exact direct host. An initial
  attempt failed closed before Session creation because explicit nonsecret
  commerce readiness values were absent; its test Customer was cleaned through
  the verified Customer-only path before the disposable branch was reset and
  rerun with rotated synthetic identifiers.

## 2026-07-21 — Background commerce foundation operationalization

- Completed Track 1A on `codex/background-commerce-foundation`: one-time verified-account credits, permanent credit/purchase ownership, persistent carts and reservations, multi-item Stripe Checkout, replay-safe fulfillment, exact-item refunds, disputes, retirement replacement credits, identifier-only reconciliation, safe user snapshots, and full-account-admin operations are implemented without adding Track 1B purchase UI or changing the public Roadmap.
- Extended `npm run stripe:readiness` without changing its membership PASS contract. Background commerce now requires the fixed 100-cent USD price, exact U.S. allowlist, current digital-purchase document version, explicit webhook/reconciliation signals, all Checkout/refund/dispute event subscriptions, and the current disabled-tax posture. International commerce remains unavailable, and automatic tax fails closed until processor tax can be reconciled into immutable order and item snapshots; output contains only booleans and safe identifiers.
- Validated the forward-only migration and verified-account backfill on a fresh disposable Neon branch after preflighting `DATABASE_URL`, `DIRECT_URL`, and `DATABASE_URL_UNPOOLED` to the same non-pooler branch host and away from production. The migration was already present in the clone; backfill pass one granted four wallets and pass two granted zero.
- Exercised a synthetic verified subscriber with a real Stripe test-mode customer and two-item Checkout Session. Because API-only Checkout cannot complete payment, completion, exact-item partial refund, and dispute delivery used the repository's in-memory signed/service fixtures against the disposable database. Completion replay changed state once, the exact one-dollar item refund produced a partial refund, an open/won dispute restored the remaining eligible purchase, and read-only reconciliation reported zero issues. No live Stripe request, browser payment, real customer PII, raw processor payload, or production database write was used.
- The real Prisma rollout exposed two checked nested-write mismatches that test doubles had accepted. Focused RED regressions now require order-item `userId` and refund-item `orderId` to derive from their parent composite relations; removing only those redundant nested scalars made the real disposable rollout pass while preserving database ownership consistency.
- Documented the migration/backfill order, exact webhook contract, final-sale legal/operator exception, dispute and retirement lifecycle, read-only reconciliation posture, audit privacy boundary, tax/country fail-closed policy, and Track 1B dependency in the billing wiki. `TODO.md` and the public Roadmap remain unchanged.

## 2026-07-21 — Carousel PR review

- Addressed verified PR review findings by normalizing deeply wrapped carousel indexes, making already-aborted shared prewarm waits reject asynchronously, covering bounded and oversized-radius mounting directly, and consolidating browser carousel centering into one documented test helper. The active-route Metal ring now waits for stable responsive-copy geometry and unmounts synchronously when that geometry is lost, preventing the upstream one-pixel SVG fallback from emitting invalid negative rectangle dimensions during route transitions.

## 2026-07-20 — Carousel production rollout

- Completed the post-rollout production review refinements: `/music` now uses a horizontally scrollable Glow-button category row with un-clipped halo paint space, icon-only Glow step navigation, no unused bottom reserve in short landscape workspaces, the accepted 24.85-pixel short-viewport stage padding, and the active station's owning category when the route remounts. Active app routes use the standard shared animated Metal ring, mounted only for the measurable app-bar copy. The full-screen Clock Background panel moves its visual filters into the header between the title and icon-only Close control, uses the accepted compact short-viewport stage spacing, and removes the faint immersive-toolbar backing box. Exact 996×597 layout assertions and screenshots, the focused Music console/playback and active-category remount routes, all ten immersive-panel browser scenarios, and the Background focus/dismissal scenario pass.
- Started Track 3B on `codex/carousel-production-rollout` and promoted the approved Carousel Lab winners into shared production primitives: a responsive Embla-backed adaptive stage/controller, production Background and Station renderers, shared Glow action styling, and the animated metal favorite glyph.
- Replaced Background selection in Clock, active Chimer, Music visualizer, and setup/idle flows with the approved responsive radial presentation. The five accepted Background profiles retain nearby radius two, bounded muted preview playback, entitlement and saved-state behavior, current selection, filters, haptics, keyboard navigation, loop behavior, and reduced-motion cleanup.
- Replaced the `/music` category rails with the approved Background Picker-style Station carousel at a fixed 192×224 center card and 193-pixel surrounding presentation on every device. Playback, stop, favorites, category-specific position, abortable prewarm, full-information dialog, Music Provider state, and route persistence remain unchanged; `/browse` keeps its compatibility grid.
- Kept the development Carousel Lab as a tuning/reference surface while moving its accepted stage/controller ownership to the shared production implementation. Cover Flow and 3D remain unexposed source references and are not part of the runtime product.
- Validation passed: `npm run lint`, `npm run typecheck`, `npm run test` (1,105 tests), `npm run build`, the 16-scenario desktop/mobile Carousel Lab browser matrix, focused public Music route, immersive Background, Music visualizer, and mobile app-shell browser checks, plus responsive screenshot comparisons at 1280×900, 412×915, and 1180×820.

## 2026-07-20 — Carousel prototype review lab

- Set the Background Existing nearby radius to two across every responsive screen profile; Music Stations retain their separate fixed geometry.
- Applied the responsive winner refinements without touching production carousels: Background Existing now selects one of five approved card/angle profiles from the available carousel width (with a short-landscape override and manual fallback), Music Station cards stay 192×224 with 193-pixel surrounding previews on every device, and selected Background/Station favorite glyphs combine the CTA-purple center fill with an animated chromatic-metal path trace that respects reduced motion. Device-local tuning moved to `massagelab-carousel-lab-v4`. Focused model/source coverage passes 32 tests, the desktop and mobile Carousel Lab matrices pass eight scenarios each, and lint, typecheck, the 1,102-test repository suite, production build, and diff checks pass.
- Finalized the Track 3A review choices without changing production carousels: retained only Backgrounds + Existing and Music Stations + Background Picker, removed Cover Flow and 3D from both surface option sets, preserved the selected Music settings (192×304, zero gap, radius four, 27-degree spread, 420-pixel arc, 0.05 falloff, loop/motion on), added pair-local card-height tuning, and made distant shells visually transparent. Both card types now use Glow actions over the artwork's upper corners with CTA purple for Favorite; Background cards omit the provider plus Shader/Video tags, and the centered Station raises its title/summary into a deeper lower-artwork overlay with a full-information dialog. Focused model/source coverage, the seven-scenario desktop and mobile Carousel Lab matrices, lint, typecheck, the 1,100-test repository suite, build, and diff checks pass; Track 3B production work has not started.
- Applied the second review batch without touching production carousels: moved Background Select/Favorite controls to the preview's upper corners across all three presentations, shortened side-card Background Cover Flow reflections, enabled real looping for every Music category, added a seventh Music Stations + Background Picker sample, and reset all screenshot-derived defaults under `massagelab-carousel-lab-v3`. Focused model/source coverage, typecheck, and the full Carousel Lab desktop/mobile browser matrix pass; the user review gate remains open.
- Implemented and validated the development-only Carousel Lab on `/dev/buttons`: six production-faithful combinations compare Existing, Cover Flow, and 3D presentations across real Background and Music Station cards with shared accessible Embla mechanics, bounded media/resource behavior, current-device tuning, reduced-motion fallback, and mutation-free access fixtures. Production carousel rollout remains paused until the user chooses one shared Background winner and one Music Station winner.
- Corrected the lab after user visual review showed the first adaptations were not faithful enough: stopped truncating Embla motion, centered looped snaps, matched Existing Background to the production 5:7 radial picker, restored Cover Flow depth/sweep/rotation/stacking/reflections, rebuilt 3D around the source-derived 16-slot cylinder and live mask geometry, enabled bounded adjacent Background video previews, added plain-language control descriptions, and reset corrected defaults under `massagelab-carousel-lab-v2`. No production carousel was replaced.

## 2026-07-19 — Immersive Music visualizer acceptance

- Added deterministic desktop and mobile browser acceptance for the shared Clock, Visual, and Background surfaces across anonymous playback continuity, exact-origin Minimize and Back behavior, stopped-station recovery, separate Clock/Music Show clock persistence, active-Chimer-only controls, dismissal/focus/portal rules, one-time hint storage fallback, safe geometry, reduced motion, resource cleanup, and signed-in default/failure/retry behavior.
- Corrected the acceptance-exposed geometry gaps in the shared nonmodal shell: active top/bottom music-player chrome contributes to dock insets, hidden Music clocks still receive a bounded internally scrollable dock, and the body-level Clock/Visual shell avoids the timer perspective containing block while centering an auto-width dock up to 58rem. Clock/Visual docks and their toolbar follow the measured visual viewport at true 200% Chromium page scale while retaining bottom-first placement and protected-display separation. Background remains the only full-screen modal surface.
- Hardened the acceptance proof with a real top-player control click, a Music-route wake-lock baseline followed by a verified visualizer request increase, desktop and mobile 200% viewport bounds, and forward-projection bounds/hit testing against the immersive control row.
- Refined the narrow Clock/Visual chrome so Clock, Visual, and Background sit beside Fullscreen as one 3rem circular glass-control row, while preserving icon tooltips, accessible names, panel/modal behavior, player-aware top offsets, and focus-scroll reachability at 200% zoom.
- Expanded Clock and Visual docks to fill their measured safe edge up to the existing protected-display gap, including the outer Clock translation but excluding inner rotation/glow transforms. Their internally scrolling content now uses tighter section rhythm, flat 44px toggle rows, compact field grouping, and reduced decorative card overhead without removing controls or changing the full-screen Background panel.
- Moved Show clock into the Clock dock header and the shared Clock color into the Visual dock header, removing the duplicate Clock-color row from Selected background controls while keeping background-specific colors in the scroller. Header actions wrap safely at 319px, Close remains reachable, selected-background rows and Global Colors now use consistent 44px swatches, and the top toolbar reserves the full 4px focus-outline extent at 319px and 200% page scale.
- Kept Global Colors visible to anonymous users but disabled its mode, harmony, swatches, save, and saved-palette actions with a sign-in hint. This reuses the existing account-color permission: signed-in free users remain enabled after preference sync, premium-only controls still use feature keys, and no entitlement policy or server authorization path changed.
- Kept Music visualizer device choices in the existing Atmosphere browser storage and signed-in defaults in the existing `UserPreference.appSettings.musicVisualizer` object. This non-PHI preference did not require a Prisma schema change, migration, or new Neon table, and account updates preserve unrelated app settings.
- Retained the source/license decision for the display effects: the camera-yaw and decorative glow concepts are adapted into native MassageLab React/CSS from wheatup's public MIT-licensed [Neon Clock (CSS)](https://codepen.io/wheatup/pen/JjzdMbK), with attribution preserved and no copied runtime dependency.
- Focused validation passed for the combined production visualizer suite (26 tests, 8 intentional project-specific skips), the isolated 539px density rerun, the 1,059-test Node suite, typecheck, lint with one pre-existing warning, and the 94-route production build.

## 2026-07-19 — Immersive clock display effects

- Added optional display rotation and forward-glow controls to the shared Clock, Chimer, and Music visualizer display. The visual concepts were adapted from the public MIT-licensed [Neon Clock (CSS)](https://codepen.io/wheatup/pen/JjzdMbK) pen by CodePen author wheatup; MassageLab retains this attribution and adapts only the 40-second ±10-degree camera yaw plus decorative transformed, blurred, and masked shadow concepts into native React and CSS, without importing the pen wholesale or adding a CodePen runtime dependency.
- Rechecked the attached source package and replaced the ineffective flat transforms with a local 45rem perspective, adjustable ±2–20-degree yaw over a 10–120-second cycle, and two clipped `-90.1deg` floor-projection layers. Forward glow now exposes 0–100% intensity, 50–200% projection length, and 0–64px blur; all five properties persist through the existing sanitized Chimer settings while both parent toggles remain off by default.
- Kept the effect layers attached only to the centered display, pointer-free, visually clipped by the primary display, and clear of the immersive toolbar. Rotation remains disabled under reduced motion. Focused Node coverage, the 539px compact-panel browser proof, the source-shaped effect browser proof, typecheck, and the 94-route production build pass.

## 2026-07-19 — Public Roadmap portfolio

- Reframed the public Roadmap as a timeless five-track product portfolio for education and anatomy, wellness, therapist and practice tools, local-first records, and audio and ambient experiences. Each track now distinguishes available capabilities from long-term direction without dates or priority order; the page retains membership and donation paths while making privacy, accessibility, informed consent, user control, and hosted-data readiness shared foundations.

## 2026-07-18 — Full-width app shell, installation, and Help

- Extended the configured top or bottom app bar across the viewport and kept its responsive home brand beside the configured drawer edge.
- Added semantic active-route glow without changing inactive Music, Clock, or Calendar variants.
- Focused signed-out quick actions on Log In, Create Account, Quick Log, and Breathing Guide while preserving signed-in role/customization resolution.
- Added explicit PWA install prompting, iOS/iPadOS Safari instructions, installed/unsupported hiding, public `/help`, and the existing `/support` feedback handoff.
- Validation: `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, focused desktop/mobile Playwright shell tests, the existing public-shell regression selection, and `git diff --check` passed.

## 2026-07-18

- Reviewed and approved the six-track product design and implementation plans in the requested planning order: app shell/install/help, outcome-focused public Roadmap, shared Clock/Chimer/Music visualizer controls, shared background palette plus DNA and Twisted Cubes, production-faithful carousel review, and permanent background commerce. The plans preserve the public Roadmap boundary, local-first/privacy constraints, feature-key entitlements, staged review gates, and cross-track dependencies; implementation has not started.

## 2026-07-17

- Restricted the CI workflow token to explicit read-only repository access and added a workflow-contract regression assertion, resolving CodeQL's `actions/missing-workflow-permissions` finding without introducing a duplicate CodeQL workflow alongside GitHub's healthy default setup.

## 2026-07-16

- Restored the shared animated site backdrop to the idle Chimer setup route while leaving active Clock/Chimer visual backgrounds route-owned, and moved the compact CTA Blue Clock Mode discovery action into the Session duration header. In-app Browser desktop review and a 390x844 Playwright fallback confirmed the open layout and phone wrapping without restoring the removed outer setup container.
- Finished the final annotated rollout cleanup: the Business Planner hub now starts directly with its tool list instead of repeating a large summary card, and the shell drawer uses one open primary category at a time so selecting another header collapses the previous group while preserving the active route group.
- Refined the annotated route rollout after visual review: Chimer now sits directly on its route background without the proof carousel or outer setup shell, exposes Clock Mode as a compact CTA Blue discovery action, and collapses Saved setups into a compact disclosure; Flashcard community-deck arrows use a filled CTA Blue face above the Study/View row; Business Planner add/reset actions use filled Secondary rather than Outline; and Clock Global Colors presents five creative palette channels, keeps disabled harmony previews visible in Custom mode, previews each harmony from the selected primary color, and keeps Visual tuning copy on a stable system color. Desktop and 390x844 route checks passed without browser console errors; Wellness remains deferred.
- Completed the annotated production-route correction pass after the shared-control rollout: Chimer now uses the approved hour/minute steppers instead of quick presets, keeps Continue full-width inside its fitted ring, removes the redundant route header, and uses compact five-step labels; Clock settings tabs use a stable three-column track; Global Colors exposes explicit Custom/Harmony modes, locks derived swatches, emits valid hex channels, and applies the resolved palette through the shared background host; and Flashcard ordinary secondary actions no longer misuse the outline-only variant. Browser QA covered the representative public, education, Chimer, Clock, pricing, auth, support, and shell routes at desktop/tablet widths plus phone-width Chimer and Flashcards. Wellness remains the sole deferred visual holdback.
- Completed the S6 visual pass across Chimer, Anatomime, `/anatomime/join`, and pricing at desktop and 390x844 phone widths in light and dark themes. Pricing's nested donation labels now sit above the Glow sheen/flicker pseudo-elements without forcing positioned theme-toggle children out of place. After distinguishing Codex Browser from Chrome, restored Chrome's original toggle-origin View Transition keyframes exactly, including the illumination blur and light falloff, while retaining the centered sun/moon and a separate measured-origin fallback only for browsers without the View Transition API. The approved dark Glow face and deferred Wellness holdback remain unchanged.

## 2026-07-15

- Started S6 on `codex/sitewide-control-system-rollout-actions` from approved review commit `38146ec`: migrated Chimer setup/navigation actions, ordinary Anatomime primary/secondary/destructive actions, and pricing donation flicker onto shared Button variants, tones, effects, press mechanics, focus, disabled behavior, and haptics. The frozen manifest excludes Chimer quick-duration/choice controls, Anatomime cards/choices, and the deferred Wellness anatomical map.
- Approved the sitewide control-system review matrix for production rollout except for the Wellness anatomical map. The map remains visible only as a clearly labeled deferred review prototype; S6-S9 migration batches must not introduce or migrate that visualization until a later anatomy-focused review approves its renderer and region geometry.
- Replaced the abstract wellness mannequin backdrop with user-approved Openclipart line-art references for direct anterior/posterior and anterior-lateral/posterior-lateral perspectives, kept perspective and anatomical view as independent controls, adapted the original black artwork for both light and dark themes, retained renderer-independent interactive region IDs, and documented the CC0/public-domain source decision.
- Replaced the transformed direct-view wellness hit regions with dedicated anterior-lateral and posterior-lateral path maps, so hover and selected anatomy regions align to the angled reference figures while retaining the same canonical IDs and two-column left/right keyboard list.
- Corrected the wellness anatomy review after visual feedback: recolored the source line art to translucent blue, clipped hover/selected zones to perspective-specific body contours, removed the misleading continuous mannequin fill, and mapped anterior left/right from the client's perspective instead of screen coordinates.
- Replaced the embedded Harmony picker's segmented track with responsive individual shared buttons after visual review. Inactive families use CTA Blue, the selected family keeps the Attention face and fitted always-on metal ring, icon inset and sibling reflection remain intact, and the choices wrap at narrow widths.
- Corrected the latest annotated control review: removed inset treatment from Outline text/icons, strengthened only the light-theme Glow halo, constrained metal attention wrappers to their button face, changed the desktop quick-create Plus to shared Default, and placed the desktop theme control at the outer edge opposite the sidebar; the existing phone bar already met the latter two contracts.
- Refined only the light-theme Glow face after visual review: it now uses translucent orange layers derived from the shared Default palette instead of the prior slate fill; the approved dark-mode Glow remains unchanged.
- Corrected the Glow interpretation after review: restored the approved dark-mode face, geometry, hover, press, and halo unchanged; limited GlassCN `liquid` inspiration to the light-mode material override; retained the same dark-slate/orange identity in light mode; and kept the separate shared light-reflection token fix for molded button upper edges.
- Corrected the next shell review pass: light-theme Glow now keeps a lighter smoky-slate dark-glass face instead of turning white; Home, Music, Clock, and Calendar phone navigation plus Music, Clock, and Calendar topbar controls use CTA Blue; quick-create and theme remain exempt; and phone/desktop drawer controls mirror the theme button's Default-in-light and Glow-in-dark variants through a shared resolved-theme hook.
- Applied the next eight annotated control-lab refinements: the pressed-state specimen now uses a filled shared button, selected Alerts use a lighter edge, loading notices use a compact randomized shared loader, the loader gallery demonstrates 18px Sphere/Swirl/Ripple button forms, Music navigation specimens use CTA Blue, switch thumbs stay neutral in light mode, and the light-theme Glow face is brighter, glassier, and easier to distinguish.
- Refined the shared review visuals from the next annotated pass: CTA and Attention retain their purple faces but now transition into the shared site blue in the lower-right, mirroring CTA Blue's opposite-color corner, and the global theme switcher uses the shared Glow button variant while dark mode is active.

## 2026-07-14

- Implemented the S1-S5 sitewide control-system review checkpoint on `codex/sitewide-control-system-review`: `/dev/buttons` now serves as the complete development-only visual QA matrix across action, choice, field, color, card, status, loading, navigation, surface, and protected-route families.
- Reinforced shared contracts for Button tones/effects/density, Tabs, native and Radix selects, Input/Textarea states, selectable cards, notices, and AppSurface variants without migrating production routes. Protected Chimer, Clock, Anatomime, pricing, and account visuals are represented for parity review.
- Added the Chimer notice-plus-hour/minute adjustment proposal and an accessible SVG-first wellness region proof backed by renderer-independent selection state for a future 3D body.
- Browser review covered desktop and 390x844 phone layouts in dark and light themes, interactive selects/choices, synchronized wellness selection, dialog/popover focus restoration, and protected Chimer duration controls. Broad route migration remains paused for user visual approval.
- Applied the first annotated visual-review revision across all 27 comments: exact shared Secondary and selected-tab colors; outline-only, theme-stable glow/highlight, loader, focus, hover/press, switch, select, notice, and progress treatments; responsive compact Harmony controls; a finished shared Anatomime specimen; detailed front/back wellness anatomy regions; and reusable accelerating step buttons whose single press changes one minute, held press accelerates, and double press changes five minutes.
- Post-feedback validation passed with `npm run typecheck`, `npm run lint`, `npm run test` (964 tests), `npm run build`, focused control-system source tests, and `git diff --check`. The earlier desktop/mobile Browser review informed this revision; the final visual checkpoint remains the user's review before production migration.

## 2026-07-13

- Synced the Chimer setup action buttons with the shared `Button` default height: `Apply selected` and `Use last setup` now render at the sitewide 40px default while retaining only their approved setup color/effect treatment. The visual-system wiki now records that route-owned buttons should preserve shared sizing/geometry by default and vary color, variant, or effects before adding route-local button shapes.
- Refined the Chimer setup quick-duration controls after browser review: duration preset buttons keep the approved setup color/no-hover behavior but use a shorter compact profile with a smaller lower lip and reduced resting lift.
- Tightened the latest Chimer/control review batch: the embedded Harmony picker now uses the shared segmented-control treatment with the Attention purple selected state, always-on metal ring, and sibling reflection targets for adjacent options; Chimer setup action and duration buttons no longer change visual treatment on hover; and enabled Continue keeps the always-on metal ring without the extra lower lip on the button face.
- Continued the route-owned pricing/Chimer review: pricing account entry points now use the Attention treatment, signed-out membership cards consolidate Create account/Sign in into one Get Started action that starts authentication before checkout, `/dev/buttons` includes the embedded Chimer Harmony picker for visual review, global color swatch containers no longer carry the vertical light-to-dark gradient, Chimer conflict sync notices hide after the user makes divergent local edits, Chimer setup hover lift is neutralized while enabled Continue keeps the always-on metal ring, and the shared app shell/sidebar content path can shrink beside an open drawer instead of letting route content overflow.
- Continued the route-owned visual review: clarified `/dev/buttons` sibling-reflection behavior as opt-in through `metalReflectionTargets`, staggered Glow flicker timing across one-time donation buttons, made membership checkout buttons explicitly use the attention variant, removed the remaining vertical gradient layers from Clock control-group containers, and organized `/dev/buttons` into grouped review tabs with a Clock container sample.
- Followed up the route-owned visual review: Chimer Continue now keeps its metal attention ring visible whenever the step can advance, one-time pricing donation buttons use the new Glow flicker treatment, and Anatomime primary/secondary/danger action icons now receive the same inset icon treatment as route cards.
- Continued the route-owned visual-control review from `/dev/buttons`: added a second Glow review variant whose orange outer ring/glow uses an irregular neon flicker while the existing Glow button remains unchanged; removed vertical gradient layers from the Clock/select field and Anatomime team-name input surfaces; strengthened Anatomime route-card/icon inset treatment; lightened selected Anatomime card detail text; and applied the user-approved `#4AAAAA` setup treatment to Chimer setup selection/action buttons while leaving step navigation such as Continue on its prior style.

## 2026-07-12

- Reverted the unpushed `codex/sitewide-route-owned-controls` Chimer select-field restyle after user review found it regressed the approved Chimer page controls. Chimer page buttons and route-owned controls are now treated as done/protected; future visual-system batches should not include Chimer control restyling unless explicitly requested.
- Added a review-only Chimer setup button example to `/dev/buttons` using the requested `#4AAAAA` face color, without changing production Chimer controls.

- Continued the July 7 sitewide visual-system rollout on `codex/sitewide-loader-controls`, including the July 12 handoff note already appended to the rollout plan.
- Promoted the Chimer loader through the `ds.asanshay.com` shadcn registry implementation into `components/ui/loader.tsx`, using the shader-based sphere/dither renderer with MassageLab orange defaults, kept `components/chimer-controls/Loader.tsx` as a compatibility re-export, and added `/dev/buttons` sphere, swirl, and ripple comparison examples.
- Updated the shared loader default so omitted `shape` randomly selects sphere, swirl, or ripple per loader instance, while explicit `shape` props still render deterministic comparison examples or route-owned choices.
- Migrated low-risk true indeterminate waits to the shared loader: the account tab fallback, account preference sync state, and support diagnostic send state. Music preparation keeps its determinate progress bar, and palette generation remains unchanged because it is currently synchronous.
- Added `docs/wiki/visual-system.md` and linked it from the wiki index so future visual-control work starts from the shared button, slider, toggle, color-control, loader, haptic, and reduced-motion guidance.
- Validation passed with `node --test tests/sitewide-loader.test.mjs`, `npm run typecheck`, `npm run test`, and `git diff --check`. In-app browser rendering of local URLs was blocked by the environment's enterprise policy, so no browser screenshot review was available for this loader batch.

## 2026-07-10

- Replaced the remaining native Chimer and calendar service color inputs with the approved shared picker. Controlled surfaces now pass color strings through explicit `value` and `onValueChange` props, while form-owned fields submit a named hidden value; the migration does not synthesize native React change events.
- Extended the route-owned visual-control treatment to the Clock background filter row and Clock font select field. The background filters now use the same inset tab-track language as the main Clock/Visual/Backgrounds tabs, select fields use the cutout route-owned control surface, and `/dev/buttons` includes both examples for review before broader dropdown/container rollout.

## Current Snapshot

- Status: private alpha.
- Current focus: anatomy data is sufficient for the alpha baseline; 3D/spatial runtime tooling is deferred. Current focus includes the setup-first flashcards/community-deck layer, where anonymous users can browse public decks and study temporary decks while signed-in users can save deck templates, persist progress summaries, earn aggregate achievements, and complete repeatable full-library mastery rounds. Anatomime now turns completed team rounds into local learning reviews with got/missed/skipped outcomes, definitions, aliases, and a final recap of terms to revisit. Anatomime shared classroom play now uses a reusable room code with separate game runs, host-judged and device-answer modes, 4-term team turns, active-team immediate scoring, possible steals from device answers, signed-in first-correct progress credit, same-code next games, review-window expiry, host transfer, ranked-choice host takeover, and host-facing QR/prefilled invite links. Anatomy admins can now use `/admin` as a compact dashboard, `/admin/anatomy/media-review` as a mobile-first image review queue for fast approve/reject/request-better-view decisions, and `/admin/anatomy` as the full anatomy browser for detailed editing, BodyParts3D coverage, candidate linking/imports, and flashcard image flags. The dedicated anatomy media review queue now supports URL-driven status, anatomy-area, problem, view, request, sort, and search filters with built-in anatomy and cleanup presets that preserve the active batch through approve/reject/request decisions. Public flashcards only use reviewed reusable media whose item-image link is approved. Reveal-review remains practice-only, and typed-check remains the saved progress path. The local-first professional-record framework remains in place with one encrypted browser vault for SOAP, intake, journal, and ROM records, tablet intake workflow continuity, user-controlled encrypted vault transfer, and role-aware shell/navigation structure before broader portal expansion. Voice transcription and SOAP-assist are recorded as future member-supported goals only; no audio capture, transcription engine, LLM processing, or hosted PHI workflow is implemented.
- Current-state source of truth: [project-state.md](project-state.md).
- Database status: Prisma schema validates; the repo has 35 migrations, including the Anatomime room/game-run migration, flashcard-compatible Anatomime progress uniqueness migration, client wellness foundation migration, legal acceptance migration, provider-neutral external calendar sync migration, external busy-block interval check migration, and calendar-sync active-connection/pending-link hardening migration.
- Product posture: clinical notes, intake forms, journals, therapist ROM sessions, transcripts, and other therapist professional-record workflows remain local-first unless hosted clinical storage passes the documented compliance gates. Therapist note-taking tools are visible but creating or viewing professional-record content requires the `therapist_documentation_tools` entitlement from an active Therapist or Team/Practice membership. SOAP, intake, journal, and therapist ROM now use one encrypted browser vault that requires therapist passphrase unlock before viewing or saving documents. Signed-in `/wellness` entries are client-owned self-tracking records in a separate consumer-health domain; anonymous users can practice logging, body-sensation tracking, and ROM in memory only. Privacy architecture separates account/contact/booking data, client-owned wellness data, therapist professional records, and a future consent-based sharing bridge.
- Public `/roadmap` route: product-facing app copy only. Internal operating state lives in [project-state.md](project-state.md) and this log.
- Architecture note: a full `graphify` run is already complete on this repo (`2026-07-05`). Future work/cross-chat review should reuse `graphify-out/` artifacts (`graph.json`, `GRAPH_REPORT.md`, `.graphify_labels.json`) instead of re-running extraction.
- Chimer visual customization note: the redesigned background picker now has generated lightweight WebM preview media for all 81 enabled Chimer backgrounds. The repo-owned generator uses the internal chrome-free `/chimer/background-preview/[backgroundId]` capture route, Playwright, and FFmpeg, emits landscape, square, and vertical WebM variants, and writes both `public/chimer/background-previews/index.json` and the TypeScript preview manifest consumed by the background registry.
- Chimer account-color note: signed-in account preference sync now preserves Clock Color, Lamp main color, and Lamp orb color for the original MassageLaba Lamp; broader visual color controls remain gated by `chimer_custom_colors`.

## Now

| Status | Priority | Work | Source | Acceptance |
| --- | --- | --- | --- | --- |
| Completed | P1 | Finish alpha release verification | [TODO](../TODO.md), [Alpha QA](alpha-qa.md), [Roadmap](roadmap.md) | `docs/alpha-qa.md` was walked with anonymous data; Chimer, Notes, Calendar, Anatomime, Support, Roadmap, Account, Security, Settings, PWA metadata, and billing gates were verified. |
| Active | P1 | Complete invited-user stabilization gate | [Invite readiness stabilization plan](superpowers/plans/2026-06-23-invite-readiness-stabilization.md), [Release checklist](wiki/release-checklist.md), [Billing wiki](wiki/billing-memberships.md), [Deployment wiki](wiki/deployment.md) | Release checklist reflects current signup, legal, billing, SEO, Sentry, Neon, and local-first boundaries; repo checks pass; production Stripe readiness is verified from an explicit live env file; a live Supporter subscription, Chimer entitlement, Customer Portal, and cancellation smoke has passed; and the live donation path plus external confirmations remain explicit follow-ups. |
| Completed | P1 | Manually verify Chimer clock-only mode | [TODO](../TODO.md) | Clock-only mode was checked on desktop and phone landscape without layout overlap, unsafe motion, or unreadable controls. |
| Completed | P1 | Manually verify Stripe checkout and webhook flow | [TODO](../TODO.md), [Billing wiki](wiki/billing-memberships.md) | Stripe test checkout, signed local webhook delivery, account membership status, and Chimer custom-color entitlement were confirmed with test-mode values. |
| Completed | P1 | Optimize signed-in account data shell | [May 17 audit](audits/2026-05-17-project-review.md) | Signed-in `/account` now streams only the active tab's data, account tab data uses short-lived per-surface caches with mutation invalidation, and sidebar calendar readiness moved off unrelated page views into route-gated calendar hydration. |
| Completed | P2 | Add browser QA harness | [May 17 audit](audits/2026-05-17-project-review.md) | Playwright browser QA now runs public desktop/mobile smoke routes, console/page-error checks, anonymous account sync guards, PWA manifest/icon checks, and local-first clinical document network guards. |
| Completed | P2 | Decide PWA offline strategy | [May 17 audit](audits/2026-05-17-project-review.md), [Privacy wiki](wiki/privacy-and-phi.md), [PWA wiki](wiki/pwa-offline-strategy.md) | MassageLab is offline-capable for anonymous public local tools only; service worker caching avoids auth, billing, calendar, booking, clinical sync, client, and `/api/*` surfaces. |
| Completed | P2 | Build calendar creation flows | [Roadmap](roadmap.md), [TODO](../TODO.md), [Calendar wiki](wiki/calendar-creation-flows.md) | Appointment, client request, personal event, class, and reminder flows use a shared event index with conservative role permissions, audit rows, and internal notification intent records. |
| Completed | P2 | Add calendar service catalog and availability readiness | [Calendar wiki](wiki/calendar-creation-flows.md) | Calendar migrations are applied; services have variants, resources, operational policy fields, scheduling snapshots, and resource conflict checks for appointments, classes, and public booking. |
| Completed | P2 | Build operator calendar workspace | [Calendar wiki](wiki/calendar-creation-flows.md) | `/calendar` uses a draggable FullCalendar workspace with provider view preferences, server-validated rescheduling, advanced provider availability, multi-service appointment composition, and clinical-rich service template references that avoid client-specific PHI. |
| Completed | P2 | Build Google Calendar provider sync | [Calendar wiki](wiki/calendar-creation-flows.md), [Google provider sync design](superpowers/specs/2026-06-24-google-calendar-provider-sync-design.md) | Externally entitled providers can connect Google from `/calendar/sync`, select calendars imported as generic busy blocks, block scheduling conflicts from active Google busy time, and publish MassageLab appointment/class/personal block events to a dedicated MassageLab calendar without client or clinical detail export. OAuth failures return to the sync settings page, Google account identity is stored from the Google token subject, disconnect deletes stored credentials and imported sync data, booking/import concurrency shares the active connection lock, the dedicated Google calendar is excluded from inbound source selection, first-push failures are persisted for retry visibility, and the database enforces one active Google connection per provider. |
| Completed | P1 | Consolidate project source of truth | [Project state](project-state.md), [Superpowers plan](superpowers/plans/2026-05-27-project-source-of-truth-consolidation.md) | `docs/project-state.md` is the read-first current-state file; `docs/project-log.md` remains chronological history; README, wiki, roadmap, TODO, and agent instructions point to the same convention. |
| Active | P2 | Build local-first client intake and SOAP handoff workflow | [Project state](project-state.md), [Intake-to-SOAP plan](superpowers/plans/2026-06-03-intake-to-soap-continuity.md), [Local-first forms plan](superpowers/plans/2026-05-27-local-first-client-intake-forms.md), [Intake form-builder plan](superpowers/plans/2026-05-28-intake-form-builder-local-documents-v1.md), [Privacy-first records plan](superpowers/plans/2026-05-30-privacy-first-records-framework.md), [Privacy wiki](wiki/privacy-and-phi.md) | Initial intake remains a fuller first-visit form; follow-up intake starts from an existing local client and asks only appointment-relevant changes, current symptoms, safety updates, and today's goals. Either response can be reviewed and used to seed a SOAP draft inside the encrypted local vault without uploading clinical content. Branch validation is in progress. |
| Active | P2 | Maintain project state and log | [Project state](project-state.md) | Future meaningful changes update the current state first and append completed plans, branch outcomes, and priority changes to this change history. |

## Next

| Status | Priority | Work | Source | Acceptance |
| --- | --- | --- | --- | --- |
| Active | P2 | Land technical SEO foundation and focused public landing copy | [Technical SEO foundation plan](superpowers/plans/2026-06-21-technical-seo-foundation.md), [Focused SEO landing pages plan](superpowers/plans/2026-06-21-focused-seo-landing-pages.md), [May 17 audit](audits/2026-05-17-project-review.md), [Roadmap](roadmap.md) | Production public pages can become indexable with canonical metadata, sitemap, robots, schema support, and search-aligned proof copy for flashcards, Anatomime, Chimer, local-first documentation, and wellness, while preview/development builds and private/auth/API/booking/shared-code/local-record routes remain blocked. |
| Completed | P2 | Sitewide visual system and account-page polish pass | Account page visual refresh | Shared page shell, surface, inset, action, notice, and status styles now come from `components/ui/app-surface.tsx`; public, account, auth, documentation, booking, calendar-management, admin, and local-first documentation surfaces use the shared treatment, with Chimer, Anatomime, and the dense calendar workspace kept as intentional route-owned exceptions. |
| Completed | P2 | Generative music spike | [Roadmap](roadmap.md), [TODO](../TODO.md), [Atmosphere audio wiki](wiki/atmosphere-audio.md) | `/browse` now hosts the hidden Atmosphere runtime spike with MassageLab-owned global playback, a local Tone proof station, a disabled Generative.fm package probe, and browser coverage for route-persistent mini-player state. |
| Completed | P2 | Trust pages and public identity | [Roadmap](roadmap.md), [TODO](../TODO.md), [Legal trust pages plan](superpowers/plans/2026-06-17-legal-trust-pages.md) | `/legal` publishes Terms, Privacy, Billing/Refund, Therapist Agreement, Cookie, and Local-First Health/Wellness notices; registration, paid checkout, and professional/practice credential requests record current-version acceptance. |
| Open | P2 | Functional/visual product surface work | [Project state](project-state.md), [Roadmap](roadmap.md) | Prefer the next branch to improve a visible, usable part of the product instead of returning immediately to transcription feasibility, hosted PHI, or backend compliance work. Shared Anatomime classroom-session polish or anatomy flashcards/study UI hardening are good branch-sized candidates. |
| Deferred | P3 | Anatomy 3D/spatial runtime tooling | [Project state](project-state.md), [Anatomy media storage](anatomy-media-storage.md) | 3D/spatial rows may remain review-only until runtime mesh/node mapping, source attribution, and product value justify the feature work. |
| Completed | P2 | Education and flashcards design | [Roadmap](roadmap.md), [TODO](../TODO.md) | Flashcards now use sourced prompt modes, setup-first deck building, public deck browsing, signed-in saved deck templates, and signed-in progress/achievement persistence. |
| Open | P2 | Payload CMS integration plan | [TODO](../TODO.md) | Blog-first Payload plan resolves database/schema ownership, auth boundaries, packages, env vars, deployment behavior, uploads, previews, and role/paid-content leakage risks. |
| Open | P2 | Remaining external calendar sync plan | [Calendar wiki](wiki/calendar-creation-flows.md), [Privacy wiki](wiki/privacy-and-phi.md) | Client calendar connection, Outlook, Apple/iCloud, CalDAV, ICS, full personal event mirroring, external reminder delivery, and richer provider-account identity handling remain separate scoped follow-up work. |
| Open | P3 | Repo-wide docstring pass | [TODO](../TODO.md) | Shared helpers, domain rules, server actions, data adapters, and non-obvious UI components have useful JSDoc/docstrings that explain intent, inputs, outputs, and important constraints without adding noise to self-explanatory code. |
| Completed | P2 | Online booking settings, public access, waitlist, and provider capacity controls | Square-inspired calendar settings review, follow-up review branch | Booking policies now cover manual vs auto-confirm approval, provider visibility/rest gaps, min/max scheduling windows, daily appointment limits, pressure-level massage-hour capacity, sequential service/add-on booking, team sequencing, waitlist conversion, staff visibility, opt-in distance notices without client location storage, tabbed provider settings, public share links, state-prefixed branded URLs, anonymous guest booking unless account-required, and a wizard-style public booking flow with weekly availability slots. |
| Completed | P2 | Role-aware module surfaces and sidebar IA | [Role-aware surfaces plan](superpowers/plans/2026-06-01-role-aware-module-surfaces.md) | Anonymous, signed-in base, student, client, therapist, Team/Practice, practice owner/staff/therapist, anatomy admin, and admin contexts resolve through a tested feature/role navigation model without inventing full dashboards before workflows are defined. |
| Open | P2 | Stripe Connect marketplace payments plan | [Billing wiki](wiki/billing-memberships.md), [Calendar wiki](wiki/calendar-creation-flows.md) | Provider onboarding, payout accounts, booking deposits, cancellation fees, taxes, packages, refunds, and platform fee rules are scoped before booking payment collection. |

## Later

- Access and memberships: define visible tools, sync capabilities, education access, practice features, support expectations, and role-specific portals before broad paywall work; use the [role-aware module surfaces plan](superpowers/plans/2026-06-01-role-aware-module-surfaces.md) as the next-branch handoff.
- Shared anatomy knowledge platform: make anatomy content reusable across Anatomime, flashcards, SOAP/body diagrams, intake workflows, demonstrations, and future games.
- Education and games: revisit Anatomime, add shared-session game concepts, support saved progress and achievements, and build education demonstrations after the shared data model stabilizes.
- Practice and therapist SaaS: design practice owner, therapist, student, client, and solo-practitioner experiences before expanding hosted clinical workflows.
- News and school module: add reputable content/news feeds and decide Moodle vs custom learning infrastructure only after source, licensing, editorial, and maintenance rules are clear.
- Voice transcription and SOAP-assist feasibility: defer until a later branch; keep it documented as a goal, but do not route the next branch there.
- Hosted clinical storage: remains future-only until HIPAA, BAA, audit, access-control, encryption, backup, legal, incident response, and operating-policy requirements are satisfied.
- Chimer and premium-background refactors: keep prop-drilling reduction, shared canvas/WebGL lifecycle utilities, context-loss handling, CSS deduplication, self-hosted 3D globe textures, per-frame canvas allocation review, and repo-wide stylelint convention decisions as later cleanup unless a focused behavior branch needs one of them sooner.

## Decision Log

| Date | Decision | Notes |
| --- | --- | --- |
| 2026-06-23 | Treat invited-user readiness as a stabilization gate, not a feature expansion. | Before inviting real users, finish the release checklist refresh, repo-owned validation, production Stripe readiness check, low-dollar live checkout and portal smoke, and explicit external confirmations such as attorney review. Do not add Stripe Connect, hosted PHI sync, transcription, SOAP AI, Payload, or 3D runtime work in this gate. |
| 2026-06-21 | Prioritize launch Neon transfer hardening over compatibility-wrapper cleanup. | The next broad-share risk is avoidable production database transfer and connection churn, not deleting one-line TS/JS wrappers. The [launch Neon transfer hardening plan](superpowers/plans/2026-06-21-launch-neon-transfer-hardening.md) and [Neon transfer readiness audit](audits/2026-06-21-neon-transfer-readiness.md) record the pooled-runtime guard, public booking projections, signed-in homepage projection, and launch operating checklist. |
| 2026-06-20 | Make codebase refactor/optimization measurement-first and branch-sized. | The [codebase refactor optimization plan](superpowers/plans/2026-06-20-codebase-refactor-optimization.md) and [June 20 refactor baseline](audits/2026-06-20-refactor-baseline.md) record current validation, source-size, route-bundle, and hotspot evidence before behavior-preserving cleanup. The first pass splits `/admin/anatomy` browser types/data loading out of the page, scopes browser data loading by active view, splits `/education/flashcards` API parsing/fetch wrappers, active runner, setup builder, and progress dashboard into focused client modules, adds cached sourced prompt catalog helpers for flashcard progress/session APIs, and decomposes calendar actions while preserving the validation gate. Calendar mutation concurrency hardening is staged immediately after action decomposition and before measured client lazy-loading. |
| 2026-06-13 | Link Anatomime name-recall progress to flashcard prompt progress. | Shared-session correct guesses use `anatomime_name_recall:<cardId>` under the flashcard progress tool namespace so signed-in Anatomime study does not fork anatomy mastery tracking into a second model. |
| 2026-06-11 | Store anatomy media accuracy on the item-image link. | `AnatomyMediaEntity` carries approval/rejection state so an image can be valid for one anatomy item and rejected for another without deleting the media asset or its provenance. |
| 2026-06-03 | Split initial intake and follow-up intake before SOAP handoff. | Initial intake should remain a fuller first-visit form. Follow-up intake should start from an existing local client, avoid re-asking stable profile basics, focus on medical changes/current symptoms/today's goals, and roll into therapist-reviewed SOAP drafting inside the encrypted local vault. |
| 2026-06-03 | Treat voice transcription and SOAP-assist as member-supported roadmap goals. | Membership copy may explain that paid support helps fund local transcription experiments, legal/security work, BAAs where required, audit controls, and future managed sync planning, but it must not imply that hosted transcription, cloud SOAP drafting, or HIPAA-ready sync is available now. |
| 2026-06-03 | Defer transcription feasibility in favor of more visible product work next. | The next branch should not be voice transcription, server transcription, or hosted PHI work. Prefer a more functional/visual site or tool surface, with anatomy flashcards/study UI as the strongest current candidate if the user wants a concrete next build. |
| 2026-06-01 | Make role-aware module surfaces a dedicated follow-up branch. | The current branch ships the privacy-first records framework. Sidebar/module personalization should be implemented next through a tested role/capability navigation resolver for anonymous, signed-in, student, client, therapist, Team/Practice, practice-role, and admin contexts. |
| 2026-06-01 | Keep role-aware surfaces structural until workflows are defined. | The implemented resolver filters sidebar groups, account menu routes, admin links, and calendar shortcuts by non-PHI auth, role, entitlement, and practice-role context while avoiding invented full dashboards. |
| 2026-05-30 | Use one encrypted local professional-record vault for therapist documentation. | SOAP, intake, journal, and ROM share `massagelab-professional-record-vault-v1`; legacy route-specific plaintext keys are one-time migration inputs only, encrypted `.mlab` full-vault bundles are the normal transfer format, and DOC/PDF output requires an explicit plaintext warning. |
| 2026-05-29 | Separate wellness data, professional records, and sharing consent. | Client-owned wellness data may become cloud-backed under a privacy-controlled consumer-health domain, therapist professional records stay local-first until hosted PHI gates pass, local records should move toward an offline passphrase-unlocked vault, and live therapist viewing of cloud wellness data is deferred. |
| 2026-05-29 | Gate therapist note-taking behind a feature entitlement. | SOAP, intake, journal, ROM, and related therapist professional-record tools remain visible, but viewing or creating records requires `therapist_documentation_tools`, currently granted by active Therapist and Team/Practice memberships only. |
| 2026-05-27 | `docs/project-state.md` is the read-first current-state tracker. | `docs/project-log.md` remains chronological history; `TODO.md`, `docs/roadmap.md`, `docs/alpha-qa.md`, audits, and wiki pages remain source evidence unless mirrored into current state. |
| 2026-05-27 | Detailed implementation plans live under `docs/superpowers/plans/`. | Use these plans for multi-step implementation handoffs while keeping current state concise. |
| 2026-05-27 | Anatomy data is sufficient for the current alpha baseline. | Further 3D/spatial runtime tooling is deferred as a later feature addition; the next core product focus is local-first client-facing forms and tablet handoff into therapist notes. |
| 2026-05-17 | `docs/project-log.md` became the active internal tracker. | Superseded on 2026-05-27 for current snapshot duties; this file remains the chronological progress and decision log. |
| 2026-05-17 | Update this log for meaningful progress. | Add change-history entries when a plan is created, priority changes, branch work completes, or a significant implementation lands. |
| 2026-05-17 | Keep PHI-bearing workflows local-first during alpha. | Account sync must not include SOAP, intake, journal, ROM, client, treatment, or other clinical content. Hosted clinical sync stays gated and unimplemented until compliance requirements are met. |
| 2026-05-17 | Use feature-based entitlements. | Code should check feature keys such as `chimer_custom_colors`, not named plans. Free and Student are internal access states; Student is not Stripe-backed. |
| 2026-05-17 | Keep Sentry privacy controls conservative. | Session Replay, User Feedback, and Logs stay disabled until route-by-route privacy review, Sentry scrubbing rules, and clinical/local-first policy are written. |
| 2026-05-17 | Refactor only for a focused branch outcome. | Avoid broad cleanup branches; use targeted refactors only when they make the next feature safer, clearer, or easier to test. |
| 2026-05-17 | Calendar creation remains roadmap-only until product rules are defined. | Role permissions, audit expectations, and notification behavior must be planned before adding creation UI. |
| 2026-05-17 | Payload CMS needs a dedicated integration plan. | Default evaluation path is a separate Payload database or schema until ownership, migrations, backups, and auth boundaries are clear. The existing Next.js App Router site remains the public head. |
| 2026-05-17 | Anatomy data remains code-first until reviewed. | Prisma persistence should wait until the seed model, sources, import/update workflow, and clinical/public education citation requirements are clear. |
| 2026-05-17 | Private-alpha `noindex` is intentional. | Public indexing becomes a launch task only after SEO, trust pages, metadata, and public messaging are ready. |
| 2026-05-18 | PWA offline support is scoped to anonymous public tools. | Offline-capable routes are home, Chimer, Anatomime, and local-first documentation pages. Account, auth, billing, calendar, booking, admin, hosted clinical sync, client, and `/api/*` surfaces stay online-only and fall back to `/offline.html` when offline. |
| 2026-05-18 | Calendar creation uses a hybrid event model. | `CalendarEvent` is the scheduling index and conflict source of truth, while appointments, personal blocks, classes, and reminders keep specialized detail records. Notification delivery is deferred; calendar flows write internal intent records only. |
| 2026-05-18 | Calendar services use variants and resources. | Provider services are templates; selected variants drive duration, buffers, displayed pricing, snapshots, and resource requirements. Payment collection, Stripe Connect payouts, taxes, package redemption, and external calendar sync are deferred. |
| 2026-05-18 | Use Team/Practice in frontend copy. | Keep the internal `PRACTICE` membership enum stable, but use `Team/Practice` where users need context for the team membership tier. |
| 2026-05-18 | Operator calendar uses FullCalendar OSS. | The open-source React/daygrid/timegrid/interaction plugins cover day, week, 5-day, month, click-to-create, drag/drop, and resize. Premium resource-lane scheduling is deferred. |
| 2026-05-20 | Elasticsearch/OpenSearch is not used for booking v1. | Public slot generation stays deterministic over Prisma-backed scheduling, service, provider, policy, capacity, and availability data. Search infrastructure can be reconsidered only after v1 behavior and scale demand it. |
| 2026-05-20 | Arbitrary custom public booking fields remain deferred. | Pressure level is the only new required public booking field in v1; broader custom intake fields wait for a dedicated requirements and PHI-boundary plan. |
| 2026-05-20 | Public booking is guest-capable by default. | Practice-wide and provider-specific policies can require a client account, but otherwise guests can request appointments with required contact details while optional sign-in/register prompts explain account benefits. |
| 2026-05-20 | Legal-name public booking URLs stay permanent. | Optional branded public booking URLs use full state slugs plus normalized custom slugs, e.g. `/book/ohio/massagewithderrick`, while `/book/[practiceSlug]` remains available. |

## Change History

### 2026-07-10

- Refreshed `main` after merging the sitewide slider-control PR, then started `codex/sitewide-toggle-controls` as the next review-sized visual-control branch.
- Added and visually approved the shared physical Switch and Toggle treatments, a density-aware labeled `ToggleControl`, a single-choice `SegmentedToggleGroup`, and live `/dev/buttons` examples. Chimer's existing `StyledToggleControl` delegates to the shared compact row, and representative account haptics, calendar display settings, and SOAP pain controls now use the approved shared treatment.
- Advanced the sitewide visual-control rollout from buttons into sliders and app-shell controls. The shared Radix slider now uses the approved split-pill track and fader thumb, `RangeControl` owns the labeled/value slider form, `/dev/buttons` shows default, compact, hue, color-slider, and color-picker examples, and representative Chimer, Clock, Music, Anatomime, and local-first notes sliders now use the new treatment or a reviewed compact route-owned version.
- Kept the accompanying app-shell polish branch-sized: the theme switcher is a single active-theme toggle across desktop, tablet, and mobile; the sidebar wordmark and collapsed logo use the existing MassageLab brand assets with larger fit rather than remade images; and the favicon/PWA icon assets remain unchanged.
- Removed the collapsed brand-link tooltip that could cover the drawer toggle, matched the compact desktop/tablet drawer and theme controls to one 31px shell token while preserving larger phone touch targets, changed the default slider fill from muted copper to the secondary-button orange, and added exact current Chimer tabs, time-format, and drawer-navigation examples to `/dev/buttons` for the next route-owned control review.
- Fixed segmented-control selection so tooltip state cannot hide the active option, then visually approved the `/dev/buttons` 9px inset tab and time-format tracks, molded active choices, and warm inset drawer-route indicator. Production Chimer/Clock settings tabs, the 12h/24h choice, and real drawer route rows now use those approved treatments while preserving their existing state and navigation behavior.
- Added a shared 420ms `cubic-bezier(0.22, 1, 0.36, 1)` active-choice slide to segmented controls, Chimer/Clock settings tabs, and the time-format choice. The active molded face moves beneath stationary labels without spring overshoot; reduced-motion devices retain a short 120ms linear position cue instead of an imperceptible jump. `/dev/buttons` now mirrors these production controls rather than maintaining separate review-only styling.
- Extended the route-owned control rollout to the Clock chrome controls and Anatomime setup surface. Clock fullscreen/settings/close/pause controls now use the same physical button language, Anatomime choices, route-owned inputs, and setup actions use cohesive physical/inset states, and `/dev/buttons` includes an Anatomime route-control sample for review.

### 2026-07-09

- Refreshed `main` after merging the sitewide visual-control foundation PR, then started `codex/sitewide-public-account-controls` for the next review-sized public/account/auth button migration batch.
- Replaced remaining low-risk `bg-primary hover:bg-brand-orange-glow` button overrides in login, register, reset password, email verification, support, pricing, membership cards, about, roadmap, account profile/tools/sign-in prompts, and account security setup with the shared default, secondary, CTA, or CTA Blue variants. Icon-only shell/navigation controls, dense account setting controls, sliders, toggles, color pickers, and loaders remain out of scope for this batch.
- Added `/dev/buttons` current-rollout examples for the exact public/auth/account button compositions in this batch, then validated with `git diff --check`, `npm run typecheck`, `npm run lint`, `npm run test`, and `npm run build`; lint kept the existing large Chimer file Babel deoptimization notes, and diff check reported Windows line-ending conversion warnings only.
- Adjusted shared tactile button lighting so face highlights, inset text/icon shadows, and pressed-state inset shadows use an upper-left light source by default and flip horizontally when the saved sidebar position is right.

### 2026-07-08

- Started `codex/sitewide-control-system-foundation` for the sitewide visual-control rollout. Added `docs/audits/2026-07-07-sitewide-visual-control-inventory.md` with the surface-by-surface decision map for mechanical primary, mechanical secondary, strategic attention, quiet utility, text/link, and route-owned controls; the notes include React Bits scrubber direction, Cult UI metal-ring CTA direction, and EinUI glass-glow direction as references rather than copied implementation.
- Promoted Chimer's haptic press behavior into `lib/press-feedback.ts`, updated `components/chimer-controls/haptics.ts` as a compatibility layer, and extended `components/ui/button.tsx` with default, secondary, CTA, blue CTA, glow, and attention variants plus shared haptic press-feedback props. Chimer button wrappers now preserve route styling while using the shared button foundation.
- Added the homepage as the first visible migration slice: primary account CTAs use the strategic attention/CTA variants, membership/pricing actions use CTA styling, and homepage tool/action buttons use the secondary variant for a visible physical-control pass.
- Refined the homepage first-slice pass after visual annotation: shared tactile buttons now more closely match Chimer's dark lower lip, squarer physical radius, lift/press transition, and disabled state, while the dense homepage shortcut section keeps descriptions outside the actual mechanical buttons.
- Updated the shared button decision after gallery review: `default` is now the canonical physical sunset button, `secondary` is the less-emphatic companion, legacy outline/ghost/link variants use the same molded construction with the blue/purple side of the palette, destructive keeps a red variant, and strategic attention buttons get a rare randomized metal-ring-inspired pulse.
- Refined the button-gallery feedback pass so the canonical Default button follows the quieter Outline-style molded geometry in the orange/sunset palette, keeps the gradient subtle and pushed toward the side, avoids a dark top/side border, stops lifting or changing gradient on hover, and reserves movement for the pressed state. Removed the default sidebar-footer divider that visually cut through the compact rail footer.
- Consolidated the gallery vocabulary after visual review: `mechanical` and `mechanicalSecondary` remain backwards-compatible aliases for `default` and `secondary`, but the visible shared system now treats Default and Secondary as the canonical physical button pair. CTA, attention, blue, destructive, outline, ghost, and link variants now share the approved molded geometry while keeping their palette or attention-ring effects.
- Continued the button-gallery refinement by pushing the orange sunset accent farther toward the edge, adding inset-like treatment to button icons, cleaning up the destructive red ramp so it matches the molded geometry, giving Glow the same physical lip while preserving its glass glow, and centering the homepage shortcut button icon/text content with current-color icons.
- Removed the temporary mirrored Default comparison after visual review favored the original right-side accent, and adjusted the orange/default gradient to use the same three-stop structure as CTA Blue: primary face color through most of the button with the accent reserved for the far edge.
- Replaced the orange button family's far-edge purple accent with the existing walnut palette token so Default, Secondary, CTA, and Attention keep the CTA Blue gradient structure without drifting out of the orange family; after visual comparison, the walnut accent was pushed closer to the right edge and the temporary Walnut/Earth gallery comparisons were removed.
- Replaced the strategic Attention ring CSS approximation with a reusable `metal-fx` attention ring plus a shared-button convenience wrapper. The ring now stays visible while its motion randomly plays for 4-20 seconds, visually settles, then pauses for 5-20 seconds before resuming; the gallery uses the same default behavior as production examples and includes a standalone wrapper section near the shared variants.
- Set the button hierarchy after gallery review: Default now uses `#FF5119` with a closer amber-orange edge instead of walnut/brown, Secondary inherits the warmer orange family with a lower lip, CTA and Attention use the selected `#B719FF` purple molded gradient, Ghost and Link return to quiet non-physical utility/text treatments, and Chimer compatibility wrappers defer to shared sitewide button variants.
- Kept broad remaining route button migrations, sliders/ranges, toggles, color pickers, and loader migration out of this first slice until the audit classifications are reviewed.

### 2026-07-05

- Added React Bits Floating Lines as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/floating-lines`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/FloatingLines/FloatingLines.jsx`, its CSS source, and the React Bits repository license. The source declares Three.js, exposes line gradient, enabled waves, line counts/distances, wave positions, animation speed, cursor bend, damping, parallax, and blend-mode controls, and renders three animated sine-wave line bands through a shader. MassageLab ports the source shader functions, wave math, cursor bend, and parallax behavior into a dependency-free raw WebGL renderer instead of adding Three.js, keeps the canvas pointer-events free, adds source/custom/harmony gradient controls plus per-band Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Side Rays as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/side-rays`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/SideRays/SideRays.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes speed, two ray colors, intensity, spread, origin, tilt, saturation, blend, falloff, and opacity controls, and renders a transparent side-origin two-color ray shader with origin flip handling. MassageLab ports the source ray-strength shader and origin flip behavior into a dependency-free raw WebGL renderer instead of adding OGL, keeps the canvas pointer-events free, adds source/custom/harmony color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Light Rays as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/light-rays`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/LightRays/LightRays.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes origin, ray color, speed, spread, length, pulsation, fade distance, saturation, mouse-follow, noise, and distortion controls, and renders a transparent full-screen ray shader with source `getAnchorAndDir` origin placement plus optional mouse-follow direction blending. MassageLab ports the source shader, placement, and mouse math into a dependency-free raw WebGL renderer instead of adding OGL, keeps the canvas pointer-events free, defaults cursor follow off while exposing it as a user toggle, adds source/custom/harmony ray-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Pixel Blast as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/pixel-blast`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/PixelBlast/PixelBlast.jsx`, its CSS source, and the React Bits repository license. The source declares Three.js plus postprocessing, exposes variant, pixel size, color, antialiasing, pattern scale/density, liquid touch texture, pixel jitter, click ripples, ripple speed/thickness/intensity, auto-pause, speed, transparency, edge fade, and noise controls, and renders a Bayer-dithered pixel-shape shader with optional liquid UV distortion. MassageLab ports the source dithering, shape masks, click uniforms, touch texture, liquid/noise behavior, and defaults into a dependency-free raw WebGL renderer instead of adding Three.js or postprocessing, keeps the canvas pointer-events free, defaults to source lavender while exposing custom/harmony color plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Color Bends as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/color-bends`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/ColorBends/ColorBends.jsx`, its CSS source, and the React Bits repository license. The source declares Three.js, exposes rotation, speed, optional color array, transparency, auto-rotate, scale, frequency, warp strength, mouse influence, parallax, noise, iterations, intensity, and band width, and renders a full-screen color-bend shader with smoothed pointer influence and source RGB bands when no color array is supplied. MassageLab ports the source shader, defaults, pointer smoothing, rotation/auto-rotate behavior, transparency branch, and color-array branch into a dependency-free raw WebGL renderer instead of adding Three.js, keeps the canvas pointer-events free, defaults cursor influence off while exposing it as a user toggle, adds source/custom/harmony color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Evil Eye as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/evil-eye`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/EvilEye/EvilEye.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, procedurally generates a noise texture, exposes eye color, background color, intensity, pupil size, iris width, glow intensity, scale, noise scale, pupil follow, flame speed, and pointer interaction, and renders a full-screen iris/flame shader. MassageLab ports the source shader, generated texture, pupil-follow math, and defaults into a dependency-free raw WebGL renderer instead of adding OGL, keeps the canvas pointer-events free, defaults pointer following off while exposing it as a user toggle, adds source/custom/harmony eye-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Line Waves as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/line-waves`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/LineWaves/LineWaves.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes speed, inner and outer line counts, warp intensity, rotation, edge fade, color-cycle speed, brightness, three line colors, optional mouse interaction, and mouse influence, and renders a transparent animated line-field shader with source displacement functions and cursor smoothing. MassageLab ports the source shader math, line/ridge pattern, color cycling, defaults, and optional pointer warp into a dependency-free raw WebGL renderer instead of adding OGL, keeps the canvas pointer-events free, defaults pointer warp off while exposing it as a user toggle, adds source/custom/harmony line-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Radar as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/radar`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Radar/Radar.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes speed, scale, ring count, spoke count, ring/spoke thickness, sweep speed, sweep width, sweep lobes, color, background color, falloff, brightness, optional mouse interaction, and mouse influence, and renders a transparent animated radar shader with rings, spokes, sweep lobes, and radial falloff. MassageLab ports the source shader math, defaults, and optional pointer offset into a dependency-free raw WebGL renderer instead of adding OGL, keeps the canvas pointer-events free, defaults pointer offset off while exposing it as a user toggle, adds source/custom/harmony radar-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Soft Aurora as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/soft-aurora`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/SoftAurora/SoftAurora.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes speed, scale, brightness, two colors, noise frequency/amplitude, band height/spread, octave decay, layer offset, color speed, optional mouse interaction, and mouse influence, and renders transparent layered Perlin aurora bands. MassageLab ports the source Perlin, cosine-gradient, band, color, and optional pointer-shift shader math into a dependency-free raw WebGL renderer instead of adding OGL, keeps the canvas pointer-events free, defaults pointer shift off while exposing it as a user toggle, adds source/custom/harmony two-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Plasma as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/plasma`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Plasma/Plasma.jsx`, its CSS source, and the React Bits repository license. The source declares OGL/WebGL2, exposes color, speed, direction, scale, opacity, optional mouse interaction, and ping-pong time behavior, and renders a transparent raymarched plasma shader. MassageLab ports the source shader loop, color branch, direction modes, ping-pong easing, and optional pointer warp into a dependency-free raw WebGL2 renderer instead of adding OGL, keeps the canvas pointer-events free, defaults pointer warp off while exposing it as a user toggle, adds source/custom/harmony color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Plasma Wave as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/plasma-wave`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/PlasmaWave/PlasmaWave.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes offsets, rotation, focal length, dual speeds, secondary wave direction, bends, and two colors, and renders a transparent two-color raymarched wave shader. MassageLab ports the source shader, offset/rotation/focal-length/speed/direction/bend uniforms, and two-color branch into a dependency-free raw WebGL renderer instead of adding OGL, keeps the canvas pointer-events free, adds source/custom/harmony two-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Particles as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/particles`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Particles/Particles.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes particle count/spread/speed/colors, hover movement, alpha particles, base size, randomness, camera distance, rotation, and pixel ratio, and renders a transparent point-sprite particle cloud. MassageLab ports the source point shaders, projection/mesh transform behavior, generated sphere distribution, color attributes, and hover translation into a dependency-free raw WebGL renderer instead of adding OGL, keeps the canvas pointer-events free with opt-in window pointer tracking, adds source/custom/harmony three-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Gradient Blinds as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/gradient-blinds`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/GradientBlinds/GradientBlinds.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes gradient colors, angle, noise, blind count/min width, damped pointer spotlight, mirror, spotlight radius/softness/opacity, distortion, shine direction, blend mode, and DPR, and renders a striped gradient shader. MassageLab ports the source shader math into a dependency-free raw WebGL renderer instead of adding OGL, keeps the canvas pointer-events free with opt-in window pointer tracking for the spotlight, adds source/custom/harmony two-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Grainient as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/grainient`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Grainient/Grainient.jsx`, its CSS source, and the React Bits repository license. The source declares OGL/WebGL2, exposes time speed, color balance, warp, blend, rotation, noise, grain, contrast, gamma, saturation, center, zoom, and three colors, and renders a grain-textured warped gradient shader. MassageLab ports the source shader math into a dependency-free raw WebGL2 renderer instead of adding OGL, keeps the canvas pointer-events free, adds source/custom/harmony three-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Grid Scan as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/grid-scan`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/GridScan/GridScan.jsx`, its CSS source, and the React Bits repository license. The source declares Three.js, postprocessing, and face-api paths for the full demo, exposes webcam/preview plus grid, scan, pointer, gyroscope, and timing controls, and renders a perspective scanner shader. MassageLab ports the source shader and scan timing into dependency-free raw WebGL, omits webcam/face-api and postprocessing from the app bundle, keeps pointer skew and click-triggered scan pulses opt-in while the canvas remains pointer-events free, adds source/custom/harmony line/scan color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Beams as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/beams`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Beams/Beams.jsx`, its CSS source, and the React Bits repository license. The source declares Three.js, `@react-three/fiber`, and `@react-three/drei`, exposes beam width, height, count, light color, speed, noise intensity, scale, and rotation controls, and renders stacked plane beams with source cnoise displacement. MassageLab ports the stacked-plane geometry, displacement shader, source timing scale, perspective camera projection, and light/noise cues into dependency-free raw WebGL instead of adding Three/R3F/Drei, keeps the canvas pointer-events free, adds source/custom/harmony light-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Pixel Snow as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/pixel-snow`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/PixelSnow/PixelSnow.jsx`, its CSS source, and the React Bits repository license. The source declares Three.js, exposes color, flake size, minimum flake size, pixel resolution, speed, depth fade, far plane, brightness, gamma, density, variant, and direction controls, and renders a transparent full-screen ray-marched pixel snow shader. MassageLab ports the source hash/ray/camera basis math, pixel quantization, variant distance functions, and timing into dependency-free raw WebGL2 instead of adding Three/R3F, keeps the canvas pointer-events free, adds source/custom/harmony snow-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Lightning as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/lightning`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Lightning/Lightning.jsx`, its CSS source, and the React Bits repository license. The source is already a raw WebGL canvas shader with hue, X offset, speed, intensity, and size controls, so MassageLab ports the same HSV, hash, noise, and FBM lightning shader into the shared background host with reduced-motion handling, visibility/resize handling, explicit WebGL cleanup, source/custom/harmony hue controls, and the existing `premium_backgrounds` gate for internal-only use.
- Added React Bits Prismatic Burst as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/prismatic-burst`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/PrismaticBurst/PrismaticBurst.jsx`, its CSS source, and the React Bits repository license. The source declares OGL/WebGL2, exposes intensity, speed, animation type, gradient colors, distortion, pause, offset, hover damping, ray count, and blend controls, and renders spectral marching rays. MassageLab ports the source shader, gradient texture, animation modes, distortion/offset/ray-count uniforms, and optional hover tracking into dependency-free raw WebGL2 instead of adding OGL, keeps the canvas pointer-events free, adds source/custom/harmony gradient controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Galaxy as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/galaxy`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Galaxy/Galaxy.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes focal point, rotation, star speed, density, hue shift, speed, mouse interaction, glow, saturation, mouse repulsion, repulsion strength, twinkle, rotation speed, center repulsion, and transparency controls, and renders a layered star-field shader. MassageLab ports the source shader math, star layers, hue shift, pointer smoothing, repulsion, twinkle, rotation, and transparency behavior into dependency-free raw WebGL instead of adding OGL, keeps the canvas pointer-events free, adds source/custom/harmony hue controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Dither as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/dither`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Dither/Dither.jsx`, its CSS source, and the React Bits repository license. The source declares Three.js, R3F, React postprocessing, and postprocessing, exposes wave speed/frequency/amplitude/color, color count, pixel size, animation disable, mouse interaction, and mouse radius controls, and renders wave noise through an 8x8 Bayer dither postprocess. MassageLab ports the source wave shader and dither postprocess into a dependency-free two-pass raw WebGL2 renderer instead of adding that stack, keeps the canvas pointer-events free, adds source/custom/harmony wave color controls plus source-shaped Chimer/Clock controls, respects reduced motion and texture/framebuffer/resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Faulty Terminal as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/faulty-terminal`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/FaultyTerminal/FaultyTerminal.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes scale, grid multiplier, digit size, time scale, scanline, glitch, flicker, noise, chromatic aberration, dither, curvature, tint, mouse reaction, cursor strength, DPR, page-load animation, and brightness controls, and renders a CRT-like terminal digit shader. MassageLab ports the source terminal shader, cursor smoothing, page-load fade, and source uniforms into dependency-free raw WebGL instead of adding OGL, keeps the canvas pointer-events free, adds source/custom/harmony tint controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Ripple Grid as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/ripple-grid`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/RippleGrid/RippleGrid.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes rainbow mode, grid color, ripple intensity, grid size, thickness, fade distance, vignette, glow, opacity, rotation, cursor interaction, and cursor radius controls, and renders a radial ripple grid shader. MassageLab ports the source shader and uniforms into dependency-free raw WebGL instead of adding OGL, keeps the canvas pointer-events free, adds source white/source rainbow/custom/harmony grid-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Dot Field as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/dot-field`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/DotField/DotField.jsx`, its CSS source, and the React Bits repository license. The source is dependency-free canvas/SVG, exposes dot radius, spacing, cursor radius/force, bulge mode/strength, glow radius/color, sparkle, wave amplitude, and gradient colors, and renders a reactive gradient dot field. MassageLab ports the source dot build loop, cursor speed engagement, bulge/force behavior, sparkle/wave logic, and SVG glow into an internal canvas/SVG renderer, keeps the layer pointer-events free, adds source/custom/harmony gradient controls plus source-shaped Chimer/Clock controls, respects reduced motion and cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Dot Grid as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/dot-grid`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/DotGrid/DotGrid.jsx`, its CSS source, and the React Bits repository license. The source imports GSAP and InertiaPlugin, exposes dot size, gap, base/active colors, proximity, speed trigger, shock radius/strength, max speed, resistance, and return duration controls, and renders a canvas dot grid with proximity color blending, speed-triggered inertia, and click shocks. MassageLab ports the source grid layout, proximity blend, speed-trigger, shock, resistance, and return-duration behavior into dependency-free canvas physics instead of adding GSAP/InertiaPlugin, keeps the canvas pointer-events free, adds source/custom/harmony base/active color controls plus source-shaped Chimer/Clock controls, respects reduced motion and cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Threads as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/threads`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Threads/Threads.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes color, amplitude, distance, and optional mouse interaction, caps internal render resolution at 1920px, and renders a transparent Perlin-noise thread-line shader with smoothed cursor coordinates. MassageLab ports the source Perlin shader, line loop, color/amplitude/distance uniforms, render-resolution cap, and optional mouse smoothing into dependency-free raw WebGL instead of adding OGL, keeps the canvas pointer-events free, adds source/custom/harmony thread-color controls plus source-shaped Chimer/Clock controls, respects reduced motion and cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Iridescence as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/iridescence`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Iridescence/Iridescence.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes color, speed, amplitude, and optional mouse reaction, and renders an eight-layer full-screen iridescence shader. MassageLab ports the source shader loop, tint/speed/amplitude/mouse uniforms, 1920px render-resolution cap, and optional mouse reaction into dependency-free raw WebGL instead of adding OGL, keeps the canvas pointer-events free, adds source/custom/harmony tint controls plus source-shaped Chimer/Clock controls, respects reduced motion and cleanup, and gates it through `premium_backgrounds` for internal app use only. Hyperspeed remains deferred for a later date.
- Added React Bits Waves as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/waves`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Waves/Waves.jsx`, its CSS source, and the React Bits repository license. The source is dependency-free canvas code, exposes line color, background color, X/Y wave speeds, X/Y amplitudes, point gaps, friction, tension, and max cursor movement, and renders a Perlin-noise vertical wave-line field with cursor spring force. MassageLab ports the source Perlin noise, point-grid generation, wave offsets, cursor velocity/spring math, and line drawing into a dependency-free canvas renderer, keeps the canvas pointer-events free, adds source/custom/harmony line-color controls plus transparent/custom background and source-shaped wave/cursor controls, respects reduced motion and cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Grid Distortion as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/grid-distortion`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/GridDistortion/GridDistortion.jsx`, its CSS source, and the React Bits repository license. The source declares Three.js, exposes grid, mouse radius, strength, relaxation, and image source props, and renders an image texture displaced by a cursor-driven float `DataTexture`. MassageLab ports the same data-texture displacement shader and update model into dependency-free raw WebGL, uses generated internal texture colors instead of arbitrary image URLs, keeps the canvas pointer-events free, adds source/custom/harmony texture-color controls plus source-shaped grid/mouse/strength/relaxation/cursor controls, respects reduced motion and cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Orb, Letter Glitch, Grid Motion, Shape Grid, Liquid Chrome, and Balatro as reviewed MIT + Commons Clause premium backgrounds after checking their React Bits pages, raw source files, CSS files where present, and the React Bits repository license. MassageLab ports Orb, Liquid Chrome, and Balatro from OGL shaders into dependency-free raw WebGL, ports Letter Glitch and Shape Grid from source-shaped canvas renderers, and keeps Grid Motion source-shaped while replacing GSAP with `requestAnimationFrame` smoothing. The batch adds Chimer/Clock/Music registry entries, `premium_backgrounds` gating, source/custom/harmony color controls, source-shaped numeric/toggle controls, optional cursor interaction where supported, reduced-motion handling, cleanup, source documentation, and focused tests. Hyperspeed and Ballpit remain intentionally deferred.
- Added React Bits Silk as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/silk`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Silk/Silk.jsx`, the missing CSS path at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Silk/Silk.css` returning 404, and the React Bits repository license. The source declares Three.js/R3F, exposes color, speed, scale, noise intensity, and rotation controls, increments shader time by `0.1 * delta`, and renders the Silk fragment shader on a viewport-scaled plane. MassageLab ports the source shader math, defaults, uniforms, and time basis into a dependency-free raw WebGL renderer instead of adding Three.js/R3F, keeps the canvas pointer-events free, adds custom/harmony color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Light Pillar as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/light-pillar`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/LightPillar/LightPillar.jsx`, its CSS source, and the React Bits repository license. The source declares Three.js, exposes top/bottom color, intensity, rotation speed, interactive cursor rotation, glow amount, pillar width/height, noise intensity, blend mode, pillar rotation, and quality controls, and renders a raymarched light-pillar shader. MassageLab ports the source shader, defaults, quality presets, and optional cursor rotation into a dependency-free raw WebGL renderer instead of adding Three.js, keeps the canvas pointer-events free, adds custom/harmony color controls plus source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Dark Veil as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/dark-veil`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/DarkVeil/DarkVeil.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes hue shift, noise intensity, scanline intensity, speed, scanline frequency, warp amount, and resolution scale controls, and renders a CPPN shader. MassageLab ports the source CPPN shader into a dependency-free raw WebGL renderer instead of adding OGL, keeps the canvas pointer-events free, adds source-shaped Chimer/Clock controls, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Prism as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/prism`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Prism/Prism.jsx`, its CSS source, and the React Bits repository license. The source declares OGL, exposes prism dimensions, rotation mode, glow, offset, noise, transparency, scale, hue shift, color frequency, hover strength, inertia, bloom, and time scale controls, and renders a ray-marched prism/pyramid shader. MassageLab ports the source shader, SDF, hue rotation, base wobble, source defaults, 3D rotation, and hover rotation behavior into a dependency-free raw WebGL renderer instead of adding OGL, keeps the canvas pointer-events free, adds source-shaped Chimer/Clock controls including optional Hover cursor mode, respects reduced motion and resource cleanup, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Liquid Ether as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/liquid-ether`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/LiquidEther/LiquidEther.jsx`, its CSS source, and the React Bits repository license. The source declares Three.js, exposes palette, cursor, viscosity, solver-iteration, delta-time, BFECC, resolution, bounce, and auto-demo controls, and renders a transparent fluid simulation. MassageLab ports the source advection, external-force, viscosity, divergence, Poisson, pressure, and palette-output passes into a dependency-free raw WebGL renderer instead of adding Three.js, keeps cursor interaction disabled by default with opt-in cursor fluid-push controls while the canvas stays pointer-events free, adds custom-color and harmony controls plus source-shaped solver/motion sliders and toggles, keeps reduced-motion and resource cleanup handling, and gates it through `premium_backgrounds` for internal app use only.

### 2026-07-04

- Added Chamaac UI Nebula as `Deep Space Nebula`, a reviewed MIT premium background after checking `https://chamaac.com/r/nebula.json` and the component page's Customize controls. The source is a deep-space domain-warping shader with fractional distortion, exposes `color1`, `color2`, `color3`, and `speed`, and declares `three`, `@react-three/fiber`, `clsx`, and `tailwind-merge`; MassageLab ports the fragment shader into a native WebGL canvas renderer instead of adding that stack. Chimer/Clock controls expose custom source colors or a primary-color harmony palette, and animation speed is displayed as a `1%`-`100%` slider mapped to the source `0.1`-`5` range. The effect is premium-gated through `premium_backgrounds`, pointer-events disabled, reduced-motion aware, explicitly cleaned up on unmount, and internal-only.
- Added Chamaac UI Grid Bloom as a reviewed MIT premium background after checking `https://chamaac.com/r/grid-bloom.json` and the component page's Customize controls. The source is a shader-driven grid with pulsing wave interference, exposes bloom color, speed, grid density, rotation, fade, distortion, flow X/Y, and cursor hover props, and declares `three`, `@react-three/fiber`, `clsx`, and `tailwind-merge`; MassageLab ports the passive fragment shader into a native WebGL canvas renderer instead of adding that stack and intentionally omits cursor interaction for the full-screen app background. Chimer/Clock controls expose bloom color, animation speed as a `1%`-`100%` slider mapped to source `0.1`-`3`, grid density, rotation speed, fade falloff, distortion, and flow X/Y. The effect is premium-gated through `premium_backgrounds`, pointer-events disabled, reduced-motion aware, explicitly cleaned up on unmount, and internal-only.
- Added Chamaac UI Liquid Chrome as a reviewed MIT premium background after checking `https://chamaac.com/r/liquid-chrome.json` and the component page's Customize controls. The source is a smooth liquid-metal shader, exposes `speed`, `timeScale`, `color`, and `color2`, and declares `three`, `@react-three/fiber`, `clsx`, and `tailwind-merge`; MassageLab ports the fragment shader into a native WebGL canvas renderer instead of adding that stack. Chimer/Clock controls expose custom chrome/shadow colors or a primary-color harmony palette, Flow Speed as a `1%`-`100%` slider mapped to source `0.01`-`2`, and Time Scale as a `1%`-`100%` slider mapped to source `0.001`-`1`. The effect is premium-gated through `premium_backgrounds`, pointer-events disabled, reduced-motion aware, explicitly cleaned up on unmount, and internal-only.
- Added Chamaac UI Waves as a reviewed MIT premium background after checking `https://chamaac.com/r/waves.json` and the component page's Customize controls. The source is a smooth shader-based wave animation over a displaced plane, exposes background color, three wave colors, Speed X/Y, and wave amplitude, and declares `three`, `@react-three/fiber`, `@react-three/drei`, `clsx`, and `tailwind-merge`; MassageLab ports the shader plane into a native WebGL canvas renderer instead of adding that stack. Chimer/Clock controls expose custom source colors or a primary-color harmony palette, Speed X/Y as `1%`-`100%` sliders mapped to source `0.001`-`0.1`, and wave amplitude. The effect is premium-gated through `premium_backgrounds`, pointer-events disabled, reduced-motion aware, explicitly cleaned up on unmount, and internal-only.
- Added Eldora UI Novatrix Background as a reviewed MIT premium background after checking `https://www.eldoraui.site/docs/components/novatrix-background`, `https://www.eldoraui.site/r/novatrix-background.json`, and the Eldora UI GitHub repository license summary. The source is an OGL/WebGL shader background, exposes `color`, `speed`, `amplitude`, and `mouseReact`, and declares `ogl`; MassageLab ports the fragment shader into a native WebGL canvas renderer instead of adding OGL, keeps pointer-events disabled, replaces cursor reaction with passive ambient drift, and exposes custom color or color-harmony controls plus Animation Speed and Amplitude as `1%`-`100%` sliders mapped to source-shaped values. The effect is premium-gated through `premium_backgrounds`, reduced-motion aware, explicitly cleaned up on unmount, and internal-only.
- Added Eldora UI Hacker Background as a reviewed MIT premium background after checking `https://www.eldoraui.site/docs/components/hacker-background`, the source file at `https://raw.githubusercontent.com/karthikmudunuri/eldoraui/main/apps/www/registry/eldoraui/hacker-background.tsx`, and the Eldora UI GitHub repository license summary. The source is a Matrix-style falling-character 2D canvas background that exposes `color`, `fontSize`, and `speed`; MassageLab keeps that behavior in a host-sized native canvas with pointer-events disabled, reduced-motion and visibility handling, cleanup on unmount, custom color or color-harmony controls, font size, and Animation Speed mapped from source `0.05`-`3` to `1%`-`100%`. The effect is premium-gated through `premium_backgrounds` and internal-only.
- Added Eldora UI Photon Beam as a reviewed MIT premium background after checking `https://www.eldoraui.site/docs/components/photon-beam`, the source file at `https://raw.githubusercontent.com/karthikmudunuri/eldoraui/main/apps/www/registry/eldoraui/photon-beam.tsx`, and the Eldora UI GitHub repository license summary. The source is a Three.js/postprocessing WebGL beam field with animated light trails, bloom, and color/line/signal/path/wave/trail controls; MassageLab adapts the visible path and signal model into a dependency-free native 2D canvas background with pointer-events disabled, reduced-motion and visibility handling, cleanup on unmount, custom color or color-harmony controls, and source-shaped beam/signal/wave/trail/bloom controls. The effect is premium-gated through `premium_backgrounds` and internal-only.
- Added Magic UI Retro Grid as a reviewed MIT premium background after checking `https://magicui.design/docs/components/retro-grid`, the source file at `https://raw.githubusercontent.com/magicuidesign/magicui/main/apps/www/registry/magicui/retro-grid.tsx`, and the Magic UI GitHub repository license summary. The source is an animated scrolling perspective-grid WebGL component that exposes `angle`, `cellSize`, `opacity`, `lightLineColor`, and `darkLineColor`; MassageLab adapts the source shader into the shared background host with a CSS fallback, no new dependency, pointer-events disabled, reduced-motion/theme/visibility handling, cleanup on unmount, custom background and line colors, and source-shaped angle/cell/opacity controls. The effect is premium-gated through `premium_backgrounds` and internal-only.
- Added Magic UI Light Rays as a reviewed MIT premium background after checking `https://magicui.design/docs/components/light-rays`, the source file at `https://raw.githubusercontent.com/magicuidesign/magicui/main/apps/www/registry/magicui/light-rays.tsx`, and the Magic UI GitHub repository license summary. The source is an animated top-down light-ray component that exposes `count`, `color`, `blur`, `opacity`, `speed`, and `length` props and declares `motion/react`; MassageLab adapts the source ray ranges and glow layers with dependency-free native CSS keyframes, deterministic ray placement, pointer-events disabled, and reduced-motion handling. Chimer/Clock controls expose background fill, ray color, count, blur, speed, length, and opacity. The effect is premium-gated through `premium_backgrounds` and internal-only.
- Added Aceternity UI 3D Globe as a reviewed Aceternity License premium background after checking `https://ui.aceternity.com/components/3d-globe`, `https://ui.aceternity.com/registry/3d-globe.json`, and the provider license page. The source declares `three`, `@react-three/fiber`, `@react-three/drei`, and `@types/three`, uses reviewed three-globe Earth texture/bump URLs, and exposes globe config plus marker avatar tips; MassageLab adapts that visual and marker model into a dependency-free native WebGL sphere renderer with fixed texture URLs, pointer-events disabled, reduced-motion and visibility handling, cleanup on unmount, Chimer/Clock controls for globe appearance/lighting/atmosphere/wireframe, and an optional browser/manual location marker. The effect is premium-gated through `premium_backgrounds` and internal-only.
- Retuned Aceternity UI 3D Globe after visual comparison against the demo: the renderer now uses a native WebGL sphere pass with a 2D marker overlay instead of flat circle clipping, stronger ambient/point-light and bump-map passes, corrected marker projection, a visible `1%`-`100%` globe-size slider mapped to internal `5%`-`95%`, plus manual/sun-position lighting controls.
- Refined Aceternity UI 3D Globe controls and marker behavior: rotation speed now clamps to `0.01x`-`2x`, reverse spin is fixed on by default, and manual spin/light controls hide while Follow Sun is active; pan controls move the globe center on X/Y screen offsets and hide their sliders when disabled; initial rotation and avatar URL controls were removed, the user marker is icon-based with an optional empty label, a MassageLab Ashland, OH marker renders with the site logo and no text label, marker leader lines are 1px, axial tilt is fixed on by default and leans across the screen relative to the fixed off-screen-left sun, and Follow Sun rotates the globe once per 24 hours from UTC subsolar longitude with an approximate orbit-distance light modulation plus a hard day/night terminator.
- Added an Eldora UI Cobe Globe-inspired Graphic view style inside the existing Aceternity 3D Globe premium option after reviewing `https://www.eldoraui.site/r/cobe-globe.json`, the MIT `cobe` package source, and the Eldora UI MIT license. MassageLab does not add or redistribute the `cobe` runtime or `react-spring`; the existing dependency-free WebGL renderer now has a Graphic mode with stable 2D overlay map dots derived from the reviewed Cobe world-map mask, density-driven Fibonacci sphere sampling capped at a 10k sample budget, a tightened Outer Glow, Cobe-style marker dots, and Chimer/Clock controls for map color, Outer Glow color, marker color, and dot density.
- Added React Bits Ferrofluid as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/ferrofluid`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Ferrofluid/Ferrofluid.jsx`, and the React Bits repository license. The source declares OGL, exposes color, speed, scale, turbulence, fluidity, rim width, sharpness, shimmer, glow, flow direction, opacity, and cursor props, and renders a liquid contour shader. MassageLab ports the shader into a dependency-free native WebGL renderer, keeps cursor interaction disabled so the app background remains pointer-events free, adds custom-color and harmony controls plus source-shaped sliders/selectors for the remaining props, keeps reduced-motion and resource cleanup handling, and gates it through `premium_backgrounds` for internal app use only.
- Added React Bits Lightfall as a reviewed MIT + Commons Clause premium background after checking `https://reactbits.dev/backgrounds/lightfall`, the source file at `https://raw.githubusercontent.com/DavidHDev/react-bits/main/src/content/Backgrounds/Lightfall/Lightfall.jsx`, and the React Bits repository license. The source declares OGL, exposes colors, background color, speed, streak count, streak width, streak length, glow, density, twinkle, zoom, background glow, opacity, and cursor props, and renders a falling light-streak shader. MassageLab ports the shader into a dependency-free native WebGL renderer, keeps cursor interaction disabled by default with opt-in cursor glow controls so the app background remains pointer-events free, adds custom-color and harmony controls plus source-shaped sliders for the remaining props, keeps reduced-motion and resource cleanup handling, and gates it through `premium_backgrounds` for internal app use only.
### 2026-07-03

- Added Chamaac UI Light Speed as a reviewed MIT premium background after checking the component page, registry payload, and Chamaac UI repository license. The source declares `three`, `@react-three/fiber`, and `@react-three/postprocessing`, so MassageLab adapts the source warp-cylinder particle model with a dependency-free 2D canvas renderer instead of adding that stack. Chimer/Clock controls expose light color, Warp speed, particle count, glow intensity, tunnel radius, and field length; the MassageLab default Warp speed is `0.10x` with a minimum of `0.01x`. The effect remains high-motion opt-in, premium-gated through `premium_backgrounds`, pointer-events disabled, reduced-motion aware, and internal-only.
- Retuned Chamaac UI Light Speed so the old `0.10x` render pace is now shown as the new `1.00x` user-facing Warp speed. Existing saved values migrate forward to that scale, particles are capped at 200, rays use colored fading strokes without white tips, and particles reset only after their projected ray has left the viewport.
- Added Chamaac UI Synthesis as a reviewed MIT premium background after checking the component page's Customize controls and provider-level Chamaac UI license. The source is a shader-like multi-layer cosmic flow; MassageLab initially adapted the visible prop model with a dependency-free internal 2D canvas renderer instead of adding a WebGL/R3F stack. Chimer/Clock controls expose Color 1, Color 2, Color 3, animation speed, complexity, zoom scale, distortion, glow intensity, and flow frequency. The effect is premium-gated through `premium_backgrounds`, pointer-events disabled, reduced-motion aware, and internal-only.
- Corrected Chamaac UI Synthesis after Playwright visual review showed the dependency-free 2D approximation did not look or behave like the source demo. The official `https://chamaac.com/r/synthesis.json` registry source uses a fragment shader through `three` and `@react-three/fiber`; MassageLab now ports that fragment shader into a native WebGL canvas renderer, removes the extra star/noise painter and overlay, keeps the existing controls and entitlement behavior, and still avoids adding the source Three/R3F dependencies.
- Updated Chamaac UI Synthesis controls so users can keep the exact custom source-color controls or switch to a primary color plus the shared harmony picker. The Synthesis animation speed slider now displays the source `0.4` default as `1.00x`, allows slower values down to `0.01x`, and stores the renderer's source-scale speed internally.
- Added Chamaac UI Electric Mist as a reviewed MIT premium background after checking `https://chamaac.com/r/electric-mist.json` and the provider-level Chamaac UI license. The source is a high-energy glowing lightning shader with waves and smoke effects, exposes `speed`, `color`, `detail`, `distortion`, and `brightness`, and declares `three`, `@react-three/fiber`, `clsx`, and `tailwind-merge`; MassageLab ports the fragment shader into a native WebGL canvas renderer instead of adding that stack. Chimer/Clock controls expose lightning color, animation speed, noise detail, distortion, and brightness. The effect is premium-gated through `premium_backgrounds`, pointer-events disabled, reduced-motion aware, explicitly cleaned up on unmount, and internal-only.
- Retuned Chamaac UI Electric Mist after visual QA showed the bright electricity passing across the screen as an obvious vertical bar, then corrected the first retune when it became too sparse and line-like. The renderer now restores the earlier dense source-like mist while replacing only the old vertical sweep with a softer irregular multi-direction mask. Chimer/Clock controls still display animation speed and brightness as percentages; brightness stores a direct 1%-100% output-intensity value with a versioned migration from the earlier source-scale multiplier.
- Added Chamaac UI Astral Flow as a reviewed MIT premium background after checking `https://chamaac.com/r/astral-flow.json` and the component page's Customize controls. The source is a breathing radial shader with deep cosmic wisps, exposes `color1`, `color2`, `color3`, `speed`, `flowMin`, and `flowMax`, and declares `three`, `@react-three/fiber`, `clsx`, and `tailwind-merge`; MassageLab ports the fragment shader into a native WebGL canvas renderer instead of adding that stack. Chimer/Clock controls expose custom source colors or a primary-color harmony palette, Flow Min/Max, and animation speed as a `10%`-`100%` slider mapped to the source `0.1`-`3` range. The effect is premium-gated through `premium_backgrounds`, pointer-events disabled, reduced-motion aware, explicitly cleaned up on unmount, and internal-only.
- Retuned Chamaac UI Astral Flow after Playwright comparison against the live Chamaac preview and a mocked premium `/clock` session. The native WebGL port now blends the transparent shader quad before browser compositing, uses mount-relative shader time to match the R3F source phase, renders over the Chamaac `#05070a` base, and softens the overlay noise so Clock no longer shows the pale washed-out center bowl while preserving the existing controls and `premium_backgrounds` gate.
- Fixed Chimer setup background leakage after user QA showed the selected Clock/Chimer background still rendering behind setup and Anatomime after leaving Clock. The Chimer setup route now keeps visual selection controls but does not mount `BackgroundHost` or `MovingBackground`; active Clock/running Chimer remains the only Chimer surface that mounts selected visual effects. `LayoutWrapper` also clears stale `chimer-running`/`chimer-alerting` body classes on non-Chimer routes so the shared app bar is restored on Anatomime, Home, Music, Calendar, and other pages after leaving Clock.
- Added Animate UI Bubble Background as a Commons Clause caution premium background after reviewing the component page, registry payload, and `LICENSE.md`. The source depends on `motion` and supports cursor interactivity, but MassageLab adapts the ambient five-bubble goo/blur motion with native CSS keyframes, omits the cursor-following interactive bubble, adds no dependency, and uses it only as an internal premium visual background.
- Added Animate UI Gradient Background as a Commons Clause caution premium background after reviewing the component page, registry payload, and `LICENSE.md`. The source uses `motion/react` to animate a 400% blue/purple/pink gradient's background position over 15 seconds; MassageLab adapts the same behavior with native CSS keyframes, adds Chimer/Clock controls for primary color, color harmony, and opacity, adds no dependency, and uses it only as an internal premium visual background.
- Added Animate UI Stars Background as a Commons Clause caution premium background after reviewing the component page, registry payload, and `LICENSE.md`. The source uses `motion/react` for three drifting star layers plus a spring parallax offset; MassageLab adapts the source star layers, 2000px repeat drift, bottom radial background, and parallax behavior with deterministic CSS keyframes and a window-level pointer listener, keeps the background pointer-events disabled, adds Chimer/Clock controls for star color, speed, density, and parallax strength, adds no dependency, and uses it only as an internal premium visual background.
- Added Animate UI Hole Background as a Commons Clause caution premium background after reviewing the component page, registry payload, and `LICENSE.md`. The source exposes stroke color, line count, disc count, and particle color props and declares `motion`; MassageLab adapts the hole grid and particle field with a dependency-free internal canvas loop plus CSS glow, adds Chimer/Clock controls for stroke color, particle color, line count, and disc count, cleans up animation/resize/visibility listeners, and uses it only as an internal premium visual background.
- Added a premium-background UX follow-up note: revisit existing backgrounds with multi-color controls and use a primary color plus harmony-mode selector where that fits the source behavior and gives users simpler customization.
- Added MassageLab Hex Grid as an internal premium background, parallel to the existing tile grid but using raised hexagonal tiles and the newer primary-color harmony selector. The dependency-free canvas implementation uses deterministic per-hex phases, flat color alpha fade-ins and fade-outs, an H:M:S fade-time picker, active hex percentage, hex size, joint size, and opacity controls, resets with the existing premium background entitlement fallback, and is documented as an internal MassageLab visual effect only.

### 2026-07-02

- Added Unlumen UI Aurora Bars as a reviewed premium background/visualizer candidate. The Unlumen free component license was reviewed from the provider license page, and the component page credits the original Framer idea by `@SannaGranqvistX`; MassageLab preserves that credit and uses the effect only as an internal premium visual background/visualizer, not as redistributed component source. The source registry declares `motion`, but MassageLab adapts the sine-wave bar-height behavior with native `requestAnimationFrame` and direct DOM style updates so no new dependency is added. Chimer/Clock use passive source-style motion only with controls for background, auto monochrome or custom five-color bar palettes, bar count, speed, blur, gap, and min/max height, while Music enables the visualizer flag only when Aurora Bars is selected and a station is playing.
- Added Aceternity UI Background Beams as the next source-matched premium background. The source registry declares a `motion` dependency, but MassageLab adapts the SVG path and gradient behavior with native SVG animation so no new runtime package is added; the option remains gated through `premium_backgrounds`, has pointer events disabled, uses a static fallback, and preserves the original MassageLaba Lamp default.
- Added Aceternity UI Background Beams With Collision as the next source-matched premium background. The source registry declares a `motion` dependency, but MassageLab adapts the falling beams, bottom collision surface, and burst particles with CSS animation so no new runtime package is added; the option remains gated through `premium_backgrounds`, reduced-motion-safe, pointer-events disabled, and the original MassageLaba Lamp default remains untouched.
- Added Aceternity UI Background Lines as the next source-matched premium background. The source registry declares a `motion` dependency, but MassageLab adapts the SVG paths with deterministic CSS dash/opacity animation so no new runtime package is added; Chimer/Clock get a duration control, the option remains premium-gated, reduced-motion-safe, pointer-events disabled, and the original MassageLaba Lamp default remains untouched.
- Added Aceternity UI Glowing Stars as the next source-matched premium background. The public page payload shows a `motion/react` card grid that highlights five stars every three seconds; MassageLab adapts that star/glow cycle with React state and CSS animation instead of adding `motion`, keeps the effect premium-gated with pointer events disabled and a static fallback, and preserves the original MassageLaba Lamp default.
- Added Aceternity UI Wavy Background as the next source-matched premium background. The registry uses `simplex-noise@4.0.3`, a small MIT package with no transitive dependencies and no audit finding attributed to it during review; Chimer/Clock get controls for background fill, five wave colors, wave width, blur, speed, and opacity, while the option remains premium-gated, reduced-motion-safe, pointer-events disabled, canvas-cleaned, and the original MassageLaba Lamp default remains untouched.
- Added an Aceternity provider-level license note to `docs/background-sources.md` so new Aceternity candidates can reuse the same license decision while still requiring component-specific source, dependency, motion, performance, fallback, cleanup, and visual review. Added Aceternity UI Meteors as a source-matched premium background; the registry source declares a `motion` dependency, but MassageLab adapts the source fade, meteor distribution, slate particle/tail styling, and documented keyframes with deterministic CSS animation so no new runtime package is added. The option remains premium-gated, reduced-motion-safe, pointer-events disabled, and the original MassageLaba Lamp default remains untouched.
- Added Aceternity UI Shooting Stars and Stars Background as the next source-reviewed premium background. The public component page is available but the direct Aceternity registry endpoint returned Unauthorized; the page credits Vijay Verma / figmaplug.in, and the credited original repo has no package license field, so MassageLab treats the Aceternity component page/license as the activation source while preserving the creator credit in `docs/background-sources.md`. The implementation adapts the source star density, twinkle, shooting-star path, speed, delay, and gradient behavior with internal SVG/JS, no new dependency, deterministic star placement, reduced-motion fallback, pointer-events disabled, a static fallback, and cleanup for ResizeObserver, timeouts, requestAnimationFrame, and visibility/motion listeners. The original MassageLaba Lamp default remains untouched.
- Added per-background Chimer/Clock controls for Shooting Stars so subscribed users can tune star, trail, and shooting-star colors plus star density, twinkle, shooting speed, and frequency. The controls persist through existing Chimer settings, pass into both setup and running timer hosts, and reset to defaults when the user loses premium background access.
- Added Aceternity UI Vortex Background as a high-motion/high-cost opt-in premium background. The source registry declares `simplex-noise` and `motion`; MassageLab reuses the existing `simplex-noise@4.0.3` dependency from Wavy and adapts the fade behavior without adding `motion`. The implementation uses a capped-DPR canvas particle field with Chimer/Clock controls for background color, hue, particle count, vertical spread, speed, and particle radius, remains gated through `premium_backgrounds`, preserves the original MassageLaba Lamp default, and cleans up requestAnimationFrame, ResizeObserver, resize listeners, visibility listeners, and motion media-query listeners.
- Added Aceternity UI Canvas Reveal Effect's dot background as `Canvas reveal dots`, a dependency-free 2D canvas passive premium background. The source registry declares `three` and `@react-three/fiber`, but MassageLab only needed the dot-matrix layer for Chimer/Clock backgrounds, so no WebGL dependency was added; users can tune background color, dot/accent colors, dot size, spacing, opacity, motion speed, and gradient overlay while the full hover reveal remains disabled for page-specific review. The option now uses deterministic pixel-dot placement plus a subtle per-dot shimmer/reveal animation, is premium-gated through `premium_backgrounds`, pointer-events disabled, reduced-motion-safe, and leaves the original MassageLaba Lamp default untouched.
- Added Aceternity UI Spotlight New as a source-matched premium background. The registry source declares `motion/react`, but MassageLab adapts the mirrored radial-gradient spotlight groups with native CSS keyframes instead of adding that runtime dependency. The option is premium-gated through `premium_backgrounds`, pointer-events disabled, reduced-motion-safe, uses a static fallback, preserves the original MassageLaba Lamp default, and adds Chimer/Clock controls for spotlight color, intensity, beam width, beam height, small beam width, vertical offset, sweep distance, and duration.
- Added Aceternity UI Lamp Section Header as a source-reviewed premium background. The public page source uses `motion/react` for a Linear-style lamp header, but MassageLab adapts the mirrored conic beams, glow layers, and center line with native CSS keyframes instead of adding that runtime dependency. The option is premium-gated through `premium_backgrounds`, pointer-events disabled, reduced-motion-safe, uses a static fallback, preserves the original MassageLaba Lamp default, and adds Chimer/Clock controls for lamp background, beam color, glow intensity, beam width, glow width, vertical offset, and pulse speed.
- Followed up the Lamp Section Header visual QA by removing the source hero mask layers from the Clock/Chimer adaptation after they appeared as separated rectangular panels behind the clock. The enabled background now keeps the adapted line, conic beams, and glow layers, scales those layers responsively in full-screen Clock mode, and documents that the source masks were intentionally omitted for full-screen use.
- Added Unlumen UI Pixel Liquid Background as a reviewed opt-in premium background. The Unlumen page describes a Three.js/WebGL Navier-Stokes fluid simulation with pixelation, Bayer dithering, film-grain noise, cursor/touch reaction, auto-demo mode, and React Bits Liquid Ether inspiration; MassageLab activates a dependency-free 2D canvas adaptation instead of importing the source component or adding `three`. The option is premium-gated through `premium_backgrounds`, pointer-events disabled, reduced-motion-safe through `BackgroundHost`, uses a static fallback, preserves the original MassageLaba Lamp default, and adds Chimer/Clock controls for background, base, accent, and highlight colors plus detail, pixel size, and ambient motion speed. Cursor-responsive behavior and the unclear auto-demo toggle were removed from the MassageLab adaptation after visual review.
- Removed the Unlumen Square Pattern / Pixel candidate from the premium-background backlog after deciding not to pursue the Pro/licensed component, and added a MassageLab-owned Tile Grid premium background instead. The internal implementation uses a dependency-free 2D canvas renderer with raised square tiles, faint grey borders, deterministic per-tile phases, independent flat color alpha fade-ins and fade-outs, no global refill loop, and no random active-count correction. The H:M:S fade-time picker now directly controls each tile's fade duration, active tile percentage controls the visible portion of each tile's cycle, and the controls include auto or custom five-color palettes, tile size, joint size, and tile opacity while remaining gated through `premium_backgrounds`.

### 2026-07-01

- Reviewed the Ali Imam background/shader source as the first one-at-a-time premium-background candidate pass, then removed Ali Imam options from the premium-background backlog after Derrick decided not to keep them queued.
- Added Aceternity UI Aurora background as the first one-at-a-time premium background implementation. The registry source is CSS/Tailwind-only with no added runtime dependency detected, and the Aceternity License allows end-product use while prohibiting source/template redistribution; MassageLab uses it only as an internal premium visual background with the original MassageLaba Lamp preserved as the free default.
- Added Aceternity UI Dotted glow background as the next one-at-a-time premium background implementation. The registry source is a canvas-based shimmer with no added runtime dependency detected; MassageLab adapts it with pointer-events disabled, static fallback, reduced-motion fallback, and cleanup for animation frames, resize observation, and event listeners.
- Added Aceternity UI Sparkles as a premium particle background. The source-matched tsparticles implementation is gated through `premium_backgrounds`, keeps Chimer/Clock controls inside the selected background card, and exposes particle color/density/speed/size controls for Chimer. An initial Music analyser-reactivity trial was removed after visual review because it made particles feel jumpy/strobing, so Sparkles now stays steady on Music while true visualizers remain future candidates. The implementation adds `@tsparticles/react`, `@tsparticles/engine`, and `@tsparticles/slim`; the source `motion` fade dependency was replaced with CSS.
- Added Aceternity UI Background Gradient Animation as the next source-matched premium background. The implementation keeps MassageLaba Lamp as the free default, scopes the upstream body-level CSS variables to the MassageLab background layer, disables pointer interactivity, adds no runtime dependency, and exposes Chimer/Clock controls for base colors, glow colors, speed, and size while keeping the effect gated through `premium_backgrounds`.

### 2026-06-26

- Added Batch 0/1 premium visual-background plumbing: a reusable background registry and host, feature-key gating through `premium_backgrounds`, Chimer/Clock selectors backed by existing Chimer preference sync, Music selector persistence in `massagelab.music.background`, and app-level low-motion handling for premium alternatives. The original MassageLaba Lamp moving gradient remains the free default and is mounted through its original component path instead of the premium host; a static gradient remains available as a free fallback option.
- Paused low-cost Batch 1 draft effects for Magic UI, Chamaac UI, and Aceternity UI review candidates after visual review showed they were not source-matched to creator examples. Added [background sources](background-sources.md) with provider URLs, license status, paused draft status, Commons Clause cautions for later React Bits candidates, and an explicit review queue for disabled Unlumen/heavy candidates until license, dependency, performance, reduced-motion, cleanup, and route visual QA pass.
- Reviewed skipped premium-background candidates and added a source-backed disposition slate: low-risk Magic UI/Chamaac/Aceternity effects are queued for a later add pass, React Bits and shader/WebGL effects remain gated by current license capture and performance review, and product-fit questions such as hero-only, interactive, themed, or unclear-provenance effects are held for user review before any add/defer/skip decision.
- Corrected Chimer/Clock visual settings: the original free background is labeled MassageLaba Lamp, the default Lamp bypasses the premium host so its original fallback/canvas behavior is preserved, selected background cards can host per-background controls, and existing paid Chimer color entitlement now unlocks premium backgrounds as a legacy-safe paid visual access path.

### 2026-06-25

- Implemented provider-side Google Calendar sync: separate calendar OAuth, provider-neutral sync tables, encrypted token storage, selected Google calendars imported as generic busy blocks, external busy conflict checks across staff and public booking writes, dedicated MassageLab Google calendar outbound events, and `/calendar/sync` settings. Client calendars, Outlook, Apple/iCloud, CalDAV, ICS, and personal event detail mirroring remain deferred.

### 2026-06-24

- Investigated a live Supporter monthly Checkout 500 from production Vercel runtime logs. The failure was not a Stripe dashboard issue; production was missing the `LegalAcceptance` table expected by the deployed checkout route.
- Applied the five pending production Prisma migrations through the repo migration deploy path: shared Anatomime room/game-run tables, flashcard progress null-tool uniqueness, open host-election uniqueness, client wellness tables, and legal acceptance records. The checkout 500 root cause was the missing `LegalAcceptance` table; Prisma now reports the production schema is up to date, and an unauthenticated production checkout POST returns the expected login redirect instead of HTTP 500.
- After the registration hardening PR was merged and deployed, a new email/password account successfully completed a live Supporter subscription, unlocked Chimer custom colors through the `chimer_custom_colors` entitlement, opened the Stripe Customer Portal, and canceled the subscription.
- Replaced invite-facing billing, pricing, Google sign-in, and shared-game unavailable messages that exposed implementation details such as webhooks, secret-key configuration, Price IDs, Vercel environment setup, or database access with user-facing recovery language.

### 2026-06-23

- Added the [invite readiness stabilization plan](superpowers/plans/2026-06-23-invite-readiness-stabilization.md) and refreshed the release checklist so the invited-user gate now covers current registration/onboarding, live billing, legal acceptance, production SEO/indexing, Sentry privacy, Neon pooled-runtime, local-first professional-record, and first-cohort monitoring requirements.
- Added a Stripe production readiness checker (`npm run stripe:readiness`) that validates required membership billing environment variables without printing secret values and can retrieve configured Stripe Prices when run with `--verify-stripe`.
- Verified that current local Stripe configuration is test/non-live ready. The first live readiness attempt failed because usable production values were unavailable locally, then the user-supplied live env file passed the production Stripe readiness check. Paid public signup stayed blocked on the real low-dollar checkout and Customer Portal return smoke until the 2026-06-24 live Supporter smoke succeeded.
- Confirmed the early-access discount should remain enabled for the current invite window, added a one-time donation Checkout path for users who want to support MassageLab without subscribing, and strengthened pricing/legal copy around current paid features, roadmap funding goals, no advertising, no data-sale funding, and the local-first PHI boundary.
- Created/reused live Stripe membership Products and recurring Prices matching the test pricing for Supporter, Therapist, and Practice, created the live Student-to-Therapist and Early Access coupons, configured the production billing webhook endpoint, enabled Stripe Customer Portal with the current legal URLs, updated Vercel Production Stripe env values, redeployed Production, verified `/pricing` renders configured prices, and confirmed the live webhook accepts an inert signed event. The subsequent real live Supporter checkout and portal cancellation smoke succeeded on 2026-06-24.
- Closed the Google signup legal-acceptance gap: Google sign-in now redirects through `/legal/accept`, requires the current registration Terms/Privacy acceptance when missing, then continues to onboarding or the preserved callback. Email/password registration still records acceptance during account creation.

### 2026-06-22

- Implemented the Big Visual Refresh: added a bottom-first responsive shell, bottom-stacked music player behavior, Radarr-style quick actions, rail-based homepage/tool discovery, compact grouped account settings, and shared visual-surface alignment across representative public routes while preserving local-first PHI boundaries, feature-key checks, drawer-side choice, and theme modes.
- Added the [business income planner design spec](superpowers/specs/2026-06-22-business-income-planner-design.md) and [implementation plan](superpowers/plans/2026-06-22-business-income-planner.md), then implemented `/tools/business-planner` and `/tools/business-planner/income` as public student/therapist business-planning surfaces. The income planner turns worksheet-style assumptions into gross revenue, hourly target, 30/60/90/120-minute session prices, workload capacity scenarios, time-off math, and optional employee-wage comparisons.
- Kept planner persistence intentionally narrow: anonymous users save one worksheet to browser storage, signed-in users save one overwritten current worksheet under `UserPreference.appSettings.businessIncomePlannerIncome`, and source guards confirm there is no dedicated Prisma planner model or account sync of PHI-shaped fields.
- Wired the business income planner into public SEO metadata, the new `/tools` index, sidebar navigation, homepage tool discovery, and onboarding/home shortcut recommendations for therapist and student practice-planning use cases.
- Added the [business plan template tools implementation plan](superpowers/plans/2026-06-22-business-plan-template-tools.md) and expanded `/tools/business-planner` with public browser-local tools derived from the student business plan template: startup costs and break-even, practice launch checklist, service menu and policy worksheet, business plan outline builder, and add-on profit calculator. These routes are wired into the business planner hub, public SEO route contract, sidebar model for the break-even calculator, and browser route coverage without adding Prisma storage or account preference writes for the new template tools.
- Added the [privacy-safe Sentry feedback plan](superpowers/plans/2026-06-22-privacy-safe-sentry-feedback.md) and implemented `/api/support/problem-report` plus a support-page diagnostic report card. User-initiated diagnostics now send only predefined issue categories, coarse tool areas, safe route buckets, browser/display hints, and optional linked Sentry event ids; human support messages stay in the email-client flow, and Session Replay, standard User Feedback, screenshots, attachments, Logs, and freeform Sentry feedback remain disabled for PHI-capable routes.

### 2026-06-21

- Added the [technical SEO foundation plan](superpowers/plans/2026-06-21-technical-seo-foundation.md) for the public-alpha share window, replacing the old sitewide private-alpha `noindex` posture with production-gated public metadata, sitemap, robots, and schema work while keeping preview/development builds and private route families blocked from indexing.
- Added the [focused SEO landing pages plan](superpowers/plans/2026-06-21-focused-seo-landing-pages.md) to layer product-proof copy onto the existing homepage, flashcards, Anatomime, Chimer, local-first documentation, and wellness routes for searches such as "massage anatomy flashcards" and "massage session timer" without creating detached marketing pages or weakening local-first clinical boundaries.
- Added a host-facing Anatomime invite panel with a scan-ready QR code, copyable prefilled join link, and `/anatomime/join?code=` support so students can skip manual room-code entry during shared classroom setup.
- Added the [launch Neon transfer hardening plan](superpowers/plans/2026-06-21-launch-neon-transfer-hardening.md) and [Neon transfer readiness audit](audits/2026-06-21-neon-transfer-readiness.md) after Neon project metadata showed about 8.16 GB of transfer before the wider student-share window. The branch adds production pooled-host validation for Prisma runtime `DATABASE_URL`, keeps direct Neon URLs reserved for migrations and maintenance, narrows signed-in homepage preference loading to `appSettings`, projects public booking sequence generation to fields used by slot generation and final booking validation, and updates deployment docs with launch transfer operating rules.
- Removed the unused `lib/anatomime-session-server.js` compatibility wrapper after fresh reference checks found no direct `.js` consumer; the canonical TypeScript implementation remains `lib/anatomime-session-server.ts`, and the remaining TS/JS wrappers stay governed by the compatibility inventory.
- Hardened the flashcard setup controls for the student-share window: start/save actions now wait for refreshed prompt counts, empty filter states say why no cards can start, and users get a reset path back to all eligible sourced prompts before study begins.
- Polished shared Anatomime classroom player states: joined players now see whether it is their active turn or a steal/practice opportunity, and devices with stale saved player passes get a clear rejoin recovery path instead of a confusing generic join state.

### 2026-06-20

- Created the measurement-first codebase refactor/optimization plan and baseline audit, then split `/admin/anatomy` browser types/data loading into focused modules and scoped maintenance data plus broad browser datasets so they load only when the active view needs them. Validation passed with `npm run prisma:validate`, `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build`, focused anatomy admin tests, and `npm run test:browser -- tests/browser/public-routes.spec.ts --project=desktop-chromium`.
- Split the `/education/flashcards` main client into focused API parsing/fetch, active runner, setup-builder, and progress-dashboard client modules without changing prompt/deck/progress behavior. Focused validation passed with `node --test tests/flashcard-community.test.mjs tests/flashcard-progress.test.mjs`, `npm run test:browser -- tests/browser/public-routes.spec.ts -g "Flashcards" --project=desktop-chromium`, `npm run typecheck`, and `npm run lint`.
- Added a process-local sourced flashcard prompt catalog cache keyed by reviewed media option signatures, then routed flashcard progress and session-start APIs through cached catalog/deck selectors while preserving skip-mastered behavior and the existing scale guard. Focused validation passed with `node --test tests/flashcard-community.test.mjs tests/flashcard-progress.test.mjs`, `npm run test:browser -- tests/browser/public-routes.spec.ts -g "Flashcards" --project=desktop-chromium`, `npm run typecheck`, and `npm run lint`.
- Finished calendar server-action decomposition by keeping `app/calendar/actions.ts` as a stable 107-line public facade while extracting preferences, setup, rescheduling, public booking, service catalog, event creation, availability, access, audit, and revalidation logic into focused modules. The branch preserved existing action names and kept calendar/public-booking tests green.
- Staged the follow-up calendar mutation concurrency branch immediately after calendar action decomposition and before measured client lazy-loading. The planned branch covers preference write serialization, stale-row protection for reschedules, and atomic free-practice quota enforcement without changing the public action surface.
- Hardened calendar mutation concurrency by serializing preference patch merges on the owner user row, locking reschedule target/overlap events in deterministic order inside the scheduling-lock phase, rejecting stale reschedule snapshots, re-running current membership/editability checks inside the reschedule transaction, moving free-practice quota checks into the locked practice creation transaction, and serializing global practice slug allocation with a transaction-scoped advisory lock. Focused calendar route/preference/workspace tests, `npm run typecheck`, and `npm run lint` passed.
- Completed the first measured lazy-loading pass by moving the Atmosphere station catalog, runtime controller, Tone proof runtime, and Generative.fm runtime out of the global `MusicProvider` static import path. Production route-bundle diagnostics dropped representative first-load JavaScript from 1,787,081 to 1,113,612 bytes on `/`, from 2,100,580 to 1,427,111 bytes on `/calendar`, and from about 1.81 MB to 1,598,886 bytes on `/music` and `/browse`, while keeping the provider globally mounted for route-persistent playback. Added a source-contract test for the lazy boundary, and the focused audio/runtime tests plus browser checks for route-persistent playback and Media Session controls passed.
- Continued the measured lazy-loading pass by moving `/anatomime`'s sourced anatomy deck and full game workspace behind the first "Choose Anatomy Terms" setup action while warming the lazy client during online idle for service-worker offline readiness. The lightweight route shell preserves team/round setup and the shared-game link, while production route-bundle diagnostics dropped `/anatomime` first-load JavaScript from 2,889,251 to 1,122,077 bytes. Added a source-contract test for the lazy boundary, and focused Anatomime helper/browser coverage passed.
- Hardened Neon transfer hotspots by narrowing DB-backed flashcard media loading to reviewed BodyParts3D image/diagram projections with approved links, adding a short in-process media-options cache for repeated flashcard API calls, projecting admin media-review rows to rendered asset/source fields, limiting selected-entity media detail links to the active entity, and projecting BodyParts3D media maintenance scripts. Added a source-contract test for these transfer boundaries.
- Hardened local-first documentation types by adding named intake workspace/template/response/document/client UI contracts, moving `SoapNoteData` and SOAP section state setter contracts into the shared SOAP type file, and replacing broad intake/SOAP `any` prop bags without changing encrypted vault storage, `.mlab` transfer, plaintext export warnings, or hosted-clinical boundaries. Focused local-first tests, typecheck, and lint passed.
- Documented the TS/JS duplicate-helper compatibility policy before deleting wrappers: TypeScript files are the canonical implementations, matching JavaScript files remain one-line ESM compatibility wrappers where tests or runtime imports still require `.js`, and future cleanup should migrate at most one pair per branch. Added a source-contract test for the wrapper shape.

### 2026-06-19

- Added the [Atmosphere Polish And QA plan](superpowers/plans/2026-06-19-atmosphere-polish-qa.md) and started the post-rollout polish branch. The branch moves the breathing guide to `/wellness/breathing`, links it from the Wellness hub and Atmosphere, groups stations by listener-facing sound feel, adds previous/next controls plus loading progress to the persistent mini-player, narrows idle prewarm to a starter station set, keeps hover/focus and play-button pointer-down metadata-only so speculative payload fetches do not compete with first sound, fades Generative.fm station handoffs, and updates QA-flagged station descriptions for Little Bells, Beneath Waves, Awash, Moment, and Neuroplasticity.
- Added the [Atmosphere Rendered Piano Batch plan](superpowers/plans/2026-06-19-atmosphere-rendered-piano-batch.md) and enabled No Refrain, Transmission, and Trees with package-compatible rendered VSCO piano sample groups. The branch published WAV indexes/manifests and Opus sidecars under each piece prefix, moving playable Generative.fm coverage to 14 stations and the remaining local-source candidate count to 25. Header checks confirmed JSON metadata and representative rendered-piano Opus range/CORS behavior, while raw and rendered audio remain outside Git.
- Expanded the same Atmosphere branch to finish the remaining currently covered source-index rollout instead of continuing in small batches. The branch now hosts 25 additional Generative.fm stations from `420hz-gamma-waves-for-big-brain` through `yesterday`, publishing 703 WAV-side R2 objects representing 2,077.7 MB of WAV payload and 703 Opus sidecar objects representing 99.9 MB of encoded payload. Metadata checks confirmed all 25 new WAV/Opus sample indexes and manifests return JSON with CORS, representative Opus/WAV range checks passed, `/wellness/atmosphere` now exposes 39 playable Generative.fm stations, and the catalog coverage scan classifies 0 remaining local-source candidates and 18 replacement/source-review pieces.
- Added the [Atmosphere Remaining Generators plan](superpowers/plans/2026-06-19-atmosphere-remaining-generators.md) and enabled the final 18 Generative.fm stations from Animalia Chordata through Zed. The branch uses VSCO, VCSL, and Signature Sounds replacements, treating the Signature Sounds About page site-wide CC0 statement as satisfactory evidence for packs under `Signature Samples`. It published 704 WAV-side R2 objects representing 1,491.6 MB of WAV payload plus 704 Opus sidecar objects representing 79.5 MB of encoded payload. All 36 new WAV/Opus sample-index URLs passed the runtime validator, representative new audio payloads returned range-readable CORS responses, `/wellness/atmosphere` now exposes all 57 Generative.fm package pieces as playable stations, and the catalog coverage scan reports 0 replacement-needed pieces and 0 blocked groups.
- Added the [Atmosphere AAC/MP3 Sidecars plan](superpowers/plans/2026-06-19-atmosphere-aac-mp3-sidecars.md) and expanded compressed sidecar coverage to every playable Generative.fm station, not just the newest batch. Observable Streams plus all 56 package stations now expose ordered `opus -> aac -> mp3 -> wav` sample-index selection. The AAC rollout published 1,814 R2 sidecar objects, representing 4,845.6 MB of WAV payload as 248.0 MB of AAC. The MP3 rollout published another 1,814 R2 sidecar objects, representing the same WAV payload as 327.5 MB of MP3. Verification confirmed all 57 AAC and all 57 MP3 sample-index URLs return JSON with CORS and required package groups, and representative Observable Streams, Peace, and Zed AAC/MP3 payloads return range-readable CORS responses with correct content types.
- Added the [Atmosphere Playback Performance plan](superpowers/plans/2026-06-19-atmosphere-playback-performance.md) and tightened first-play startup behavior now that all 57 Generative.fm stations are playable. Idle prewarm remains metadata/module-only, while station hover/focus and play-button pointer-down now warm up to 24 selected compressed sample payload URLs for the chosen browser-supported format without starting Tone, Transport, output nodes, or WAV fallback downloads before playback. Startup timing now reports completed sample-payload warmup reuse and payload warmup count, and the mini-player loading status says `Preparing audio...`.
- Added the [Atmosphere Performance And Audio QA plan](superpowers/plans/2026-06-19-atmosphere-performance-audio-qa.md) and started the follow-up station reliability pass. The runtime now wraps the Generative.fm provider with bounded sample fetch/decode batches and per-AudioContext decoded-buffer reuse, extends startup timing with provider request/decode counts, and keeps compressed payload warmup limited to a tiny idle starter subset plus deliberate hover on healthy connections. This improves crash resistance and diagnostics before broader station-by-station sound retuning.
- Continued the Atmosphere performance/QA branch by making `/atmosphere` the canonical public station route while leaving `/wellness/atmosphere` as a compatibility redirect, updating navigation/homepage/wellness links, and registering Media Session metadata plus play/pause/stop/previous/next handlers for supported mobile notification and lock-screen controls.
- Scoped the Generative.fm-inspired browsing polish to `/atmosphere`: listener-facing station categories now render as swipe/scroll rails with deterministic organic-geometric SVG station artwork, while `/browse` remains the compatibility grid/workbench for the same station runtime.
- Started the sitewide audio player toolbar follow-up after Chimer mobile testing showed the floating player could cover timer size controls. The audio player now renders as a full-width toolbar on the edge opposite the user's app-bar position preference, Account settings exposes top/bottom app-bar placement, regular pages reserve scroll space for the player, Chimer immersive controls inherit player-aware offsets, and the existing Atmosphere cross-route browser test now asserts the toolbar placement. A light Generative.fm codebase recon found useful future ideas in Alex Bainter's older web-provider cache/offline strategy, but no wholesale dependency adoption because the public player app is stale, mp3-only, and built around older Tone/Transport assumptions.
- Continued the sitewide toolbar/navigation branch by moving the public station browser to `/music`, removing the old `/atmosphere` and `/wellness/atmosphere` page routes instead of keeping redirects, adding `/clock` as a standalone Chimer clock-mode route, adding a top-bar clock shortcut beside the calendar button, and making Atmosphere a sidebar category with Music and Clock beneath it.
- Added the [CI Build Cache plan](superpowers/plans/2026-06-19-ci-build-cache.md) and wired GitHub Actions to persist Next.js `.next/cache` with a pinned `actions/cache@v4` step. The existing `actions/setup-node` npm cache remains in place, the cache key refreshes on lockfile/config/source/schema changes while falling back by lockfile, stale pull-request runs are canceled when newer commits arrive, CI avoids a duplicate Prisma generate by building through `npm run build:next` after the explicit generate step, and Playwright browser installation remains explicit instead of cached because Linux dependency installation still has to run.

### 2026-06-18

- Added the [Atmosphere R2 Sample Hosting plan](superpowers/plans/2026-06-18-atmosphere-r2-sample-hosting.md), public-media R2 upload helper, and CLI commands for the first Observable Streams VSCO adaptation. The workflow targets `massagelab-public-media`, keeps raw audio outside Git, connects `https://media.massagelab.app` with browser-read CORS, uploads the 24-WAV hosted sample-index payload plus manifest, keeps `massagelab-anatomy-media` and `massagelab-private-media` out of the public Atmosphere sample path, and splits cache behavior so WAV samples stay immutable while generated JSON metadata revalidates quickly. Observable Streams remains disabled until a follow-up branch wires the Generative.fm adapter to the hosted index and verifies playback.
- Added the [Atmosphere Generative.fm Adapter plan](superpowers/plans/2026-06-18-atmosphere-generative-fm-adapter.md) and wired Observable Streams as the first playable hosted Generative.fm station. The branch adds hosted sample-index validation, a browser-only Generative.fm runtime adapter, persistent-player volume integration, station catalog playability, Next/Turbopack package aliases for the older Tone/Generative.fm package shape, focused tests, and browser smoke coverage while keeping raw WAV files outside Git. The temporary Observable Streams browser smoke reached `Playing` after the package prerender step; hosted prerendered rendered-instrument samples remain the likely next performance follow-up.
- Added the [Atmosphere Prerendered Samples plan](superpowers/plans/2026-06-18-atmosphere-prerendered-samples.md), a reproducible PCM WAV renderer, R2 upload support for `--include-rendered`, and focused tests. The public-media upload now publishes the existing 24 source WAVs plus 30 rendered Observable Streams WAVs and refreshes `sample-index.json` so `observable-streams__vsco2-piano-mf`, `observable-streams__vsco2-violin-arcvib`, and `observable-streams__sso-cor-anglais` are available to the runtime before it falls back to source keys. Verification confirmed the hosted index returns those rendered keys with 16/8/6 notes and that a rendered piano WAV supports browser CORS plus range reads.
- Added the [Atmosphere Wellness Surface plan](superpowers/plans/2026-06-18-atmosphere-wellness-surface.md) and exposed the hosted station UI at `/wellness/atmosphere` as a public Wellness tool while keeping `/browse` as a compatibility workbench. The branch adds Wellness page, sidebar, homepage, and onboarding shortcut discovery for Atmosphere, keeps playback route-persistent through the existing global mini-player, adds a first public Calmness-style breathing guide beside the station controls, and expands the station catalog to the full 57-piece Alex Bainter Generative.fm package collection. The current hosted rendered sample index enables Observable Streams and keeps the remaining 56 pieces visible but disabled with exact missing package-compatible sample-group reasons until additional samples are hosted and verified.
- Added the [Atmosphere Generative.fm Sample Coverage plan](superpowers/plans/2026-06-18-atmosphere-generative-fm-sample-coverage.md), reusable catalog-wide sample coverage helper, and `npm run atmosphere:samples:coverage` CLI. The local `C:\Users\derri\code\audio` scan confirms CC0 evidence for VSCO 2 Community Edition, Versilian Community Sample Library, and selected Signature Sounds packs, keeps raw SSO samples blocked, maps configured SSO roles to CC0 adaptations (`sso-cor-anglais` to VSCO sustained oboe, `sso-chorus-female` to Signature Sounds children choir ambience, and `sso-chorus-male` to Signature Sounds men-of-choirs WAVs), maps `waves` to Signature Sounds beach ambience WAVs, classifies 38 currently disabled pieces as local-source candidates that still need rendered upload/index/browser verification, and flags 18 pieces for replacement or separate source/licensing review. Other downloaded Signature Sounds folders are now documented as useful future candidates for custom generators and palette refreshes, not current station coverage.
- Added the [Atmosphere Rendered Sample Planner plan](superpowers/plans/2026-06-18-atmosphere-rendered-sample-planner.md), package-derived first-batch render/source metadata, focused tests, `npm run atmosphere:samples:render-plan`, and `npm run atmosphere:samples:generative:r2:upload`. The first follow-up batch keeps additional stations disabled while planning `aisatsana` as a `vsco2-piano-mf` source-index station, `at-sunrise` as eight rendered vibraphone notes, and `little-bells` as ten rendered glock notes. The real local dry run against `C:\Users\derri\code\audio` reports 64 planned R2 objects across the three pieces, including source indexes and rendered WAV objects, with no R2 upload or station enablement. FFprobe and SOX recognized a temporary rendered At Sunrise C3 WAV as 16-bit signed PCM WAV, 44.1 kHz stereo, 20.58 seconds. The next branch should publish package-compatible sample indexes/manifests, browser-smoke each station, and only then mark new sample groups hosted.
- Added the [Atmosphere First Batch Hosting plan](superpowers/plans/2026-06-18-atmosphere-first-batch-hosting.md), uploaded the first three additional Generative.fm station indexes/manifests and WAV payloads to `massagelab-public-media`, and wired piece-scoped hosted metadata for `aisatsana`, `at-sunrise`, and `little-bells`. The upload published 64 R2 objects, approximately 140.6 MB of WAV payload, and verified hosted JSON plus WAV range/CORS behavior at `https://media.massagelab.app/atmosphere/generative-fm/{aisatsana,at-sunrise,little-bells}/`. `/wellness/atmosphere` now has four playable Generative.fm stations and 53 visible sample-pending catalog entries; shared source groups such as `vsco2-piano-mf` remain piece-scoped so other piano-backed packages do not become playable without their own verified index/browser smoke.
- Added the [Atmosphere Second Batch Hosting plan](superpowers/plans/2026-06-18-atmosphere-second-batch-hosting.md), uploaded four more VSCO piano source-index station payloads to `massagelab-public-media`, and wired piece-scoped hosted metadata for `day-dream`, `eno-machine`, `impact`, and `lemniscate`. The upload published 100 R2 objects, approximately 344.6 MB of WAV payload, and verified hosted JSON plus WAV range/CORS behavior at `https://media.massagelab.app/atmosphere/generative-fm/{day-dream,eno-machine,impact,lemniscate}/`. `/wellness/atmosphere` now has eight playable Generative.fm stations and 49 visible sample-pending catalog entries; the coverage scan classifies the remaining catalog as 31 local-source candidates and 18 replacement/source-review pieces.
- Added the [Atmosphere Third Batch Listener Copy plan](superpowers/plans/2026-06-18-atmosphere-third-batch-listener-copy.md), uploaded three more direct-piano source-index station payloads to `massagelab-public-media`, and wired piece-scoped hosted metadata for `pinwheels`, `sevenths`, and `uun`. The upload published 75 R2 objects, approximately 258.5 MB of WAV payload, and verified hosted JSON plus WAV range/CORS behavior at `https://media.massagelab.app/atmosphere/generative-fm/{pinwheels,sevenths,uun}/`. `/wellness/atmosphere` now has eleven playable Generative.fm stations and 46 visible sample-pending catalog entries; station cards now use listener-facing sound descriptions instead of backend hosting/sample-index copy, while exact missing package-compatible groups remain in runtime metadata for planning. The coverage scan classifies the remaining catalog as 28 local-source candidates and 18 replacement/source-review pieces.
- Added the [Atmosphere Startup Performance plan](superpowers/plans/2026-06-18-atmosphere-startup-performance.md) and implemented metadata/module prewarm for playable Generative.fm stations. The branch lets hosted sample-index requests honor browser cache behavior, caches shared Tone/provider/library imports and per-station prepared runtime promises, prewarms enabled stations during idle time plus station hover/focus, keeps Tone startup and WAV loading inside user-initiated playback, and emits a `massagelab:atmosphere-startup-timing` browser event for future first-play performance comparisons. Audio format conversion, CI build caching, and new station enablement remain separate follow-up branches.
- Added the [Atmosphere Web Audio Format Pilot plan](superpowers/plans/2026-06-18-atmosphere-web-audio-format-pilot.md), an FFmpeg-backed Opus sidecar upload workflow, and browser runtime selection for Opus-capable clients. The Observable Streams upload published 54 Ogg Opus audio payloads plus `sample-index.opus.json` and `manifest.opus.json` under `massagelab-public-media`, reducing the represented payload from 172.1 MB of WAV to 10.7 MB of Opus while preserving the existing WAV objects and sample index as fallback. Header verification confirmed JSON/CORS metadata and representative Opus range reads; startup timing now includes the selected `sampleFormat`.
- Added the [Atmosphere Hosted Opus Sidecars plan](superpowers/plans/2026-06-18-atmosphere-hosted-opus-sidecars.md), a hosted Generative.fm Opus upload workflow, and Opus metadata for the ten non-Observable playable stations. The upload published 219 Ogg Opus audio payloads plus 20 sidecar metadata objects under each hosted piece prefix, reducing the represented batch payload from 743.7 MB of WAV to 31.3 MB of Opus while preserving existing WAV fallback indexes. Header verification confirmed all ten Opus sample indexes and representative source/rendered range reads.

### 2026-06-17

- Added the [Legal Trust Pages implementation plan](superpowers/plans/2026-06-17-legal-trust-pages.md), based on the approved Branch 1 legal decisions: adult accounts only, functional-only cookies, current Stripe membership billing/refund terms, local-first professional-record boundaries, client-owned wellness separation, and future branches for account deletion/export, provider payments, AI/transcription, research consent, and hosted clinical storage.
- Implemented the legal trust page branch: `/legal` and `/legal/[slug]` now render Terms of Service, Privacy Policy, Membership/Billing/Refund Terms, Therapist Agreement, Functional Cookie Notice, and Local-First Health and Wellness Data Notice from one versioned registry; `LegalAcceptance` stores user id, document key/version, timestamp, IP, and user agent where available; registration requires Terms and Privacy; paid checkout requires membership billing/refund terms before Stripe Checkout; and professional/practice credential requests require the Therapist Agreement.
- Added the [Anatomy Media Review Filters implementation plan](superpowers/plans/2026-06-17-anatomy-media-review-filters.md) and implemented URL-driven anatomy media review batches: status tabs, anatomy-area presets, cleanup/problem presets, advanced filters, active chips, and decision redirects that keep admins inside the current filtered batch.
- Added the hidden Atmosphere audio runtime spike: `/browse` now hosts a public workbench for MassageLab-owned audio playback, the global music provider survives client-side route navigation, the bottom mini-player controls active audio, the first audible station uses a local Tone.js proof generator, and the Generative.fm Observable Streams package remains a disabled probe until sample-index and sample-hosting rights are verified.
- Added the [Atmosphere Sample Asset Intake implementation plan](superpowers/plans/2026-06-17-atmosphere-sample-asset-intake.md), repeatable sample scanner, and Observable Streams evidence report: the local `C:\Users\derri\code\audio` scan found CC0 VSCO piano, violin, and sustained-oboe candidate files. The staging script now dry-runs a 24-WAV VSCO sample-index plan without committing raw audio. SSO `sso-cor-anglais` raw samples are excluded because Sampling Plus licensing is not a clean hosted-browser fit, so the first Observable Streams path maps that package role to a CC0 VSCO oboe adaptation and remains disabled until the generated sample index is intentionally hosted and wired.

### 2026-06-16

- Added the [Public Client Wellness Tools implementation plan](superpowers/plans/2026-06-16-public-client-wellness-tools.md), sequencing client-owned wellness records, public practice logging, first-slice ROM device-orientation/manual measurement, body/sensation tracking, calendar reminders, gamified engagement, voice, and future sensor integrations while keeping therapist professional records local-first and therapist sharing behind a future consent bridge.
- Implemented the client wellness and ROM foundation branch: added owner-scoped `ClientWellnessEntry`, `ClientWellnessPreference`, and `ClientWellnessVocabularySuggestion` models; `/wellness` public practice mode; signed-in quick-log and ROM saving; manual and device-orientation ROM capture; timeline export/delete; wellness navigation; account-preference leak guards; and source tests that keep wellness data out of calendar, notification, Sentry, and professional-record-vault paths.
- Added the [Body Sensation Tracker implementation plan](superpowers/plans/2026-06-16-body-sensation-tracker.md) and implemented the first body-sensation branch: `/wellness` now has a 2D body-region selector, richer body-sensation fields for duration/position/movement effect/activity context, private signed-in custom sensation suggestions, and timeline filters by category, body region, and sensation/context while keeping therapist sharing and global vocabulary review deferred.
- Added the [Client Calendar Reminders implementation plan](superpowers/plans/2026-06-16-client-calendar-reminders.md) and implemented the first calendar companion branch: signed-in `/wellness` users can see their own appointment summaries through `PracticeClient.userId` and save generic wellness reminder schedules in `ClientWellnessPreference.settings.reminderSchedules`, while anonymous visitors can practice reminder setup in memory only and no practice calendar reminder, notification, audit, Sentry, or professional-record path is added.
- Added the [Wellness Patterns And Reports implementation plan](superpowers/plans/2026-06-16-wellness-patterns-and-reports.md) and implemented the first pattern-report branch: `/wellness` now builds browser-local aggregate reports from already-loaded self-tracking entries, shows weekly counts, trend rows, confidence-labeled reflection prompts, repeated-ROM reference changes, and browser JSON downloads without adding server actions, calendar payloads, analytics, delivery, therapist sharing, or professional-record paths.

### 2026-06-15

- Implemented Anatomime's Room + Game Run shared classroom model: reusable join codes, Host Judged, Device Typed, and Device Multiple Choice modes, 4-term team turns, possible steals, signed-in first-correct progress credit, same-code next games, host transfer, ranked-choice host takeover, and 30-minute review-window expiry.
- Refreshed the public homepage into a product-proof entry point: the hero now communicates multi-role usefulness with a restrained Flip Words-style role accent, anonymous visitors get an optional action router without saved onboarding data, account value is framed around safe preferences/progress/profile defaults, and the bottom of the page lists the available tools with current routes and availability labels.

### 2026-06-13

- Added Anatomime Learning Surfaces V1: completed turns now produce a local teach-back review with got/missed/skipped outcomes, anatomy definitions, aliases, and per-term metadata without changing the existing team timer or sourced anatomy adapter.
- Added a final Anatomime recap that aggregates terms across completed turns and sorts missed or skipped terms first so a class can quickly review weak spots before replaying.
- Added tested helper functions for turn review generation, outcome totals, and final recap ordering; validated with `node --test tests/anatomime-game.test.mjs`, `npm run typecheck`, `npm run lint`, `npm run build`, and Playwright desktop/mobile verification of `/anatomime`.
- Added the branch plan at [Anatomime shared sessions](superpowers/plans/2026-06-13-anatomime-shared-sessions.md), covering sourced setup filters, shared game codes, scoring, progress hooks, and optional realtime updates.
- Added shared Anatomime session persistence and API routes for host creation, session lookup, player join, host start, guesses, and realtime-token requests.
- Expanded Anatomime setup to all sourced anatomy study categories/regions with fixed 4-term rounds, configurable round count, difficulty, typed or multiple-choice shared answers, host lobby controls, player join/play routes, and polling fallback when realtime is unavailable.
- Added `anatomime_name_recall` as a flashcard-compatible prompt mode so signed-in correct shared-session guesses update existing flashcard progress and Anatomime achievements without storing raw typed answers.
- Applied `20260613160000_anatomime_shared_sessions` with `npm run prisma:migrate:deploy`; validated with focused game/shared tests, Prisma generate/validate, typecheck, lint, full unit tests, production build, and a Playwright smoke of session create, player join, and host start against a fresh local Next server.
- Addressed PR review findings for shared-session reliability: player tokens no longer appear in URLs, route errors are sanitized, host/player fetches recover from network failures, Ably publish failures are bounded, signed-in rejoins reuse one player row, session expiry/advance paths use conditional transitions, name-recall prompts are cached, and the follow-up `20260613210000_anatomime_player_user_role_unique` migration is applied.

### 2026-06-12

- Started `codex/anatomy-image-review-queue` from refreshed `main` after merging PR 55 to make image QA faster than the full anatomy browser on a phone.
- Added the branch plan at [Anatomy image review queue](superpowers/plans/2026-06-12-anatomy-image-review-queue.md), scoping a compact admin dashboard, dedicated image review route, and queue decisions that reuse item-image link review state plus desired-view requests.
- Added a mobile-first `/admin/anatomy/media-review` queue with one large image card at a time, status tabs, skip/open/source actions, approve/reject controls, and a "needs better view" flow that creates an open media view request.
- Added a separate `/admin` dashboard with image-review counts and entry points while keeping `/admin/anatomy` as the full anatomy browser for detailed editing.
- Started `codex/anatomy-media-view-coverage` from `main` to add superior/inferior BodyParts3D candidate coverage and desired-view requests for transverse/custom or wider-context replacement images.
- Added the `AnatomyMediaViewRequest` migration and admin request workflow so anatomy admins can record the view needed, reason, notes, optional BodyParts3D URL, and request status without making unreviewed images public.
- Added view coverage chips, the `too_tight` review reason, needs-review default behavior for new BodyParts3D imports, an open-request report command, and an idempotent superior/inferior coverage import script with an optional import limit.
- Updated `AGENTS.md` so future changed/new non-obvious code should include useful docstrings or comments, while leaving the repo-wide docstring pass for a later branch.
- Applied `20260612110000_anatomy_media_view_requests` with `npm run prisma:migrate:deploy`, then ran the superior/inferior BodyParts3D coverage import; the audit command now reports no missing eligible superior/inferior candidate links.
- Expanded the full anatomy browser toward textbook-style navigation with split entity tabs, 11 organ-system tabs, and a separate tissue-type row for epithelial, connective, muscle, and nervous tissue. Seeded `belongs_to_system` and `belongs_to_tissue_type` relationships across modeled anatomy items without adding taxonomy tables, kept musculoskeletal/sensory as practical concepts rather than body-system tabs, and reran `npm run anatomy:seed` so the configured database has the new taxonomy memberships.
- Expanded the code-backed taxonomy coverage for body-system and tissue-type browsing with additional textbook-level structures and concepts: heart chambers and blood, bronchioles, digestive tract segments, hypothalamus and pineal gland, nephron substructures, reproductive accessory/external structures, respiratory muscle cross-system membership, endocrine cross-membership for pancreas/ovary/testis, and core tissue subtype/tissue membrane concepts. Tightened broad relationship inference so stale false positives such as thyroid cartilage under endocrine, lymphatic vessels under cardiovascular, and pectoral fascia under digestive are cleaned on seed rerun.

### 2026-06-11

- Started `codex/anatomy-media-review` from `main` to turn the anatomy admin item detail view into a practical visual review surface for flashcard image accuracy.
- Added link-level media review state to `AnatomyMediaEntity`: approved/needs-review/rejected status, reason, note, display priority, and reviewer metadata.
- Updated sourced flashcard media loading so rejected or needs-review item-image links are hidden from public prompts, while approved replacement views can produce additional media-identification prompts.
- Added an anatomy admin media review panel with image previews, linked-image review controls, existing candidate approval, and BodyParts3D still-image import to R2 with source/license citation rows.
- Added an admin-only flashcard image flag flow so bad match or bad view flags reject the current item-image link and remove that prompt from the active study run.
- Applied the pending flashcard and anatomy media review migrations to the configured Neon/Postgres database, then applied the follow-up `20260611143000_anatomy_media_review_default_needs_review` migration so new item-image links default to needs review; `npx prisma migrate status` reports the schema is up to date.
- Addressed PR review findings for BodyParts3D URL override sanitization, tree-aware asset identity, transactional import writes, role-specific media review rows, optional R2 public delivery URLs, flashcard flag inventory cleanup, and media override rebuild behavior.
- Added and applied the follow-up `20260611153000_anatomy_media_review_backfill_needs_review` migration so item-image links populated by the earlier approved default are moved into the review queue.
- Addressed mobile anatomy-browser annotations by removing redundant mobile framing, compacting the sticky search toolbar, moving citations and external IDs to collapsed bottom evidence sections, and clarifying link review, existing-image linking, and BodyParts3D import language.
- Updated focused tests for BodyParts3D URL normalization, link-level prompt filtering, admin media review UI, study media overrides, and flashcard image flagging.

### 2026-06-10

- Started `codex/flashcard-mastery-rounds` from refreshed `main` to make signed-in flashcard mastery repeatable across the full sourced prompt library.
- Planned Flashcard Mastery Rounds V1: completion requires mastering every sourced prompt in the current round, awards a repeatable round snapshot badge, resets current-round prompt counts, and preserves lifetime attempt/correct/incorrect totals for long-term progress.
- Implemented the mastery-round V1 mechanics with lifetime progress metadata, a signed-in next-round API action, round-aware progress dashboard copy, and focused progress/browser fixture coverage.

### 2026-06-08

- Started `codex/flashcard-runner-polish` with a focused handoff plan for making the active study runner feel like a physical flashcard workflow without changing flashcard persistence, sourced anatomy facts, achievements, voice support, or 3D runtime scope.
- Polished the flashcard runner card surface with clearer front/back treatment, review-mode card-click/keyboard reveal affordance, practice-only status labeling, tighter progress badges, and nearby controls for previous/next/check/finish actions.
- Updated browser route coverage so review-mode fallback study verifies the card-surface flip interaction, sourced answer reveal, and Correct/Missed marking path on desktop and mobile.
- Addressed flashcard annotation feedback by loading public deck views into setup before study, compacting the runner header, adding a two-card sortable community deck carousel, allowing review cards to flip back to the prompt side, scrolling started sessions to the card, and restricting flashcard image prompts to reviewed uploaded BodyParts3D media without answer-revealing front captions.

### 2026-06-07

- Added Flashcard Mastery Progress V1: signed-in flashcard sessions now accumulate per-prompt attempt/correct/incorrect counts in `LearningProgress` metadata, treat 10 correct answers as mastery, and expose a signed-in progress dashboard with mastered/active prompt totals, aggregate accuracy, sessions, badges, best time, and recent prompt progress.
- Added a signed-in setup option to skip mastered prompts when starting a deck. The server filters mastered prompt ids before creating the study session and returns a clear exhausted-deck response if every selected prompt is already mastered.
- Added flashcard session duration storage on `FlashcardStudySession`, kept completion idempotent, and continued storing only prompt ids and result summaries rather than raw typed answers.

### 2026-06-04

- Reworked `/education/flashcards` into a setup-first deck builder: users select category, region, depth, deck size, answer mode, and prompt modes before any card appears.
- Added sourced flashcard prompt generation over the anatomy foundation for media identification, name-to-summary, name-to-region, name-to-category, and muscle origin/insertion/action/innervation; strict typed checking normalizes case, whitespace, punctuation, and hyphens without fuzzy typo matching.
- Added public deck browsing and starter deck templates. Anonymous users can browse public decks, build temporary decks, and study without saved progress; signed-in users can save public/private deck templates, start persisted study sessions, store aggregate prompt progress, and earn flashcard achievements.
- Added the `FlashcardDeck` and `FlashcardStudySession` Prisma models plus API routes for public deck listing/detail, signed-in deck save/update, signed-in session start, and signed-in session completion without storing raw typed answers.
- Implemented the sourced anatomy flashcards and Education V1 pass: public `/education` and `/education/flashcards` now expose self-study flashcards with category, region, depth, deck size, shuffle/reset, previous/next, answer reveal, aliases, reviewed media when available, and source attribution.
- Added a sourced study adapter over `ANATOMY_FOUNDATION_SEED` for flashcards, Anatomime, and future education tools. Public study cards are restricted to reviewed facts backed by open-reuse or commercial-safe sources, and public attribution is limited to reviewed answer/media sources.
- Completed the legacy Anatomime coverage pass for all 333 archived `lib/anatomy-legacy.js` rows. The audit reports every old row as sourced or mapped; broad groups such as skull, cuneiforms, vertebral column, rib groups, trapezius, quadriceps femoris, erector spinae, and multifidus are represented as sourced group entities, while `interfoveolar` is intentionally preserved as a ligament/structure rather than a muscle.
- Archived the old hand-entered Anatomy/Anatomime data as reference-only `lib/anatomy-legacy.js`, restored `lib/anatomy.js` as a compatibility wrapper over the sourced adapter, and removed the old generic `AnatomyTerm` seeding path from `prisma/seed.ts`.
- Made the Education sidebar group visible with Flashcards and updated tests for legacy coverage, sourced card generation, Anatomime compatibility, foundation section assembly, navigation, and public route smoke coverage.

### 2026-06-03

- Added the next-branch handoff plan at [Intake to SOAP continuity](superpowers/plans/2026-06-03-intake-to-soap-continuity.md), clarifying that initial intake and follow-up intake are separate local-first workflows before either rolls into SOAP.
- Recorded that follow-up intake should start from an existing local client, avoid re-asking stable basic information, collect appointment-relevant changes and safety updates, and seed SOAP only through a therapist-reviewed local action inside the encrypted professional-record vault.
- Implemented the intake-to-SOAP continuity branch: added a built-in follow-up intake template, normalized intake workflow types on local templates/responses/documents, added a pure intake-to-SOAP seed helper with explicit append/replace behavior, and wired the intake dashboard to preview saved local documents and seed SOAP drafts without URLs, APIs, Prisma, account preferences, or hosted clinical sync.
- Added the member-supported voice notes roadmap signal plan at [Member-supported voice notes signals](superpowers/plans/2026-06-03-member-supported-voice-notes-signals.md), capturing voice transcription, intake conversation support, and therapist-reviewed SOAP assistance as future goals rather than active hosted PHI features.
- Updated `/notes`, pricing, roadmap, billing, and privacy docs so membership support is framed as funding legal, security, BAA, audit, consent, and managed-sync groundwork without promising that transcription or cloud SOAP drafting is available today.
- Recorded that voice transcription feasibility should wait for a later task and that the next branch should be more functional/visual product work, with anatomy flashcards/study UI, richer Anatomime learning surfaces, or trust/public identity pages as better near-term candidates.

### 2026-06-01

- Added the next-branch handoff plan for role-aware module surfaces and sidebar IA, capturing anonymous, signed-in, student, client, therapist, Team/Practice, practice-role, and admin navigation expectations without inventing full role dashboards yet.
- Implemented the structural role-aware navigation resolver: sidebar groups, account menu entries, anatomy admin links, and calendar shortcuts now resolve from route metadata using auth state, verified account roles, feature keys, and practice roles. Direct professional-record routes remain absent from sidebar navigation, and `/notes` keeps its preview/gated local-first behavior.

### 2026-05-30

- Implemented the shared privacy-first professional-record framework: SOAP, intake, journal, and ROM now use one passphrase-unlocked encrypted browser vault under `massagelab-professional-record-vault-v1`.
- Added encrypted full-vault `.mlab` bundle export/import, one-time migration from legacy plaintext localStorage draft keys, non-destructive handling for malformed legacy drafts and older encrypted intake vaults, and source guards that block clinical upload paths.
- Removed plaintext JSON/TXT/research transfer controls from therapist documentation routes; DOC/PDF output remains available only after an explicit plaintext-output warning.
- Added the implementation plan at [Privacy-first records framework](superpowers/plans/2026-05-30-privacy-first-records-framework.md).

### 2026-05-29

- Added the privacy-first data architecture wiki to separate account/contact/booking data, future client-owned wellness records, therapist professional-record vault data, sharing consent, and intentional professional-record references.
- Recorded that cloud wellness storage is a future privacy-controlled domain, while therapist professional records remain local-first and should move toward an offline passphrase-unlocked encrypted vault before broader local documentation expansion.
- Documented that encryption alone does not remove BAA obligations for cloud providers maintaining ePHI on behalf of a covered entity or business associate, and added current HHS, FTC, Vercel, and Cloudflare source links for future compliance planning.
- Implemented the encrypted in-browser intake vault: therapists must create or unlock a local passphrase vault before using `/notes/intake`, saved intake workspaces are stored as AES-GCM ciphertext, existing plaintext workspaces can be migrated in place, and browser QA verifies the clinical sentinel is not stored in plaintext.
- Added the `therapist_documentation_tools` entitlement gate for therapist note-taking surfaces: `/notes` still shows SOAP, intake, journal, and ROM tools, while direct tool access renders a membership-required gate unless the session has the feature-backed local clinical tools capability.

### 2026-05-28

- Implemented the local-first intake form-builder workspace direction: built-in full-intake and pain/discomfort-map templates, therapist-created local templates, tablet fill mode, local client/document dashboards, native JSON/DOC/print-PDF exports, and optional encrypted `.mlab` workspace bundles.
- Documented that pre-arrival remote/client account behavior remains limited to contact/profile and booking status until hosted PHI storage passes the compliance gates; clinical intake responses, signatures, and pain maps stay local-first.

### 2026-05-27

- Added `docs/project-state.md` as the read-first current-state source of truth and clarified that `docs/project-log.md` is the chronological progress and decision log.
- Added `AGENTS.md` so future agents read project state first, preserve local-first PHI boundaries, avoid secrets/row-level database details in docs, and use feature-key entitlement checks.
- Added `docs/superpowers/plans/2026-05-27-project-source-of-truth-consolidation.md` for implementation tracking and updated README, wiki index, TODO, and roadmap references to the same documentation convention.
- Added `docs/superpowers/plans/2026-05-27-local-first-client-intake-forms.md` and moved the active product direction from anatomy expansion to tablet-friendly, local-first client forms.
- Marked anatomy 3D/spatial runtime tooling as deferred because it is a later feature layer, while the current alpha needs more useful client and therapist form workflows.
- Verified the Prisma schema and configured database state during consolidation: 19 migrations are present and the configured Neon/Postgres schema was reported up to date.
- Recorded the latest anatomy database direction: 3D/spatial body-map tables, entity maps, movement visualization records, and joint/movement/ROM integrity constraints are now part of the anatomy foundation.
- Reconciled dependency notes with the May 27 security review: no high or critical advisories are documented, with remaining Next/PostCSS, Nodemailer, and dependency-level findings tracked as accepted residual risk.

### 2026-05-21

- Completed `codex/sitewide-visual-system-polish`: added shared app surface primitives, wired account settings to the shared source of truth, migrated public/auth/account/documentation/booking/calendar-management/admin/local-first documentation route surfaces, and removed stale global page/card utility classes while preserving route-owned Chimer, Anatomime, and dense calendar workspace visuals.
- Extended the visual-system pass with Intent-inspired sitewide sidebar/topbar polish: the sidebar now uses a tighter docked rail with icon tooltips, calendar action menus, footer account controls, and request/waitlist badges; the mini calendar moved into a global topbar popover that respects sidebar side preferences and hides during Chimer immersive states.

### 2026-05-20

- Completed `codex/calendar-booking-settings-plan`: added booking policy storage, provider booking policy/capacity rules, service primary/add-on roles, public booking pressure selection, deterministic sequential service/add-on slot solving, team sequencing, waitlist entries and conversion, public location distance notices, and `/calendar/booking`.
- Replaced the planned fake-it/scarcity idea with provider capacity protection: total daily/weekly massage minutes, pressure-level budgets from 1-5, provider rest gaps, and server-side revalidation before booking writes.
- Completed the CR review hardening pass for booking v1: transactional provider policy/capacity rechecks, atomic waitlist conversion, practice-timezone waitlist conversion, practice-local booking horizons, public-provider empty-state handling, add-on cap consistency, and multi-service migration backfill are now recorded as landed constraints.
- Closed the public sequence-generation follow-up: `/book/[practiceSlug]` no longer precomputes every public service/add-on/pressure/provider combination server-side, and selected descriptors load options on demand through the deterministic solver with short-TTL caching while submit actions still revalidate before writes.
- Completed `codex/calendar-booking-followups`: `/calendar/booking` is tabbed, providers can see/copy/share public booking links, optional state-prefixed branded booking URLs route to the same renderer, and anonymous clients can book or waitlist unless practice/provider policy requires an account.
- Reworked the public booking UI into a single-route wizard with Services, Details, and Time steps; single-provider preference selectors are hidden, sign-in/register prompts are optional for guests, and time selection uses a responsive weekly availability grid with directly clickable start slots.
- Hardened follow-up review findings: sequence-option API failures return a generic client message while logging server-side, public booking load errors are logged, account-required fallback only appears for provider-eligibility shortfalls, share/copy failures are handled, login callback URLs reject backslashes, staff email is not used as public provider-label fallback, toggle controls expose `aria-pressed`, and brittle resource-booking test slicing now asserts markers first.

### 2026-05-18

- Completed `codex/calendar-creation-flows-plan`: added the hybrid calendar event model, dedicated creation routes for appointments, personal events, classes, and reminders, request review, calendar audit rows, internal notification intent records, and the calendar creation flow wiki.
- Added the calendar service catalog: service templates, variants, resources, resource bookings, appointment/class snapshots, public booking variant selection, service management routes, and free/basic vs Therapist/Team scheduling entitlement keys.
- Added the operator calendar workspace: FullCalendar grid views, provider view preferences, server-validated drag/resize rescheduling, advanced availability schedules/overrides, multi-service appointment composition, provider eligibility, and generic local-first clinical template references on services.
- Added the sitewide visual system/account-page polish pass to the P2 backlog so shared surface, inset, page shell, action, and status styles can become one cohesive source of truth.
- Completed alpha release readiness verification on `codex/alpha-release-readiness`: automated gate, browser route pass, Chimer timer and clock-only checks, PWA manifest, diagnostic route gates, local-first documentation network checks, account membership display, Stripe test checkout creation, signed local webhook processing, and Chimer custom-color entitlement.
- Fixed an anonymous Chimer preference-sync regression found during browser QA. Chimer now checks the client session before calling `/api/account/preferences`, preventing unsigned users from generating account preference 401 console errors.
- Confirmed `/debug-hydration` is absent, `/api/debug/sentry` stays disabled with `MASSAGELAB_ENABLE_SENTRY_TEST_ROUTE=false`, and the PWA manifest serves standalone metadata with 192/512 icons.
- Completed `codex/optimize-account-data-shell`: `/account` keeps its shell/navigation immediate while loading only the active tab's server data, account panel data is cached briefly and invalidated after relevant mutations, and sidebar calendar context now hydrates only on calendar/booking routes instead of running readiness checks from the global layout.
- Completed `codex/add-browser-qa-harness`: added Playwright browser QA, desktop/mobile public route smoke coverage, anonymous account sync request guards, PWA manifest/icon checks, local-first clinical document upload guards, and GitHub Actions CI wiring.
- Completed `codex/pwa-offline-strategy`: documented the alpha PWA boundary, refreshed install icons, and constrained service worker offline route caching to anonymous public tools while excluding account, auth, billing, calendar, booking, clinical sync, client, and `/api/*` surfaces.

### 2026-05-17

- Added this consolidated project log as the active planning and progress tracker.
- Consolidated open work from `TODO.md`, `docs/roadmap.md`, and the May 17 project review into Now / Next / Later sections.
- Preserved existing roadmap, audit, QA, TODO, and wiki files as source evidence instead of merging their full contents here.
- Recorded standing decisions for local-first PHI boundaries, feature-based entitlements, Sentry privacy limits, targeted refactors, Payload planning, anatomy data, calendar planning, and public SEO readiness.
- Noted the May 17 audit status: anonymous sync gating, Sentry scrubbing, brand asset caching, and mobile background reductions were completed after the audit; remaining audit backlog includes account data splitting, browser QA, PWA strategy, and public SEO launch work.

### 2026-05-15

- Billing and membership foundation work established feature-based entitlements, Stripe subscription records, student access rules, and `chimer_custom_colors` as the first paid feature gate.
- Dependency security notes documented no high or critical advisories, with remaining Next/PostCSS and Nodemailer/Auth.js findings tracked as dependency-watch items.

### 2026-05-08 to 2026-05-13

- Alpha refresh shipped Chimer display polish, sidebar/navigation IA, brand and PWA assets, Sentry monitoring with privacy scrubbing, and runtime/dependency hygiene.

## Source Evidence Index

- [Project state](project-state.md): read-first current state, database status, live surfaces, open priorities, and update rules.
- [TODO](../TODO.md): preserved checkbox list and source evidence for active and recently completed work.
- [Roadmap](roadmap.md): branch-ready and phased product direction.
- [Alpha QA](alpha-qa.md): private-alpha verification checklist.
- [May 17 project review](audits/2026-05-17-project-review.md): performance, Sentry, privacy, PWA, architecture, dependency, and remediation backlog review.
- [Wiki index](wiki/index.md): repo-backed operational documentation entrypoint.
- [Privacy-first data architecture](wiki/privacy-first-data-architecture.md): account/contact/booking, client wellness, therapist professional-record vault, sharing consent, and hosted PHI gate boundaries.
- [Release checklist](wiki/release-checklist.md): alpha release gate and manual focus areas.
- [Privacy and PHI posture](wiki/privacy-and-phi.md): local-first and hosted clinical sync boundaries.
- [PWA offline strategy](wiki/pwa-offline-strategy.md): install/offline behavior, public-tool route allowlist, and online-only exclusions.
- [Billing and memberships](wiki/billing-memberships.md): feature-based access and Stripe membership setup.
- [Deployment and environment](wiki/deployment.md): environment variables, Sentry limits, Stripe setup, and migration rules.
- [Dependency security notes](wiki/dependency-security.md): accepted residual dependency risk and audit commands.
