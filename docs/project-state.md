# MassageLab Project State

Verified: 2026-07-04

This is the read-first source of truth for MassageLab's current project state. Use it before `docs/project-log.md`, `docs/roadmap.md`, `TODO.md`, audits, or wiki pages when deciding what is active now.

## Current Snapshot

- Status: private alpha.
- Current invite-readiness focus: stabilize the repo and operating checklist for a first small invited-user cohort. The production Stripe readiness check passes from an explicit live env file, the early-access discount intentionally remains enabled for now, and a real live Supporter subscription smoke has verified email registration, Checkout completion, `chimer_custom_colors` entitlement access in Chimer, Customer Portal access, and subscription cancellation. Remaining invite-readiness follow-ups are the one-time donation checkout smoke, legal/trust review, and first-cohort production monitoring.
- Active branch at last verification: post-merge invite-readiness status update after registration and live Supporter subscription smoke.
- Current focus: public-alpha Education flashcards now use a setup-first deck builder over sourced prompt modes: media identification, name-to-summary, name-to-region, name-to-category, Anatomime name recall, and muscle origin/insertion/action/innervation. Anonymous users can browse public decks, create temporary browser decks, and study; signed-in users can save public/private deck templates, persist progress summaries, earn aggregate achievements, view per-prompt mastery totals, optionally skip prompts mastered after 10 correct answers, and complete repeatable full-library mastery rounds. Reveal-review remains practice-only and typed-check remains the saved progress path. Public image prompts use uploaded BodyParts3D media only when the asset is reviewed, reusable, and the item-image link is approved; rejected or needs-review links are hidden from prompt generation. Anatomy admins can use `/admin` as a compact dashboard for review counts and entry points, `/admin/anatomy` as the full anatomy browser for deep data work, and `/admin/anatomy/media-review` as a dedicated mobile-first image review queue for approving, rejecting, or requesting better BodyParts3D views for linked item images. The dedicated anatomy media review queue now supports URL-driven status, anatomy-area, problem, view, request, sort, and search filters with built-in anatomy and cleanup presets that preserve the active batch through approve/reject/request decisions. The full anatomy browser is being organized more like a textbook, with separated entity tabs, relationship-backed tabs for the 11 organ systems, expanded textbook-level major structures, and a separate tissue-type row for epithelial, connective, muscle, and nervous tissue plus their core tissue subtypes. Musculoskeletal and sensory remain practical/search concepts rather than body-system tabs. They can still see BodyParts3D view coverage, attach existing candidates, import curated still BodyParts3D views into R2 as needs-review candidates, capture desired-view requests for transverse/custom or wider-context replacements, and flag bad flashcard images directly from image prompts. Flashcards and Anatomime still use one reviewed sourced anatomy study adapter over `ANATOMY_FOUNDATION_SEED`; old Anatomime data remains archived reference only. Anatomime now keeps local per-turn learning reviews with got/missed/skipped outcomes, definitions, aliases, and a final recap that prioritizes terms to revisit. Anatomime shared classroom play now uses a reusable room code with separate game runs, host-judged and device-answer modes, 4-term team turns, active-team immediate scoring, possible steals from device answers, signed-in first-correct progress credit, same-code next games, review-window expiry, host transfer, ranked-choice host takeover, and host-facing QR/prefilled invite links. Realtime updates use Ably when configured and fall back to polling. 3D/spatial runtime tooling remains deferred. The local-first professional-record framework remains in place: one encrypted browser vault for SOAP, intake, journal, and ROM records, tablet intake workflow continuity, user-controlled encrypted vault transfer, and tested sidebar/module visibility by auth, role, entitlement, and practice-role context. Voice transcription and SOAP-assist are recorded as member-supported future goals only; no audio capture, transcription engine, LLM processing, or hosted PHI workflow is implemented.
- Product posture: clinical notes, intake forms, journals, therapist ROM sessions, transcripts, and other therapist professional-record workflows remain local-first. Therapist note-taking tools are visible but creating or viewing professional-record content requires the `therapist_documentation_tools` entitlement from an active Therapist or Team/Practice membership. SOAP, intake, journal, and therapist ROM now use one passphrase-unlocked encrypted browser vault before viewing or saving documents. Signed-in `/wellness` entries, reminder schedules, and pattern reports are client-owned self-tracking records in a separate consumer-health domain; anonymous users can practice logging, body-sensation tracking, ROM, reminder setup, and in-session pattern review in memory only. Hosted clinical sync remains gated and unimplemented until compliance requirements are met. The privacy architecture separates account/contact/booking data, client-owned wellness data, therapist professional records, and a future consent-based sharing bridge.
- Public `/roadmap` route: product-facing copy only. Internal operating state lives here and in `docs/project-log.md`.

## Database State

- Database stack: Prisma with Neon/Postgres.
- Last verified checks: focused Atmosphere node tests; Atmosphere playback-performance sample warmup tests; catalog coverage and render-plan scans; WAV dry runs and uploads for `no-refrain`, `transmission`, `trees`, the 25-piece expanded source-index rollout, and the remaining 18 Generative.fm pieces; Opus dry runs and uploads for the same pieces; AAC and MP3 uploads for Observable Streams plus all 56 Alex Bainter package stations; all 57 AAC/MP3 sample-index HTTP checks; representative AAC/MP3 range/CORS checks; all 18 remaining-generator WAV/Opus sample-index HTTP checks; representative new WAV/Opus range/CORS checks; CI workflow cache/concurrency shape inspection; `npm run prisma:validate`; `npm run prisma:generate`; `npm run build:next`; `npm run typecheck`; `npm run lint`; `npm run test`; `npm run build`; `npm run test:browser -- tests/browser/public-routes.spec.ts -g "Atmosphere lists" --project=desktop-chromium`; and `git diff --check` passed for the Atmosphere remaining-generators/playback-performance/CI-cache branches. The Atmosphere polish/QA branch passed `node --test tests/atmosphere-stations.test.mjs tests/atmosphere-station-groups.test.mjs tests/atmosphere-runtime-controller.test.mjs tests/navigation-model.test.mjs`, `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build`, `npm run test:browser -- tests/browser/public-routes.spec.ts -g "Atmosphere|Breathing" --project=desktop-chromium`, `git diff --check`, and an in-app browser rendered check for the standalone breathing route; the in-app browser does not complete Tone playback reliably, so Chromium Playwright remains the audio-route validation signal. The Atmosphere performance/audio QA follow-up branch additionally passed `node --test tests/atmosphere-stations.test.mjs tests/atmosphere-station-groups.test.mjs tests/atmosphere-runtime-controller.test.mjs tests/navigation-model.test.mjs tests/atmosphere-generative-fm-provider.test.mjs`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test`, `PLAYWRIGHT_PORT=3011 npm run test:browser -- tests/browser/public-routes.spec.ts -g "Atmosphere|Breathing" --project=desktop-chromium`, and `git diff --check` after the `/atmosphere` route and Media Session control changes. The sitewide audio player toolbar follow-up passed `node --test tests/app-settings.test.mjs tests/account-preferences.test.mjs tests/account-page-tabs.test.mjs`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test`, `PLAYWRIGHT_PORT=3011 npm run test:browser -- tests/browser/public-routes.spec.ts -g "Atmosphere proof station keeps global player state across client routes"`, `git diff --check`, and an in-app browser load of `http://localhost:3000/atmosphere`. The rendered-piano upload published 112 WAV-side public-media R2 objects and 112 Opus sidecar objects for three stations, representing 360.5 MB of WAV payload as 16.9 MB of Opus. The expanded source-index rollout published 703 WAV-side R2 objects and 703 Opus sidecar objects for 25 stations, representing 2,077.7 MB of WAV payload as 99.9 MB of Opus. The remaining-generator rollout published 704 WAV-side R2 objects and 704 Opus sidecar objects for 18 stations, representing 1,491.6 MB of WAV payload as 79.5 MB of Opus while keeping WAV fallback indexes available. The all-station AAC upload published 1,814 sidecar objects representing 4,845.6 MB of WAV payload as 248.0 MB of AAC, and the all-station MP3 upload published 1,814 sidecar objects representing the same WAV payload as 327.5 MB of MP3.
- Latest route/navigation validation: the `/music` station route, `/clock` standalone clock route, top-bar Music and Clock shortcuts, sidebar Atmosphere category, removed legacy station routes, PWA `/clock` allowlist, station attribution, and renamed browser-route coverage passed focused Node tests, `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`, focused Playwright public-route tests for `/music` and `/clock`, `git diff --check`, and in-app browser render checks for `http://localhost:3000/music` and `http://localhost:3000/clock`.
- Migration count at last verification: 35 migrations in the repo. Production migration status was verified up to date on 2026-06-25 after applying the provider-neutral external calendar sync migration, the external busy-block interval check migration, and the calendar-sync active-connection/pending-link hardening migration. The live checkout 500 root cause was the missing `LegalAcceptance` table expected by the deployed checkout route.
- Latest migration area: provider-neutral external calendar sync records for Google calendar connections, selected source calendars, imported busy blocks, outbound event links, sync run health, valid external busy-block intervals, one active Google connection per provider, and pending outbound sync error links before Google returns an event id.
- No hosted clinical database work is planned for the next forms branch; intake and note content should remain browser-local unless the documented compliance gates are met. Future client-owned wellness storage must remain separate from therapist professional records and therapist remote access until the consent/sharing bridge is reviewed and intentionally enabled.
- Do not document database rows, secrets, connection strings, or `.env.local` values in project docs.

## Website And Tool Surface

- Public app shell: refreshed role-spanning home page with optional anonymous tool router, available-tools catalog, `/tools` index, support, pricing with memberships plus one-time project support, public roadmap, about, legal/trust documents, login/register, password reset, and email verification. The responsive shell now defaults to a bottom-first main bar on phones, keeps the music player bottom-stacked above that bar, exposes Home/Music/Clock/quick-create/theme/Calendar/More controls, and uses role-aware quick actions plus rail-based homepage discovery while preserving the configurable drawer side and existing theme modes. Support now keeps human support messages in the user-controlled email flow while offering a privacy-safe Sentry diagnostic report that sends only predefined issue categories, coarse tool areas, safe route buckets, browser/display hints, and optional linked Sentry event ids.
- Role-aware navigation shell: sidebar groups, account menu entries, admin links, and calendar shortcut actions resolve through tested route metadata using anonymous/signed-in state, verified account roles, feature keys, and practice roles. This is structural IA only; it does not create full role dashboards for workflows that are still undefined.
- Chimer: treatment-room timer with custom colors gated by `chimer_custom_colors`; `/clock` exposes the same standalone clock mode as its own public route. Chimer and Clock now keep the original free MassageLaba Lamp moving gradient mounted through its original component path as the default, with a free static gradient fallback option. Selected visual backgrounds mount only while Clock, a running Chimer session, or an approved music visualizer is active; the Chimer setup page keeps the saved selection and controls without rendering the selected effect behind the setup content. The premium visual-background plumbing now includes Aceternity UI Aurora background, Dotted glow background, Sparkles, Gradient animation, Background beams, Background beams with collision, Background lines, Glowing stars, Meteors, Shooting stars and stars background, Canvas reveal dots, Spotlight New, Lamp Section Header, Vortex background, Wavy background, 3D Globe, Unlumen UI Pixel liquid background, Unlumen UI Aurora Bars, Animate UI Bubble Background, Animate UI Gradient Background, Animate UI Stars Background, Animate UI Hole Background, MassageLab tile grid, MassageLab hex grid, Magic UI Retro Grid, Magic UI Light Rays, Chamaac UI Light Speed, Chamaac UI Electric Mist, Chamaac UI Astral Flow, Chamaac UI Deep Space Nebula, Chamaac UI Grid Bloom, Chamaac UI Liquid Chrome, Chamaac UI Waves, Chamaac UI Synthesis, Eldora UI Novatrix Background, Eldora UI Hacker Background, Eldora UI Photon Beam, React Bits Ferrofluid, and React Bits Lightfall as reviewed premium options, while the remaining Batch 1 draft candidates stay paused until they can be reviewed and added one at a time from source-matched implementations or explicitly approved MassageLab-owned effects.
- Business planner: `/tools/business-planner` is the public hub for student/therapist business planning tools. `/tools/business-planner/income` lets users estimate session prices, take-home goals, time off, workload capacity, and optional employee-wage comparisons; anonymous worksheets persist in browser storage, while signed-in worksheets persist as one current non-PHI business payload in `UserPreference.appSettings.businessIncomePlannerIncome` with no new scenario/history table. The student business-plan template also now has browser-local public tools for startup costs and break-even (`/tools/business-planner/break-even`), practice launch tasks (`/tools/business-planner/launch-checklist`), service menus and client policies (`/tools/business-planner/service-menu`), written business-plan outline notes (`/tools/business-planner/plan-outline`), and retail/add-on profit estimates (`/tools/business-planner/add-on-profit`). These new template tools remain browser-local and do not add Prisma storage or account sync.
- Atmosphere: `/music` is the public audio surface for MassageLab-hosted stations, and `/atmosphere` plus `/wellness/atmosphere` are intentionally not retained after moving Atmosphere to a navigation category. `/wellness/breathing` is the separate public Calmness-style breathing guide page linked from the Wellness hub. Atmosphere lists the full 57-piece Alex Bainter Generative.fm package catalog as MassageLab stations; current public-media sample indexes enable all 57 Generative.fm pieces, including Observable Streams and the remaining-generator rollout through Zed. No Generative.fm catalog entries remain sample-pending. The `/music` page uses listener-facing category rails with swipe/scroll navigation, deterministic organic-geometric SVG artwork per station, and optional premium visual backgrounds gated by `premium_backgrounds`; anonymous/free users fall back to the free MassageLab moving gradient or static gradient option, and Music stores its chosen background in `massagelab.music.background`. The station cards use sound descriptions rather than backend hosting/sample-index details, and the polish/QA branch groups stations by listener-facing feel while softening expectations for sparse, wave-like, or voice-like stations flagged during manual testing. It reuses the global music provider and sitewide audio toolbar so started stations can continue while users navigate to Chimer, flashcards, wellness tools, or other app routes. The audio player now renders as a full-width toolbar on the edge opposite the user's app-bar position preference, includes previous/next station controls plus restart/stop/volume, reserves scroll space on regular routes, and offsets Chimer immersive controls so timer size buttons are not covered. The global music provider also registers Media Session metadata and play/pause/stop/previous/next handlers so supported mobile browsers can expose notification or lock-screen controls for the active station. The playable Generative.fm runtime prewarms hosted sample-index metadata and shared browser modules for a small starter set during idle, escalates compressed payload warmup only for a tiny starter subset and deliberate hover on healthy connections, and wraps sample fetch/decode requests in bounded batches with per-AudioContext decoded-buffer reuse. Generative.fm station handoffs fade the outgoing output down and fade the incoming output up to soften abrupt first notes while keeping Tone start, Transport start, output nodes, and WAV fallback payloads inside user-initiated playback. Startup timing includes selected sample format, metadata reuse, retained payload-warmup diagnostic fields, and provider request/decode counts. All playable Generative.fm stations expose ordered Ogg Opus, AAC/M4A, and MP3 sidecar sample indexes for browser coverage, then keep WAV sample indexes as the final fallback. `/browse` remains a compatibility grid/workbench for the same station runtime while product discovery points to `/music`.
- Client wellness: `/wellness` is public for anonymous practice logging, 2D body-sensation tracking, practice ROM measurement, practice reminder setup, and in-session pattern review, with clear no-persistence prompts. Signed-in users can save owner-scoped quick logs, body-sensation entries, private custom sensation suggestions, ROM measurements, and generic wellness reminder schedules; view their own appointment summaries when linked through `PracticeClient.userId`; review non-diagnostic pattern reports with trend rows, confidence-labeled prompts, ROM references, and browser JSON downloads; filter their own timeline by category, body region, and sensation/context; export JSON; and soft-delete entries. Therapist viewing, 3D body maps, external reminder delivery, voice capture, passive detection, and sharing remain follow-up work.
- Local-first notes: SOAP notes, intake, journal, and ROM routes are visible from `/notes`, but professional-record viewing/creation is gated by `therapist_documentation_tools`. Once unlocked by membership, these routes require the local professional-record vault passphrase before showing clinical content. The shared encrypted browser vault stores SOAP drafts, intake workspaces, journal drafts, and ROM sessions under `massagelab-professional-record-vault-v1`; legacy plaintext route keys are migration inputs only. Intake now separates full initial forms from existing-client follow-up forms, and selected intake documents can seed a therapist-reviewed SOAP draft through an explicit append/replace action. `/notes` also shows friendly member-supported roadmap signals for future voice notes, intake conversation transcription, SOAP assistance, and managed sync; these are funding goals, not active hosted clinical features. Encrypted `.mlab` full-vault bundles are the normal transfer format, while DOC/PDF output requires an explicit plaintext warning.
- Calendar and booking: operator calendar workspace, appointment/class/personal/reminder creation, service catalog, availability, booking settings, public booking links, guest-capable booking, waitlist, provider capacity controls, and Google Calendar provider sync. Google Calendar provider sync lets externally entitled therapists and owners connect Google from `/calendar/sync`, import selected calendars as generic provider busy blocks for conflict checks, and publish MassageLab-originated appointment/class/personal block events to a dedicated MassageLab Google calendar without exporting client or clinical details. The dedicated MassageLab Google calendar is outbound-only and excluded from selectable inbound busy sources.
- Accounts and billing: profile/preferences, security settings, role verification, membership status, live Stripe Checkout products/prices for Supporter, Therapist, and Practice memberships, a separate one-time donation Checkout path that grants no entitlements, Stripe Customer Portal, signed webhook delivery, and versioned legal acceptance capture for registration, paid membership checkout, and professional/practice credential requests. Google sign-in now returns through a signed-in Terms/Privacy acceptance gate before onboarding or the preserved callback when current registration acceptances are missing. Live production pricing, an inert signed webhook request, the live Stripe membership readiness check, and one live Supporter subscription smoke through Chimer entitlement use and Customer Portal cancellation have been validated; the live one-time donation checkout path still needs a smoke test.
- Anatomy and education foundation: public `/education`, setup-first `/education/flashcards`, public deck detail routes under `/education/flashcards/decks/[slug]`, Anatomime, Anatomime shared-session routes under `/anatomime/join` and `/anatomime/play/[code]`, `/admin` dashboard, mobile-first admin image review queue, full admin anatomy browser, anatomy correction workflow, code-backed seed content, Postgres anatomy tables, anatomy media catalog/provenance, R2 media workflow docs, item-image media review state, relationship-backed body-system and tissue-type classification with textbook-level major structures and tissue subtypes, and 3D/spatial mapping schema. Flashcards and Anatomime use the sourced study adapter; archived old Anatomime data is not runtime product truth. Public flashcard deck templates store sourced deck configuration only, not custom user-authored anatomy facts. The flashcard setup waits for refreshed prompt counts before start/save actions and explains empty filter states with a reset path. Signed-in flashcard study stores prompt ids, correctness summaries, per-prompt mastery metadata, and session duration only; raw typed answers are not persisted. Anatomime shared rooms store reusable codes, teams, players, game runs, guesses, scoring state, host transfer/election state, and review expiry, while signed-in first-correct guesses update flashcard-linked name-recall progress. Shared Anatomime host screens now show QR/prefilled join links, and player screens distinguish active-team turns from steal/practice mode and offer a rejoin path when a saved device player pass no longer matches the room. Anatomy media review decisions are stored on item-image links so one asset can be approved for one item and rejected for another. This baseline is considered enough for the current alpha; 3D runtime tooling and voice-answer capture are later feature additions, not current core priorities.
- PWA/offline: offline support is intentionally limited to anonymous public local tools; account, auth, billing, calendar, booking, clinical sync, client, and `/api/*` surfaces remain online-only.

## Open Priorities

- P1: Keep this file and `docs/project-log.md` current when meaningful project state changes.
- P1: Complete the [invite readiness stabilization plan](superpowers/plans/2026-06-23-invite-readiness-stabilization.md) before inviting real users: refresh the release gate, pass the repo-owned automated checks, keep production Stripe readiness verified from an explicit live env file, preserve the completed live Supporter subscription and Customer Portal smoke evidence, smoke the one-time donation checkout path, and keep all clinical/professional-record workflows local-first.
- P2: Use the [codebase refactor optimization plan](superpowers/plans/2026-06-20-codebase-refactor-optimization.md), [June 20 refactor baseline](audits/2026-06-20-refactor-baseline.md), and [launch Neon transfer hardening plan](superpowers/plans/2026-06-21-launch-neon-transfer-hardening.md) for behavior-preserving performance/reliability cleanup before broad launch. The first pass established the build/bundle/source baseline, split `/admin/anatomy` browser types/data loading out of the page, scoped browser data loading by active view, split `/education/flashcards` API parsing/fetch wrappers, active runner, setup builder, and progress dashboard into focused client modules, added process-local sourced prompt/media caching for repeated flashcard APIs, decomposed `app/calendar/actions.ts` into focused action modules while preserving stable public exports, hardened calendar mutation concurrency for preference patches, reschedules, and free-practice setup quotas, moved the Atmosphere station catalog plus Tone/Generative.fm runtime out of the shared first-load shell behind a lazy global music runtime, moved `/anatomime`'s sourced deck/game workspace behind a lightweight setup shell that renders the full client after the first setup action while warming it during online idle for offline readiness, narrowed Neon transfer hotspots by projecting anatomy media/admin queries and maintenance scripts, hardened local-first intake/SOAP UI type contracts without changing encrypted vault storage or export behavior, documented the TS/JS duplicate-helper compatibility policy with a source-contract guard, and added launch hardening for Neon pooled runtime URLs plus narrower homepage/public booking projections. Treat the refactor as launch-ready; continue remaining compatibility-wrapper migrations only in later low-risk branches.
- P2: Continue validation for local-first intake-to-SOAP continuity and shared vault UX: full initial intake and shorter existing-client follow-up intake are implemented locally, and either intake response can seed a therapist-reviewed SOAP draft inside the encrypted local vault on the same device.
- P2: Continue functional/visual product work rather than backend or compliance-heavy expansion; shared Anatomime classroom-session polish or flashcard UX hardening are better fits for the next work session.
- P2: Harden the shared encrypted professional-record vault UX, backup guidance, and manual QA before expanding therapist-generated records beyond the current local-first alpha workflows.
- P2: Keep future module additions wired through the role-aware navigation resolver instead of adding one-off sidebar role checks.
- P2: Continue public/client wellness tools from the [public client wellness tools plan](superpowers/plans/2026-06-16-public-client-wellness-tools.md): the foundation branch adds public practice logging, signed-in owner-scoped wellness persistence, self-tracking privacy controls, quick logs, first ROM device-orientation/manual measurement, export/delete, navigation, and source guards. The body-sensation branch adds a first 2D region selector, richer body-sensation fields, private custom term suggestions, and timeline filters. The calendar reminder branch adds own appointment summaries and generic owner-scoped wellness reminder schedules without practice calendar payloads. The pattern-report branch adds local aggregate trend rows, weekly summary counts, confidence-labeled reflection prompts, ROM references, and browser report downloads. 3D body maps, therapist sharing, external reminder delivery, voice, and passive activity detection should remain separate follow-up branches.
- P2: Review the new legal/trust documents with an attorney before broader public reliance, then scope account deletion/export request workflows and future provider/client payment terms as separate branches.
- P2: Plan remaining client calendar connection, Outlook, Apple/iCloud, CalDAV, ICS, and full personal event mirroring separately from the implemented Google provider sync.
- P2: Plan Stripe Connect marketplace payments before booking payment collection.
- P2: Plan Payload CMS integration before adding it as a dependency.
- P2: Continue the Atmosphere audio branch series from the hidden runtime spike through the remaining-generator rollout: `/music` exposes a MassageLab-hosted global audio provider, a local Tone proof station, all 57 playable Generative.fm stations from the Alex Bainter package catalog, grouped station browsing, Atmosphere-only swipe/scroll station rails with deterministic organic-geometric SVG artwork, route-persistent mini-player behavior, loading progress, previous/next station controls, and supported-browser Media Session notification controls. The old `/atmosphere` and `/wellness/atmosphere` station URLs are intentionally not retained, and `/wellness/breathing` now carries the public breathing guide as its own Wellness tool. The public station cards use listener-facing sound descriptions rather than backend hosting, package, sample-index, or bucket details. The playable Generative.fm runtime prewarms hosted sample-index metadata and shared browser modules for a small starter set during idle, warms compressed payloads only for a tiny starter subset and deliberate hover on healthy connections, batches provider sample fetch/decode requests, reuses same-session decoded buffers, fades station handoffs, and emits startup timing with selected sample format, metadata reuse, retained payload-warmup fields, and provider request/decode counts while keeping Tone start, Transport start, output nodes, and WAV fallback payloads inside user-initiated playback. All 57 playable Generative.fm stations have Ogg Opus, AAC/M4A, and MP3 sidecar sample indexes and manifests from `massagelab-public-media`; the runtime chooses `opus -> aac -> mp3 -> wav` by browser capability so older iOS/Android browsers avoid WAV when they can use a compressed fallback. The remaining-generator rollout adds 704 WAV-side public R2 objects and 704 Opus sidecar objects for the final 18 stations, reducing the represented 1,491.6 MB WAV payload to 79.5 MB of Opus. The all-station AAC/MP3 rollout adds 1,814 AAC sidecar objects and 1,814 MP3 sidecar objects across Observable Streams plus all 56 package stations, representing 4,845.6 MB of WAV payload as 248.0 MB AAC and 327.5 MB MP3. The local `C:\Users\derri\code\audio` scan now confirms CC0 evidence for VSCO 2 Community Edition, Versilian Community Sample Library, selected Signature Sounds local license files, and the Signature Sounds site-wide CC0 statement for packs under `Signature Samples`. The catalog-wide coverage command (`npm run atmosphere:samples:coverage -- "C:\Users\derri\code\audio"`) currently classifies 57 hosted/playable Generative.fm pieces, 0 local-source candidate pieces, 0 pieces that need replacement or separate source/licensing review, and 0 blocked groups. Shared source-group matches such as `vsco2-piano-mf` remain piece-scoped and are not treated as global playable coverage because other packages may require exact rendered note names. The public-media bucket is separated from anatomy and private media buckets, has browser-read CORS for audio fetches, applies long-lived immutable caching to audio payloads, and applies short revalidating cache headers to generated sample-index/manifest metadata. Raw SSO samples are excluded because Sampling Plus licensing is not a clean hosted-browser fit; configured SSO roles use CC0 adaptations: VSCO sustained oboe for `sso-cor-anglais`, Signature Sounds children choir ambience for `sso-chorus-female`, and Signature Sounds men-of-choirs WAVs for `sso-chorus-male`. GitHub Actions CI now persists Next.js `.next/cache` separately from the existing npm cache, cancels stale pull-request runs, avoids duplicate Prisma generation in the build step, and leaves Playwright browser installation explicit. Custom ambient mixes, account sync, feature gates, user-authored generator controls, and YouTube support remain later work.
- P2: Premium visual backgrounds are being added one source-matched candidate at a time. The original MassageLaba Lamp remains the free default, Static gradient remains the free fallback, and Aceternity Aurora/Dotted glow/Sparkles/Gradient animation/Background beams/Background beams with collision/Background lines/Glowing stars/Meteors/Shooting stars and stars background/Canvas reveal dots/Spotlight New/Lamp Section Header/Vortex background/Wavy background/3D Globe plus Unlumen Pixel liquid background, Unlumen Aurora Bars, Animate UI Bubble background, Animate UI Gradient background, Animate UI Stars background, Animate UI Hole background, MassageLab tile grid, MassageLab hex grid, Magic UI Retro Grid, Magic UI Light Rays, Chamaac Light Speed, Chamaac Electric Mist, Chamaac Astral Flow, Chamaac Deep Space Nebula, Chamaac Grid Bloom, Chamaac Liquid Chrome, Chamaac Waves, Chamaac Synthesis, Eldora Novatrix Background, Eldora Hacker Background, Eldora Photon Beam, React Bits Ferrofluid, and React Bits Lightfall are the active premium options. The Aceternity License is captured once in `docs/background-sources.md`; each new Aceternity candidate still gets component-specific source, dependency, motion, performance, fallback, cleanup, and visual review before activation. Gradient animation includes per-background Chimer/Clock controls for base colors, glow colors, speed, and size. Background lines includes a Chimer/Clock duration control. Wavy background adds Chimer/Clock controls for background fill, five wave colors, wave width, blur, speed, and opacity, and uses `simplex-noise@4.0.3` as a small MIT canvas-noise dependency. Aceternity 3D Globe adapts the source Three/R3F/Drei globe component into a dependency-free native WebGL sphere renderer with the reviewed Earth texture and bump-map URLs fixed in code, controls for background, globe tint, `0.01x`-`2x` rotation with fixed reverse spin, fixed off-screen sun/manual lighting, screen pan X/Y, fixed axial tilt, size, bump, atmosphere, and wireframe, plus an optional icon-based browser/manual location marker with an optional label and an always-on MassageLab Ashland, OH marker. Pixel liquid background adapts the reviewed Unlumen Pixel Liquid Background page as a dependency-free 2D canvas premium effect instead of importing the source Three.js/WebGL component or adding `three`; it exposes Chimer/Clock controls for background, base, accent, and highlight colors plus detail, pixel size, and ambient motion speed, removes cursor-responsive behavior from the MassageLab adaptation, cleans up animation/resize/visibility/motion listeners, and remains high-motion opt-in. Aurora Bars adapts the reviewed Unlumen free component as a dependency-free bar field instead of adding its source `motion` dependency; Chimer/Clock expose controls for background, auto monochrome or custom five-color bar palettes, bar count, speed, blur, gap, and min/max height, while Music passes a visualizer-active flag only when Aurora Bars is selected and a station is playing. Magic UI Retro Grid adapts the source WebGL perspective-grid renderer with a CSS fallback, no new dependency, custom background/light-line/dark-line colors, angle, cell size, and opacity controls, pointer-events-free rendering, reduced-motion handling, and cleanup for observers, listeners, animation frames, and WebGL resources. Magic UI Light Rays adapts the source Motion top-down light ray component with native CSS keyframes, no new dependency, custom background/ray colors, count, blur, speed, length, and opacity controls, deterministic ray placement, pointer-events-free rendering, and reduced-motion handling. Chamaac Light Speed adapts the MIT source's warp-cylinder particle props with a dependency-free 2D canvas renderer instead of adding `three`, `@react-three/fiber`, or `@react-three/postprocessing`; Chimer/Clock users can adjust light color, Warp speed, particle count, glow intensity, tunnel radius, and field length, with the MassageLab default Warp speed set to `1.00x` where `1.00x` maps to the previous `0.10x` renderer pace, a minimum of `0.10x`, particles capped at 200, colored fading rays without a white tip, and viewport-based ray reset. Chamaac Electric Mist ports the MIT source fragment shader for high-energy lightning, waves, and smoke into a native WebGL canvas renderer instead of adding `three`, `@react-three/fiber`, `clsx`, or `tailwind-merge`; Chimer/Clock users can adjust lightning color, animation speed percentage, noise detail, distortion, and brightness from 1% to 100%, with the dense source-like mist restored and only the old vertical sweep softened into an irregular multi-direction mask after visual QA. Chamaac Astral Flow ports the MIT source fragment shader for a breathing radial cosmic-wisp field into a native WebGL canvas renderer without adding `three`, `@react-three/fiber`, `clsx`, or `tailwind-merge`; Chimer/Clock users can choose custom source colors or a primary-color harmony palette and adjust animation speed as a `10%`-`100%` slider mapped to source `0.1`-`3`, plus Flow Min and Flow Max. Chamaac Deep Space Nebula ports the MIT source domain-warping nebula shader into a native WebGL canvas renderer without adding `three`, `@react-three/fiber`, `clsx`, or `tailwind-merge`; Chimer/Clock users can choose custom source colors or a primary-color harmony palette and adjust animation speed as a `1%`-`100%` slider mapped to source `0.1`-`5`. Chamaac Grid Bloom ports the MIT source pulsing interference-grid shader into a native WebGL canvas renderer without adding `three`, `@react-three/fiber`, `clsx`, or `tailwind-merge`; Chimer/Clock users can adjust bloom color, animation speed as a `1%`-`100%` slider mapped to source `0.1`-`3`, grid density, rotation speed, fade falloff, distortion, and flow X/Y, while source cursor interaction is intentionally omitted. Chamaac Liquid Chrome ports the MIT source liquid-metal fragment shader into a native WebGL canvas renderer without adding `three`, `@react-three/fiber`, `clsx`, or `tailwind-merge`; Chimer/Clock users can choose custom chrome/shadow colors or a primary-color harmony palette, adjust Flow Speed as a `1%`-`100%` slider mapped to source `0.01`-`2`, and adjust Time Scale as a `1%`-`100%` slider mapped to source `0.001`-`1`. Chamaac Waves ports the MIT source shader-displaced wave plane into a native WebGL canvas renderer without adding `three`, `@react-three/fiber`, `@react-three/drei`, `clsx`, or `tailwind-merge`; Chimer/Clock users can choose custom background/wave colors or a primary-color harmony palette, adjust Speed X/Y as `1%`-`100%` sliders mapped to source `0.001`-`0.1`, and tune wave amplitude. React Bits Ferrofluid ports the MIT + Commons Clause source OGL fragment shader into a native WebGL renderer without adding OGL; Chimer/Clock users can choose custom source colors or a primary-color harmony palette and tune animation speed, scale, turbulence, fluidity, rim width, sharpness, shimmer, glow, flow direction, and opacity, while source cursor interaction is intentionally omitted. Animate UI Bubble adapts the reviewed Bubble Background under MIT + Commons Clause caution with native CSS keyframes and an SVG goo filter instead of adding the source `motion` dependency; cursor interactivity and the sixth mouse-following bubble are intentionally omitted, and the effect remains an internal MassageLab premium visual background. Animate UI Gradient adapts the reviewed Gradient Background under MIT + Commons Clause caution with native CSS keyframes instead of adding the source `motion` dependency; Chimer/Clock users can adjust primary color, color harmony, and layer opacity. Animate UI Hole adapts the reviewed Hole Background under MIT + Commons Clause caution with an internal 2D canvas grid/particle loop and CSS glow instead of adding the source `motion` dependency; Chimer/Clock users can adjust stroke color, particle color, line count, and disc count. MassageLab tile grid is an internal dependency-free 2D canvas premium effect with raised square tiles, faint grey borders, deterministic per-tile phases, independent flat color alpha fade-ins and fade-outs, no global refill loop, no random active-count correction, analogous auto palette controls, five custom color controls, tile size, an H:M:S fade-time picker, active tile percentage, and tile opacity controls. MassageLab hex grid is an internal dependency-free 2D canvas premium effect with raised hexagonal tiles, faint grey borders, deterministic per-hex phases, independent flat color alpha fade-ins and fade-outs, no global refill loop, no random active-count correction, a primary color plus harmony-mode palette selector, hex size, an H:M:S fade-time picker, active hex percentage, and hex opacity controls. Spotlight New adds Chimer/Clock controls for spotlight color, intensity, beam width, beam height, small beam width, vertical offset, sweep distance, and duration while adapting the source mirrored gradient beams with native CSS keyframes instead of adding `motion/react`. Lamp Section Header adapts the source mirrored conic beams, glow layers, and center line as a passive CSS-keyframe background with controls for lamp background, beam color, glow intensity, beam width, glow width, vertical offset, and pulse speed, without adding `motion/react`; the source hero masks are intentionally omitted from the full-screen Clock/Chimer adaptation because visual QA showed hard rectangular panels. Sparkles is a steady ambient particle background; the initial Music analyser-reactivity trial was removed after visual review because it made the particles feel jumpy/strobing. Background beams, Background beams with collision, Background lines, and Meteors use source path/beam/meteor behavior with native/CSS animation instead of adding the source `motion` dependency. Shooting stars adapts the Aceternity star field and shooting-star behavior with internal SVG/JS, deterministic star placement, Chimer/Clock controls for star colors, density, twinkle, shooting speed, and frequency, and cleanup for ResizeObserver, timeouts, requestAnimationFrame, and visibility/motion listeners, with no new dependency. Canvas reveal dots adapts only the passive dot-matrix layer from Aceternity Canvas Reveal Effect with a dependency-free 2D canvas renderer, deterministic pixel-dot placement, a subtle per-dot shimmer/reveal animation, capped canvas DPR, and cleanup for ResizeObserver, requestAnimationFrame, visibility, resize, and motion listeners, avoiding the source `three` and `@react-three/fiber` dependency path while adding Chimer/Clock controls for background color, dot/accent colors, dot size, spacing, opacity, motion speed, and gradient overlay. Vortex adapts the Aceternity canvas particle field with the existing `simplex-noise@4.0.3` dependency, no added `motion` dependency, capped canvas DPR, Chimer/Clock controls for source props, and cleanup for animation, resize, visibility, and motion listeners; it is high-motion/high-cost and remains opt-in. Glowing stars adapts the source Motion-powered star/glow cycle with React state and CSS animation instead of adding `motion`. Follow-up UX note: revisit existing backgrounds with multi-color controls and use a primary color plus harmony-mode selector where that fits the source behavior and gives users simpler customization. Heavier, unclear-license, not-yet-source-matched, or true visualizer candidates remain review-only until license, dependency, performance, reduced-motion, cleanup, and route visual QA are captured.
- P2: Aceternity 3D Globe now also includes an Eldora UI Cobe Globe-inspired Graphic view style as part of the existing premium 3D Globe option, with dependency-free native WebGL sphere rendering plus structured stable 2D overlay map-dot rendering from the reviewed Cobe map mask, Outer Glow, marker-dot colors, and density controls capped at a 10k sample budget; the source `cobe` and `react-spring` runtimes are not added.
- P2: Chamaac Light Speed has been retuned after visual QA: its old `0.10x` renderer pace is now displayed as the new `1.00x` Warp speed. Chamaac Electric Mist is active as a native WebGL port with Chimer/Clock controls for lightning color, animation speed percentage, noise detail, distortion, and brightness from 1% to 100%, with the dense source-like mist restored and only the old vertical sweep softened into an irregular multi-direction mask. Chamaac Astral Flow is active as a native WebGL port of the source breathing radial shader with custom source colors or primary-color harmony palettes, Flow Min/Max controls, and source speed `0.1`-`3` displayed as `10%`-`100%`; after Playwright visual QA it uses source-style alpha blending, mount-relative shader time, and the Chamaac `#05070a` base so Clock avoids the washed-out center-bowl phase. Chamaac Deep Space Nebula is active as a native WebGL port of the source domain-warping nebula shader with custom source colors or primary-color harmony palettes and source speed `0.1`-`5` displayed as `1%`-`100%`. Chamaac Grid Bloom is active as a native WebGL port of the source pulsing interference-grid shader with bloom color, grid density, rotation speed, fade falloff, distortion, flow X/Y, and source speed `0.1`-`3` displayed as `1%`-`100%`; cursor interaction from the source is intentionally omitted. Chamaac Liquid Chrome is active as a native WebGL port of the source liquid-metal shader with custom chrome/shadow colors or primary-color harmony palettes, Flow Speed source `0.01`-`2` displayed as `1%`-`100%`, and Time Scale source `0.001`-`1` displayed as `1%`-`100%`. Chamaac Synthesis is also active as a native WebGL port of the source fragment shader, without adding the source `three`/R3F dependency stack, with Chimer/Clock controls for custom source colors or primary-color harmony palettes, animation speed where source `0.4` displays as `1.00x` and the slow end reaches `0.01x`, complexity, zoom scale, distortion, glow intensity, and flow frequency.
- P2: Eldora UI Novatrix Background is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT premium visual background; MassageLab adapts the source WebGL shader with a native canvas renderer instead of adding `ogl`, keeps source-shaped color/speed/amplitude controls with optional color harmony, and replaces source cursor reaction with pointer-events-free ambient drift.
- P2: Eldora UI Hacker Background is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT premium visual background; MassageLab adapts the source Matrix-style falling-character 2D canvas with host sizing, reduced-motion and visibility handling, pointer-events-free rendering, cleanup, custom color or harmony controls, font size, and speed mapped from source `0.05`-`3` to `1%`-`100%`.
- P2: Eldora UI Photon Beam is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT premium visual background; MassageLab adapts the source Three.js/postprocessing light-trail beam field into a dependency-free native 2D canvas renderer with host sizing, reduced-motion and visibility handling, pointer-events-free rendering, cleanup, custom color or harmony controls, and source-shaped controls for beam lines, signals, spread, depth, curve, wave, opacity, trail, bloom, and speed mapped from source `0.02`-`2` to `1%`-`100%`.
- P2: Aceternity UI 3D Globe is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only Aceternity License premium visual background; MassageLab adapts the source Three/R3F/Drei globe, tooltip, and avatar-marker model into a dependency-free native WebGL sphere renderer with a 2D marker overlay, fixed reviewed Earth texture/bump URLs, reduced-motion/visibility/resize cleanup, pointer-events-free rendering, and Chimer/Clock controls for background, globe tint, `0.01x`-`2x` rotation speed with fixed reverse spin, Follow Sun fixed off-screen lighting that hides irrelevant manual controls, screen pan X/Y with hidden sliders until pan is enabled, fixed sun-relative axial tilt, scale, bump, atmosphere, wireframe, and optional icon-based browser/manual location marker with an optional label. Follow Sun uses a hard day/night terminator while rotating once per 24 hours from UTC subsolar longitude. The adaptation also renders a MassageLab Ashland, OH marker with the site logo, no text label, and 1px leader lines.
- P2: Magic UI Retro Grid is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT premium visual background; MassageLab adapts the source WebGL perspective-grid renderer with a CSS fallback, pointer-events-free rendering, reduced-motion/theme/visibility handling, cleanup, custom background and light/dark line colors, and source-shaped angle, cell size, and opacity controls.
- P2: Magic UI Light Rays is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT premium visual background; MassageLab adapts the source Motion top-down light-ray component into dependency-free native CSS keyframes with deterministic ray placement, pointer-events-free rendering, reduced-motion fallback, custom background/ray colors, and source-shaped count, blur, speed, length, and opacity controls.
- P2: React Bits Ferrofluid is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL fragment shader into a dependency-free native WebGL renderer, keeps cursor interaction disabled for pointer-events-free app use, respects reduced motion and cleanup, and exposes Chimer/Clock controls for custom or harmony colors plus source-shaped speed, scale, turbulence, fluidity, rim width, sharpness, shimmer, glow, flow direction, and opacity.
- P2: React Bits Lightfall is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL falling light-streak shader into a dependency-free native WebGL renderer, keeps cursor interaction disabled by default but exposes opt-in cursor glow controls while the canvas remains pointer-events free, respects reduced motion and cleanup, and exposes Chimer/Clock controls for custom or harmony streak colors plus source-shaped background color, speed, streak count, width, length, glow, density, twinkle, zoom, background glow, cursor strength/radius/smoothing, and opacity.
- P2: Animate UI Stars is also active after 2026-07-03 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause caution background; MassageLab adapts the source three drifting star layers, radial bottom background, 2000px repeat drift, and parallax behavior with CSS/window events instead of adding `motion`, with Chimer/Clock controls for star color, speed, density, and parallax strength.
- P3: Plan a repo-wide docstring pass for shared helpers, domain rules, server actions, data adapters, and non-obvious UI components so code is easier to understand in-place.
- P3: Revisit anatomy 3D/spatial runtime tooling after the core local-first practice tools are more useful.
- P3: Revisit voice notes, intake conversation transcription, and SOAP-assist later, after a separate feasibility branch proves local/on-device options and any hosted path passes legal, BAA, security, consent, audit, and retention review.
- P2: Land the technical SEO foundation and focused public landing-page copy before the broad public-alpha share window: production public pages can become indexable, preview/development builds stay noindex, private/auth/API/booking/shared-code/local-record routes stay out of the sitemap and robots allowlist, and the homepage plus flashcards, Anatomime, Chimer, notes, and wellness routes carry clear search-aligned product proof without weakening the local-first PHI boundary.

## Documentation Rules

- Update this file first when the current focus, database state, live surfaces, or priority order changes.
- Update `docs/project-log.md` for chronological change history, completed branches, meaningful decisions, and new plans.
- Keep `docs/roadmap.md` as product-direction evidence, not the current operating tracker.
- Keep `TODO.md` as preserved checklist evidence; mirror meaningful status changes here and in `docs/project-log.md`.
- Keep wiki pages under `docs/wiki/` for stable operational documentation.
- Store detailed implementation plans under `docs/superpowers/plans/`.
- Preserve local-first PHI boundaries, feature-key entitlement checks, conservative Sentry privacy posture, and targeted branch-sized refactors.
