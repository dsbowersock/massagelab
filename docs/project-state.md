# MassageLab Project State

Verified: 2026-09-02

This is the read-first source of truth for MassageLab's current project state. Use it before `docs/project-log.md`, `docs/roadmap.md`, `TODO.md`, audits, or wiki pages when deciding what is active now.

## Current Snapshot

- Family-and-friends abuse/cost Layer A is reconciled locally onto exact current main on `codex/family-friends-05-abuse-cost-foundation-r2`. One closed operational-limiter policy owner and one Serializable, atomic, privacy-safe persistence service cover the planned abuse/cost operations; this layer wires outbound authentication/security email into that foundation without importing later public-provider, booking, or Anatomime traffic work. Public-auth mail consumes both a 70-attempt fixed 24-hour deployment ceiling and the 90-attempt total-email ceiling; security/account-change mail consumes only the total 90. Missing or invalid policy, limiter unavailability, and exhausted quota fail closed before Nodemailer transport construction, while an allowed provider failure or timeout remains charged. Admin email delivery commits a five-minute exact-token claim before the classified SMTP boundary, finalizes outside the provider call, reports uncertain completion as ambiguous, and permanently binds only hashed retry-operation keys to their originating intent through append-only ownership. The repository now has 46 migration directories. A separately authorized exact-current-main Production deployment at `06a730fcc6b7ed54f91e7d6330c023f9e06262c8` verified all 45 baseline migrations current and membership webhook writes enabled with the pause flag at `0`; only the new additive `20260831120000_operational_rate_limit_bucket` migration is pending for this layer. It must be separately authorized, applied, and verified as the 46th current migration before Layer A runtime serves. This reconciliation did not send email, change a provider or environment, connect to the database, apply the operational migration, deploy, push, merge, charge a payment, or perform another Production action.
- Fresh Layer A reconciliation evidence passes Prisma validation/generation, the exact 117/117 focused operational-limiter/mail/registration/Admin matrix, TypeScript, and base-to-head diff checks. No full repository suite, lint, build, Browser QA, hosted Linux result, database-backed proof, or live/provider behavior is claimed by this focused task.
- Family-and-friends bootstrap and pricing cost hardening is implemented locally on `codex/family-friends-feedback` under plan `e4438ad767611682635adde2be2eddd3f17a7a43`. The server projects one account-shell bootstrap (`7963a63067afbb577011bda6d983764c597b2e3f`, `c9f5458d190166b46f0017106092abc59c56ab23`, `11e26bb1eeb6ad29254cdc864e72dccb7a8f0d08`, `4e48f93bc304e3b99a4d1c7ea13d37c816588636`, `c4e80d1b7b83a27cc81b1a64936d113c0966df63`), background commerce is owner-keyed and intent-driven (`c0aaed961165ddbdf499bd6db969a925a833d28b`, `61cf511b6d88cf887f8ec310eee01e533899bf97`), public pricing has one bounded shared cache owner (`b741583de25464c04243ece98d2a510f9d81108e`, `51a45ab08b63e3840fb5b378fefca1ac5f8bc31b`), and request-scoped React caching deduplicates Server Component auth snapshots without a persistent or TTL user cache (`12afc1694360987dcc1a3f0972a954f6b2eaa3a2`, `5aa5a77d20eea4edc93d2ef4836a37cce0b9cbec`). The historical ordinary non-practice shell workload was measured from an unreproducible dirty Task 9 working-tree snapshot based on runtime SHA `706c52167466f984f3e405986af11ff3d2343a02`, with no pending guest-cart merge or active commerce consumer: one auth snapshot, one user-graph read, one temporary-grant read, one entitlement build, one preference read, one practice-role read, four logical ORM operations, zero client bootstrap endpoints, and zero ordinary commerce snapshots. Settings and Music consume the sanitized server projection; a shared fallback request occurs only after a server preference read fails, owner changes abort and reset old work, therapist local hydration remains immediate while its cloud read is lazy and deduplicated, and zero-practice users skip the calendar endpoint. Public pricing caches the public display catalog only with process-local single-flight, a five-minute stable configured/unconfigured result TTL, a fifteen-second configured lookup/projection failure TTL, and bounded read-only Stripe Price requests with a 2.5-second timeout and one SDK network retry. Unconfigured slots remain `Price unavailable` and release-not-ready even though their absence is stable. Checkout, Portal, entitlements, customers, and webhooks remain uncached and server-owned. That same dirty snapshot historically measured 178/178 focused Node tests, a fresh scoped combined Browser-QA run with 127 Browser-QA passes, 37 documented authorization-gated skips, and zero failures, plus the local validation and timing receipts; skips were not counted as passes. Its uncommitted Task 9 patch identity was not recorded, so none of those receipts is reproducible from runtime SHA `706c52167466f984f3e405986af11ff3d2343a02` alone and the totals are not exact-head proof. Later commit `f139df0e941e72c87c944ca648c1f021c28ae6ea` owns the evidence/report only; it does not identify or reconstruct the dirty measured patch. The final local timing receipt returned HTTP `200` for 21/21 samples across seven fixed routes with three samples each; local timing `first` is not platform cold evidence. The full Windows Node suite is not green: 3,613 passed, 10 established line-ending/fixture checks failed, and 3 intentionally skipped across 3,626 tests, so hosted Linux remains required. `MASSAGELAB_PUBLIC_REGISTRATION_PAUSED` and `MASSAGELAB_SUPPORTER_CHECKOUT_PAUSED` remain independent, default-open, exact-lowercase-`true` server controls: registration pause preserves existing-account login/recovery, while Checkout pause preserves existing subscriptions, entitlements, status, and Portal access. Live Stripe verification is `NOT RUN`; private database rows, provider configuration, OAuth/mail delivery, deployment, push, merge, and every other live action remain separately gated. PHI-bearing workflows remain local-first.
- The later identity/subscription predecessor-branch bullets preserve implementation and validation evidence. Their migration and runtime rollout status is historical and is superseded by the exact-current-main Production status in the Layer A bullet above.
- Navigation and action feedback is implemented locally on `codex/family-friends-feedback`; the current committed behavior head is the independently approved held-navigation repair `eb3690791c0fdeb5db2c089102dc88179b9f6104`, after application head `eaa0b4742ad0a8e14fbba7e0c4c30ccc01df0b7e`, Task 1 and Task 3 test-contract drift repairs `44da2a3ae2e4140b7a71b611b279b170e3e549c3` and `9fba6a6ea52faeaef994ab871f60cd600ef8bffb`, and Task 4 browser acceptance/docs. Root App Router loading feedback, owned Link descendants, imperative navigation, shared async buttons, framework Server Action forms, native POST/303 forms, launch-critical account/auth actions, and billing redirects now expose bounded pending state without adding a global click/history interceptor or changing their existing authority. Task 4 explicitly passes desktop 1280×900, mobile portrait 390×844 at 200% root text, compact landscape 844×390 with reduced motion, keyboard Tab/Enter activation, no focus theft, one live route announcement per held journey, pointer-inert feedback, no horizontal overflow, uncovered app/player controls, a real Loader canvas that contributes composited pixels and remains rendered identically across two samples 400ms apart under reduced motion, and persistent music-toolbar identity. Exact lane proof passes 21/21 with 14 ordinary specs, 28 desktop/mobile project-spec assignments, and four nonempty lanes; the current focused Node matrix passes 50/50; and the fresh built-app interaction spec passes 14 public cases with 3 explicit private authorization-gated skips in each ordinary project. Prisma validation/generation, TypeScript, lint, the ordinary 114-page build, diff checks, and a fresh 114-page Browser-QA build pass. The 3,374-test Windows suite remains at the established host-line-ending baseline: 3,361 pass, 10 fail, and 3 intentional skips, comprising nine AtmoShaper CRLF fixture-hash failures and one auth-migration exact-newline failure; no unrelated fixture was changed. The complete Browser-QA release gate is red/incomplete. Its parallel run advanced to about 699/721 before a worker hang and emitted no final totals. The approved one-worker rerun reached 711/721, left 39 failure artifacts across app-shell (7), control-system-review (20), public identity (6), public membership return (2), background commerce (1), public routes (2), and WebKit music media (1), then stalled after an otherwise completed test while only the Playwright worker remained and no browser process was active; the exact owned process tree was stopped and no final pass is claimed. Task 4's 28 public cases remained green with 6 private authorization-gated skips inside that run. A clean exact-candidate hosted Windows-independent unit result and a complete unfiltered Browser-QA exit remain release gates. No database, account, OAuth, email, Stripe/payment/refund/cancellation, provider, private fixture, deployment, push, merge, or Production mutation occurred.

- Subscription entitlement convergence is implemented locally on `codex/family-friends-subscription` at exact runtime head `6838ff574695b23d206d2272da241d30bed91507`. The combined branch has 45 migration directories, and the wholly pending order before any runtime deployment is: obtain a zero-collision normalized-email preflight; apply `20260828120000_identity_method_safety`; apply and monitor `20260828121000_identity_normalized_email_index` with its `CREATE UNIQUE INDEX CONCURRENTLY` stop/recovery boundary; apply `20260828130000_membership_subscription_convergence`; apply `20260901100000_auth_method_intent_two_factor_purposes`; then apply `20260901101000_auth_method_intent_registration_callback`. All five reviewed migrations must verify current, with no extras or failure, before the first bridge-capable deployment and two-deployment runtime cutover proceeds. The first deployment must serve this bridge-capable runtime from the Production alias with `MASSAGELAB_MEMBERSHIP_WEBHOOK_WRITES_PAUSED` exactly `1`; after alias cutover, wait at least the current configured maximum invocation lifetime and prove no pre-bridge SHA is receiving or executing webhook requests. During that bounded pause Stripe retries membership deliveries, access may remain in processing guidance, and operators must not manually replay or acknowledge held deliveries. Only after drain proof may the same bridge-capable runtime deploy again with the flag absent or exactly `0`; held deliveries must then reach `2xx` and terminal receipts, and persisted access must converge. Rollback after bridge cutover is allowed only to a bridge-capable deployment with the flag exactly `1`, never to the pre-bridge legacy writer. The additive schema stays in place without destructive migration rollback. Retain the bridge through held-delivery/backlog proof and the rollout window; its removal is a separately reviewed retirement. The additive membership migration creates the privacy-bounded unique `MembershipWebhookReceipt` owner and adds nullable provider-event and local-authoritative watermarks without backfilling or rewriting existing `MembershipSubscription` rows; all five reviewed migrations remain unapplied locally. Signed membership Checkout completion and all five subscription events now use the sole convergence service after raw-body signature verification. Completed duplicate receipts and same-event duplicates are no-ops; events already proven stale short-circuit without provider retrieval; newer unambiguous provider events apply; and equal-time, legacy-null-watermark, Checkout-completion, and every different event not already proven stale after an authoritative read retrieve current Stripe state outside the transaction and pass a short compare-and-commit barrier that never compares Stripe's clock with the local authoritative-read clock. Once an envelope can identify or create a receipt, a failure before terminal completion leaves that receipt `RECEIVED` with only an allowlisted failure code and returns a private retryable `503` because no background worker owns it. A malformed envelope may reject before a receipt can be identified or created, but it still returns non-2xx/retry and grants nothing. Receipt-creation races recover once only for the exact legacy Prisma or installed PrismaNeon PostgreSQL `P2002` shape for `MembershipWebhookReceipt_provider_providerEventId_key` / ordered `provider, providerEventId` fields; near-miss metadata is not retried. The two legacy membership writers are removed with no runtime caller. Fix A at `1f0250f311ca28f85756a24135e8ef0e03f788bd` added the exact-`1` pause bridge before membership provider/database/cache work while preserving non-membership routing; Fix B at the current runtime head normalized conflicting Account return outcomes to one controller/notice owner. Checkout and Portal return through identifier-free Account URLs to an authenticated `private, no-store`, database-only status projection; Checkout performs up to five reads/attempts at incremental waits `[0,1000,2000,4000,8000]`, uses all five on exhaustion for the preserved 15-second wait schedule, and bounds each fetch plus JSON consumption to 1.5 seconds. It does not attribute an old adverse row to the new payment without a changed revision and offers only safe status retry after exhaustion. Portal availability remains derived from the persisted customer owner, while access is decided from persisted feature keys such as `premium_backgrounds`, never displayed plan names or URL state. Exact diff review confirms membership price/tax/Checkout idempotency semantics, background-commerce receipt/routing, and customer-owned Portal creation remain unchanged; raw provider payloads, payment/address data, secrets, and tokens are not stored. A fresh exact-runtime-head focused rerun of the bridge, Account return, and focused Portal contracts passed 62/62. The last complete Task 5 Prisma/type/lint gate, 379/379 focused matrix, 3,342-test Windows suite, ordinary 114-page Production build, and Browser-QA attempt remain predecessor-head evidence at `04881a884d6e5cce656e1ff939bd3b998c67bc18`; they were not rerun after Fix A/B, so no final full-suite or exact-runtime-head release pass is claimed. That predecessor Windows suite reported 3,329 pass, 10 known host-line-ending failures, and 3 intentional skips: nine established Atmoshaper CRLF fixture failures whose committed blobs matched both pinned hashes and one Windows CRLF exact-newline failure in `auth-schema-migration.test.mjs`. Hosted Linux remains required. The predecessor Browser-QA build failed only while restricted network access prevented fetching Inter from Google Fonts; no stale artifact or policy workaround was used, so the desktop/mobile membership-return runs were not started. A successful fresh exact-candidate gate, hosted Linux, Browser-QA build, public desktop/mobile membership-return acceptance, and separately authorized fingerprint-bound disposable-database rows with all five reviewed migrations applied remain gates. Production additionally requires separately authorized exact-target migration application, both runtime deployments and writer-drain/backlog proof, exact deployed-commit and migration-status proof, live Stripe configuration/webhook readback, and separately authorized provider/live behavior if still required. Historical billing smokes are context only, not exact-head proof. No live or test Stripe/payment/provider action, Checkout, event emission, delivery replay, refund, cancellation, Portal/provider-setting or environment change, database connection, migration application, private row or fixture, deployment, push, or merge occurred.
- Identity and account-method safety is implemented locally on `codex/family-friends-identity`, including final-review remediation commits `39317f958413e4238affb6eb4d52c544e56f7e12`, `70c372739e2e81d8c5a578724c2b57db8db65ca4`, `404598c00d3d8834ca50db6aac9d0ff58e3fd7af`, and Fix Round 1 runtime commit `0b9af8aa970b72aad03e9cb4553615a5de82202c`. Registration and Google race recovery now recognize only approved legacy or PrismaNeon PostgreSQL `P2002` identity constraints, including the installed adapter's exact truncated functional-index field `lower(btrim(email` for the User normalized-email owner only. Accepted registration and reset requests consume the same ACCOUNT+NETWORK limiter path and schedule exactly one post-response task before any lookup, Argon2, token, transaction, pruning, or mail work. Password CHANGE proves the current method before hashing, while ADD requires cookie-bound Google reauthentication before hashing; both hash once outside the short revalidating transaction. A dormant operational command can scan bounded `PENDING`, `FAILED`, and expired `PROCESSING` security-notice intents only after exact direct-target fingerprint, database, send, and `--max-rows=1..100` authorization. The branch has 42 migration directories: `20260828120000_identity_method_safety` remains the expansion migration, and dedicated single-statement `20260828121000_identity_normalized_email_index` builds `User_normalized_email_key` with PostgreSQL `CREATE UNIQUE INDEX CONCURRENTLY`; neither was applied. The reset checklist now forbids only a second Admin evidence/action bundle and requires the transactionally queued `PASSWORD_RECOVERED` notice through the account-security delivery owner. Fresh local Prisma validation/generation, TypeScript, lint, the complete 178/178 affected identity matrix, the 113-page Production build, and diff checks pass. The full Windows suite remains at the established platform baseline: 3,249 total, 3,237 pass, 9 fixture-checksum failures, and 3 intentional skips; both committed Atmoshaper fixture blobs still match their pinned SHA-256 values, so hosted Linux remains required. No real database, fingerprint, or configuration value was read or printed, and no migration, security-notice retry, legacy cleanup, private fixture, Google/provider setting, real OAuth, SMTP/mail, Browser QA, deploy, push, merge, or private browser proof occurred. Release still requires the count-only zero-collision preflight, both ordered migrations with concurrent-index monitoring/recovery, atomic runtime cutover, and exact deployment/SHA-scoped pre-bridge drain proof mapped by immutable deployment ID and full Git SHA. Before any `AuthAttempt` cleanup, that read-only proof must show separately for `POST /api/account/register`, `POST /api/auth/callback/credentials`, and `POST /api/account/password-reset/request` that zero pre-bridge deployments receive or start requests after alias cutover and zero pre-bridge executions remain at the drain boundary; aggregate or ambiguous route/deployment coverage forbids cleanup. Separately authorized fingerprint-bound operational actions, provider callback/origin publishing, SPF/DKIM/DMARC/bounce proof, hosted Linux QA, Browser QA, fingerprint-authorized private rows with cleanup, and any future legacy-table drop remain external gates.
- Anonymous operational Sentry boundary: PR #186 remains open and unmerged. The exact preview runtime proof at `77ec81e2db77faaa3fd6694ce62075761e4513db` uses deny-by-default collection, removes supplied user values, sends only Sentry's anonymous null-IP opt-out marker, disables server-name capture, automatic breadcrumbs, browser sessions, Replay, standard User Feedback, attachments, Logs, and Application Metrics, and retains only sanitized errors, coarse performance evidence, and the voluntary enum-only diagnostic flow. Server-side data scrubbing, default scrubbers, IP-storage prevention, geographic/server-name advanced scrubbers, and disabled public issue sharing were saved or read back. One authorized exact-preview diagnostic stored no identity, IP, location, server name, request body, header, cookie, breadcrumb, Replay, or attachment and produced exactly one application `POST` with status `200`; the provider over-filtered its message to `[Filtered]`. The provider did not expose an exact retention duration through the connected settings readback or visible account UI, so retention remains explicitly unverified rather than guessed. No product analytics, pageviews, background-use tracking, user journeys, or consent popup were added.
- Production migration integrity: the 2026-08-27 Google sign-in incident was a schema-deployment mismatch, not an OAuth credential or account failure. Production code expected `User.authSessionVersion`, while four committed Admin User Operations migrations from PRs #173, #175, #178, and #179 had never been applied. The explicitly authorized recovery applied all four migrations through the direct Neon maintenance connection. Prisma now reports all 40 migrations current, the required column and six new Admin tables exist, and the migration transformations completed. Production builds now run a read-only migration-status gate before Prisma generation. The gate is inert outside Vercel Production, requires `DIRECT_URL` or `DATABASE_URL_UNPOOLED` in Production, and fails the new build without applying anything when status is unavailable or migrations are pending, leaving the prior healthy deployment active until a separately authorized migration deploy and redeploy complete.
- AtmoShaper production ambient release: the reviewer approved all 51 concepts in the prepared handoff—four checksum-bound processed concepts plus 47 exact dynamic concepts. The resulting browser catalog is revision `df8cb2ceffa9148194c0e60e8d10b6c0d83ad944e3968bd671f97a72301b65fa`, with 450 source references deduplicated to 410 unique payloads. Exact source bytes plus Ogg Opus, AAC/M4A, and MP3 renditions are published as 1,640 immutable objects under `atmosphere/atmoshaper/v1/audio/` in `massagelab-public-media`; the total staged/public payload is 6,308,427,694 bytes. Every public audio object passed content-length, media-type, byte-range, and CORS verification, and the exact 968,170-byte catalog body passed SHA-256 verification at its immutable revision URL. The `/music` AtmoShaper integration now has a searchable/category-filtered Ambient sounds library, optional recording selection for White Noise, exact reviewed scheduling, private per-layer Web Audio output, preview promotion, mix controls, and the selected processed Boiling Water loop. It prewarms the shared Tone module before Play or Preview becomes actionable and synchronously unlocks the suspended audio context inside that accepted gesture before asynchronous owner cleanup or mixer creation. Final review hardening makes a new White Noise recording replace the prior selection for that concept, propagates post-start ambient scheduler failures into retryable failed-layer state, and attaches the interruption monitor when an AtmoShaper-first session prepares its shared audio context. Prisma validation, TypeScript, lint, all 3,078 repository tests (`3,075` pass, `0` fail, `3` intentional skips), `git diff --check`, the 109-page Production build, the production-bundle guard, and focused mobile/desktop Playwright acceptance pass. The mobile cold-start regression proves that one AtmoShaper tap makes the first resume attempt inside the initiating turn and reaches playing; desktop acceptance proves that changing White Noise leaves exactly one layer with the new recording. Supported in-app browser inspection could not enter even the already-open localhost tab because the app-admin policy verifier was unavailable; that safeguard was not bypassed. Fresh exact-head Codex review found no major issues, all review threads are resolved, and every hosted check passed, including all four browser lanes and the aggregate `qa` gate. PR #187 merged as `6f3b9559f85c614d742e0305f74554be0990702c`; Vercel Production deployment `HroSneuwSpwVdFQowNhsJJ48YERi` completed successfully. The live `https://massagelab.app/music` alias resolves to `https://www.massagelab.app/music`, returns HTTP 200, and exposes AtmoShaper; the immutable R2 catalog still returns HTTP 200 with its exact 968,170-byte body.
- AtmoShaper processed speech routing: Batch 21 uses the genuine nine-source v2 no-vocals rerender, while Batch 35 London Ambience and Batch 45 Stadium Crowd retain their exact heard v1 speech-reduced bytes. The reviewer preferred Stadium's earlier speech-stage sound over the later dynamics/leveling experiment and accepted the restored version as finished for the current catalog stage. Processed-audition fingerprint `581844bfabfe92024656ea7686c8aff4e729bc0bd575da5316634571e4254ea1` is the immutable finalization input; current Pass fingerprint `9db93767ce5667a5831a6b803520712b657634eee88268769088886aac17bf35` also closes the abandoned dynamics/leveling requirements. The technically valid six-file experiment remains preserved externally under declaration `9c80ffb6d9beb09a6760a7dba03245b0a8e6f909ffe6551b3990d2eedffb0cb0`, but it is inactive: no page, current review fingerprint, or audio route selects it. The live page returns the retained v1 outputs and the Pass. No prior bundle was overwritten or deleted, and no publication or production qualification is inferred.
- AtmoShaper Signature Batch 09–51 reviewer amendments: the current development queue contains 42 surviving concepts plus stable redirects from Batches 32, 39, 40, and 48 into their merged survivors. Forty-one current direct chat Passes are fingerprint-bound and require no page decision buttons. Dropdown batch identities remain authoritative; the page labels filtered position separately as Review progress. Heavy Rain's fresh random `0–143.413s` window of at least 20 seconds with a 10-second return crossfade is Pass. Church Bells' transparent median-centered gain treatment and 15-second crossfade are Pass. Batch 45's preferred six-source speech-reduced version is Pass. Batch 49's four-source pool, including only `0:00–0:06` of `The next station is - Announcement. 2.wav`, is Pass; stable Batch 47 remains unchanged and source-insufficient. No raw audio was edited, and these review decisions do not qualify or publish production media.
- AtmoShaper Signature expansion review: stable Batches 52, 55, 56, 57, 60, and 61 remain active without renumbering, while Batches 53, 54, 58, and 59 remain removed. Grinding Pepper triggers its 18 events at 100 events per minute with 15% jitter and is an exact fingerprint-bound Pass alongside Bagpipes Outside and Open Fields. Ambient Loops keeps 22 sources (`10 / 1 / 10 / 1` Pads, Strings, Piano, and Mallets) after 12 exact filename removals. One random recording plays three to six consecutive times total before a different recording enters. The default source switch uses four 4-beat bars at 77 BPM (`16` beats, `960/77`, about `12.468s`); the three retained short recordings—String Loop 03, Piano Loop 04, and Piano Loop 06—use two bars (`8` beats, `480/77`, about `6.234s`). That exact revised Batch 56 pool and schedule is now Pass at fingerprint `d8aca0fa338435811fb7f8aff453307552deb583a892efff57986672506da05d`. Crowd Walla keeps all 13 sources and applies measured constant gain to a common `-27.2 LUFS` target with at least `1 dB` true-peak headroom; that exact setup is a Pass. White Noise still plays only the chosen one of 11 recordings and repeats it using a plain `0.25s` self-overlap with no fade; it is Pass at fingerprint `51f255a2bd993831479c0b8e65caa984ab9b711e22b534a3b4b22a6a0bf3f081`. Batch 01 Campfire is retired from the active review/prepared handoff because Batch 30 Fireplace replaces it; its immutable manifest, QA, and external audio remain historical evidence, and old Batch 01 URLs redirect to Fireplace. The four active processed-audio reviews and all surviving concept reviews share one Concept review queue dropdown. `/dev/candidates/prepared` derives 51 completed concepts (four terminal processed-audio concepts plus 47 fingerprint-bound dynamic Passes), 51 non-generated Moodist concepts that still need a usable recording, and three native-generated noise concepts that need no recording. Each prepared row can play the exact processed or dynamic concept on demand and link back to Review; only one row plays at a time. Batches 02–05 show final assembled auditions for Air Traffic Control, selected Wide dual echo x2 whistles, selected 8-second Boiling Water, and selected Dry Dryer. Batch 04 exposes the exact `0:00–1:30` one-time opening, `0:15–1:30` repeating region, 8-second equal-power return crossfade, artifact-local `1:22` loop point, and a live rendered timeline. Batch 05 identifies the original Dry selection, displays its adaptive whole-recording policy with a fresh `3.75–10s` crossfade lead at every boundary, and shows the selected recording timeline before playback plus the seekable live timeline after start. Of the recording backlog, 47 have no construction candidate and four—Subway Station, Underwater, Wind, and Wind in Trees—have a group but no usable included recording. Cave remains complete at 5/5, Coffee Shop at 13/13, Light Rain owns its six folder recordings, and London Ambience remains 12/12. Those 51 exact completed concepts are the closed input to production catalog revision `df8cb2ceffa9148194c0e60e8d10b6c0d83ad944e3968bd671f97a72301b65fa`; future review changes require a new revision.
- AtmoShaper Boiling Water Batch 04 current correction: this v2 state supersedes the v1 Batch 04 details retained later in the aggregate history while preserving that external v1 directory unchanged. The exact reviewer recipe plays source `0:00–1:30` once, then repeats only `0:15–1:30` with `2s`, `4s`, or `8s` equal-power return crossfades. The three immutable stereo 48 kHz/24-bit PCM v2 candidates total 134,496,306 bytes; exact durations are `161/157/149s`, first-loop offsets are `88/86/82s`, loop-region durations are `73/71/67s`, measured loudness is `-37.1/-37.0/-36.9` LUFS, and all peak at `-14.3 dBTP`. Every output hash/size matches declaration `7ba27607e61fdd09f3cad2c6898fec8726dd9762d798ad1437ff8120ae6e16e4` and manifest `dbbdb177e8cd47fa2923528f9de834a5ad1cdb87fe3c2cc3c780bf67a2a6c5c6`. The development player starts each artifact at zero and uses sample-accurate Web Audio regional loop points so `0:00–0:15` never repeats. The reviewer directly selected the exact `Long 8-second crossfade` as Pass; a committed selection record binds that choice to output identity `d63ea4082951404502d5d48c3fdfc2df6af907ca897c06f95956f52b0b96bf04` without fabricating browser playback timestamps. Batch 04 is `audible-qa-passed`; the `2s` and `4s` comparisons remain undecided rather than rejected, and production/publication qualification remains separate.
- AtmoShaper Dryer Batch 05 selection: the exact `moodist:dryer` source was checksum-verified, trimmed from `1.8–17.7s`, given `0.15s` boundary fades, and rendered as one 15.9-second stereo 48 kHz/24-bit PCM comparison WAV. Its 4,579,302 bytes and SHA-256 `a7d6dc5e5195d4dc7d37d2e0ff9898c71b4752e71203f3ec3b0730034c25e546` independently match declaration `15ae5817239e3b43da3a520bae261fdf03756401d05220b06885996b18cfe2b9` and manifest `2612f3cf58c2be61ad3f609fc5e6237af34143c1185a983f5a892a58a11d9d9d`; no partial output remains. After hearing the whole-concept comparison, the reviewer directly selected `Dry concept`. A committed record binds that Pass to the exact raw source, comparison artifact, and previously approved per-boundary `3.75–10s` crossfade policy without calling the trimmed candidate rejected. The development page restores the choice after restart, and Batch 05 is terminal as `audible-qa-complete-dry-selected`. Production/publication qualification remains separate.
- AtmoShaper Signature master whole-concept review queue (pre-amendment snapshot, superseded by the Batch 09–51 state above): development-only `/dev/candidates/processing` originally exposed 46 accepted, source-backed concepts as Batches 06–51 and recorded the first seven direct chat Passes without page buttons. This retained history established the concrete policy readback, per-source timelines, fingerprint-bound raw-audio route, and no-publication boundary that the current amended queue continues to use.
- Status: private alpha.
- Repository licensing: MassageLab is source-visible proprietary software, not open-source software. Copyright © 2025–2026 Derrick Bowersock, doing business as Massage Lab. All rights reserved. The repository remains public for CodeRabbit and source inspection, but public access does not grant permission to reuse, modify, or redistribute MassageLab-owned source code or assets. The root `LICENSE` contains the complete terms, the package manifests report `UNLICENSED`, and expanded or drawer sidebar footers display the compact ownership notice.
- CI and PR feedback: PR #185 remains open and unmerged with the four-lane repository-owned QA workflow proven on substantively reviewed implementation head `728eaa405e5e1a00730ece36dd0a43b2db460547`. One Production browser build feeds four exact-once Playwright project/spec lanes, while Code quality runs independently and the stable final `qa` job fails for any non-success dependency. Hosted run `32039251082` accepted cold attempt 4 plus exact-primary-hit warm attempts 5 and 6: runner-to-`qa` times were 8m 58s, 8m 50s, and 9m 00s; every attempt was green, discovered all 310 ordinary tests in nine spec files, and contained no retry or flaky marker. Lane browser-step medians were `288 / 291 / 291 / 325s`, a 12.85% spread under the approved 20% limit. Earlier attempts 2 and 3 are excluded because GitHub codeload HTTP 429 failures stopped one lane during action setup before full browser coverage; they were not application or test failures. Repository rules still do not require status checks, so mandatory `qa` remains a separate governance decision.
- Atmosphere Android media/player acceptance: PR #183 remains open, draft, and unmerged at exact physically tested merge head `ca429f7f4c3cc1c40bac2a850be73a8226981c2e`. On the recorded Samsung Galaxy S24 Ultra with Chrome `151.0.7922.137`, immutable deployment `https://massagelab-1ia9a0227-dsbteam.vercel.app/music` passed the affected Round 4 matrix: first touch starts playback; sharpened notification art looks good and stays paired through Previous/Next; the vinyl uses a 52-second revolution and respects reduced motion; rail vinyl keeps approximately 7px clearance; portrait center-card height is stable across expanded/collapsed player states; touch-only station buttons are conditional on reduced motion or a fine pointer; station navigation loops both ways; constrained landscape uses the global side rail; and explicit Stop retires the rail at 60 seconds while restart cancels retirement. Exact-head local and hosted automation is green, including the split four-lane Chromium QA and local WebKit media smoke. The native notification Favorite/heart remains deferred to a future Android wrapper; installed-PWA, Bluetooth/headset, real-call, meeting-app, controlled carrier-failure, broader lock/background-return, current-session preference, and physical Apple rows remain pending. The QA report records the three final screenshot rasters as `1492×667`, `1492×667`, and `720×1452`; Android Chrome searched the requested address-bar diagnostic, so no CSS viewport, DPR, or safe-area values are inferred from them. This docs-only closeout does not change the physically tested bundle; the remaining PR gate is a fresh substantive CodeRabbit review with all hosted checks green. Do not merge without a separate decision.
- AtmoShaper ambient catalog audit: Moodist media is retired with no binary fallback; only its exact 84 non-binaural concept identities and upstream references remain research evidence, while the five binaural presets are separate. The [2026-08-23 read-only Signature Sounds audit](superpowers/reports/2026-08-23-atmoshaper-signature-sound-catalog-audit.md) covers 100 top-level packs and 3,693 audio files totaling 9,995,726,103 bytes, with 3,587 WAV, 69 MP3, 35 AIF, 2 AIFF, and 239 duplicate-checksum groups; MIDI is excluded. The user accepts Signature Sounds' own site-wide CC0 statement for this downloaded library, with pack-specific CC0 files retained as stronger evidence, but license evidence does not pass technical, listening, or processing gates. Current decisions are 0 qualified Moodist matches, 7 mapped candidates needing audition or processing, 74 recording/source gaps, and 9 exploratory Signature-only concept candidates outside the 84-concept production taxonomy. All 16 candidates remain active and technical-, listening-, and processing-pending; no listening approval is recorded. The repeatable audit defaults to stdout with no write, and the external-output processing planner emits inert `ffmpeg` argument arrays only. Its current real result is `no-qualified-assignments`, `not-run`, and zero sources with no output-directory creation. Remaining gates are full-candidate listening, technical measurements and decisions, approved assignments, separately authorized external encoding, decoded seam/loudness verification, and processing verification; Git media, hosted manifests, R2 or other uploads, runtime wiring, deployment, push, and merge each remain separately authorized work. Local implementation history is `a5b0f0a2` for the catalog model, `9cce4abf` for the audit/report, `75b0f422` for the inert planner, and `afb4e30c` for linked-worktree output-root confinement; none of those commits was pushed, merged, deployed, or uploaded by this work.
- AtmoShaper exhaustive Signature review: the follow-up pass now gives every scanned recording exactly one visible discovery state instead of interpreting an undeclared mapping as proof that the library lacks a source. The deterministic manifest covers all 100 packs and 3,693 files as 926 proposed candidate sources, 2,738 explicit exclusions, and 29 unclassified recordings; proposals currently touch 34 Moodist concepts and 59 Signature-only concepts. This recovers all 8 `Footsteps In Snow` recordings for `walk-in-snow` and all 246 `Walking On Stoney Pathway` recordings for `walk-on-gravel` plus the proposed `Walk on Stone` extra. The fingerprint-matched user export is normalized into 354 explicit Keeps, 113 explicit Maybes, 360 source-only Rejects, and 99 contextual Maybes; excluded and unclassified discovery sources are unchanged. All 93 concept groups stay active, including seven with no non-rejected ingredient. Development-only `/dev/candidates` is now a lightweight hub for separate recording and concept review pages backed by one fingerprint-bound version-3 browser workspace. Existing version-1 recording and version-2 group work migrates automatically without deleting either legacy record. Recording review supports overall observations, concept-specific Include/Remove and notes, multi-concept assignment, and local custom concepts. Concept review exposes every included or removed recording, targeted playback inside the exact dynamic setup, per-ingredient notes, and the four curated strategy families. Approval binds exact included source IDs, strategy, and settings; ingredient/strategy changes clear stale evidence while note edits preserve it. Either page exports one complete handoff, and the interface remains available for future concepts. The server owns `ATMOSHAPER_SIGNATURE_SOUNDS_ROOT`; no page, manifest, or export exposes it. This reusable scheduler is a local design-preview owner only: it does not mutate the strategy declaration, pass technical/listening/processing gates, create final processed audio, wire production dynamic playback, upload, deploy, push, or merge.
- AtmoShaper Signature construction reconciliation: the returned version-3 review is now preserved as immutable evidence and reconciled with the exact version-1 listening export into `signature-sound-construction-interpretations.json` plus a deterministic generated construction review. The closed authority chain reproduces 3,693 recordings, 93 groups, and all 38 non-empty group/ingredient notes as 36 structured dispositions, 0 deferred, and 2 naming decisions. It records exact speech-removal versus speech-obscuring outcomes, source-specific trims, removed-source no-assignments, playback spacing/repeat/transition requirements, renames, source gaps, and rebuilt-audition requirements without parsing free text at runtime. The reconciliation CLI writes nothing by default and may atomically replace only the fixed generated owner after canonical containment and reread validation. This is construction intent only: audio editing, speech treatment, level/loop work, technical QA, rebuilt listening, processing verification, qualification, production runtime, publication, upload, deployment, push, merge, staging, and commit remain pending or separately authorized.
- AtmoShaper Signature playback-construction audition: development-only `/dev/candidates/construction` now performs the exact reconciled scheduler requirements through the existing one-player preview owner. Its owner-verified generated projection contains Dryer, Walk on Gravel, Walk on Leaves, Air Traffic Control, Horror Suspense, Moon Footsteps, Sci-Fi Whistles, Walking in Puddles, and visibly blocked Walk on Stone. It enforces three/four-selection repeat history, per-boundary `3.75–10` second Dryer transitions, exact spaced-event ranges, Gravel crossfade/overlap A/B, Moon next-event overlap, and the approved cadence settings. Exact-heard Pass still binds the construction fingerprint, scheduler version, exact source IDs, settings, policy, timestamp, note, and playback-only or complete-construction scope. Needs Rebuild and Reject may instead be note-backed without fabricating heard evidence or QA scope; Pass remains unavailable until the exact current setup is confirmed. The page retains isolated browser-local storage, closed JSON import, and JSON export without migrating or deleting earlier review records. The returned historical QA export preserves six exact heard configurations plus the two walking notes. The validated current decision input records Walk on Gravel and Walk on Leaves as `needs-rework`; exact `complete-construction` Pass decisions for Horror Suspense, Moon Footsteps, and Walking in Puddles; and exact `playback-only` Pass decisions for Air Traffic Control's spaced-event policy, Dryer's per-boundary `3.75–10` second transitions, and Sci-Fi Whistles' `0–8` second spaced-event sequence. Air Traffic's normalized artifacts, Dryer's pending trim work, and Sci-Fi Whistles' pending echo/delay processing remain distinct from scheduler evidence; none of those future integrations is inferred as already heard. Blocked Walk on Stone remains without a construction decision. The returned complete-review export is byte-identical to the already preserved canonical fixture, so no recording or concept review restart is required.
- AtmoShaper Signature derived-audio Batches 01-04: the user authorized external processing for known workable concepts. Closed portable declarations, a batch registry, and checksum-bound runners verify source bytes, canonical root separation, the exact FFmpeg 9.0 build, EBU R128 measurements, deterministic output identity, no-overwrite behavior, technical output properties, and all-or-nothing publication. Campfire Batch 01 rendered four lossless stereo PCM WAVs at 48 kHz/24-bit totaling 129,799,918 bytes; its manifest SHA-256 is `04f96575eb156fc39913244b9e7f30d46025f7b3580031e15af2cc1e8c3b53a8`, and all four exact artifacts are `audible-qa-passed`. Air Traffic Control Batch 02 preserved 12 exact mono recordings and speech, applied quietest-input attenuation from `0` to `-9.1` dB, and rendered 8,802,360 bytes of mono 48 kHz/24-bit PCM, with all outputs measuring `-23.1` LUFS. Its declaration fingerprint is `87a79b019a119ef781640fb344a8f3b688a2aa7fd0a0457a7da29a1b1da17008` and its external manifest SHA-256 is `fb0163b77a656f92f61bcc2bd63d46b9ad74bbc55fb9b4557268beab95a1ef94`; the user's 12 exact output-bound Pass decisions make Batch 02 `audible-qa-passed`. Exploratory Sci-Fi Whistles Batch 03 v2 keeps all 18 exact stereo sources and renders four time-effect treatments per source—short delay, medium echo, wide dual echo, and wide dual echo ×2—for 72 stereo 48 kHz/24-bit PCM WAVs totaling 71,680,872 bytes. The ×2 option preserves the `260/520 ms` timing, doubles decay to `0.68/0.34`, and uses `-3 dB` safety attenuation. Its declaration fingerprint is `e05aadb04e87f56e9df4f69cba180ce37ca0f4481fbfc0fa2c981bdd4fb4163a`; external manifest SHA-256 is `8ff6856b1342a8d88366eeae48e1307b7cbc0921ef0ef7755fce8ac4f7faa6c6`; all file hashes/sizes and technical properties independently match, and measured output peaks span `-23.9` to `-6.2` dBTP. The prior 54-file v1 directory remains untouched, and the three retained v2 treatments are byte-identical to their v1 counterparts. The reviewer directly passed the complete `wide-dual-echo-x2` concept, and a committed selection record binds that decision to the exact 18 ×2 output identities plus the immutable declaration and manifest; Batch 03 is now `audible-qa-passed`. Short delay, medium echo, and the original wide dual echo remain undecided comparison variants rather than inferred rejections. Boiling Water Batch 04 uses the one exact source and measured `4.0–111.4s` clean window to render three cyclic equal-power candidates with `2s`, `4s`, and `8s` seams. The three stereo 48 kHz/24-bit PCM WAVs have independently matching hashes and sizes, exact durations `105.4`, `103.4`, and `99.4s`, and measured peaks of `-1.9 dBTP`; declaration SHA-256 is `228d2dcf42b7e04b4ae0768748f5fd6d42847099a4d12a67199c246e41845fda` and manifest SHA-256 is `189d5b59d51ec36a878d9f61b245b8c6e44a83a1d78c24d4fd1bcfa43a806687`. Batch 04 is `rendered-pending-audible-qa`; none of its seam variants is selected. Development-only `/dev/candidates/processing` now accepts a closed batch selector against a server-owned catalog root while retaining the exact single-root fallback. A bare catalog URL selects the first non-passed anchor, currently Batch 04. Its dedicated Boiling review compares the dry source with each manifest-bound artifact and requires two real file-end-to-file-start crossings before Pass; browser QA remains local and identity-bound without cross-batch reuse. No batch is published or production-qualified. Dryer remains trim-parameter-gated; walking ingredient repair, voice treatment, separation work, other concept recipes, production runtime, publication, upload, deployment, push, merge, staging, and commit remain pending or separately authorized.
- Background branding implementation: all 84 enabled background IDs remain immutable while the exported registry now applies the seven user-approved naming batches from `data/background-branding-catalog.json`. Each option has one canonical catalog label plus a literal visual descriptor across picker, acquisition, credit-confirmation, and account-ownership surfaces. Prior public labels are retained only as hidden search/support aliases. `Massage Laba Lamp` is the sole approved Massage Lab-branded title, and the compact catalog marks internally conceived signature originals without changing entitlement or renderer identity. All 84 full-catalog previews are visually approved and independently validate as 82 animated entries plus 2 truthful poster-only entries, with exactly 1,476 videos and 252 posters. The immutable `catalog-approved-1` release contains 1,728 referenced objects totaling 822.1 MiB; the remaining 577.5 MiB of the 1.367 GiB local directory is generation and validation material. Every approved recipe setting, rendition URL, hash, and media byte is preserved, and the approved catalog is active in Production.
- Background preview publication: all 1,728 allowlisted objects (862,078,635 bytes) are uploaded to `massagelab-public-media/chimer/background-preview-catalog/catalog-approved-1` and publicly served from `https://media.massagelab.app/chimer/background-preview-catalog/catalog-approved-1`. Complete remote verification passed every manifest byte count, SHA-256 digest, MIME type, and `public, max-age=31536000, immutable` cache header. Production sets `NEXT_PUBLIC_CHIMER_PREVIEW_CATALOG_BASE_URL` to that immutable release. Deployment `dpl_BU9RUjSNWigCEe6oEL5Xx5o3rWp2` is READY and owns the primary aliases. Hosted Chimer verification proves poster-first rendering with zero default players, exact center-plus-two-on-each-side playback in an all-animated window, vertical high-quality VP9 sources at ready state 4, truthful omission of static videos, clean return to zero players when paused, and successful `200` poster plus `206` video responses from R2. The bounded Vercel runtime error scan returned no logs. Rollback removes the Production catalog-base setting and restores prior READY deployment `dpl_EHT1dFikZJzDG26VCxd6UP5vYHjo` (`https://massagelab-crvxw5uj9-dsbteam.vercel.app`) so the fail-closed v1 fallback resumes; immutable release objects are retained.
- Background access and colors: subscriptions sell `premium_backgrounds` access. Chimer display colors are unrestricted, while each free, membership-included, credit-owned, or purchased background exposes its supported color controls through the canonical selected-background access decision; locked premium backgrounds do not expose those controls. Saved palette, Clock, and Lamp settings are retained without migration.
- Admin user operations: authority is split into full `ADMIN`, review-only `ANATOMY_REVIEWER`, and review-plus-edit `ANATOMY_EDITOR`; retired `ANATOMY_ADMIN` assignments normalize to Editor during migration and compatibility reads. Admin pages reload database roles rather than trusting session claims, the dashboard exposes only freshly authorized capabilities, and every destination retains its own guard. Original Admin User Operations Branches 2-7 provide authorization, evidence, directory/detail, role, security, background-credit, and temporary-access controls; original Admin User Operations Branch 8 provides full-Admin-only USD invoice-credit goodwill for exactly one eligible active/trialing Supporter Customer/subscription identity. Exact known-transaction verification binds the provider transaction to its expected Customer, mode, `usd` amount, originating operation/key, and safe transaction-time `ending_balance`; that immutable balance is credit immediately after the exact credit, while a later current Customer balance is an optional separate observation. Any path that could create at Stripe makes a final fresh full-Admin database check after eligibility and replay-clock reads. Only typed database-confirmed revocation may record `ADMIN_AUTHORITY_REVOKED`, and only while the creating invocation still exclusively owns a never-attempted no-ID `PREPARED` operation. Every definite pre-provider failure uses that same exact creator-only claim, and a lost claim preserves the reconciler's canonical no-ID or transaction-bearing state. Unknown failures, lost ownership, and races remain unresolved and provider-ambiguous. All unresolved work retains the same operation, idempotency key, and origin evidence, including reconciliation-only ownership before provider I/O and the conservative 23-hour-55-minute replay margin. Live creation still requires Production identity, the explicit live flag, and separate authorization naming the controlled account and exact amount. Corrected-head Admin Operations Closure Branch 2 validation passed 232/232 focused and adjacent tests, typecheck, lint with only the existing `app/chimer/running-timer.tsx` Babel large-file note, Prisma validation with no schema change, all 2,480 tests with 2,479 passes and one intentional skip, prebuild Prisma generation, and the 104/104-page build. Ready PR #181 is open and unmerged. Exact pushed head `182ed627` passed all non-CodeRabbit hosted checks, including Browser QA. A substantive hosted CodeRabbit review completed at `2026-08-13T19:54:03Z` as review `4931055672` / run `c4c9b211-a7ff-4ada-9155-379caaa0197f`; it verified the four earlier corrections and raised two valid minor follow-ups. The four earlier threads are resolved. The required follow-up head must contain both current corrections: a comment clarifying per-actor fixture load sequencing and canonical/lifecycle records reflecting the exact hosted state. Fresh substantive review of that exact head and resolution of the two current threads remain required. No Production activation is claimed and the live gate remains closed; Admin Operations Closure Branch 3 Production activation, migration deployment, and browser acceptance must wait for that merge. Successful self-service or Admin-requested password-reset consumption atomically changes the credential, consumes every outstanding link, increments `User.authSessionVersion` once, and invalidates existing compatibility sessions without creating a second Admin evidence bundle; old JWTs fail on their next successful database-backed refresh, and compatibility-session invalidation is not an active-JWT or signed-out-user count. Detailed billing procedure and historical proof are maintained in the Admin runbook and project log.
- Approved adaptive-preview pilot: the eight stable IDs use frozen `recipe-1` passive captures in independently rendered landscape, square, and vertical compositions. The low/standard/high ladders are `384×216`/`640×360`/`960×540`, `256×256`/`512×512`/`768×768`, and `216×384`/`360×640`/`540×960`. VP9 uses `libvpx-vp9`, CRF 30, zero target bitrate, `good` deadline, and CPU-used 2; H.264 uses `libx264`, CRF 21, slow preset, high profile, `yuv420p`, and fast-start. Both use Lanczos scaling and no audio. Constellations and Scanline use 30 fps; the other six use 24 fps. Decoded validation enforces a maximum normalized seam difference of `0.080` for natural loops and `0.120` for crossfades. All 8 entries, 144 final videos, and 24 posters pass completeness, hash, decode, motion, dimension, duration, pixel-format, and seam checks, and the user approved every background/aspect/quality/codec comparison on 2026-08-06. The v2 pilot manifest and binaries remain preserved as local evidence; the approved full catalog is the active hosted release described above.
- Background review outcomes: the auth-free development-only `/dev/clock` surface is available for Device Toolbar review. Solid Color is a free, low-cost static option in Chimer, Clock, Music, and Ambient; its `solid-color` renderer and picker fallback both default to MassageLab orange (`#FF7A1A`), Custom maps its sole Color role to Swatch 1, Swatches 2-7 are unused, Harmony is unavailable, and it has no tuning controls. Static Gradient now uses a real low-cost CSS renderer with two to seven active shared-palette colors, linear/radial geometry, direction or radial shape/reach/center controls, and ordered color-stop positions; its existing `static-gradient` ID and entitlement behavior are unchanged. Retro Grid now consumes the Host's resolved motion contract for both its WebGL frame loop and CSS fallback, so explicit animation remains active without a second browser-media-query veto and resolved reduced motion remains static. DNA now presents its authored node and strand speeds as `1.0x` defaults on bounded `0.1x-2.0x` controls, exposes strand-rotation on/off and clockwise/counterclockwise direction, gives node size its own `25%-200%` control, makes spacing move strands without resizing nodes, and presents its authored scale as `100%` on a real `1%-100%` range. Twisted Cubes now caps Rotation speed at `2x` and gives Layer depth a clearly visible but bounded orthographic projection while preserving the user-approved staged three-axis rotation and easing. Electric Mist now uses a `1%-100%` animation-speed control where `50%` preserves the authored default and `100%` maps to the authored `200%` ceiling; existing saved speeds migrate without changing their rendered pace unless they exceed the new ceiling. Liquid Ether's autonomous-motion controls now use the concise labels Motion, Speed, Intensity, Resume, and Ramp while retaining the existing saved settings and renderer behavior. Dark Veil can now enter Custom mode for its dedicated shared-style Hue shift slider directly beneath the seven read-only reference swatches; Harmony remains unavailable because the shader has no concrete palette roles. Its slider preview follows the renderer's reverse, nonlinear YIQ hue response using a nine-point visual calibration rather than treating the signed shift as a standard HSL wheel. Resolution scale is presented as `25%-100%` and reduced values visibly retain their lower-resolution pixels without changing full-viewport geometry. Gradient Blinds now keeps both its gradient and blind cell boundaries fixed while every slat retains a visible source-color floor; a bounded passive spotlight sweeps slowly through the full set and gently flexes the shading inside each cell like a light breeze. Opt-in cursor spotlight control remains available, and its noise is spatially stable rather than flickering between frames. Grid Scan now derives its tunnel width from the live viewport aspect ratio while retaining one reference height and one shared far-depth cutoff, so the opaque black distant opening matches the user's screen proportions while the ceiling, floor, and both walls end together in portrait, landscape, resize, and rotation states. Dot Field now defaults to Sparkle on, a `4.0` dot radius, and `6.0` dot spacing; untouched former defaults migrate once while customized saved settings remain unchanged. Dot Grid and Grid Distortion now offer opt-in simulated cursor interaction with persisted `0.3x-2.0x` Fake cursor speed controls. Dot Grid uses its existing proximity/inertia response and Grid Distortion uses its existing data-texture displacement response; both yield to real pointer input, remain active on compact viewports through their existing reduced-DPR paths, and are off by default. Grid Distortion's passive cursor remains additive to its continuous ambient drift. Tile Grid and Hex Grid now default Fade time to `20s` and use one shared logarithmic slider in both Visual panels instead of separate hour, minute, and second inputs; the slider preserves fine short-fade control across the existing `0.25s`-`23h 59m 59.9s` domain and displays the actual duration while retaining the persisted seconds contract. Untouched former `1.2s` defaults migrate once, while previously customized fade times remain unchanged. The shared seven-swatch editor now tints swatches assigned to the selected background with the Apply action's leaf-green token family and unused swatches with the destructive action's red token family; the existing assigned role labels and dashed unused border remain redundant non-color cues in Source, Custom, and Harmony modes across every responsive layout. Reveal Dots now retains its fixed source-like matrix while individual dots brighten and dim on slow seeded timings with a phone-legible opacity range; the rejected directional sweep is removed, dot position and size stay unchanged, and reduced motion resolves to the authored steady frame. Vortex now reserves editable Swatch 7 for its independent background and exposes a persisted `0°-360°` Particle hue slider beneath the Custom swatches; the renderer retains its authored continuous 100-degree particle range, while Harmony is unavailable because discrete harmony roles would misrepresent that procedural field. Retro Grid, Aerial Rays, Wave Current, Photon Beam, Reveal Dots, 3D Globe, Lamp Glow, Wave flow, Pixel liquid, and Aurora bars now also reserve saved Swatch 7 for their full-canvas Background role; Harmony regenerates only their visual colors in Swatches 1-6, and no enabled literal Background role defaults to Swatch 1.
- Shape Grid now defaults to raw speed `0.25`, accepts the full raw `0.0-2.0` renderer range, and presents that range as `0%-100%` in both Visual panels; the default therefore appears as `12.5%`, and `0%` produces a genuinely stationary grid. The exact untouched former `1.0` default migrates once while customized legacy speeds are preserved and clamped only when outside the corrected range. The screenshot-matched Chrome Flow (`massage-lab-liquid-chrome`, with Speed/Amplitude/Frequency controls) now accepts raw Speed `0.001-3.0` and presents it as `0.1%-100%` with tenth-percent steps, while retaining the raw `0.2` default. The separate `massage-lab-chrome-flow` Flow speed/Time scale renderer is unchanged. Photon Beam now defaults to a moderate 12 lines and 12 signals in the shared settings and direct renderer fallback; only the exact untouched former 80-line/94-signal pair migrates, so customized saved densities remain unchanged. Aurora Field now exposes Motion speed, Intensity, Blur, and Field reach in both Visual panels. Source mode preserves the authored background and five aurora-band colors. Custom maps the five bands to Swatches 1-5 and the background to Swatch 7; Harmony continues generating only the five band colors while displaying, preserving, and allowing edits to the saved Swatch 7 background color. Swatch 6 remains unused by default. Dotted Glow now follows the same independent-background contract: Source preserves the authored background, dot, and glow colors; Custom maps Dots to Swatch 1, Glow to Swatch 2, and Background to Swatch 7; Harmony regenerates only Dots and Glow while keeping Swatch 7 saved and editable. Both Visual panels expose Motion speed, Dot size, Dot spacing, Dot opacity, and Glow strength. The fixed center-weighted radial cloud and its fallback flare are removed, leaving an even background with glow only on the pulsing dots. Beam Field now uses the same independent-background pattern: Source retains its authored compound backdrop and cyan/violet beam gradients; Custom maps three traveling beam colors to Swatches 1-3 and Background to Swatch 7; Harmony regenerates only the three beam colors while retaining the saved Swatch 7 background. Both Visual panels expose Motion speed, Intensity, Beam width, and Glow strength. Collision Beams now also follows that contract: Source retains its authored layered backdrop and beam, accent, particle, and impact-surface colors; Custom maps those four foreground roles to Swatches 1-4 and Background to Swatch 7; Harmony regenerates only the foreground roles while retaining the saved Swatch 7 background. Both Visual panels expose Motion speed, Intensity, Beam width, and Burst size. Glowing Stars now preserves its authored compound Source backdrop while Custom/Harmony map base stars, peak stars, afterglow, glow core, and glow aura to Swatches 1-5 and retain Background independently in Swatch 7. Both Visual panels expose Motion speed, Intensity, Active stars, Star size, and Glow strength. Meteors likewise preserves its authored Source backdrop while Custom/Harmony map meteor heads, tails, glow, and edge to Swatches 1-4 and retain Background independently in Swatch 7. Both Visual panels expose Motion speed, Intensity, Meteor count, Meteor size, and Tail length. Light Lines preserves its authored 21-color Source cycle and compound backdrop; Custom/Harmony repeat Swatches 1-6 through the line field while retaining Background independently in Swatch 7. Both Visual panels expose Line duration, Intensity, Line count, Line width, and Glow strength. Bubble Field preserves its authored compound Source background and five bubble colors; Custom/Harmony map Bubble 1-5 to Swatches 1-5 while retaining Background independently in Swatch 7. Both Visual panels expose Motion speed, Intensity, Bubble size, Blur, and Blend strength, while cursor interaction and the sixth mouse-following bubble remain intentionally omitted for passive use. Existing background IDs and entitlement behavior are unchanged.
- Security dependency Fix Waves 1 and 2 are merged through PRs #162 and #164;
  Wave 2's merge commit is `b7dbe6a36b13005b3748fa46e5c6a064355bddf2`.
  The post-merge Dependabot rescan leaves one medium, development-only alert:
  `@hono/node-server@2.0.5` through shadcn's MCP toolchain. MassageLab has no
  Hono import or WebSocket upgrade route, so the advisory's public malformed
  WebSocket handshake precondition is absent from the deployed app. The
  focused closure raises the override and lockfile to the first patched release,
  `2.0.10`, and raises the regression floor with it. Exact installation,
  focused dependency tests (4/4), Prisma generation and validation, the shadcn
  CLI smoke, typecheck, lint, all 1,902 unit tests, and the 101-page Production
  build pass. npm's install summary still reports 10 aggregate findings (1 low,
  9 high); detailed audit mapping remains unavailable because the environment
  does not authorize uploading the repository dependency graph to npm, and the
  optional AgentOps security-gate scripts are absent. GitHub's public advisory
  catalog reports no advisory affecting the warned development-only
  `glob@10.5.0` copy. The inventory is not recorded as clean until the closure
  lands and GitHub rescans the default branch.
- Current invite-readiness focus: stabilize the repo and operating checklist for a first small invited-user cohort. Track 1B background purchasing is production-enabled after taxed sandbox and controlled live Checkout/fulfillment smokes. The Supporter catalog migration is `COMPLETED`: three amount-specific Products represent one user-facing Supporter Membership at $1/$10, $2/$20, and $5/$50; all three Products use the confirmed `txcd_10000000` classification, and all six Prices use exclusive tax behavior; Customer Portal permits immediate cross-Product amount changes with unchanged billing anchors and no proration; verified legacy Prices and professional Products are inactive; both unused coupons are removed; and Production has the exact six current Price mappings plus every recurring-tax readiness gate. Recurring Supporter and background-commerce live readiness pass against Stripe and the pinned webhook endpoint. PR #155's exact merge commit is READY in Production, where the GET-only live Stripe retrieval passed all recurring, background-commerce, and separately classified one-time-support gates. PR #156 merged as `4b9bb291820edd67f184e51f0ad9d7cbe9bea881` and that exact commit is READY in Production. The authorized $5 one-time-support smoke then completed successfully: Stripe Automatic Tax was enabled and completed through Stripe, the exclusive line item used the reviewed `txcd_90000001` classification, Stripe calculated $0.00 tax, and the succeeded payment captured exactly $5.00. Aggregate Track 1 implementation and rollout readiness is complete.
- The controlled Supporter subscription retry is complete: Production created the selected $1 monthly Supporter subscription with Stripe-calculated Ohio tax, and the account showed current Supporter access. PRs #150 and #151 are merged and verified READY in Production at the exact merged commit. The focused Change support amount or billing period action changed the controlled account from $1 monthly to $2 monthly and returned to Account. Read-only Stripe verification confirms an active $2 monthly Supporter Price with exclusive tax behavior and Automatic Tax enabled. The billing anchor and current-period start remain the original subscription start; the latest invoice remains the paid, non-prorated initial $1 subtotal plus $0.07 Ohio tax, so the switch created no immediate proration invoice or charge. Read-only Production persistence verification confirms that the app resolves the current subscription as active Supporter access and grants the `premium_backgrounds` feature key. The next $2 renewal has not occurred, so no completed $2 tax invoice is claimed. The unexercised $5 change, payment/address update, period-end cancellation, and annual Checkout/cancellation-or-switch paths are an expanded billing matrix for first-cohort Production monitoring or separately authorized follow-up smokes; they are not unfinished Track 1 rollout gates. Catalog or Portal-configuration cleanup remains outside this branch and requires a separately reviewed, incident-specific recovery plan and explicit approval.
- Track 1A background commerce foundation is implemented on `codex/background-commerce-foundation`: verified accounts receive exactly two idempotent credits, credit redemption and paid multi-item Checkout fulfillment create permanent database ownership, carts and 30-minute reservations persist by account, signed webhook handling is replay-safe across completion/expiration/async payment/refund/dispute events, full account admins have exact-item refund and reconciliation operations, and safe no-store user APIs expose the current wallet/cart/ownership/order snapshot for Track 1B. Subscription access and permanent ownership remain separate; subscribers may still purchase; subscription entitlements do not grant premium-background ownership; anatomy-only admins cannot operate commerce. The fixed one-dollar U.S.-only readiness gate rejects disabled tax and requires Stripe Tax mode, exact product code `txcd_10000000`, and explicit provider/registration readiness. Checkout uses exclusive automatic tax and required billing-address collection; paid fulfillment atomically freezes Stripe's order and per-item tax totals before its existing payment/ownership checks, while incomplete, unsupported, or conflicting tax evidence moves to review without granting ownership. One active Ohio state sales-tax registration was verified in Stripe on July 23, 2026. Production purchasing was enabled after the Stripe origin, registration, classification, environment, exact 15-event webhook contract, current migrations, idempotent credit backfill, clean reconciliation, taxed sandbox smoke, and controlled live purchase all passed. The live smoke recorded a $1.00 Silk subtotal, $0.07 Stripe-calculated Ohio tax, a successful $1.07 payment, processed completion webhook, fulfilled order, active purchase ownership, and clean post-fulfillment reconciliation. Test rollout cleanup fails closed unless the real test Session is expired and re-retrieved as expired and the real test Customer is deleted and re-retrieved as deleted.
- Track 1B is merged on `main` and production-enabled. Clock, active Chimer, and the Music visualizer share server-authoritative credit, ownership, cart, reservation, and reversal states; locked cards expose credit, one-dollar purchase, and membership routes; subscriber cards identify `Included with membership` access and the current rollout follow-up replaces the long optional permanent-ownership label with a dollar icon that opens the existing explanatory purchase/credit dialog. Active purchase or credit ownership displays a crown only after the authoritative snapshot reports permanent ownership, while the favorite retains dedicated responsive space. Account/Billing shows wallet, portfolio, order history, reversals, and safe support references. Anonymous users may keep validated background IDs in a browser-local intent cart and are asked to sign in or create an account only at checkout; after authentication, those IDs are revalidated and merged through the account cart API before consent or Stripe Checkout. The `codex/background-commerce-rollout` follow-up replaces membership-specific permanent-access wording with account-based purchase wording and preserves the originating Background route, query, and reopened cart through existing login, Google registration, and email verification. The conditional generic site cart appears for guest/account items or an account reservation, remains absent from Calendar/provider-selling surfaces, and never grants ownership.
- July 18 numbered-program reconciliation: Track 1A/1B, Track 2, Track 3A/3B, Track 4A, and Track 4B are complete. Track 4A's shared seven-swatch Source/Custom/Harmony palette, presets, bounded draft history, navigation guards, access-aware local/account sync, typed adapters, and responsive Visual panel are landed and production-verified through PR #158 (`dd481ad85056f968b3c58674038d7ef1ea983baf`). Track 4B merged through PR #160 as `3ababd8d3754419ecd7d3cc076fe69b8604dc246`, and that exact merge commit is READY in Production; live Chimer verification confirmed both backgrounds and their R2-backed preview media without application or media errors. `massage-lab-dna` and `massage-lab-twisted-cubes` are enabled premium backgrounds in the exact `chimer`, `clock`, `music`, and `ambient` categories; active `premium_backgrounds` membership access remains separate from permanent credit/purchase ownership, and customization follows the canonical selected-background access decision. Both effects use the Track 4A palette, draft/preset, sync, and sanitized-settings contracts. DNA keeps 7-81 strands with mount-stable complementary A-T/T-A/G-C/C-G pairs and deterministic nucleotide color roles. Its authored raw `0.06` node speed, `0.02` rotation speed, and `0.5` scale preserve the reviewed default visual while the Visual panel now labels them as `1.0x`, `1.0x`, and `100%`; the user-facing ranges are `0.1x-2.0x` and `1%-100%`. Strand rotation defaults on and clockwise, node size defaults to `100%`, and centered strand offsets keep the default `0.50vmin` spacing independent from node diameter. DNA otherwise retains `70` strands, `30deg` angle, centered position, `94%` connector width, `15%` connector thickness, and `0.10vmin` outline. Its fixed `26vmin` by `max(240vmin, 230vmax)` authored scene retains the reviewed off-screen envelope at the user-facing 100% default. Twisted Cubes keeps 6-30 count-relative layers, continuous Source HSL plus six-anchor Custom/Harmony interpolation, separate speed/stagger, and a representative reduced-motion state; rotation defaults to `0.25x`, has a `0.01x-2x` range, and preserves the reviewed staged three-axis sequence and cubic-bezier easing. Layer depth produces a visible bounded orthographic scale response while retaining the no-perspective identity. Scale defaults to `30%` with a `10%` minimum and applies uniformly to the complete wireframe progression; at the approved 20-layer default the outer layer reaches `120vmax`, and twelve thin CSS edges per layer preserve the nested full-bleed outline without viewport-size transparent faces. Each background has three committed WebM picker previews and three WebP poster fallbacks with declared dimensions. The 13-object Track 4B preview manifest/video/poster set is published under `media.massagelab.app/chimer/background-previews/` and verified byte-for-byte against the committed files. Task 9 compared the reviewed 2026 Jhey MIT archives with the production renderers and retained the approved native React/CSS adaptations.
- Latest immersive Clock/Music visualizer verification: shared Clock, Visual, and full-screen Background controls preserve separate ordinary Clock and Music Show clock preferences, keep active-Chimer-only timer and wake controls context-specific, reacquire wake lock when Music enters its visualizer, retain the selected station and renderer preferences across Minimize/Back/stop flows, and preserve focus, modal, hint, reduced-motion, and signed-in default/retry contracts. The body-level Clock/Visual shell stays outside the timer's perspective containing block, centers its auto-width dock up to 58rem, reserves active top/bottom player chrome, remains internally scrollable when the Music clock is hidden, and tracks the visual viewport at desktop, 539x597 compact desktop, phone portrait, 844x390 short landscape, and true 200% Chromium page scale without covering the protected display. Clock and Visual share the measured edge dock below a 16:9 visual-viewport aspect ratio, so 4:3 tablet/desktop layouts retain the approved bottom presentation; Visual remains capped to half of the visual-viewport height. At 16:9 or wider both panels become side sheets no wider than half of the visual viewport, open opposite the configured app sidebar, and reserve that horizontal stage edge so the protected clock refits and recenters in the visible remainder. Side-sheet contents suppress horizontal overflow, and the immersive Visual editor uses approximately one-third smaller swatch faces while preserving responsive multi-row swatches in narrow bottom docks. Both layouts honor visual-viewport and safe-area insets, while Background keeps its full-screen presentation. Show clock now uses the Clock dock header, the shared Clock color uses the Visual dock header without a Selected-background duplicate, and background-specific color rows plus Global Colors use compact 44px swatches. Background customization follows the selected background's canonical use decision: free backgrounds, active credit or purchase ownership, and membership-included backgrounds expose Custom, Harmony, presets, and renderer-property controls, while locked backgrounds do not. Non-background Clock and Chimer styling colors are unrestricted. On narrow phones, Clock, Visual, and Background join Fullscreen in the top control row as 3rem circular glass controls, with padded horizontal focus scrolling that keeps the full orange focus ring visible at 319px and 200% scale. Display rotation now uses a local 45rem perspective with user-controlled yaw range and cycle duration; Forward glow uses two clipped, masked floor layers with user-controlled intensity, projection length, and blur. Both effects remain off by default, follow whichever timer or clock is centered, and rotation stops for reduced motion. Background remains the only full-screen modal surface. Device state remains browser-local, and signed-in defaults continue to use the existing `UserPreference.appSettings.musicVisualizer` object; no Prisma schema or migration was added.
- Latest route-polish checkpoint: annotated Chimer regressions are corrected with shared accelerating duration steppers, full-width Continue, compact five-step labels, no proof carousel or redundant header, the shared animated site backdrop behind its open route layout, compact collapsed Saved setups, and a compact CTA Blue Clock action aligned in the Session duration header; Clock tabs use a stable three-column track; Clock/Chimer Global Colors now has explicit Custom/Harmony editing semantics, five visible creative colors, stable harmony previews, valid six-digit derived colors, and an authoritative shared background-host palette; Flashcard carousel arrows sit above their deck actions and ordinary Flashcard actions use filled Secondary/Default hierarchy instead of Outline; Business Planner begins directly with its tool list and uses the filled Secondary family for add/reset actions; and shell drawer categories follow a single-open accordion contract. The representative Browser route pass and phone-width Chimer/Flashcards fallback are complete; the Wellness anatomical map remains excluded.
- Current focus: public-alpha Education flashcards now use a setup-first deck builder over sourced prompt modes: media identification, name-to-summary, name-to-region, name-to-category, Anatomime name recall, and muscle origin/insertion/action/innervation. Anonymous users can browse public decks, create temporary browser decks, and study; signed-in users can save public/private deck templates, persist progress summaries, earn aggregate achievements, view per-prompt mastery totals, optionally skip prompts mastered after 10 correct answers, and complete repeatable full-library mastery rounds. Reveal-review remains practice-only and typed-check remains the saved progress path. Public image prompts use uploaded BodyParts3D media only when the asset is reviewed, reusable, and the item-image link is approved; rejected or needs-review links are hidden from prompt generation. Anatomy Reviewers and Editors can use `/admin` for their permitted review counts and entry points and `/admin/anatomy/media-review` for approving, rejecting, or requesting better BodyParts3D views; Anatomy Editors can also use `/admin/anatomy` as the full anatomy browser for deep data work. The dedicated anatomy media review queue now supports URL-driven status, anatomy-area, problem, view, request, sort, and search filters with built-in anatomy and cleanup presets that preserve the active batch through approve/reject/request decisions. The full anatomy browser is being organized more like a textbook, with separated entity tabs, relationship-backed tabs for the 11 organ systems, expanded textbook-level major structures, and a separate tissue-type row for epithelial, connective, muscle, and nervous tissue plus their core tissue subtypes. Musculoskeletal and sensory remain practical/search concepts rather than body-system tabs. Editors can still see BodyParts3D view coverage, attach existing candidates, import curated still BodyParts3D views into R2 as needs-review candidates, and capture desired-view requests for transverse/custom or wider-context replacements; both Reviewers and Editors can flag bad flashcard images directly from image prompts. Flashcards and Anatomime still use one reviewed sourced anatomy study adapter over `ANATOMY_FOUNDATION_SEED`; old Anatomime data remains archived reference only. Anatomime now keeps local per-turn learning reviews with got/missed/skipped outcomes, definitions, aliases, and a final recap that prioritizes terms to revisit. Anatomime shared classroom play now uses a reusable room code with separate game runs, host-judged and device-answer modes, 4-term team turns, active-team immediate scoring, possible steals from device answers, signed-in first-correct progress credit, same-code next games, review-window expiry, host transfer, ranked-choice host takeover, and host-facing QR/prefilled invite links. Realtime updates use Ably when configured and fall back to polling. 3D/spatial runtime tooling remains deferred. The local-first professional-record framework remains in place: one encrypted browser vault for SOAP, intake, journal, and ROM records, tablet intake workflow continuity, user-controlled encrypted vault transfer, and tested sidebar/module visibility by auth, role, entitlement, and practice-role context. Voice transcription and SOAP-assist are recorded as member-supported future goals only; no audio capture, transcription engine, LLM processing, or hosted PHI workflow is implemented.
- Product posture: clinical notes, intake forms, journals, therapist ROM sessions, transcripts, and other therapist professional-record workflows remain local-first. Therapist note-taking tools are visible but creating or viewing professional-record content requires the `therapist_documentation_tools` entitlement from an active Therapist or Team/Practice membership. SOAP, intake, journal, and therapist ROM now use one passphrase-unlocked encrypted browser vault before viewing or saving documents. Signed-in `/wellness` entries, reminder schedules, and pattern reports are client-owned self-tracking records in a separate consumer-health domain; anonymous users can practice logging, body-sensation tracking, ROM, reminder setup, and in-session pattern review in memory only. Hosted clinical sync remains gated and unimplemented until compliance requirements are met. The privacy architecture separates account/contact/booking data, client-owned wellness data, therapist professional records, and a future consent-based sharing bridge.
- Public `/roadmap` route: product-facing copy only. Internal operating state lives here and in `docs/project-log.md`.

## Database State

- Database stack: Prisma with Neon/Postgres.
- Last verified checks: focused Atmosphere node tests; Atmosphere playback-performance sample warmup tests; catalog coverage and render-plan scans; WAV dry runs and uploads for `no-refrain`, `transmission`, `trees`, the 25-piece expanded source-index rollout, and the remaining 18 Generative.fm pieces; Opus dry runs and uploads for the same pieces; AAC and MP3 uploads for Observable Streams plus all 56 Alex Bainter package stations; all 57 AAC/MP3 sample-index HTTP checks; representative AAC/MP3 range/CORS checks; all 18 remaining-generator WAV/Opus sample-index HTTP checks; representative new WAV/Opus range/CORS checks; CI workflow cache/concurrency shape inspection; `npm run prisma:validate`; `npm run prisma:generate`; `npm run build:next`; `npm run typecheck`; `npm run lint`; `npm run test`; `npm run build`; `npm run test:browser -- tests/browser/public-routes.spec.ts -g "Atmosphere lists" --project=desktop-chromium`; and `git diff --check` passed for the Atmosphere remaining-generators/playback-performance/CI-cache branches. The Atmosphere polish/QA branch passed `node --test tests/atmosphere-stations.test.mjs tests/atmosphere-station-groups.test.mjs tests/atmosphere-runtime-controller.test.mjs tests/navigation-model.test.mjs`, `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build`, `npm run test:browser -- tests/browser/public-routes.spec.ts -g "Atmosphere|Breathing" --project=desktop-chromium`, `git diff --check`, and an in-app browser rendered check for the standalone breathing route; the in-app browser does not complete Tone playback reliably, so Chromium Playwright remains the audio-route validation signal. The Atmosphere performance/audio QA follow-up branch additionally passed `node --test tests/atmosphere-stations.test.mjs tests/atmosphere-station-groups.test.mjs tests/atmosphere-runtime-controller.test.mjs tests/navigation-model.test.mjs tests/atmosphere-generative-fm-provider.test.mjs`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test`, `PLAYWRIGHT_PORT=3011 npm run test:browser -- tests/browser/public-routes.spec.ts -g "Atmosphere|Breathing" --project=desktop-chromium`, and `git diff --check` after the `/atmosphere` route and Media Session control changes. The sitewide audio player toolbar follow-up passed `node --test tests/app-settings.test.mjs tests/account-preferences.test.mjs tests/account-page-tabs.test.mjs`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test`, `PLAYWRIGHT_PORT=3011 npm run test:browser -- tests/browser/public-routes.spec.ts -g "Atmosphere proof station keeps global player state across client routes"`, `git diff --check`, and an in-app browser load of `http://localhost:3000/atmosphere`. The rendered-piano upload published 112 WAV-side public-media R2 objects and 112 Opus sidecar objects for three stations, representing 360.5 MB of WAV payload as 16.9 MB of Opus. The expanded source-index rollout published 703 WAV-side R2 objects and 703 Opus sidecar objects for 25 stations, representing 2,077.7 MB of WAV payload as 99.9 MB of Opus. The remaining-generator rollout published 704 WAV-side R2 objects and 704 Opus sidecar objects for 18 stations, representing 1,491.6 MB of WAV payload as 79.5 MB of Opus while keeping WAV fallback indexes available. The all-station AAC upload published 1,814 sidecar objects representing 4,845.6 MB of WAV payload as 248.0 MB of AAC, and the all-station MP3 upload published 1,814 sidecar objects representing the same WAV payload as 327.5 MB of MP3.
- Latest route/navigation validation: the `/music` station route, `/clock` standalone clock route, top-bar Music and Clock shortcuts, sidebar Atmosphere category, removed legacy station routes, PWA `/clock` allowlist, station attribution, and renamed browser-route coverage passed focused Node tests, `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`, focused Playwright public-route tests for `/music` and `/clock`, `git diff --check`, and in-app browser render checks for `http://localhost:3000/music` and `http://localhost:3000/clock`.
- Migration count at last verification: exact-current-main Production deployment `06a730fcc6b7ed54f91e7d6330c023f9e06262c8` verified all 45 baseline migrations current; this branch has 46 migration directories and only `20260831120000_operational_rate_limit_bucket` remains pending for Layer A. The forward-only background-commerce schema and verified-user backfill were previously exercised on a disposable Neon branch with all migration/runtime URL variables pinned to that branch's direct host: migration deployment was verified as up to date, the first backfill granted four existing verified accounts, the second granted zero, and a later synthetic verified account received exactly two credits once through the shared provisioner. During read-only rollout gate validation, never apply, repair, or roll back a Production migration. Explicitly authorized migration or recovery work may use the reviewed direct maintenance path only after preflighting every resolved host.
- Latest migration area: provider-neutral external calendar sync records for Google calendar connections, selected source calendars, imported busy blocks, outbound event links, sync run health, valid external busy-block intervals, one active Google connection per provider, and pending outbound sync error links before Google returns an event id.
- No hosted clinical database work is planned for the next forms branch; intake and note content should remain browser-local unless the documented compliance gates are met. Future client-owned wellness storage must remain separate from therapist professional records and therapist remote access until the consent/sharing bridge is reviewed and intentionally enabled.
- Do not document database rows, secrets, connection strings, or `.env.local` values in project docs.

## Website And Tool Surface

- Public app shell: refreshed role-spanning home page with optional anonymous tool router, available-tools catalog, `/tools` index, support, pricing with memberships plus one-time project support, public roadmap, about, legal/trust documents, login/register, password reset, and email verification. The responsive shell now defaults to a bottom-first main bar on phones, keeps the music player bottom-stacked above that bar, exposes Home/Music/Clock/quick-create/theme/Calendar/More controls, and uses role-aware quick actions plus rail-based homepage discovery while preserving the configurable drawer side and existing theme modes. The global app bar now spans the viewport, keeps its drawer control in the configured-side corner with the responsive MassageLab home brand immediately medial, preserves tool variants while glowing the current route, focuses anonymous quick actions on account access and Wellness, and exposes conditional PWA installation plus public Help and privacy-safe feedback from the account menu. Support now keeps human support messages in the user-controlled email flow while offering a privacy-safe Sentry diagnostic report that sends only predefined issue categories, coarse tool areas, safe route buckets, browser/display hints, and optional linked Sentry event ids. The public Roadmap now presents five equally weighted product tracks with per-track available-now and long-term-direction summaries, keeps dates and delivery priority out of the public vision, and treats privacy, accessibility, consent, local-first professional records, and hosted-data readiness as shared foundations.
- Sitewide visual-control foundation: `docs/audits/2026-07-07-sitewide-visual-control-inventory.md` records the approved surface-by-surface decision map for physical primary/default, secondary, strategic attention, quiet utility, text/link, and route-owned controls. The shared button primitive now owns a canonical molded `#FF5119` default style with a closer amber-orange edge, a less-proud orange secondary companion, selected `#B719FF` strategic CTA/attention variants, quiet non-physical Ghost/Link utility variants, a reusable always-visible `metal-fx` chromatic attention ring plus shared-button convenience wrapper whose motion randomly plays for 4-20 seconds, settles, and pauses for 5-20 seconds, and shared haptic press feedback through `lib/press-feedback.ts`; Chimer button wrappers now defer to those shared sitewide variants. The homepage uses the visible first-slice variants for primary account CTAs, membership/pricing CTAs, tool actions, and homepage rail actions. Public/auth/account one-off orange button overrides have moved to shared default, secondary, CTA, and CTA Blue variants. The merged slider batch added the shared split-pill Radix slider treatment, labeled `RangeControl`, compact dense sliders, hue sliders whose thumb center reflects the covered hue, `/dev/buttons` slider/color-control examples, and representative Chimer, Clock, music, Anatomime, and local-first notes slider migrations; the default slider fill now uses the brighter secondary-button orange instead of muted copper. The approved toggle foundation adds physical switches, standalone toggles, segmented groups, and density-aware labeled rows, with representative account haptics, calendar display settings, SOAP pain, and Chimer settings migrated to the shared treatment. Segmented controls preserve their visible selection independently from tooltip state and use a visible 420ms mechanical slide for the active face, reduced to a short 120ms linear position cue when the device requests reduced motion. Chimer/Clock settings tabs, the time-format choice, and the Clock background filter row now use the same inset tracks, molded active choices, recessed light labels, and no inactive hover lift. Route-owned select fields such as Clock font use the matching cutout dropdown surface. Drawer route rows use the approved warm inset active indicator without turning navigation into full physical buttons, and `/dev/buttons` mirrors these production treatments for future review. Compact desktop/tablet top-bar drawer and theme controls share a 31px shell token while phone bottom-bar controls retain larger touch targets. All Chimer and calendar service color fields now use the approved shared picker through explicit string-valued `value`/`onValueChange` contracts; form submission uses a named hidden value rather than synthetic native input events. The shared shader-based dither loader now randomly selects sphere, swirl, or ripple when callers omit `shape`, while skeletons and determinate progress bars remain in place when they communicate more useful state; `docs/wiki/visual-system.md` captures the current component guidance and route-owned exceptions.
- Sitewide control-system review checkpoint: the development-only `/dev/buttons` route now reviews buttons, segmented controls/tabs, native and Radix selects, toggles, sliders, color controls, text fields, selectable cards, notices, loaders/progress/skeletons, navigation, app surfaces, booking/admin examples, and protected Clock/Chimer/Anatomime/pricing/account visuals in one matrix. Shared `Button`, accelerating step button, field, select, tabs, selectable-card, notice, and `AppSurface` contracts own reusable mechanics while route code chooses intent, tone, density, and effects. The first visual-feedback revision stabilizes light-mode button highlights and glow faces, uses the requested blue Secondary and purple selected-tab colors, keeps switch thumbs theme-stable while tracks stay still, strengthens segmented hover/press/focus, gives all selects the Clock-style inset treatment, compacts the Harmony/palette controls, improves notice/progress contrast, completes the shared Anatomime specimen, and adds one-minute/hold/five-minute-double-press Chimer duration behavior. Subsequent review refinements give the shared CTA and Attention faces a blue lower-right corner mirroring CTA Blue; use the shared Glow variant for the dark-active theme switcher and a lighter, brighter glass treatment in light mode; keep switch thumbs neutral while lightening the selected alert edge; use randomized compact shared loaders in loading notices and 18px mini loader examples in buttons; and show Music navigation with CTA Blue. The wellness proof now uses detailed accessible front/back SVG bodies plus a keyboard region list over shared canonical state so a future 3D renderer can reuse the same model. The review checkpoint is approved for staged production rollout except for Wellness; S6 ordinary actions are the first narrow migration batch.
- Carousel production rollout: the approved Track 3A winners now ship through shared production owners under `components/carousels/`, `components/backgrounds/background-carousel.tsx`, and `components/atmosphere/station-carousel.tsx`. Background selection in Clock, active Chimer, Music visualizer, and setup/idle flows uses the responsive radial profiles—164×312/22 degrees, 200×240/26 degrees, 220×304/29 degrees, 256×360/33 degrees, and 280×388/36 degrees—with gap zero, radius 420, scale falloff 0.08, and nearby radius two except for short landscape's radius one/three-card presentation. Background artwork cards contain only preview media and selected styling; one permanent external tray owns metadata, access/commerce state, favorite, navigation, animated-preview preference, and accessible compact Info, below the stage normally and to its right in short landscape without requiring page or panel scrolling. `/music` uses the approved fixed 192×224 Station carousel with a 193-pixel surrounding presentation, gap zero, radius 420, scale falloff 0.05, and nearby radius four on every screen. Both presentations loop real identities, honor reduced motion, expose keyboard/side-card/icon-only Glow navigation, use Glow actions with a CTA-purple favorite treatment, and retain the approved animated metal favorite glyph. Station artwork/title/description is the shared drag-and-tap details surface; horizontal drag navigates, a short tap opens information, and Play/Stop plus Favorite remain protected controls. Music categories are a horizontally scrollable Glow-button row with protected halo paint space; short landscape Music workspaces drop the unused bottom reserve. The Clock Background modal keeps its filters in the header between its title and icon-only Close control, while active app-bar routes use the animated Metal ring only on their measurable responsive copy. Cover Flow and 3D remain unexposed review references and do not ship in production.
- Latest control review refinement: the previously approved dark-mode Glow face, geometry, hover, press, and halo are restored unchanged. Only the light-mode override borrows GlassCN `liquid` material cues—layered sheen, bottom lens, side rails, and backdrop blur/saturation—while retaining the dark-slate Glow identity and orange edge. Molded button upper edges use a dedicated light-reflection token so they remain light in both themes. Ordinary Home, Music, Clock, and Calendar phone navigation and Music, Clock, and Calendar topbar controls use CTA Blue; quick-create and theme controls remain exceptions, while both phone and desktop drawer controls mirror the theme button's shared Default-in-light and Glow-in-dark variants through the resolved-theme contract.
- Current Glow review correction: the slate-filled light-mode iteration is superseded by a translucent orange-glass face derived from the shared Default palette. The approved dark-mode Glow remains unchanged.
- Latest control-lab correction: Outline content is intentionally flat with no inset text or icon treatment; the light-only Glow halo is stronger while the protected dark-mode Glow remains unchanged; metal attention wrappers fit their child controls instead of stretching with review-grid rows; and the topbar quick-create action uses shared Default while the theme control occupies the outer edge opposite the sidebar. The phone bar already followed the requested quick-create and theme-edge contracts.
- Current route-owned visual rollout note: Clock chrome controls now use the physical icon-button language, Clock control-group containers and Chimer global color swatch cards no longer carry the leftover vertical surface gradient, Anatomime route-owned inputs/choices/setup actions have cohesive physical and inset states, pricing account/membership entry points use the Attention treatment with a single signed-out Get Started path into authentication before checkout, and `/dev/buttons` is tabbed with Clock container, Anatomime route-control, and embedded Harmony picker review samples. The embedded Harmony picker uses responsive individual CTA Blue buttons rather than a segmented track; its selected Attention button keeps the fitted always-on metal ring and opt-in sibling reflection targets. Chimer page buttons and route-owned controls are considered done/protected after user review except for explicitly reopened setup/navigation issues; the current exceptions keep setup-button hover lift neutralized, setup action buttons aligned to the shared 40px default button height with color/effect-only differentiation, quick duration presets on a shorter compact setup profile, enabled Continue wrapped in the always-on metal ring without an extra lower-lip face treatment, conflict sync notices suppressed after divergent local edits, and the shared app shell content constrained so routes shrink beside an open drawer instead of overflowing.
- Role-aware navigation shell: sidebar groups, account menu entries, admin links, and calendar shortcut actions resolve through tested route metadata using anonymous/signed-in state, verified account roles, feature keys, and practice roles. This is structural IA only; it does not create full role dashboards for workflows that are still undefined.
- Chimer: treatment-room timer whose background color and property controls follow access to the selected background—free, active credit-owned, active purchased, membership-included, or temporary premium access—while non-background Clock styling retains its separate feature policy; `/clock` exposes the same standalone clock mode as its own public route. Chimer and Clock keep the original free moving gradient mounted through its existing component path under the approved `Massage Laba Lamp` catalog label, with Static Gradient and Solid Color as free static options. Selected visual backgrounds mount only while Clock, a running Chimer session, or an approved music visualizer is active; the Chimer setup page keeps the saved selection and controls without rendering the selected effect behind the setup content. The background picker reads all 84 canonical labels and literal descriptors from the stable-ID branding catalog and uses the active immutable R2 preview release: posters render before device preference hydration, animated previews default on and persist locally, reduced motion suppresses playback without overwriting that saved intent, eligible nearby cards play within the responsive three- or five-card window, aspect-specific low/standard/high renditions adapt at loop boundaries, and the two truthful static entries remain poster-only. Storage denial—including a throwing `localStorage` property getter—falls back safely to the default-on session choice. Background IDs, renderers, entitlements, preferences, commerce keys, and legacy search/support aliases remain stable when display names change. Clock/Chimer allows up to ten seconds for the initial Auth.js session and account-entitlement request plus successful JSON-body reads so cold database-backed requests can establish subscription access without an Account-page detour while stalled bodies still release the retry lock. It also retries account access synchronization on focus, online, page-show, and visible transitions; transient or non-authoritative failures retain only the last verified in-memory snapshot, fresh mounts remain fail-closed, and authoritative sign-out or access changes still replace that snapshot.
- Chimer visual-background follow-up: the additional reviewed premium backgrounds are active through the same stable-ID Chimer/Clock/Music plumbing with source-shaped rendering and `premium_backgrounds` gating. Their approved names now come from the canonical branding catalog rather than this state summary. Hyperspeed and Ballpit are intentionally left for later.
- Business planner: `/tools/business-planner` is the public hub for student/therapist business planning tools. `/tools/business-planner/income` lets users estimate session prices, take-home goals, time off, workload capacity, and optional employee-wage comparisons; anonymous worksheets persist in browser storage, while signed-in worksheets persist as one current non-PHI business payload in `UserPreference.appSettings.businessIncomePlannerIncome` with no new scenario/history table. The student business-plan template also now has browser-local public tools for startup costs and break-even (`/tools/business-planner/break-even`), practice launch tasks (`/tools/business-planner/launch-checklist`), service menus and client policies (`/tools/business-planner/service-menu`), written business-plan outline notes (`/tools/business-planner/plan-outline`), and retail/add-on profit estimates (`/tools/business-planner/add-on-profit`). These new template tools remain browser-local and do not add Prisma storage or account sync.
- Atmosphere: `/music` is the public audio surface for MassageLab-hosted stations, and `/atmosphere` plus `/wellness/atmosphere` are intentionally not retained after moving Atmosphere to a navigation category. `/wellness/breathing` is the separate public Calmness-style breathing guide page linked from the Wellness hub. Atmosphere lists the full 57-piece Alex Bainter Generative.fm package catalog as MassageLab stations; current public-media sample indexes enable all 57 Generative.fm pieces, including Observable Streams and the remaining-generator rollout through Zed. No Generative.fm catalog entries remain sample-pending. The `/music` page uses one listener-facing category selector with the approved fixed-size adaptive Station carousel, deterministic organic-geometric SVG artwork per station, and optional premium visual backgrounds gated by `premium_backgrounds`; anonymous/free users fall back to the free MassageLab moving gradient or static gradient option, and Music stores its chosen background in `massagelab.music.background`. The station cards use sound descriptions rather than backend hosting/sample-index details, and the polish/QA branch groups stations by listener-facing feel while softening expectations for sparse, wave-like, or voice-like stations flagged during manual testing. It reuses the global music provider and sitewide audio toolbar so started stations can continue while users navigate to Chimer, flashcards, wellness tools, or other app routes. The audio player renders as a full-width non-scrolling toolbar on the edge opposite the user's app-bar position preference: narrow expanded mode places station identity/status over a complete icon-only green action row, narrow collapsed mode keeps identity/status with Play/Stop and Expand in one row, and wide mode retains volume. Previous/next, state-derived Play/Stop, Background, volume, collapse/expand, and safe-area-aware shell reservation replace the superseded Restart-plus-Stop presentation; Chimer immersive controls remain offset so timer size buttons are not covered. The global music provider also registers Media Session metadata and play/pause/stop/previous/next handlers so supported mobile browsers can expose notification or lock-screen controls for the active station. The playable Generative.fm runtime prewarms hosted sample-index metadata and shared browser modules for a small starter set during idle, escalates compressed payload warmup only for a tiny starter subset and deliberate hover on healthy connections, and wraps sample fetch/decode requests in bounded batches with per-AudioContext decoded-buffer reuse. Generative.fm station handoffs fade the outgoing output down and fade the incoming output up to soften abrupt first notes while keeping Tone start, Transport start, output nodes, and WAV fallback payloads inside user-initiated playback. Startup timing includes selected sample format, metadata reuse, retained payload-warmup diagnostic fields, and provider request/decode counts. All playable Generative.fm stations expose ordered Ogg Opus, AAC/M4A, and MP3 sidecar sample indexes for browser coverage, then keep WAV sample indexes as the final fallback. `/browse` remains a compatibility grid/workbench for the same station runtime while product discovery points to `/music`.
- Client wellness: `/wellness` is public for anonymous practice logging, 2D body-sensation tracking, practice ROM measurement, practice reminder setup, and in-session pattern review, with clear no-persistence prompts. Signed-in users can save owner-scoped quick logs, body-sensation entries, private custom sensation suggestions, ROM measurements, and generic wellness reminder schedules; view their own appointment summaries when linked through `PracticeClient.userId`; review non-diagnostic pattern reports with trend rows, confidence-labeled prompts, ROM references, and browser JSON downloads; filter their own timeline by category, body region, and sensation/context; export JSON; and soft-delete entries. Therapist viewing, 3D body maps, external reminder delivery, voice capture, passive detection, and sharing remain follow-up work.
- Local-first notes: SOAP notes, intake, journal, and ROM routes are visible from `/notes`, but professional-record viewing/creation is gated by `therapist_documentation_tools`. Once unlocked by membership, these routes require the local professional-record vault passphrase before showing clinical content. The shared encrypted browser vault stores SOAP drafts, intake workspaces, journal drafts, and ROM sessions under `massagelab-professional-record-vault-v1`; legacy plaintext route keys are migration inputs only. Intake now separates full initial forms from existing-client follow-up forms, and selected intake documents can seed a therapist-reviewed SOAP draft through an explicit append/replace action. `/notes` also shows friendly member-supported roadmap signals for future voice notes, intake conversation transcription, SOAP assistance, and managed sync; these are funding goals, not active hosted clinical features. Encrypted `.mlab` full-vault bundles are the normal transfer format, while DOC/PDF output requires an explicit plaintext warning.
- Calendar and booking: operator calendar workspace, appointment/class/personal/reminder creation, service catalog, availability, booking settings, public booking links, guest-capable booking, waitlist, provider capacity controls, and Google Calendar provider sync. Google Calendar provider sync lets externally entitled therapists and owners connect Google from `/calendar/sync`, import selected calendars as generic provider busy blocks for conflict checks, and publish MassageLab-originated appointment/class/personal block events to a dedicated MassageLab Google calendar without exporting client or clinical details. The dedicated MassageLab Google calendar is outbound-only and excluded from selectable inbound busy sources.
- Accounts and billing: profile/preferences, security settings, role verification, membership status, the current three-Product Supporter catalog, a separate one-time-support Checkout path that grants no entitlements, Stripe Customer Portal, signed webhook delivery, and versioned legal acceptance capture for registration, paid membership checkout, professional/practice credential requests, and digital-background purchases. Google sign-in now returns through a signed-in Terms/Privacy acceptance gate before onboarding or the preserved callback when current registration acceptances are missing. Public enrollment is one Supporter Membership with identical benefits at $1/$2/$5 monthly or $10/$20/$50 annually; Therapist and Practice remain unavailable until differentiated professional features are ready for beta; the unused Student-to-Therapist and Early Access coupon paths are removed; Portal switching is limited to Supporter amounts; roadmap interests are independent from price and entitlements; and One-time support states that it is not charitable, tax-deductible, or benefit-granting. The separate Supporter migration plan is retained as completed historical and verification evidence. Do not rerun its catalog apply operation; use only its completed-state readiness and focused amount/interval-switch follow-up.
- Supporter membership: the application publicly accepts only the six current Supporter amount/interval choices and maps every choice to identical Supporter entitlements, including all premium backgrounds; Chimer colors are unrestricted except that selected-background controls follow the canonical access decision; one-time support grants no membership; and roadmap interests remain independent from billing. The completed three-Product migration and Production environment implement Automatic Tax, required billing-address collection, Customer address updates, exclusive Price tax behavior, confirmed Product tax code `txcd_10000000`, immediate no-proration Portal amount changes, and the exact current webhook contract. Verified legacy catalog objects are inactive, both unused coupons are removed, and no non-terminal subscriber required grandfathering. The focused amount-change action is deployed, and the controlled $1-to-$2 monthly change has passed sanitized Stripe amount/tax/anchor/no-proration verification, Production persistence and feature-key entitlement verification, and the visible Account return.
- One-time support: the reviewed classification is fixed at `txcd_90000001`, independently from Supporter access and permanent-background commerce. The user reverified that live Stripe Tax is ready and that the applicable Ohio registration is registered and collecting. Production has all five explicit one-time-support gates for Automatic Tax, exact classification, provider readiness, registrations readiness, and classification confirmation. PR #155's exact merge commit `6e5d65106d8a16f1c2723311dc41c884f7c522c2` is READY in Production, where the required GET-only `stripe:readiness --live --verify-stripe --no-dotenv` path performed live Stripe retrieval and passed the recurring Supporter, one-time-support, and background-commerce gates without printing secret values. The authorized live $5 smoke subsequently returned to the completed Pricing state. Sanitized read-only Stripe retrieval confirms a live paid/completed payment-mode Session with required billing-address collection, Stripe-powered Automatic Tax enabled and complete, one exclusive $5.00 line using `txcd_90000001`, $0.00 calculated tax, and a succeeded $5.00 PaymentIntent. Stripe identifies the code as `Cash Donation`; its zero-tax result is the applied tax evidence for this reviewed nothing-in-return classification, not evidence that Automatic Tax was disabled. The Session attached no Customer, client reference, Subscription, or Invoice, and the existing purpose/webhook contract remains non-entitling: no membership, feature, credit, or ownership is granted. The public not-charitable/not-tax-deductible disclosures remain unchanged.
- Anatomy and education foundation: public `/education`, setup-first `/education/flashcards`, public deck detail routes under `/education/flashcards/decks/[slug]`, Anatomime, Anatomime shared-session routes under `/anatomime/join` and `/anatomime/play/[code]`, `/admin` dashboard, mobile-first admin image review queue, full admin anatomy browser, anatomy correction workflow, code-backed seed content, Postgres anatomy tables, anatomy media catalog/provenance, R2 media workflow docs, item-image media review state, relationship-backed body-system and tissue-type classification with textbook-level major structures and tissue subtypes, and 3D/spatial mapping schema. Flashcards and Anatomime use the sourced study adapter; archived old Anatomime data is not runtime product truth. Public flashcard deck templates store sourced deck configuration only, not custom user-authored anatomy facts. The flashcard setup waits for refreshed prompt counts before start/save actions and explains empty filter states with a reset path. Signed-in flashcard study stores prompt ids, correctness summaries, per-prompt mastery metadata, and session duration only; raw typed answers are not persisted. Anatomime shared rooms store reusable codes, teams, players, game runs, guesses, scoring state, host transfer/election state, and review expiry, while signed-in first-correct guesses update flashcard-linked name-recall progress. Shared Anatomime host screens now show QR/prefilled join links, and player screens distinguish active-team turns from steal/practice mode and offer a rejoin path when a saved device player pass no longer matches the room. Anatomy media review decisions are stored on item-image links so one asset can be approved for one item and rejected for another. This baseline is considered enough for the current alpha; 3D runtime tooling and voice-answer capture are later feature additions, not current core priorities.
- PWA/offline: offline support is intentionally limited to anonymous public local tools; account, auth, billing, calendar, booking, clinical sync, client, and `/api/*` surfaces remain online-only.

## Open Priorities

- P1: Keep this file and `docs/project-log.md` current when meaningful project state changes.
- P1: Complete the [invite readiness stabilization plan](superpowers/plans/2026-06-23-invite-readiness-stabilization.md) before inviting real users: refresh the release gate, pass the repo-owned automated checks, preserve the completed Supporter catalog/readiness, taxed background-purchase and subscription smoke, focused $1-to-$2 monthly switch evidence, and the completed one-time-support GET-only readiness plus $5 live-payment smoke, and keep all clinical/professional-record workflows local-first. The completed [three-Product Portal follow-up](superpowers/plans/2026-07-26-supporter-membership-three-product-portal-followup.md) is retained as historical and verification evidence; do not rerun its catalog migration. Attorney review and first-cohort Production monitoring remain external invite-readiness gates, not unfinished Track 1 implementation.
- P2: Use the [codebase refactor optimization plan](superpowers/plans/2026-06-20-codebase-refactor-optimization.md), [June 20 refactor baseline](audits/2026-06-20-refactor-baseline.md), and [launch Neon transfer hardening plan](superpowers/plans/2026-06-21-launch-neon-transfer-hardening.md) for behavior-preserving performance/reliability cleanup before broad launch. The first pass established the build/bundle/source baseline, split `/admin/anatomy` browser types/data loading out of the page, scoped browser data loading by active view, split `/education/flashcards` API parsing/fetch wrappers, active runner, setup builder, and progress dashboard into focused client modules, added process-local sourced prompt/media caching for repeated flashcard APIs, decomposed `app/calendar/actions.ts` into focused action modules while preserving stable public exports, hardened calendar mutation concurrency for preference patches, reschedules, and free-practice setup quotas, moved the Atmosphere station catalog plus Tone/Generative.fm runtime out of the shared first-load shell behind a lazy global music runtime, moved `/anatomime`'s sourced deck/game workspace behind a lightweight setup shell that renders the full client after the first setup action while warming it during online idle for offline readiness, narrowed Neon transfer hotspots by projecting anatomy media/admin queries and maintenance scripts, hardened local-first intake/SOAP UI type contracts without changing encrypted vault storage or export behavior, documented the TS/JS duplicate-helper compatibility policy with a source-contract guard, and added launch hardening for Neon pooled runtime URLs plus narrower homepage/public booking projections. Treat the refactor as launch-ready; continue remaining compatibility-wrapper migrations only in later low-risk branches.
- P2: Continue validation for local-first intake-to-SOAP continuity and shared vault UX: full initial intake and shorter existing-client follow-up intake are implemented locally, and either intake response can seed a therapist-reviewed SOAP draft inside the encrypted local vault on the same device.
- P2: Continue functional/visual product work rather than backend or compliance-heavy expansion; shared Anatomime classroom-session polish or flashcard UX hardening are better fits for the next work session.
- P2: Harden the shared encrypted professional-record vault UX, backup guidance, and manual QA before expanding therapist-generated records beyond the current local-first alpha workflows.
- P2: Keep future module additions wired through the role-aware navigation resolver instead of adding one-off sidebar role checks.
- P2: Continue public/client wellness tools from the [public client wellness tools plan](superpowers/plans/2026-06-16-public-client-wellness-tools.md): the foundation branch adds public practice logging, signed-in owner-scoped wellness persistence, self-tracking privacy controls, quick logs, first ROM device-orientation/manual measurement, export/delete, navigation, and source guards. The body-sensation branch adds a first 2D region selector, richer body-sensation fields, private custom term suggestions, and timeline filters. The calendar reminder branch adds own appointment summaries and generic owner-scoped wellness reminder schedules without practice calendar payloads. The pattern-report branch adds local aggregate trend rows, weekly summary counts, confidence-labeled reflection prompts, ROM references, and browser report downloads. 3D body maps, therapist sharing, external reminder delivery, voice, and passive activity detection should remain separate follow-up branches.
- P2: Review the new legal/trust documents with an attorney before broader public reliance, then scope account deletion/export request workflows and future provider/client payment terms as separate branches.
- P2: Plan remaining client calendar connection, Outlook, Apple/iCloud, CalDAV, ICS, and full personal event mirroring separately from the implemented Google provider sync.
- P2: Plan Stripe Connect marketplace payments before booking payment collection.
- P2: Plan Payload CMS integration before adding it as a dependency.
- P2: Continue the Atmosphere audio branch series from the hidden runtime spike through the remaining-generator rollout: `/music` exposes a MassageLab-hosted global audio provider, a local Tone proof station, all 57 playable Generative.fm stations from the Alex Bainter package catalog, grouped station browsing, Atmosphere-only swipe/scroll station rails with deterministic organic-geometric SVG artwork, route-persistent mini-player behavior, loading progress, previous/next station controls, and supported-browser Media Session notification controls. The old `/atmosphere` and `/wellness/atmosphere` station URLs are intentionally not retained, and `/wellness/breathing` now carries the public breathing guide as its own Wellness tool. The public station cards use listener-facing sound descriptions rather than backend hosting, package, sample-index, or bucket details. The playable Generative.fm runtime prewarms hosted sample-index metadata and shared browser modules for a small starter set during idle, warms compressed payloads only for a tiny starter subset and deliberate hover on healthy connections, batches provider sample fetch/decode requests, reuses same-session decoded buffers, fades station handoffs, and emits startup timing with selected sample format, metadata reuse, retained payload-warmup fields, and provider request/decode counts while keeping Tone start, Transport start, output nodes, and WAV fallback payloads inside user-initiated playback. All 57 playable Generative.fm stations have Ogg Opus, AAC/M4A, and MP3 sidecar sample indexes and manifests from `massagelab-public-media`; the runtime chooses `opus -> aac -> mp3 -> wav` by browser capability so older iOS/Android browsers avoid WAV when they can use a compressed fallback. The remaining-generator rollout adds 704 WAV-side public R2 objects and 704 Opus sidecar objects for the final 18 stations, reducing the represented 1,491.6 MB WAV payload to 79.5 MB of Opus. The all-station AAC/MP3 rollout adds 1,814 AAC sidecar objects and 1,814 MP3 sidecar objects across Observable Streams plus all 56 package stations, representing 4,845.6 MB of WAV payload as 248.0 MB AAC and 327.5 MB MP3. The local `C:\Users\derri\code\audio` scan now confirms CC0 evidence for VSCO 2 Community Edition, Versilian Community Sample Library, selected Signature Sounds local license files, and the Signature Sounds site-wide CC0 statement for packs under `Signature Samples`. The catalog-wide coverage command (`npm run atmosphere:samples:coverage -- "C:\Users\derri\code\audio"`) currently classifies 57 hosted/playable Generative.fm pieces, 0 local-source candidate pieces, 0 pieces that need replacement or separate source/licensing review, and 0 blocked groups. Shared source-group matches such as `vsco2-piano-mf` remain piece-scoped and are not treated as global playable coverage because other packages may require exact rendered note names. The public-media bucket is separated from anatomy and private media buckets, has browser-read CORS for audio fetches, applies long-lived immutable caching to audio payloads, and applies short revalidating cache headers to generated sample-index/manifest metadata. Raw SSO samples are excluded because Sampling Plus licensing is not a clean hosted-browser fit; configured SSO roles use CC0 adaptations: VSCO sustained oboe for `sso-cor-anglais`, Signature Sounds children choir ambience for `sso-chorus-female`, and Signature Sounds men-of-choirs WAVs for `sso-chorus-male`. GitHub Actions CI now persists Next.js `.next/cache` separately from the existing npm cache, cancels stale pull-request runs, avoids duplicate Prisma generation in the build step, and leaves Playwright browser installation explicit. Custom ambient mixes, account sync, feature gates, user-authored generator controls, and YouTube support remain later work.
- P2: MassageLab 3D Globe now also includes an MassageLab Cobe Globe-inspired Graphic view style as part of the existing premium 3D Globe option, with dependency-free native WebGL sphere rendering plus structured stable 2D overlay map-dot rendering from the reviewed Cobe map mask, Outer Glow, marker-dot colors, and density controls capped at a 10k sample budget; the source `cobe` and `react-spring` runtimes are not added.
- P2: MassageLab Light Speed has been retuned after visual QA: its old `0.10x` renderer pace is now displayed as the new `1.00x` Warp speed. MassageLab Electric Mist is active as a native WebGL port with Chimer/Clock controls for lightning color, animation speed percentage, noise detail, distortion, and brightness from 1% to 100%, with the dense source-like mist restored and only the old vertical sweep softened into an irregular multi-direction mask. MassageLab Astral Flow is active as a native WebGL port of the source breathing radial shader with custom source colors or primary-color harmony palettes, Flow Min/Max controls, and source speed `0.1`-`3` displayed as `10%`-`100%`; after Playwright visual QA it uses source-style alpha blending, mount-relative shader time, and the MassageLab `#05070a` base so Clock avoids the washed-out center-bowl phase. MassageLab Deep Space Nebula is active as a native WebGL port of the source domain-warping nebula shader with custom source colors or primary-color harmony palettes and source speed `0.1`-`5` displayed as `1%`-`100%`. MassageLab Grid Bloom is active as a native WebGL port of the source pulsing interference-grid shader with bloom color, grid density, rotation speed, fade falloff, distortion, flow X/Y, and source speed `0.1`-`3` displayed as `1%`-`100%`; cursor interaction from the source is intentionally omitted. MassageLab Chrome Flow is active as a native WebGL port of the source liquid-metal shader with custom chrome/shadow colors or primary-color harmony palettes, Flow Speed source `0.01`-`2` displayed as `1%`-`100%`, and Time Scale source `0.001`-`1` displayed as `1%`-`100%`. MassageLab Synthesis is also active as a native WebGL port of the source fragment shader, without adding the source `three`/R3F dependency stack, with Chimer/Clock controls for custom source colors or primary-color harmony palettes, animation speed where source `0.4` displays as `1.00x` and the slow end reaches `0.01x`, complexity, zoom scale, distortion, glow intensity, and flow frequency.
- P2: MassageLab Novatrix is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT premium visual background; MassageLab adapts the source WebGL shader with a native canvas renderer instead of adding `ogl`, keeps source-shaped color/speed/amplitude controls with optional color harmony, and replaces source cursor reaction with pointer-events-free ambient drift.
- P2: MassageLab Matrix Rain is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT premium visual background; MassageLab adapts the source Matrix-style falling-character 2D canvas with host sizing, reduced-motion and visibility handling, pointer-events-free rendering, cleanup, custom color or harmony controls, font size, and speed mapped from source `0.05`-`3` to `1%`-`100%`.
- P2: MassageLab Photon Beam is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT premium visual background; MassageLab adapts the source Three.js/postprocessing light-trail beam field into a dependency-free native 2D canvas renderer with host sizing, reduced-motion and visibility handling, pointer-events-free rendering, cleanup, custom color or harmony controls, and source-shaped controls for beam lines, signals, spread, depth, curve, wave, opacity, trail, bloom, and speed mapped from source `0.02`-`2` to `1%`-`100%`.
- P2: MassageLab 3D Globe is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only reviewed source license premium visual background; MassageLab adapts the source Three/R3F/Drei globe, tooltip, and avatar-marker model into a dependency-free native WebGL sphere renderer with a 2D marker overlay, fixed reviewed Earth texture/bump URLs, reduced-motion/visibility/resize cleanup, pointer-events-free rendering, and Chimer/Clock controls for background, globe tint, `0.01x`-`2x` rotation speed with fixed reverse spin, Follow Sun fixed off-screen lighting that hides irrelevant manual controls, screen pan X/Y with hidden sliders until pan is enabled, fixed sun-relative axial tilt, scale, bump, atmosphere, wireframe, and optional icon-based browser/manual location marker with an optional label. Follow Sun uses a hard day/night terminator while rotating once per 24 hours from UTC subsolar longitude. The adaptation also renders a MassageLab Ashland, OH marker with the site logo, no text label, and 1px leader lines.
- P2: MassageLab Retro Grid is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT premium visual background; MassageLab adapts the source WebGL perspective-grid renderer with a CSS fallback, pointer-events-free rendering, reduced-motion/theme/visibility handling, cleanup, custom background and light/dark line colors, and source-shaped angle, cell size, and opacity controls.
- P2: MassageLab Aerial Rays is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT premium visual background; MassageLab adapts the source Motion top-down light-ray component into dependency-free native CSS keyframes with deterministic ray placement, pointer-events-free rendering, reduced-motion fallback, custom background/ray colors, and source-shaped count, blur, speed, length, and opacity controls.
- P2: MassageLab Ferrofluid is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL fragment shader into a dependency-free native WebGL renderer, keeps cursor interaction disabled for pointer-events-free app use, respects reduced motion and cleanup, and exposes Chimer/Clock controls for custom or harmony colors plus source-shaped speed, scale, turbulence, fluidity, rim width, sharpness, shimmer, glow, flow direction, and opacity.
- P2: MassageLab Lightfall is active after 2026-07-04 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL falling light-streak shader into a dependency-free native WebGL renderer, keeps cursor interaction disabled by default but exposes opt-in cursor glow controls while the canvas remains pointer-events free, respects reduced motion and cleanup, and exposes Chimer/Clock controls for custom or harmony streak colors plus source-shaped background color, speed, streak count, width, length, glow, density, twinkle, zoom, background glow, cursor strength/radius/smoothing, and opacity.
- P2: MassageLab Liquid Ether is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source Three.js transparent fluid simulation pass pipeline into a dependency-free raw WebGL renderer, keeps cursor interaction disabled by default but exposes opt-in cursor fluid-push controls while the canvas remains pointer-events free, respects reduced motion and cleanup, and exposes Chimer/Clock controls for custom or harmony palette colors plus source-shaped mouse force, cursor size, viscosity, solver iterations, delta time, BFECC, resolution, bounce, auto-demo speed/intensity/resume/ramp, and opacity.
- P2: MassageLab Prism is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL ray-marched prism/pyramid shader into a dependency-free raw WebGL renderer with the source defaults, 3D rotation, base wobble, glow/bloom, hue controls, optional cursor-hover rotation mode, reduced-motion handling, and cleanup.
- P2: MassageLab Dark Veil is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL CPPN shader into a dependency-free raw WebGL renderer with source defaults, hue shift, noise, scanline, warp, and resolution-scale controls, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Light Pillar is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source Three.js raymarched light-pillar shader into a dependency-free raw WebGL renderer with source defaults, quality presets, custom or harmony top/bottom colors, source-shaped intensity, speed, glow, width, height, noise, blend, rotation controls, optional cursor rotation, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Silk is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source Three/R3F shader plane into a dependency-free raw WebGL renderer with source defaults, custom or harmony silk color, source-shaped speed, scale, noise-intensity, and rotation controls, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Floating Lines is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source Three.js wave-line shader into a dependency-free raw WebGL renderer with source blue/pink colors, custom or harmony line gradients, per-band line/wave controls, optional cursor bend and parallax, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Side Rays is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL side-origin ray shader into a dependency-free raw WebGL renderer with source yellow/blue colors, custom or harmony ray colors, source-shaped speed/intensity/spread/origin/tilt/saturation/blend/falloff/opacity controls, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: Light Rays is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL ray shader into a dependency-free raw WebGL renderer with source white color, custom or harmony ray color, source-shaped origin/speed/spread/length/pulsation/fade-distance/saturation/mouse/noise/distortion controls, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Pixel Blast is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source Three.js/postprocessing Bayer-dithered pixel-shape shader into dependency-free raw WebGL with source lavender, custom, or harmony pixel color, source-shaped shape/pixel/pattern/ripple/liquid/noise controls, optional click ripples, optional liquid pointer warp, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Color Bends is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source Three.js full-screen color-bend shader into dependency-free raw WebGL with source RGB bands, optional custom or harmony color arrays, source-shaped rotation/speed/auto-rotate/scale/frequency/warp/noise/iteration/intensity/band controls, optional pointer interaction, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Line Waves is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL animated line-field shader into dependency-free raw WebGL with source white lines, optional custom or harmony three-color palettes, source-shaped speed/inner-line/outer-line/warp/rotation/edge-fade/color-cycle/brightness controls, optional pointer warp, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Radar is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL radar-ring shader into dependency-free raw WebGL with source purple, optional custom or harmony radar color, background color, source-shaped speed/scale/ring/spoke/thickness/sweep/falloff/brightness controls, optional pointer offset, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Soft Aurora is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL Perlin aurora-band shader into dependency-free raw WebGL with source white/magenta colors, optional custom or harmony two-color palettes, source-shaped speed/scale/brightness/noise/band/layer/color-speed controls, optional pointer shift, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Plasma is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source WebGL2/OGL raymarched plasma shader into dependency-free raw WebGL2 with source white color, optional custom or harmony color, source-shaped speed/direction/scale/opacity controls, optional pointer warp, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Plasma Wave is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL two-color raymarched plasma-wave shader into dependency-free raw WebGL with source violet/cyan colors, optional custom or harmony color pairs, source-shaped offset/rotation/focal-length/speed/direction/bend controls, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Particles is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL point-sprite particle cloud into dependency-free raw WebGL with source white particles, optional custom or harmony three-color palettes, source-shaped count/spread/speed/hover/alpha/base-size/randomness/camera/rotation/pixel-ratio controls, opt-in cursor movement, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Gradient Blinds is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL striped-gradient shader into dependency-free raw WebGL with source magenta/violet colors, optional custom or harmony color pairs, source-shaped angle/noise/blind/spotlight/distortion/shine/blend/pixel-ratio controls, a passive sweeping spotlight with opt-in cursor control, fixed blind-cell geometry, an always-visible slat floor, breeze-like internal shading flex, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Grainient is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL/WebGL2 grain-textured warped gradient shader into dependency-free raw WebGL2 with source magenta/violet/mauve colors, optional custom or harmony three-color palettes, source-shaped time/color-balance/warp/blend/rotation/noise/grain/contrast/gamma/saturation/center/zoom controls, pointer-events-free rendering, reduced-motion handling, and cleanup.
- P2: MassageLab Grid Scan is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source perspective scanner shader into dependency-free raw WebGL with source dark-grid/magenta-scan colors, optional custom or harmony color pairs, source-shaped sensitivity/line/scan/jitter/noise/glow/timing controls, viewport-aspect-aware ceiling/floor/wall geometry with a shared far-depth endpoint, opt-in pointer skew and click scan pulses, pointer-events-free rendering, reduced-motion handling, and cleanup, while omitting source webcam/face-api and postprocessing paths from the app bundle.
- P2: MassageLab Beams is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source Three/R3F/Drei stacked-plane beam geometry, cnoise displacement, perspective projection, and light/noise cues into dependency-free raw WebGL with source white, custom, or harmony light-color controls plus source-shaped beam width, height, count, speed, noise, scale, and rotation controls.
- P2: MassageLab Pixel Snow is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source Three.js full-screen pixel-snow shader into dependency-free raw WebGL2 with source white, custom, or harmony snow-color controls plus source-shaped flake size, minimum flake size, pixel resolution, speed, depth fade, far plane, brightness, gamma, density, variant, and direction controls.
- P2: MassageLab Lightning is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source raw WebGL lightning shader into the shared host with source hue, custom-color-derived hue, or primary-color harmony hue controls plus source-shaped X offset, speed, intensity, and size controls.
- P2: MassageLab Prismatic Burst is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL/WebGL2 prismatic-ray shader into dependency-free raw WebGL2 with source spectral, custom, or harmony gradient controls plus source-shaped intensity, speed, animation type, distortion, offset, hover damping, ray count, and blend controls.
- P2: MassageLab Galaxy is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL layered star-field shader into dependency-free raw WebGL with source hue-shift, custom-color-derived hue, or primary-color harmony hue controls plus source-shaped focal point, rotation, star speed, density, speed, glow, saturation, cursor interaction, repulsion, twinkle, rotation speed, center repulsion, and transparency controls.
- P2: MassageLab Dither is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source Three/R3F wave shader plus postprocessing Bayer dither effect into dependency-free two-pass raw WebGL2 with source grey, custom, or harmony wave-color controls plus source-shaped wave speed, frequency, amplitude, color count, pixel size, cursor interaction, and mouse-radius controls.
- P2: MassageLab Faulty Terminal is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL CRT terminal shader into dependency-free raw WebGL with source white, custom, or harmony tint controls plus source-shaped scale, grid, digit size, time scale, scanline, glitch, flicker, noise, chromatic aberration, dither, curvature, cursor reaction, page-load animation, and brightness controls.
- P2: MassageLab Ripple Grid is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL ripple grid shader into dependency-free raw WebGL with source white, source rainbow, custom, or harmony grid-color controls plus source-shaped ripple intensity, grid size, thickness, fade, vignette, glow, opacity, rotation, cursor interaction, and cursor-radius controls.
- P2: MassageLab Dot Field is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source dependency-free canvas/SVG dot field into a source-shaped internal renderer with source purple, custom, or harmony dot-gradient controls plus source-shaped dot radius, spacing, cursor force/bulge, glow, sparkle, wave, alpha, and cursor-interaction controls.
- P2: MassageLab Dot Grid is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source GSAP/InertiaPlugin canvas dot-grid model into dependency-free internal canvas physics with source/custom/harmony base/active color controls plus source-shaped dot size, gap, proximity, speed trigger, shock, max speed, resistance, return duration, cursor interaction, and click-shock controls.
- P2: MassageLab Threads is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL Perlin thread-line shader into dependency-free raw WebGL with source/custom/harmony thread-color controls plus source-shaped amplitude, distance, and optional mouse-interaction controls.
- P2: MassageLab Iridescence is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source OGL full-screen iridescence shader into dependency-free raw WebGL with source/custom/harmony tint controls plus source-shaped speed, amplitude, and optional mouse-reaction controls. Hyperspeed is intentionally left for a later date.
- P2: MassageLab Waves is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source dependency-free canvas Perlin wave-line simulation with source/custom/harmony line-color controls plus transparent/custom background, wave speed/amplitude, point gap, friction, tension, max cursor movement, and optional cursor-interaction controls.
- P2: MassageLab Grid Distortion is active after 2026-07-05 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause premium visual background; MassageLab ports the source Three.js image/data-texture displacement shader into dependency-free raw WebGL with generated internal texture colors, source/custom/harmony texture-color controls, and source-shaped grid, mouse radius, strength, relaxation, real cursor, and opt-in simulated-cursor controls.
- P2: MassageLab Orb, Letter Glitch, Grid Motion, Shape Grid, Liquid Chrome, and Balatro are active after 2026-07-05 review. They are documented in `docs/background-sources.md` as internal-only MIT + Commons Clause premium visual backgrounds; MassageLab ports the Orb, Liquid Chrome, and Balatro OGL shaders into dependency-free raw WebGL, ports Letter Glitch and Shape Grid from source-shaped canvas renderers, and keeps Grid Motion source-shaped while replacing GSAP with requestAnimationFrame smoothing. Each uses `premium_backgrounds` gating, source/custom/harmony color controls where applicable, optional cursor interaction where the source supports it, reduced-motion handling, and cleanup. Ballpit is intentionally left for later.
- P2: MassageLab Stars is also active after 2026-07-03 review. It is documented in `docs/background-sources.md` as an internal-only MIT + Commons Clause caution background; MassageLab adapts the source three drifting star layers, radial bottom background, 2000px repeat drift, and parallax behavior with CSS/window events instead of adding `motion`, with Chimer/Clock controls for star color, speed, density, and parallax strength.
- P3: Track Chimer and premium-background refactor follow-ups separately from this background rollout: grouped background settings instead of long prop drilling, shared canvas/WebGL helpers including context-loss handling, CSS deduplication, self-hosted 3D globe textures with license notes, per-frame canvas allocation review, and any repo-wide stylelint convention decisions.
- P3: Plan a repo-wide docstring pass for shared helpers, domain rules, server actions, data adapters, and non-obvious UI components so code is easier to understand in-place.
- P3: Revisit anatomy 3D/spatial runtime tooling after the core local-first practice tools are more useful.
- P3: Revisit voice notes, intake conversation transcription, and SOAP-assist later, after a separate feasibility branch proves local/on-device options and any hosted path passes legal, BAA, security, consent, audit, and retention review.
- P2: Land the technical SEO foundation and focused public landing-page copy before the broad public-alpha share window: production public pages can become indexable, preview/development builds stay noindex, private/auth/API/booking/shared-code/local-record routes stay out of the sitemap and robots allowlist, and the homepage plus flashcards, Anatomime, Chimer, notes, and wellness routes carry clear search-aligned product proof without weakening the local-first PHI boundary.

### Track 4B final accepted renderer correction

- This records the accepted correction to the earlier face-based Twisted Cubes model documented in `docs/project-log.md`: Scale now applies uniformly to the complete count-relative progression; at the approved 20-layer, 30% default the outer wireframe reaches `120vmax`. Twelve thin CSS edges per layer preserve the full-bleed nested outline without the viewport-size transparent faces that caused the rejected page-wide slowdown. The snapshot above already reflects this state. The user accepted this exact local visual result before the PR loop resumed.
- DNA's palette contract now maps base identity to a stable role: A to Adenine, T to Thymine, G to Guanine, and C to Cytosine. The Show base letters setting is presentation-only, so hiding the letters never changes the educational color mapping. This records the accepted correction to the earlier randomized endpoint-role model documented in `docs/project-log.md`; focused desktop/mobile browser proof passed and the user accepted the corrected local visual result before the PR loop resumed.

## Documentation Rules

- Update this file first when the current focus, database state, live surfaces, or priority order changes.
- Update `docs/project-log.md` for chronological change history, completed branches, meaningful decisions, and new plans.
- Keep `docs/roadmap.md` as product-direction evidence, not the current operating tracker.
- Keep `TODO.md` as preserved checklist evidence; mirror meaningful status changes here and in `docs/project-log.md`.
- Keep wiki pages under `docs/wiki/` for stable operational documentation.
- Store detailed implementation plans under `docs/superpowers/plans/`.
- Preserve local-first PHI boundaries, feature-key entitlement checks, conservative Sentry privacy posture, and targeted branch-sized refactors.
