# AtmoShaper Repository Migration Execution Checkpoint

## TodoCheckpointDraft

- Current todo: Task 3, complete the authorized Browser-QA capture and accept the repeatable Playwright source baseline.
- Completed todos: design and implementation plan approved; execution protocols initialized; canonical context and TaskStartSnapshot captured; Task 1 source lock and authority documents completed; Task 2 exact export manifest plus compatibility, provider, domain, local-data, cleanup, and refactor inventories completed after two reviewed corrections.
- Active slice: the seven-file parity/config/harness/manifest/plan package has passed specification and quality review after two fix rounds; supply the approved QA environment, build the owned current checkout, execute 22 tests, capture 24 desktop/mobile PNGs, repeat without changes, verify cleanup, re-review the completed package, and commit.
- Evidence refs: `10-intent.md`; `90-evidence.md`; approved design `14f60fc1`; approved plan `3cb3f893`; Task 1 commit `67545daee7773985f2b975ee4ccd7cadb80a6021`; Task 2 commit `421a4f5ecf53012bf6af19a385b8a82f82b4883f`; Superpowers plan-scoped ledger/reports/review packages.
- Blocked on: the parent task lacks `MASSAGELAB_BROWSER_QA_DATABASE_URL`, `MASSAGELAB_BROWSER_QA_DIRECT_URL`, and `MASSAGELAB_BROWSER_QA_DATABASE_FINGERPRINT`. Without all three exact approved values, the fail-closed gate forbids the signed-in fixture and snapshot capture. No substitute database, production target, invented fingerprint, real account, or skipped signed-in test is allowed.
- Next step: after the operator supplies those three values to this task environment, resume `/root/task3_implementer` (or a fresh capture-only implementer if unavailable) with the existing Task 3 report; run the amended Task 3 Step 4 fresh-child command from the current checkout.

## ResumeStateHint

Resume in `C:/Users/derri/code/my_projects/massagelab` on `codex/atmoshaper-migration-preflight`. Re-read this checkpoint, the intent, the Superpowers ledger, branch/HEAD/status, and local/tracked/live `main`. Tasks 1 and 2 are complete at `67545dae` and `421a4f5e`; Task 3 has a reviewed seven-file text package but no PNGs or commit. Confirm its exact staged/working-tree state rather than reimplementing it. Resume capture only when all three approved QA variables are present; otherwise remain stopped. Do not touch unrelated worktrees. No merge, deploy, provider, DNS, production database, payment, email, media, or legal-owner mutation is authorized.

## DriftCheckDraft

- Original intent alignment: yes.
- Goal/stop-condition alignment: yes.
- Scope fence: yes; the uncommitted Task 3 delta is limited to package/config/test harness/parity spec and migration-owned manifest/inventory/plan documentation; no runtime source changed.
- Compatibility boundary: unchanged.
- New owner/fallback/adapter/branch: one exact migration-only Playwright invocation owner and named harness exclusion; ordinary CI lanes and runtime owners remain unchanged.
- Retirement track: explicit; old repository remains intact and the migration-only spec/snapshots retain the post-Phase-6 review trigger.
- Evidence growth: TaskStartSnapshot; Tasks 1-2 receipts; Task 3 exact 22-test discovery; 45/45 focused checks; seven-file staged-package reviews; named CI-harness correction; owned-server enforcement; fixed Clock with no mask; first-hop mutation blocking; context-close observation; exact manifest counts `1/7/37/6/39` and totals `1,856/1,892/1,898`; explicit fail-closed capture blocker.
- Execution Readiness View alignment: yes across intent, scope, baseline, compatibility, retirement, test, and review locks.
- Decision: stop at the Task 3 external gate until the operator supplies the approved disposable Browser-QA URL pair and fingerprint.
