# AtmoShaper External Account Checklist

Source: `dsbowersock/massagelab` at `fa78ca01a42179329cc223df77c76f308e76320b`; source contracts inspected 2026-09-06 and the narrowly allowed GitHub, Vercel, and Neon Task 4 readbacks recorded 2026-09-08. Rows other than those three systems remain repository evidence or pending provider checks, not fresh provider-console verification. The dated readbacks omit credentials, configuration secrets, provider/account/customer identifiers, database rows, connection strings, and URLs containing tokens.

The [charter](atmoshaper-migration-charter.md) and [design](../superpowers/specs/2026-09-06-atmoshaper-repository-migration-design.md) control authority. Phase 2 new-repository publication is the only planned external creation authorized by the migration scope, and only after its baseline gates. All other writes require separate explicit target, effect, rollback and readback authorization. Phase numbers identify the earliest planned work, not automatic permission.

| System | Current evidence owner | Current identity/domain | Read-only check | Domain dependency | Stable identifier | Phase allowed to change | Mutation authorization | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| GitHub repository | Charter; `.github/workflows/ci.yml`; `README.md`; Task 4 redacted readback dated 2026-09-08 | `dsbowersock/massagelab`; destination `dsbowersock/atmoshaper` | Task 4 confirmed the source repository is public, uses `main` as its default branch, and is not archived. Task 5 refreshes local/tracked/live source identity; Task 9 verifies target absence/ownership and new public repo settings. | Repository URL, badges, Actions secrets/integrations | Old history/commit refs; existing repo retained | Phase 2 destination only | Gated by Tasks 1-8; no old-repo rename/archive/visibility change | Source readback passed 2026-09-08; source unchanged; destination uncreated and pending |
| GitHub Actions / checks | `.github/workflows/ci.yml`, `tests/browser/ci-lanes.mjs` | Existing source workflow and four QA lanes | Inspect workflow/check names, environment/secret **names**, branch rules, app installations and exact-head results. | Vercel/check/repository integration | CI lane membership and test contracts | Phase 2 destination checks | Separate authorization for settings/integration writes | Repo evidence gathered; hosted settings pending |
| Vercel | `docs/project-state.md`, `docs/wiki/deployment.md`, `next.config.mjs`; Task 4 redacted readback dated 2026-09-08 | Project `massagelab`; framework Next.js; Node.js 22.x; production domains `massagelab.app` and `www.massagelab.app` | Vercel CLI `59.11.7` read back project metadata and domain inventory without reading environment values or changing project state. Later phases must still read the exact deployment SHA, aliases, environment roles, and any separately authorized destination linkage. | Production/auth/canonical hosts and Preview URLs | Existing project/deployment rollback identity | Phase 7 isolated parallel project; Phase 10 traffic | Separate explicit creation/configuration/deployment authorization | Task 4 source identity readback passed 2026-09-08; no project, deployment, alias, domain, or environment mutation; destination pending |
| Namecheap / DNS | Approved design Phase 9/10; domain plan | Intended new primary/short domains; old apex/www | Confirm ownership, registrar, nameservers, current records, TTL and rollback record set. | All public hosts, mail verification, media custom domain | Existing zones and verification records | Phase 9 staging / Phase 10 cutover | Separate DNS authorization | Ownership, current records and TTL unverified |
| Neon | `prisma/schema.prisma`, `prisma.config.ts`, deployment wiki; Task 4 redacted QA receipt dated 2026-09-08 | Existing production Postgres integration plus one authorization-bound temporary empty QA project; names and targets deliberately not recorded | The temporary project received exactly the 46 committed migrations, received no production copy, and retained zero Browser-QA fixture rows in the five checked application tables after cleanup. It was then deleted and verified absent. A read-only identity check confirmed the production project remained present and untouched. | Vercel env role, auth/billing persistence | All 46 committed migrations, schema, durable IDs and historical memberships | Phase 7 isolated branch; production only separately authorized | The temporary empty Task 4 QA project was explicitly authorized; any other database/provider action remains separately gated | Task 4 temporary QA complete and deleted 2026-09-08; production data and configuration untouched; future Neon gates pending |
| Google OAuth consent identity | `auth.ts`, deployment wiki | Google sign-in for existing product; console text unknown | Read consent status, displayed identity, support/contact, scopes and intended user availability. | Provider verification and new domain support | OAuth client/provider account mappings | Phase 9 | Separate consent/provider authorization and separate real-account proof | Pending console readback |
| Google authorized origins / callbacks | `auth.ts`, deployment wiki | One exact current production origin required; cannot infer `AUTH_URL` from SEO | Compare saved origin and `/api/auth/callback/google` URI with exact auth host; verify parallel-domain support. | Apex and www are distinct | Existing client IDs/redirect relationships retained | Phase 9 staging / Phase 10 traffic | Separate provider authorization | Canonical auth-host readback pending |
| Auth.js origin / callback enforcement | `auth.ts`, `lib/trusted-form-origin.js`, auth route owners | Runtime configured auth origin; source helper recognizes old apex/www alias | Inspect configured-origin shape without printing values; test allowed/rejected host and callback cases in isolated QA. | Auth origin, trusted form origins, cookie host/security | Existing JWT/binding/security contracts; see local-data plan | Phase 8/9 host proof | No production config change authorized | Code verified; actual production auth configuration pending |
| Google Calendar | `lib/google-calendar-adapter.ts`, `lib/calendar-sync-constants.ts`, `lib/calendar-sync-env.ts`, `app/api/calendar/google/` | Generated calendar summary `MassageLab`; separate Calendar OAuth callback | Read client application identity, callback allowlist and scope status; inspect aggregate integration health only. | `/api/calendar/google/callback`; token encryption and OAuth state origin | `GOOGLE`; existing calendar/event mappings and `MASSAGELAB_GOOGLE_CALENDAR_SUMMARY` lookup behavior | Phase 9 | Separate provider authorization; no calendar creation/sync | Code verifies lookup by summary; rename could create duplicates |
| Resend / SMTP transport | `lib/auth-mail.ts`, `docs/wiki/deployment.md`, `.env.example` | Docs describe SMTP and display example `MassageLab <no-reply@massagelab.app>`; actual provider unverified | Read transport/provider, verified sender domain and delivery/bounce status; no send. Confirm whether Resend is actually used. | Sender verification/authentication; links use configured app origin | SMTP env names, durable notice/intent and claim ownership | Phase 9 test preparation; live transition separately | Separate provider and sending authorizations | SMTP code present; Resend account and live sender unverified |
| SPF | Deployment wiki | Existing sender-domain policy unknown | Read published record and sending-provider authorization; store only pass/fail/record-owner evidence. | Sender domain DNS | Existing legitimate senders must remain authorized | Phase 9 | Separate DNS/mail authorization | Pending |
| DKIM | Deployment wiki | Existing signing domain/selectors unknown | Read signing/verification status and alignment without key material. | Sender domain DNS | Existing verified selector continuity | Phase 9 | Separate DNS/mail authorization | Pending |
| DMARC | Deployment wiki | Existing alignment/policy unknown | Read policy/alignment and aggregate health; no recipient/event data. | From domain and SPF/DKIM alignment | Existing mail policy until approved replacement | Phase 9 | Separate DNS/mail authorization | Pending |
| Support email / forwarding | `lib/support-contact.js` | Public `contactmassagelab@gmail.com`; future address undecided | Verify ownership/reachability/forwarding without sending or reading messages; identify retention owner. | Future display/support domain and provider contact fields | Existing address remains available through recovery | Phase 9 | Separate support/mail authorization; do not retire old address | Public code verified; forwarding/account status pending |
| Stripe business / receipts / support details | Deployment and billing wikis | Source product uses MassageLab; dashboard identity unknown | Read business display/statement/receipt/support settings with sensitive data omitted. | Site/support URLs and legal operator | Legal business/account ownership | Phase 9 | Separate Stripe authorization; no legal transition implied | Pending dashboard readback |
| Stripe Checkout | `lib/stripe-billing.js`, `lib/stripe-price-contract.js` | MassageLab Supporter and One-time support | Read existing branding/return origins and contract configuration; never create a Session for inventory. | Success/cancel URLs, pricing return, auth host | `supporter_membership_v1_checkout_v1` and existing session reconciliation | Phase 9 | Separate Stripe/config/test-flow authorization | Code evidence gathered |
| Stripe Portal | `lib/stripe-billing.js`, billing wiki | Existing billing-management portal | Read branding, return URL and six-Price update/cancellation/payment-method contract. | Portal return/support origins | Existing subscription/customer and portal configuration relationships | Phase 9 | Separate Stripe authorization; do not create Portal session | Configuration readback pending |
| Stripe catalog | `lib/stripe-price-contract.js`, `lib/stripe-readiness.js`, billing wiki | Three Supporter amounts; six current recurring Prices; historical Price mappings | Read current/legacy catalog classification and account mode without object/customer identifiers. | Public labels/receipts; pricing readiness | `supporter_membership_v1`; six amount-specific env slots and legacy webhook-only mappings | Phase 9 only with dedicated compatibility evidence | Separate Stripe authorization; no product/price mutation | Repo contract verified; live/test catalog readback pending |
| Stripe webhook | `lib/stripe-webhook-contract.js`, `app/api/billing/webhook/route.ts` | Pinned source URL `https://www.massagelab.app/api/billing/webhook` | Read enabled endpoint, event/API-version and aggregate failure status; never replay/create events. | Exact endpoint and signing-secret role | Existing reconciliation, event deduplication and historical membership authority | Phase 9 staging / Phase 10 traffic | Separate endpoint authorization/readback | Pinned code verified; hosted endpoint pending |
| Stripe metadata | `lib/stripe-readiness.js`, `lib/stripe-billing.js`, migration contract | Private namespace retains `app=massagelab` | Inventory key/value contracts from code and read aggregate compatibility without exposing objects. | Technical identity independent of new display name | `massagelab_catalog`, `massagelab_membership_level`, `massagelab_supporter_amount_choice`; all current user/order/contract metadata | Dedicated later migration only | No namespace rename authorized | Retain as compatibility |
| Stripe idempotency | `lib/stripe-billing.js`, `lib/donation-checkout-attempt.ts` | Private request identity | Compare complete key constructors and retry bounds without constructing provider requests. | Browser attempt recovery and historical provider requests | `massagelab-customer:`, `massagelab-membership-checkout:`, `massagelab-membership-checkout-expire:`, `massagelab-donation-v1:`, `background-purchase:` families | Dedicated later migration only | No namespace rename/replay authorized | Retain as compatibility |
| Cloudflare R2 buckets / media objects | Deployment/audio wikis; `lib/atmosphere/r2-sample-hosting.js`; active media manifests | Docs name `massagelab-public-media`, example anatomy bucket `massagelab-anatomy-media`, public `media.massagelab.app` | Read approved bucket/domain configuration and aggregate operation counts; inspect only known public objects if later needed. | Media custom host and clients on old/new origins | Bucket/object/prefix/release/hash identities, exact-file licenses | Phase 9 read/staging; changes separately scoped | Separate R2 authorization; no upload/move/delete/list of private objects | Source evidence only; no media/provider action |
| Cloudflare media CORS / cache | `docs/cloudflare/massagelab-public-media-cors.json`, deployment wiki | Checked-in public GET/HEAD, Range header, wildcard origins; immutable public cache policy | Compare saved policy and known public header readbacks, Range/206/content type; no purge. | Both origins need media reads; custom-domain DNS/cache | Existing object URLs and revisioned identities | Phase 9 | Separate CORS/domain/cache authorization | Repository policy verified; saved provider state pending |
| Sentry | `sentry.options.ts`, `lib/sentry-options.js`, `lib/sentry-privacy.js`, deployment wiki | Existing project/environment/release names unknown | Read project linkage, environment roles, quotas and aggregate error/privacy status; no event payload or test capture. | Release repository mapping, allowed origins, separated Preview environment | Existing privacy scrubbing and `SENTRY_*` / `NEXT_PUBLIC_SENTRY_*` config names | Phase 7 isolated environment / Phase 9 settings | Separate provider authorization | Code evidence gathered; hosted identity pending |
| Ably / Anatomime realtime | `lib/anatomime-realtime.ts`, `app/api/anatomime/sessions/[code]/realtime-token/route.ts`, `app/anatomime/shared-session-client.tsx` | Server REST publisher at `rest.ably.io`; browser SDK from `cdn.ably.com`; hosted app/configuration unknown | Read environment-to-app/key-role binding, channel capability policy, any configured origin restrictions, connection/message quotas and aggregate usage/health; no secret/token/room data, token issuance, message publication or presence mutation. | Isolated staging app/key role and browser SDK/connectivity from approved hosts; identical room codes must not connect staging to production | `ABLY_API_KEY`; normalized `anatomime:<ROOM>` channel namespace; existing event names, one-hour signed TokenRequest and channel-scoped `subscribe`/`presence` capabilities; database remains authoritative | Phase 7 separately authorized isolated staging; Phase 9/10 approved production binding only | Separate app/key/configuration authorization; no publication authorized by inventory or migration verification | Source contract verified; provider configuration/capabilities/quotas and environment isolation unverified |
| Search / sitemap tools | `lib/seo.js`, `app/robots.ts`, `app/sitemap.ts` | Canonical `https://www.massagelab.app`; Preview noindex | Read verified search properties, crawl/index status and public robots/sitemap; no submission. | New canonical host, redirect readiness, old-origin recovery | Existing excluded auth/booking/local-record routes | Phase 10 only after cutover gates | Separate search-property/submission authorization | Source canonical policy verified; search accounts unknown |
| Social accounts | Source SEO and approved design | No verified migration account inventory | Identify existing public profile/ownership/link evidence; do not claim handles or reserve/change them. | Site/support links and supplied social artwork | Existing account ownership | Phase 9 | Separate account/profile authorization | Unresolved; no handles inferred |
| PWA / possible app-store identity | `app/manifest.ts`, `components/providers/service-worker-provider.tsx`, `public/sw.js` | MassageLab standalone PWA; no verified app-store listing | Read metadata/install behavior on both legacy origins; inspect any listing only if identified. | Install identity/scope and storage origin | Current manifest id/scope, icons, SW URL and caches | Phase 6 supplied display assets; Phase 8 transition; Phase 10 host | No store listing or install retirement authorized | PWA code verified; store presence unresolved |

## Readback and sequencing rules

Each later receipt records date, exact source/destination SHA, environment role, public host, observed status, evidence owner and remaining gate. Configuration names may be listed; values that contain secrets or target/account/customer identities may not. A source document's past production receipt is not a new live check.

Google Calendar needs a dedicated naming decision: `ensureAppCalendar` searches by the existing summary before creation, so treating its visible name as ordinary brand copy can create a second calendar. Stripe metadata, idempotency, catalog and webhook rows are distinct compatibility contracts and must survive any display-only change.

Provider staging uses an approved non-production Neon target, Stripe test mode, non-sending or separately approved test email, approved OAuth callbacks, safe media reads, a separate Sentry environment and the explicit [Ably staging gate](#ably-staging-gate). Resolve the [host matrix](atmoshaper-domain-cutover-plan.md) and [local-data recovery](atmoshaper-local-data-and-pwa-plan.md) before production traffic moves. No pending row is a request or permission to mutate.

## Ably staging gate

The locked server reads `ABLY_API_KEY`, publishes compact change events with a three-second timeout, and signs one-hour TokenRequests limited to the normalized room channel with `subscribe` and `presence` capabilities. The browser obtains the local realtime token, loads the Ably SDK, subscribes to that same channel, and wakes its polling owner on a signal; connection/setup failure displays the automatic-refresh fallback. The database remains authoritative. Keep the environment variable, channel normalization, event identifiers and token capability boundary unchanged. Public product naming is not permission to rename these contracts.

Before staging, record which of these two modes is deliberately selected, with source and destination environment roles stated explicitly:

- **Isolated Ably:** separately authorize an isolated non-production app/key configuration, then read back its environment binding, server publishing capability, client subscribe/presence scope and quota/health status without exposing credentials or real room identities. Preserve the existing channel namespace inside the isolated app so identical room codes cannot cross into production. Authorized synthetic connection/authentication/subscription checks must not publish messages or enter/update presence. Use the existing local Ably harness in `tests/browser/anatomime-traffic.spec.ts` to verify signal-triggered refresh and subscription cleanup without sending provider messages; distinguish this mocked signal proof from hosted connectivity evidence.
- **Provider disabled / fallback:** deliberately leave Ably unconfigured in the isolated verification environment, verify the bounded unavailable path and automatic polling, and record that realtime was not exercised. A working polling fallback does not establish enabled-realtime parity. If the source baseline requires configured realtime, keep that parity gate pending until the isolated-Ably checks are complete; do not silently accept the degraded path.

Rollback preserves the prior approved environment-to-app/key binding and restores only that separately authorized binding/configuration if necessary. Do not rename channels, rotate or retire a production key/app, replay events or publish a recovery message. If fallback is the approved temporary rollback mode, record the missing realtime capability explicitly. Provider configuration remains unverified in Task 2; no Ably request, token issuance or publication was performed.
